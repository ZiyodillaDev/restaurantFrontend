/*!
   Copyright 2008-2021 SpryMedia Ltd.

 This source file is free software, available under the following license:
   MIT license - http://datatables.net/license

 This source file is distributed in the hope that it will be useful, but
 WITHOUT ANY WARRANTY; without even the implied warranty of MERCHANTABILITY
 or FITNESS FOR A PARTICULAR PURPOSE. See the license files for details.

 For details please refer to: http://www.datatables.net
 DataTables 1.11.3
 ©2008-2021 SpryMedia Ltd - datatables.net/license
*/
(($jscomp = $jscomp || {}).scope = {}),
  ($jscomp.findInternal = function (t, e, i) {
    t instanceof String && (t = String(t));
    for (var n = t.length, a = 0; a < n; a++) {
      var r = t[a];
      if (e.call(i, r, a, t)) return { i: a, v: r };
    }
    return { i: -1, v: void 0 };
  }),
  ($jscomp.ASSUME_ES5 = !1),
  ($jscomp.ASSUME_NO_NATIVE_MAP = !1),
  ($jscomp.ASSUME_NO_NATIVE_SET = !1),
  ($jscomp.SIMPLE_FROUND_POLYFILL = !1),
  ($jscomp.ISOLATE_POLYFILLS = !1),
  ($jscomp.defineProperty =
    $jscomp.ASSUME_ES5 || "function" == typeof Object.defineProperties
      ? Object.defineProperty
      : function (t, e, i) {
          return (
            t == Array.prototype || t == Object.prototype || (t[e] = i.value), t
          );
        }),
  ($jscomp.getGlobal = function (t) {
    t = [
      "object" == typeof globalThis && globalThis,
      t,
      "object" == typeof window && window,
      "object" == typeof self && self,
      "object" == typeof global && global,
    ];
    for (var e = 0; e < t.length; ++e) {
      var i = t[e];
      if (i && i.Math == Math) return i;
    }
    throw Error("Cannot find global object");
  }),
  ($jscomp.global = $jscomp.getGlobal(this)),
  ($jscomp.IS_SYMBOL_NATIVE =
    "function" == typeof Symbol && "symbol" == typeof Symbol("x")),
  ($jscomp.TRUST_ES6_POLYFILLS =
    !$jscomp.ISOLATE_POLYFILLS || $jscomp.IS_SYMBOL_NATIVE),
  ($jscomp.polyfills = {}),
  ($jscomp.propertyToPolyfillSymbol = {}),
  ($jscomp.POLYFILL_PREFIX = "$jscp$");
var $jscomp,
  $jscomp$lookupPolyfilledValue = function (t, e) {
    var i = $jscomp.propertyToPolyfillSymbol[e];
    return null == i ? t[e] : void 0 !== (i = t[i]) ? i : t[e];
  };
($jscomp.polyfill = function (t, e, i, n) {
  e &&
    ($jscomp.ISOLATE_POLYFILLS
      ? $jscomp.polyfillIsolated(t, e, i, n)
      : $jscomp.polyfillUnisolated(t, e, i, n));
}),
  ($jscomp.polyfillUnisolated = function (t, e, i, n) {
    for (i = $jscomp.global, t = t.split("."), n = 0; n < t.length - 1; n++) {
      var a = t[n];
      if (!(a in i)) return;
      i = i[a];
    }
    (e = e((n = i[(t = t[t.length - 1])]))) != n &&
      null != e &&
      $jscomp.defineProperty(i, t, {
        configurable: !0,
        writable: !0,
        value: e,
      });
  }),
  ($jscomp.polyfillIsolated = function (t, e, i, n) {
    var a = t.split(".");
    (t = 1 === a.length),
      (n = a[0]),
      (n = !t && n in $jscomp.polyfills ? $jscomp.polyfills : $jscomp.global);
    for (var r = 0; r < a.length - 1; r++) {
      var s = a[r];
      if (!(s in n)) return;
      n = n[s];
    }
    (a = a[a.length - 1]),
      null !=
        (e = e((i = $jscomp.IS_SYMBOL_NATIVE && "es6" === i ? n[a] : null))) &&
        (t
          ? $jscomp.defineProperty($jscomp.polyfills, a, {
              configurable: !0,
              writable: !0,
              value: e,
            })
          : e !== i &&
            (($jscomp.propertyToPolyfillSymbol[a] = $jscomp.IS_SYMBOL_NATIVE
              ? $jscomp.global.Symbol(a)
              : $jscomp.POLYFILL_PREFIX + a),
            (a = $jscomp.propertyToPolyfillSymbol[a]),
            $jscomp.defineProperty(n, a, {
              configurable: !0,
              writable: !0,
              value: e,
            })));
  }),
  $jscomp.polyfill(
    "Array.prototype.find",
    function (t) {
      return (
        t ||
        function (t, e) {
          return $jscomp.findInternal(this, t, e).v;
        }
      );
    },
    "es6",
    "es3"
  ),
  (function (t) {
    "function" == typeof define && define.amd
      ? define(["jquery"], function (e) {
          return t(e, window, document);
        })
      : "object" == typeof exports
      ? (module.exports = function (e, i) {
          return (
            e || (e = window),
            i ||
              (i =
                "undefined" != typeof window
                  ? require("jquery")
                  : require("jquery")(e)),
            t(i, e, e.document)
          );
        })
      : (window.DataTable = t(jQuery, window, document));
  })(function (t, e, i, n) {
    function a(e) {
      var i,
        n,
        r = {};
      t.each(e, function (t, s) {
        (i = t.match(/^([^A-Z]+?)([A-Z])/)) &&
          -1 !== "a aa ai ao as b fn i m o s ".indexOf(i[1] + " ") &&
          ((n = t.replace(i[0], i[2].toLowerCase())),
          (r[n] = t),
          "o" === i[1] && a(e[t]));
      }),
        (e._hungarianMap = r);
    }
    function r(e, i, s) {
      var o;
      e._hungarianMap || a(e),
        t.each(i, function (a, l) {
          (o = e._hungarianMap[a]) === n ||
            (!s && i[o] !== n) ||
            ("o" === o.charAt(0)
              ? (i[o] || (i[o] = {}),
                t.extend(!0, i[o], i[a]),
                r(e[o], i[o], s))
              : (i[o] = i[a]));
        });
    }
    function s(t) {
      var e = Wt.defaults.oLanguage,
        i = e.sDecimal;
      if ((i && Ft(i), t)) {
        var n = t.sZeroRecords;
        !t.sEmptyTable &&
          n &&
          "No data available in table" === e.sEmptyTable &&
          Mt(t, t, "sZeroRecords", "sEmptyTable"),
          !t.sLoadingRecords &&
            n &&
            "Loading..." === e.sLoadingRecords &&
            Mt(t, t, "sZeroRecords", "sLoadingRecords"),
          t.sInfoThousands && (t.sThousands = t.sInfoThousands),
          (t = t.sDecimal) && i !== t && Ft(t);
      }
    }
    function o(t) {
      if (
        (ue(t, "ordering", "bSort"),
        ue(t, "orderMulti", "bSortMulti"),
        ue(t, "orderClasses", "bSortClasses"),
        ue(t, "orderCellsTop", "bSortCellsTop"),
        ue(t, "order", "aaSorting"),
        ue(t, "orderFixed", "aaSortingFixed"),
        ue(t, "paging", "bPaginate"),
        ue(t, "pagingType", "sPaginationType"),
        ue(t, "pageLength", "iDisplayLength"),
        ue(t, "searching", "bFilter"),
        "boolean" == typeof t.sScrollX &&
          (t.sScrollX = t.sScrollX ? "100%" : ""),
        "boolean" == typeof t.scrollX && (t.scrollX = t.scrollX ? "100%" : ""),
        (t = t.aoSearchCols))
      )
        for (var e = 0, i = t.length; e < i; e++)
          t[e] && r(Wt.models.oSearch, t[e]);
    }
    function l(t) {
      ue(t, "orderable", "bSortable"),
        ue(t, "orderData", "aDataSort"),
        ue(t, "orderSequence", "asSorting"),
        ue(t, "orderDataType", "sortDataType");
      var e = t.aDataSort;
      "number" != typeof e || Array.isArray(e) || (t.aDataSort = [e]);
    }
    function u(i) {
      if (!Wt.__browser) {
        var n = {};
        Wt.__browser = n;
        var a = t("<div/>")
            .css({
              position: "fixed",
              top: 0,
              left: -1 * t(e).scrollLeft(),
              height: 1,
              width: 1,
              overflow: "hidden",
            })
            .append(
              t("<div/>")
                .css({
                  position: "absolute",
                  top: 1,
                  left: 1,
                  width: 100,
                  overflow: "scroll",
                })
                .append(t("<div/>").css({ width: "100%", height: 10 }))
            )
            .appendTo("body"),
          r = a.children(),
          s = r.children();
        (n.barWidth = r[0].offsetWidth - r[0].clientWidth),
          (n.bScrollOversize =
            100 === s[0].offsetWidth && 100 !== r[0].clientWidth),
          (n.bScrollbarLeft = 1 !== Math.round(s.offset().left)),
          (n.bBounding = !!a[0].getBoundingClientRect().width),
          a.remove();
      }
      t.extend(i.oBrowser, Wt.__browser),
        (i.oScroll.iBarWidth = Wt.__browser.barWidth);
    }
    function c(t, e, i, a, r, s) {
      var o = !1;
      if (i !== n) {
        var l = i;
        o = !0;
      }
      for (; a !== r; )
        t.hasOwnProperty(a) &&
          ((l = o ? e(l, t[a], a, t) : t[a]), (o = !0), (a += s));
      return l;
    }
    function d(e, n) {
      var a = Wt.defaults.column,
        r = e.aoColumns.length;
      (a = t.extend({}, Wt.models.oColumn, a, {
        nTh: n || i.createElement("th"),
        sTitle: a.sTitle ? a.sTitle : n ? n.innerHTML : "",
        aDataSort: a.aDataSort ? a.aDataSort : [r],
        mData: a.mData ? a.mData : r,
        idx: r,
      })),
        e.aoColumns.push(a),
        ((a = e.aoPreSearchCols)[r] = t.extend({}, Wt.models.oSearch, a[r])),
        h(e, r, t(n).data());
    }
    function h(e, i, a) {
      i = e.aoColumns[i];
      var s = e.oClasses,
        o = t(i.nTh);
      if (!i.sWidthOrig) {
        i.sWidthOrig = o.attr("width") || null;
        var u = (o.attr("style") || "").match(/width:\s*(\d+[pxem%]+)/);
        u && (i.sWidthOrig = u[1]);
      }
      a !== n &&
        null !== a &&
        (l(a),
        r(Wt.defaults.column, a, !0),
        a.mDataProp === n || a.mData || (a.mData = a.mDataProp),
        a.sType && (i._sManualType = a.sType),
        a.className && !a.sClass && (a.sClass = a.className),
        a.sClass && o.addClass(a.sClass),
        t.extend(i, a),
        Mt(i, a, "sWidth", "sWidthOrig"),
        a.iDataSort !== n && (i.aDataSort = [a.iDataSort]),
        Mt(i, a, "aDataSort"));
      var c = i.mData,
        d = he(c),
        h = i.mRender ? he(i.mRender) : null;
      (a = function (t) {
        return "string" == typeof t && -1 !== t.indexOf("@");
      }),
        (i._bAttrSrc =
          t.isPlainObject(c) && (a(c.sort) || a(c.type) || a(c.filter))),
        (i._setter = null),
        (i.fnGetData = function (t, e, i) {
          var a = d(t, e, n, i);
          return h && e ? h(a, e, t, i) : a;
        }),
        (i.fnSetData = function (t, e, i) {
          return pe(c)(t, e, i);
        }),
        "number" != typeof c && (e._rowReadObject = !0),
        e.oFeatures.bSort || ((i.bSortable = !1), o.addClass(s.sSortableNone)),
        (e = -1 !== t.inArray("asc", i.asSorting)),
        (a = -1 !== t.inArray("desc", i.asSorting)),
        i.bSortable && (e || a)
          ? e && !a
            ? ((i.sSortingClass = s.sSortableAsc),
              (i.sSortingClassJUI = s.sSortJUIAscAllowed))
            : !e && a
            ? ((i.sSortingClass = s.sSortableDesc),
              (i.sSortingClassJUI = s.sSortJUIDescAllowed))
            : ((i.sSortingClass = s.sSortable),
              (i.sSortingClassJUI = s.sSortJUI))
          : ((i.sSortingClass = s.sSortableNone), (i.sSortingClassJUI = ""));
    }
    function p(t) {
      if (!1 !== t.oFeatures.bAutoWidth) {
        var e = t.aoColumns;
        pt(t);
        for (var i = 0, n = e.length; i < n; i++)
          e[i].nTh.style.width = e[i].sWidth;
      }
      ("" === (e = t.oScroll).sY && "" === e.sX) || dt(t),
        zt(t, null, "column-sizing", [t]);
    }
    function f(t, e) {
      return "number" == typeof (t = v(t, "bVisible"))[e] ? t[e] : null;
    }
    function m(e, i) {
      return (e = v(e, "bVisible")), -1 !== (i = t.inArray(i, e)) ? i : null;
    }
    function g(e) {
      var i = 0;
      return (
        t.each(e.aoColumns, function (e, n) {
          n.bVisible && "none" !== t(n.nTh).css("display") && i++;
        }),
        i
      );
    }
    function v(e, i) {
      var n = [];
      return (
        t.map(e.aoColumns, function (t, e) {
          t[i] && n.push(e);
        }),
        n
      );
    }
    function _(t) {
      var e,
        i,
        a,
        r = t.aoColumns,
        s = t.aoData,
        o = Wt.ext.type.detect,
        l = 0;
      for (e = r.length; l < e; l++) {
        var u = r[l],
          c = [];
        if (!u.sType && u._sManualType) u.sType = u._sManualType;
        else if (!u.sType) {
          var d = 0;
          for (i = o.length; d < i; d++) {
            var h = 0;
            for (a = s.length; h < a; h++) {
              c[h] === n && (c[h] = x(t, h, l, "type"));
              var p = o[d](c[h], t);
              if (!p && d !== o.length - 1) break;
              if ("html" === p && !Kt(c[h])) break;
            }
            if (p) {
              u.sType = p;
              break;
            }
          }
          u.sType || (u.sType = "string");
        }
      }
    }
    function y(e, i, a, r) {
      var s,
        o,
        l,
        u = e.aoColumns;
      if (i)
        for (s = i.length - 1; 0 <= s; s--) {
          var c = i[s],
            h = c.targets !== n ? c.targets : c.aTargets;
          Array.isArray(h) || (h = [h]);
          var p = 0;
          for (o = h.length; p < o; p++)
            if ("number" == typeof h[p] && 0 <= h[p]) {
              for (; u.length <= h[p]; ) d(e);
              r(h[p], c);
            } else if ("number" == typeof h[p] && 0 > h[p])
              r(u.length + h[p], c);
            else if ("string" == typeof h[p]) {
              var f = 0;
              for (l = u.length; f < l; f++)
                ("_all" == h[p] || t(u[f].nTh).hasClass(h[p])) && r(f, c);
            }
        }
      if (a) for (s = 0, e = a.length; s < e; s++) r(s, a[s]);
    }
    function b(e, i, a, r) {
      var s = e.aoData.length,
        o = t.extend(!0, {}, Wt.models.oRow, {
          src: a ? "dom" : "data",
          idx: s,
        });
      (o._aData = i), e.aoData.push(o);
      for (var l = e.aoColumns, u = 0, c = l.length; u < c; u++)
        l[u].sType = null;
      return (
        e.aiDisplayMaster.push(s),
        (i = e.rowIdFn(i)) !== n && (e.aIds[i] = o),
        (!a && e.oFeatures.bDeferRender) || M(e, s, a, r),
        s
      );
    }
    function w(e, i) {
      var n;
      return (
        i instanceof t || (i = t(i)),
        i.map(function (t, i) {
          return (n = D(e, i)), b(e, n.data, i, n.cells);
        })
      );
    }
    function x(t, e, i, a) {
      "search" === a ? (a = "filter") : "order" === a && (a = "sort");
      var r = t.iDraw,
        s = t.aoColumns[i],
        o = t.aoData[e]._aData,
        l = s.sDefaultContent,
        u = s.fnGetData(o, a, { settings: t, row: e, col: i });
      if (u === n)
        return (
          t.iDrawError != r &&
            null === l &&
            (Dt(
              t,
              0,
              "Requested unknown parameter " +
                ("function" == typeof s.mData
                  ? "{function}"
                  : "'" + s.mData + "'") +
                " for row " +
                e +
                ", column " +
                i,
              4
            ),
            (t.iDrawError = r)),
          l
        );
      if ((u !== o && null !== u) || null === l || a === n) {
        if ("function" == typeof u) return u.call(o);
      } else u = l;
      return null === u && "display" === a
        ? ""
        : ("filter" === a &&
            (t = Wt.ext.type.search)[s.sType] &&
            (u = t[s.sType](u)),
          u);
    }
    function S(t, e, i, n) {
      t.aoColumns[i].fnSetData(t.aoData[e]._aData, n, {
        settings: t,
        row: e,
        col: i,
      });
    }
    function T(e) {
      return t.map(e.match(/(\\.|[^\.])+/g) || [""], function (t) {
        return t.replace(/\\\./g, ".");
      });
    }
    function C(t) {
      return ie(t.aoData, "_aData");
    }
    function E(t) {
      (t.aoData.length = 0),
        (t.aiDisplayMaster.length = 0),
        (t.aiDisplay.length = 0),
        (t.aIds = {});
    }
    function P(t, e, i) {
      for (var a = -1, r = 0, s = t.length; r < s; r++)
        t[r] == e ? (a = r) : t[r] > e && t[r]--;
      -1 != a && i === n && t.splice(a, 1);
    }
    function L(t, e, i, a) {
      var r,
        s = t.aoData[e],
        o = function (i, n) {
          for (; i.childNodes.length; ) i.removeChild(i.firstChild);
          i.innerHTML = x(t, e, n, "display");
        };
      if ("dom" !== i && ((i && "auto" !== i) || "dom" !== s.src)) {
        var l = s.anCells;
        if (l)
          if (a !== n) o(l[a], a);
          else for (i = 0, r = l.length; i < r; i++) o(l[i], i);
      } else s._aData = D(t, s, a, a === n ? n : s._aData).data;
      if (
        ((s._aSortData = null),
        (s._aFilterData = null),
        (o = t.aoColumns),
        a !== n)
      )
        o[a].sType = null;
      else {
        for (i = 0, r = o.length; i < r; i++) o[i].sType = null;
        k(t, s);
      }
    }
    function D(t, e, i, a) {
      var r,
        s,
        o = [],
        l = e.firstChild,
        u = 0,
        c = t.aoColumns,
        d = t._rowReadObject;
      a = a !== n ? a : d ? {} : [];
      var h = function (t, e) {
          if ("string" == typeof t) {
            var i = t.indexOf("@");
            -1 !== i && ((i = t.substring(i + 1)), pe(t)(a, e.getAttribute(i)));
          }
        },
        p = function (t) {
          (i !== n && i !== u) ||
            ((r = c[u]),
            (s = t.innerHTML.trim()),
            r && r._bAttrSrc
              ? (pe(r.mData._)(a, s),
                h(r.mData.sort, t),
                h(r.mData.type, t),
                h(r.mData.filter, t))
              : d
              ? (r._setter || (r._setter = pe(r.mData)), r._setter(a, s))
              : (a[u] = s)),
            u++;
        };
      if (l)
        for (; l; ) {
          var f = l.nodeName.toUpperCase();
          ("TD" != f && "TH" != f) || (p(l), o.push(l)), (l = l.nextSibling);
        }
      else for (l = 0, f = (o = e.anCells).length; l < f; l++) p(o[l]);
      return (
        (e = e.firstChild ? e : e.nTr) &&
          (e = e.getAttribute("id")) &&
          pe(t.rowId)(a, e),
        { data: a, cells: o }
      );
    }
    function M(e, n, a, r) {
      var s,
        o,
        l = e.aoData[n],
        u = l._aData,
        c = [];
      if (null === l.nTr) {
        var d = a || i.createElement("tr");
        (l.nTr = d), (l.anCells = c), (d._DT_RowIndex = n), k(e, l);
        var h = 0;
        for (s = e.aoColumns.length; h < s; h++) {
          var p = e.aoColumns[h];
          ((l = (o = !a) ? i.createElement(p.sCellType) : r[h])._DT_CellIndex =
            { row: n, column: h }),
            c.push(l),
            (!o &&
              ((!p.mRender && p.mData === h) ||
                (t.isPlainObject(p.mData) && p.mData._ === h + ".display"))) ||
              (l.innerHTML = x(e, n, h, "display")),
            p.sClass && (l.className += " " + p.sClass),
            p.bVisible && !a
              ? d.appendChild(l)
              : !p.bVisible && a && l.parentNode.removeChild(l),
            p.fnCreatedCell &&
              p.fnCreatedCell.call(e.oInstance, l, x(e, n, h), u, n, h);
        }
        zt(e, "aoRowCreatedCallback", null, [d, u, n, c]);
      }
    }
    function k(e, i) {
      var n = i.nTr,
        a = i._aData;
      n &&
        ((e = e.rowIdFn(a)) && (n.id = e),
        a.DT_RowClass &&
          ((e = a.DT_RowClass.split(" ")),
          (i.__rowc = i.__rowc ? se(i.__rowc.concat(e)) : e),
          t(n).removeClass(i.__rowc.join(" ")).addClass(a.DT_RowClass)),
        a.DT_RowAttr && t(n).attr(a.DT_RowAttr),
        a.DT_RowData && t(n).data(a.DT_RowData));
    }
    function A(e) {
      var i,
        n,
        a = e.nTHead,
        r = e.nTFoot,
        s = 0 === t("th, td", a).length,
        o = e.oClasses,
        l = e.aoColumns;
      s && (n = t("<tr/>").appendTo(a));
      var u = 0;
      for (i = l.length; u < i; u++) {
        var c = l[u],
          d = t(c.nTh).addClass(c.sClass);
        s && d.appendTo(n),
          e.oFeatures.bSort &&
            (d.addClass(c.sSortingClass),
            !1 !== c.bSortable &&
              (d
                .attr("tabindex", e.iTabIndex)
                .attr("aria-controls", e.sTableId),
              xt(e, c.nTh, u))),
          c.sTitle != d[0].innerHTML && d.html(c.sTitle),
          Nt(e, "header")(e, d, c, o);
      }
      if (
        (s && B(e.aoHeader, a),
        t(a).children("tr").children("th, td").addClass(o.sHeaderTH),
        t(r).children("tr").children("th, td").addClass(o.sFooterTH),
        null !== r)
      )
        for (u = 0, i = (e = e.aoFooter[0]).length; u < i; u++)
          ((c = l[u]).nTf = e[u].cell), c.sClass && t(c.nTf).addClass(c.sClass);
    }
    function I(e, i, a) {
      var r,
        s,
        o = [],
        l = [],
        u = e.aoColumns.length;
      if (i) {
        a === n && (a = !1);
        var c = 0;
        for (r = i.length; c < r; c++) {
          for (o[c] = i[c].slice(), o[c].nTr = i[c].nTr, s = u - 1; 0 <= s; s--)
            e.aoColumns[s].bVisible || a || o[c].splice(s, 1);
          l.push([]);
        }
        for (c = 0, r = o.length; c < r; c++) {
          if ((e = o[c].nTr)) for (; (s = e.firstChild); ) e.removeChild(s);
          for (s = 0, i = o[c].length; s < i; s++) {
            var d = (u = 1);
            if (l[c][s] === n) {
              for (
                e.appendChild(o[c][s].cell), l[c][s] = 1;
                o[c + u] !== n && o[c][s].cell == o[c + u][s].cell;

              )
                (l[c + u][s] = 1), u++;
              for (; o[c][s + d] !== n && o[c][s].cell == o[c][s + d].cell; ) {
                for (a = 0; a < u; a++) l[c + a][s + d] = 1;
                d++;
              }
              t(o[c][s].cell).attr("rowspan", u).attr("colspan", d);
            }
          }
        }
      }
    }
    function z(e, i) {
      var a = zt(e, "aoPreDrawCallback", "preDraw", [e]);
      if (-1 !== t.inArray(!1, a)) ut(e, !1);
      else {
        a = [];
        var r = 0,
          s = e.asStripeClasses,
          o = s.length,
          l = e.oLanguage,
          u = e.iInitDisplayStart,
          c = "ssp" == Bt(e),
          d = e.aiDisplay;
        (e.bDrawing = !0),
          u !== n &&
            -1 !== u &&
            ((e._iDisplayStart = c ? u : u >= e.fnRecordsDisplay() ? 0 : u),
            (e.iInitDisplayStart = -1)),
          (u = e._iDisplayStart);
        var h = e.fnDisplayEnd();
        if (e.bDeferLoading) (e.bDeferLoading = !1), e.iDraw++, ut(e, !1);
        else if (c) {
          if (!e.bDestroying && !i) return void R(e);
        } else e.iDraw++;
        if (0 !== d.length)
          for (i = c ? e.aoData.length : h, l = c ? 0 : u; l < i; l++) {
            c = d[l];
            var p = e.aoData[c];
            null === p.nTr && M(e, c);
            var f = p.nTr;
            if (0 !== o) {
              var m = s[r % o];
              p._sRowStripe != m &&
                (t(f).removeClass(p._sRowStripe).addClass(m),
                (p._sRowStripe = m));
            }
            zt(e, "aoRowCallback", null, [f, p._aData, r, l, c]),
              a.push(f),
              r++;
          }
        else
          (r = l.sZeroRecords),
            1 == e.iDraw && "ajax" == Bt(e)
              ? (r = l.sLoadingRecords)
              : l.sEmptyTable &&
                0 === e.fnRecordsTotal() &&
                (r = l.sEmptyTable),
            (a[0] = t("<tr/>", { class: o ? s[0] : "" }).append(
              t("<td />", {
                valign: "top",
                colSpan: g(e),
                class: e.oClasses.sRowEmpty,
              }).html(r)
            )[0]);
        zt(e, "aoHeaderCallback", "header", [
          t(e.nTHead).children("tr")[0],
          C(e),
          u,
          h,
          d,
        ]),
          zt(e, "aoFooterCallback", "footer", [
            t(e.nTFoot).children("tr")[0],
            C(e),
            u,
            h,
            d,
          ]),
          (s = t(e.nTBody)).children().detach(),
          s.append(t(a)),
          zt(e, "aoDrawCallback", "draw", [e]),
          (e.bSorted = !1),
          (e.bFiltered = !1),
          (e.bDrawing = !1);
      }
    }
    function O(t, e) {
      var i = t.oFeatures,
        n = i.bFilter;
      i.bSort && yt(t),
        n ? Z(t, t.oPreviousSearch) : (t.aiDisplay = t.aiDisplayMaster.slice()),
        !0 !== e && (t._iDisplayStart = 0),
        (t._drawHold = e),
        z(t),
        (t._drawHold = !1);
    }
    function N(e) {
      var i = e.oClasses,
        n = t(e.nTable);
      n = t("<div/>").insertBefore(n);
      var a = e.oFeatures,
        r = t("<div/>", {
          id: e.sTableId + "_wrapper",
          class: i.sWrapper + (e.nTFoot ? "" : " " + i.sNoFooter),
        });
      (e.nHolding = n[0]),
        (e.nTableWrapper = r[0]),
        (e.nTableReinsertBefore = e.nTable.nextSibling);
      for (
        var s, o, l, u, c, d, h = e.sDom.split(""), p = 0;
        p < h.length;
        p++
      ) {
        if (((s = null), "<" == (o = h[p]))) {
          if (((l = t("<div/>")[0]), "'" == (u = h[p + 1]) || '"' == u)) {
            for (c = "", d = 2; h[p + d] != u; ) (c += h[p + d]), d++;
            "H" == c ? (c = i.sJUIHeader) : "F" == c && (c = i.sJUIFooter),
              -1 != c.indexOf(".")
                ? ((u = c.split(".")),
                  (l.id = u[0].substr(1, u[0].length - 1)),
                  (l.className = u[1]))
                : "#" == c.charAt(0)
                ? (l.id = c.substr(1, c.length - 1))
                : (l.className = c),
              (p += d);
          }
          r.append(l), (r = t(l));
        } else if (">" == o) r = r.parent();
        else if ("l" == o && a.bPaginate && a.bLengthChange) s = rt(e);
        else if ("f" == o && a.bFilter) s = W(e);
        else if ("r" == o && a.bProcessing) s = lt(e);
        else if ("t" == o) s = ct(e);
        else if ("i" == o && a.bInfo) s = Q(e);
        else if ("p" == o && a.bPaginate) s = st(e);
        else if (0 !== Wt.ext.feature.length)
          for (d = 0, u = (l = Wt.ext.feature).length; d < u; d++)
            if (o == l[d].cFeature) {
              s = l[d].fnInit(e);
              break;
            }
        s && ((l = e.aanFeatures)[o] || (l[o] = []), l[o].push(s), r.append(s));
      }
      n.replaceWith(r), (e.nHolding = null);
    }
    function B(e, i) {
      var n, a, r;
      (i = t(i).children("tr")), e.splice(0, e.length);
      var s = 0;
      for (r = i.length; s < r; s++) e.push([]);
      for (s = 0, r = i.length; s < r; s++) {
        var o = i[s];
        for (n = o.firstChild; n; ) {
          if (
            "TD" == n.nodeName.toUpperCase() ||
            "TH" == n.nodeName.toUpperCase()
          ) {
            var l = 1 * n.getAttribute("colspan"),
              u = 1 * n.getAttribute("rowspan");
            (l = l && 0 !== l && 1 !== l ? l : 1),
              (u = u && 0 !== u && 1 !== u ? u : 1);
            var c = 0;
            for (a = e[s]; a[c]; ) c++;
            var d = c,
              h = 1 === l;
            for (a = 0; a < l; a++)
              for (c = 0; c < u; c++)
                (e[s + c][d + a] = { cell: n, unique: h }), (e[s + c].nTr = o);
          }
          n = n.nextSibling;
        }
      }
    }
    function j(t, e, i) {
      var n = [];
      i || ((i = t.aoHeader), e && B((i = []), e)), (e = 0);
      for (var a = i.length; e < a; e++)
        for (var r = 0, s = i[e].length; r < s; r++)
          !i[e][r].unique || (n[r] && t.bSortCellsTop) || (n[r] = i[e][r].cell);
      return n;
    }
    function F(e, i, n) {
      if (
        (zt(e, "aoServerParams", "serverParams", [i]), i && Array.isArray(i))
      ) {
        var a = {},
          r = /(.*?)\[\]$/;
        t.each(i, function (t, e) {
          (t = e.name.match(r))
            ? ((t = t[0]), a[t] || (a[t] = []), a[t].push(e.value))
            : (a[e.name] = e.value);
        }),
          (i = a);
      }
      var s = e.ajax,
        o = e.oInstance,
        l = function (t) {
          var i = e.jqXhr ? e.jqXhr.status : null;
          (null === t || ("number" == typeof i && 204 == i)) &&
            V(e, (t = {}), []),
            (i = t.error || t.sError) && Dt(e, 0, i),
            (e.json = t),
            zt(e, null, "xhr", [e, t, e.jqXHR]),
            n(t);
        };
      if (t.isPlainObject(s) && s.data) {
        var u = s.data,
          c = "function" == typeof u ? u(i, e) : u;
        (i = "function" == typeof u && c ? c : t.extend(!0, i, c)),
          delete s.data;
      }
      (c = {
        data: i,
        success: l,
        dataType: "json",
        cache: !1,
        type: e.sServerMethod,
        error: function (i, n, a) {
          (a = zt(e, null, "xhr", [e, null, e.jqXHR])),
            -1 === t.inArray(!0, a) &&
              ("parsererror" == n
                ? Dt(e, 0, "Invalid JSON response", 1)
                : 4 === i.readyState && Dt(e, 0, "Ajax error", 7)),
            ut(e, !1);
        },
      }),
        (e.oAjaxData = i),
        zt(e, null, "preXhr", [e, i]),
        e.fnServerData
          ? e.fnServerData.call(
              o,
              e.sAjaxSource,
              t.map(i, function (t, e) {
                return { name: e, value: t };
              }),
              l,
              e
            )
          : e.sAjaxSource || "string" == typeof s
          ? (e.jqXHR = t.ajax(t.extend(c, { url: s || e.sAjaxSource })))
          : "function" == typeof s
          ? (e.jqXHR = s.call(o, i, l, e))
          : ((e.jqXHR = t.ajax(t.extend(c, s))), (s.data = u));
    }
    function R(t) {
      t.iDraw++,
        ut(t, !0),
        F(t, $(t), function (e) {
          H(t, e);
        });
    }
    function $(e) {
      var i = e.aoColumns,
        n = i.length,
        a = e.oFeatures,
        r = e.oPreviousSearch,
        s = e.aoPreSearchCols,
        o = [],
        l = _t(e),
        u = e._iDisplayStart,
        c = !1 !== a.bPaginate ? e._iDisplayLength : -1,
        d = function (t, e) {
          o.push({ name: t, value: e });
        };
      d("sEcho", e.iDraw),
        d("iColumns", n),
        d("sColumns", ie(i, "sName").join(",")),
        d("iDisplayStart", u),
        d("iDisplayLength", c);
      var h = {
        draw: e.iDraw,
        columns: [],
        order: [],
        start: u,
        length: c,
        search: { value: r.sSearch, regex: r.bRegex },
      };
      for (u = 0; u < n; u++) {
        var p = i[u],
          f = s[u];
        (c = "function" == typeof p.mData ? "function" : p.mData),
          h.columns.push({
            data: c,
            name: p.sName,
            searchable: p.bSearchable,
            orderable: p.bSortable,
            search: { value: f.sSearch, regex: f.bRegex },
          }),
          d("mDataProp_" + u, c),
          a.bFilter &&
            (d("sSearch_" + u, f.sSearch),
            d("bRegex_" + u, f.bRegex),
            d("bSearchable_" + u, p.bSearchable)),
          a.bSort && d("bSortable_" + u, p.bSortable);
      }
      return (
        a.bFilter && (d("sSearch", r.sSearch), d("bRegex", r.bRegex)),
        a.bSort &&
          (t.each(l, function (t, e) {
            h.order.push({ column: e.col, dir: e.dir }),
              d("iSortCol_" + t, e.col),
              d("sSortDir_" + t, e.dir);
          }),
          d("iSortingCols", l.length)),
        null === (i = Wt.ext.legacy.ajax) ? (e.sAjaxSource ? o : h) : i ? o : h
      );
    }
    function H(t, e) {
      var i = function (t, i) {
          return e[t] !== n ? e[t] : e[i];
        },
        a = V(t, e),
        r = i("sEcho", "draw"),
        s = i("iTotalRecords", "recordsTotal");
      if (((i = i("iTotalDisplayRecords", "recordsFiltered")), r !== n)) {
        if (1 * r < t.iDraw) return;
        t.iDraw = 1 * r;
      }
      for (
        a || (a = []),
          E(t),
          t._iRecordsTotal = parseInt(s, 10),
          t._iRecordsDisplay = parseInt(i, 10),
          r = 0,
          s = a.length;
        r < s;
        r++
      )
        b(t, a[r]);
      (t.aiDisplay = t.aiDisplayMaster.slice()),
        z(t, !0),
        t._bInitComplete || nt(t, e),
        ut(t, !1);
    }
    function V(e, i, a) {
      if (
        ((e =
          t.isPlainObject(e.ajax) && e.ajax.dataSrc !== n
            ? e.ajax.dataSrc
            : e.sAjaxDataProp),
        !a)
      )
        return "data" === e ? i.aaData || i[e] : "" !== e ? he(e)(i) : i;
      pe(e)(i, a);
    }
    function W(e) {
      var n = e.oClasses,
        a = e.sTableId,
        r = e.oLanguage,
        s = e.oPreviousSearch,
        o = e.aanFeatures,
        l = '<input type="search" class="' + n.sFilterInput + '"/>',
        u = r.sSearch;
      (u = u.match(/_INPUT_/) ? u.replace("_INPUT_", l) : u + l),
        (n = t("<div/>", {
          id: o.f ? null : a + "_filter",
          class: n.sFilter,
        }).append(t("<label/>").append(u)));
      var c = function (t) {
        var i = this.value ? this.value : "";
        (s.return && "Enter" !== t.key) ||
          i == s.sSearch ||
          (Z(e, {
            sSearch: i,
            bRegex: s.bRegex,
            bSmart: s.bSmart,
            bCaseInsensitive: s.bCaseInsensitive,
            return: s.return,
          }),
          (e._iDisplayStart = 0),
          z(e));
      };
      o = null !== e.searchDelay ? e.searchDelay : "ssp" === Bt(e) ? 400 : 0;
      var d = t("input", n)
        .val(s.sSearch)
        .attr("placeholder", r.sSearchPlaceholder)
        .on("keyup.DT search.DT input.DT paste.DT cut.DT", o ? _e(c, o) : c)
        .on("mouseup", function (t) {
          setTimeout(function () {
            c.call(d[0], t);
          }, 10);
        })
        .on("keypress.DT", function (t) {
          if (13 == t.keyCode) return !1;
        })
        .attr("aria-controls", a);
      return (
        t(e.nTable).on("search.dt.DT", function (t, n) {
          if (e === n)
            try {
              d[0] !== i.activeElement && d.val(s.sSearch);
            } catch (t) {}
        }),
        n[0]
      );
    }
    function Z(t, e, i) {
      var a = t.oPreviousSearch,
        r = t.aoPreSearchCols,
        s = function (t) {
          (a.sSearch = t.sSearch),
            (a.bRegex = t.bRegex),
            (a.bSmart = t.bSmart),
            (a.bCaseInsensitive = t.bCaseInsensitive),
            (a.return = t.return);
        },
        o = function (t) {
          return t.bEscapeRegex !== n ? !t.bEscapeRegex : t.bRegex;
        };
      if ((_(t), "ssp" != Bt(t))) {
        for (
          Y(t, e.sSearch, i, o(e), e.bSmart, e.bCaseInsensitive, e.return),
            s(e),
            e = 0;
          e < r.length;
          e++
        )
          U(t, r[e].sSearch, e, o(r[e]), r[e].bSmart, r[e].bCaseInsensitive);
        G(t);
      } else s(e);
      (t.bFiltered = !0), zt(t, null, "search", [t]);
    }
    function G(e) {
      for (
        var i, n, a = Wt.ext.search, r = e.aiDisplay, s = 0, o = a.length;
        s < o;
        s++
      ) {
        for (var l = [], u = 0, c = r.length; u < c; u++)
          (n = r[u]),
            (i = e.aoData[n]),
            a[s](e, i._aFilterData, n, i._aData, u) && l.push(n);
        (r.length = 0), t.merge(r, l);
      }
    }
    function U(t, e, i, n, a, r) {
      if ("" !== e) {
        var s = [],
          o = t.aiDisplay;
        for (n = q(e, n, a, r), a = 0; a < o.length; a++)
          (e = t.aoData[o[a]]._aFilterData[i]), n.test(e) && s.push(o[a]);
        t.aiDisplay = s;
      }
    }
    function Y(t, e, i, n, a, r) {
      a = q(e, n, a, r);
      var s = t.oPreviousSearch.sSearch,
        o = t.aiDisplayMaster;
      (r = []), 0 !== Wt.ext.search.length && (i = !0);
      var l = X(t);
      if (0 >= e.length) t.aiDisplay = o.slice();
      else {
        for (
          (l ||
            i ||
            n ||
            s.length > e.length ||
            0 !== e.indexOf(s) ||
            t.bSorted) &&
            (t.aiDisplay = o.slice()),
            e = t.aiDisplay,
            i = 0;
          i < e.length;
          i++
        )
          a.test(t.aoData[e[i]]._sFilterRow) && r.push(e[i]);
        t.aiDisplay = r;
      }
    }
    function q(e, i, n, a) {
      return (
        (e = i ? e : fe(e)),
        n &&
          (e =
            "^(?=.*?" +
            t
              .map(e.match(/"[^"]+"|[^ ]+/g) || [""], function (t) {
                if ('"' === t.charAt(0)) {
                  var e = t.match(/^"(.*)"$/);
                  t = e ? e[1] : t;
                }
                return t.replace('"', "");
              })
              .join(")(?=.*?") +
            ").*$"),
        new RegExp(e, a ? "i" : "")
      );
    }
    function X(t) {
      var e,
        i,
        n = t.aoColumns,
        a = !1,
        r = 0;
      for (e = t.aoData.length; r < e; r++) {
        var s = t.aoData[r];
        if (!s._aFilterData) {
          var o = [];
          for (a = 0, i = n.length; a < i; a++) {
            var l = n[a];
            l.bSearchable
              ? (null === (l = x(t, r, a, "filter")) && (l = ""),
                "string" != typeof l && l.toString && (l = l.toString()))
              : (l = ""),
              l.indexOf &&
                -1 !== l.indexOf("&") &&
                ((me.innerHTML = l), (l = ge ? me.textContent : me.innerText)),
              l.replace && (l = l.replace(/[\r\n\u2028]/g, "")),
              o.push(l);
          }
          (s._aFilterData = o), (s._sFilterRow = o.join("  ")), (a = !0);
        }
      }
      return a;
    }
    function K(t) {
      return {
        search: t.sSearch,
        smart: t.bSmart,
        regex: t.bRegex,
        caseInsensitive: t.bCaseInsensitive,
      };
    }
    function J(t) {
      return {
        sSearch: t.search,
        bSmart: t.smart,
        bRegex: t.regex,
        bCaseInsensitive: t.caseInsensitive,
      };
    }
    function Q(e) {
      var i = e.sTableId,
        n = e.aanFeatures.i,
        a = t("<div/>", {
          class: e.oClasses.sInfo,
          id: n ? null : i + "_info",
        });
      return (
        n ||
          (e.aoDrawCallback.push({ fn: tt, sName: "information" }),
          a.attr("role", "status").attr("aria-live", "polite"),
          t(e.nTable).attr("aria-describedby", i + "_info")),
        a[0]
      );
    }
    function tt(e) {
      var i = e.aanFeatures.i;
      if (0 !== i.length) {
        var n = e.oLanguage,
          a = e._iDisplayStart + 1,
          r = e.fnDisplayEnd(),
          s = e.fnRecordsTotal(),
          o = e.fnRecordsDisplay(),
          l = o ? n.sInfo : n.sInfoEmpty;
        o !== s && (l += " " + n.sInfoFiltered),
          (l = et(e, (l += n.sInfoPostFix))),
          null !== (n = n.fnInfoCallback) &&
            (l = n.call(e.oInstance, e, a, r, s, o, l)),
          t(i).html(l);
      }
    }
    function et(t, e) {
      var i = t.fnFormatNumber,
        n = t._iDisplayStart + 1,
        a = t._iDisplayLength,
        r = t.fnRecordsDisplay(),
        s = -1 === a;
      return e
        .replace(/_START_/g, i.call(t, n))
        .replace(/_END_/g, i.call(t, t.fnDisplayEnd()))
        .replace(/_MAX_/g, i.call(t, t.fnRecordsTotal()))
        .replace(/_TOTAL_/g, i.call(t, r))
        .replace(/_PAGE_/g, i.call(t, s ? 1 : Math.ceil(n / a)))
        .replace(/_PAGES_/g, i.call(t, s ? 1 : Math.ceil(r / a)));
    }
    function it(t) {
      var e = t.iInitDisplayStart,
        i = t.aoColumns,
        n = t.oFeatures,
        a = t.bDeferLoading;
      if (t.bInitialised) {
        N(t),
          A(t),
          I(t, t.aoHeader),
          I(t, t.aoFooter),
          ut(t, !0),
          n.bAutoWidth && pt(t);
        var r = 0;
        for (n = i.length; r < n; r++) {
          var s = i[r];
          s.sWidth && (s.nTh.style.width = vt(s.sWidth));
        }
        zt(t, null, "preInit", [t]),
          O(t),
          ("ssp" != (i = Bt(t)) || a) &&
            ("ajax" == i
              ? F(t, [], function (i) {
                  var n = V(t, i);
                  for (r = 0; r < n.length; r++) b(t, n[r]);
                  (t.iInitDisplayStart = e), O(t), ut(t, !1), nt(t, i);
                })
              : (ut(t, !1), nt(t)));
      } else
        setTimeout(function () {
          it(t);
        }, 200);
    }
    function nt(t, e) {
      (t._bInitComplete = !0),
        (e || t.oInit.aaData) && p(t),
        zt(t, null, "plugin-init", [t, e]),
        zt(t, "aoInitComplete", "init", [t, e]);
    }
    function at(t, e) {
      (e = parseInt(e, 10)),
        (t._iDisplayLength = e),
        Ot(t),
        zt(t, null, "length", [t, e]);
    }
    function rt(e) {
      var i = e.oClasses,
        n = e.sTableId,
        a = e.aLengthMenu,
        r = Array.isArray(a[0]),
        s = r ? a[0] : a;
      (a = r ? a[1] : a),
        (r = t("<select/>", {
          name: n + "_length",
          "aria-controls": n,
          class: i.sLengthSelect,
        }));
      for (var o = 0, l = s.length; o < l; o++)
        r[0][o] = new Option(
          "number" == typeof a[o] ? e.fnFormatNumber(a[o]) : a[o],
          s[o]
        );
      var u = t("<div><label/></div>").addClass(i.sLength);
      return (
        e.aanFeatures.l || (u[0].id = n + "_length"),
        u
          .children()
          .append(e.oLanguage.sLengthMenu.replace("_MENU_", r[0].outerHTML)),
        t("select", u)
          .val(e._iDisplayLength)
          .on("change.DT", function (i) {
            at(e, t(this).val()), z(e);
          }),
        t(e.nTable).on("length.dt.DT", function (i, n, a) {
          e === n && t("select", u).val(a);
        }),
        u[0]
      );
    }
    function st(e) {
      var i = e.sPaginationType,
        n = Wt.ext.pager[i],
        a = "function" == typeof n,
        r = function (t) {
          z(t);
        };
      i = t("<div/>").addClass(e.oClasses.sPaging + i)[0];
      var s = e.aanFeatures;
      return (
        a || n.fnInit(e, i, r),
        s.p ||
          ((i.id = e.sTableId + "_paginate"),
          e.aoDrawCallback.push({
            fn: function (t) {
              if (a) {
                var e,
                  i = t._iDisplayStart,
                  o = t._iDisplayLength,
                  l = t.fnRecordsDisplay(),
                  u = -1 === o;
                for (
                  i = u ? 0 : Math.ceil(i / o),
                    o = u ? 1 : Math.ceil(l / o),
                    l = n(i, o),
                    u = 0,
                    e = s.p.length;
                  u < e;
                  u++
                )
                  Nt(t, "pageButton")(t, s.p[u], u, l, i, o);
              } else n.fnUpdate(t, r);
            },
            sName: "pagination",
          })),
        i
      );
    }
    function ot(t, e, i) {
      var n = t._iDisplayStart,
        a = t._iDisplayLength,
        r = t.fnRecordsDisplay();
      return (
        0 === r || -1 === a
          ? (n = 0)
          : "number" == typeof e
          ? (n = e * a) > r && (n = 0)
          : "first" == e
          ? (n = 0)
          : "previous" == e
          ? 0 > (n = 0 <= a ? n - a : 0) && (n = 0)
          : "next" == e
          ? n + a < r && (n += a)
          : "last" == e
          ? (n = Math.floor((r - 1) / a) * a)
          : Dt(t, 0, "Unknown paging action: " + e, 5),
        (e = t._iDisplayStart !== n),
        (t._iDisplayStart = n),
        e && (zt(t, null, "page", [t]), i && z(t)),
        e
      );
    }
    function lt(e) {
      return t("<div/>", {
        id: e.aanFeatures.r ? null : e.sTableId + "_processing",
        class: e.oClasses.sProcessing,
      })
        .html(e.oLanguage.sProcessing)
        .insertBefore(e.nTable)[0];
    }
    function ut(e, i) {
      e.oFeatures.bProcessing &&
        t(e.aanFeatures.r).css("display", i ? "block" : "none"),
        zt(e, null, "processing", [e, i]);
    }
    function ct(e) {
      var i = t(e.nTable),
        n = e.oScroll;
      if ("" === n.sX && "" === n.sY) return e.nTable;
      var a = n.sX,
        r = n.sY,
        s = e.oClasses,
        o = i.children("caption"),
        l = o.length ? o[0]._captionSide : null,
        u = t(i[0].cloneNode(!1)),
        c = t(i[0].cloneNode(!1)),
        d = i.children("tfoot");
      d.length || (d = null),
        (u = t("<div/>", { class: s.sScrollWrapper })
          .append(
            t("<div/>", { class: s.sScrollHead })
              .css({
                overflow: "hidden",
                position: "relative",
                border: 0,
                width: a ? (a ? vt(a) : null) : "100%",
              })
              .append(
                t("<div/>", { class: s.sScrollHeadInner })
                  .css({
                    "box-sizing": "content-box",
                    width: n.sXInner || "100%",
                  })
                  .append(
                    u
                      .removeAttr("id")
                      .css("margin-left", 0)
                      .append("top" === l ? o : null)
                      .append(i.children("thead"))
                  )
              )
          )
          .append(
            t("<div/>", { class: s.sScrollBody })
              .css({
                position: "relative",
                overflow: "auto",
                width: a ? vt(a) : null,
              })
              .append(i)
          )),
        d &&
          u.append(
            t("<div/>", { class: s.sScrollFoot })
              .css({
                overflow: "hidden",
                border: 0,
                width: a ? (a ? vt(a) : null) : "100%",
              })
              .append(
                t("<div/>", { class: s.sScrollFootInner }).append(
                  c
                    .removeAttr("id")
                    .css("margin-left", 0)
                    .append("bottom" === l ? o : null)
                    .append(i.children("tfoot"))
                )
              )
          );
      var h = (i = u.children())[0];
      s = i[1];
      var p = d ? i[2] : null;
      return (
        a &&
          t(s).on("scroll.DT", function (t) {
            (t = this.scrollLeft), (h.scrollLeft = t), d && (p.scrollLeft = t);
          }),
        t(s).css("max-height", r),
        n.bCollapse || t(s).css("height", r),
        (e.nScrollHead = h),
        (e.nScrollBody = s),
        (e.nScrollFoot = p),
        e.aoDrawCallback.push({ fn: dt, sName: "scrolling" }),
        u[0]
      );
    }
    function dt(i) {
      var a = i.oScroll,
        r = a.sX,
        s = a.sXInner,
        o = a.sY;
      a = a.iBarWidth;
      var l = t(i.nScrollHead),
        u = l[0].style,
        c = l.children("div"),
        d = c[0].style,
        h = c.children("table");
      c = i.nScrollBody;
      var m = t(c),
        g = c.style,
        v = t(i.nScrollFoot).children("div"),
        _ = v.children("table"),
        y = t(i.nTHead),
        b = t(i.nTable),
        w = b[0],
        x = w.style,
        S = i.nTFoot ? t(i.nTFoot) : null,
        T = i.oBrowser,
        C = T.bScrollOversize;
      ie(i.aoColumns, "nTh");
      var E,
        P = [],
        L = [],
        D = [],
        M = [],
        k = function (t) {
          ((t = t.style).paddingTop = "0"),
            (t.paddingBottom = "0"),
            (t.borderTopWidth = "0"),
            (t.borderBottomWidth = "0"),
            (t.height = 0);
        },
        A = c.scrollHeight > c.clientHeight;
      if (i.scrollBarVis !== A && i.scrollBarVis !== n)
        (i.scrollBarVis = A), p(i);
      else {
        if (((i.scrollBarVis = A), b.children("thead, tfoot").remove(), S)) {
          var I = S.clone().prependTo(b),
            z = S.find("tr");
          I = I.find("tr");
        }
        var O = y.clone().prependTo(b);
        (y = y.find("tr")),
          (A = O.find("tr")),
          O.find("th, td").removeAttr("tabindex"),
          r || ((g.width = "100%"), (l[0].style.width = "100%")),
          t.each(j(i, O), function (t, e) {
            (E = f(i, t)), (e.style.width = i.aoColumns[E].sWidth);
          }),
          S &&
            ht(function (t) {
              t.style.width = "";
            }, I),
          (l = b.outerWidth()),
          "" === r
            ? ((x.width = "100%"),
              C &&
                (b.find("tbody").height() > c.offsetHeight ||
                  "scroll" == m.css("overflow-y")) &&
                (x.width = vt(b.outerWidth() - a)),
              (l = b.outerWidth()))
            : "" !== s && ((x.width = vt(s)), (l = b.outerWidth())),
          ht(k, A),
          ht(function (i) {
            var n = e.getComputedStyle
              ? e.getComputedStyle(i).width
              : vt(t(i).width());
            D.push(i.innerHTML), P.push(n);
          }, A),
          ht(function (t, e) {
            t.style.width = P[e];
          }, y),
          t(A).height(0),
          S &&
            (ht(k, I),
            ht(function (e) {
              M.push(e.innerHTML), L.push(vt(t(e).css("width")));
            }, I),
            ht(function (t, e) {
              t.style.width = L[e];
            }, z),
            t(I).height(0)),
          ht(function (t, e) {
            (t.innerHTML = '<div class="dataTables_sizing">' + D[e] + "</div>"),
              (t.childNodes[0].style.height = "0"),
              (t.childNodes[0].style.overflow = "hidden"),
              (t.style.width = P[e]);
          }, A),
          S &&
            ht(function (t, e) {
              (t.innerHTML =
                '<div class="dataTables_sizing">' + M[e] + "</div>"),
                (t.childNodes[0].style.height = "0"),
                (t.childNodes[0].style.overflow = "hidden"),
                (t.style.width = L[e]);
            }, I),
          b.outerWidth() < l
            ? ((z =
                c.scrollHeight > c.offsetHeight ||
                "scroll" == m.css("overflow-y")
                  ? l + a
                  : l),
              C &&
                (c.scrollHeight > c.offsetHeight ||
                  "scroll" == m.css("overflow-y")) &&
                (x.width = vt(z - a)),
              ("" !== r && "" === s) ||
                Dt(i, 1, "Possible column misalignment", 6))
            : (z = "100%"),
          (g.width = vt(z)),
          (u.width = vt(z)),
          S && (i.nScrollFoot.style.width = vt(z)),
          !o && C && (g.height = vt(w.offsetHeight + a)),
          (r = b.outerWidth()),
          (h[0].style.width = vt(r)),
          (d.width = vt(r)),
          (s = b.height() > c.clientHeight || "scroll" == m.css("overflow-y")),
          (d[(o = "padding" + (T.bScrollbarLeft ? "Left" : "Right"))] = s
            ? a + "px"
            : "0px"),
          S &&
            ((_[0].style.width = vt(r)),
            (v[0].style.width = vt(r)),
            (v[0].style[o] = s ? a + "px" : "0px")),
          b.children("colgroup").insertBefore(b.children("thead")),
          m.trigger("scroll"),
          (!i.bSorted && !i.bFiltered) || i._drawHold || (c.scrollTop = 0);
      }
    }
    function ht(t, e, i) {
      for (var n, a, r = 0, s = 0, o = e.length; s < o; ) {
        for (n = e[s].firstChild, a = i ? i[s].firstChild : null; n; )
          1 === n.nodeType && (i ? t(n, a, r) : t(n, r), r++),
            (n = n.nextSibling),
            (a = i ? a.nextSibling : null);
        s++;
      }
    }
    function pt(i) {
      var n,
        a = i.nTable,
        r = i.aoColumns,
        s = i.oScroll,
        o = s.sY,
        l = s.sX,
        u = s.sXInner,
        c = r.length,
        d = v(i, "bVisible"),
        h = t("th", i.nTHead),
        m = a.getAttribute("width"),
        _ = a.parentNode,
        y = !1,
        b = i.oBrowser;
      for (
        s = b.bScrollOversize,
          (n = a.style.width) && -1 !== n.indexOf("%") && (m = n),
          n = 0;
        n < d.length;
        n++
      ) {
        var w = r[d[n]];
        null !== w.sWidth && ((w.sWidth = ft(w.sWidthOrig, _)), (y = !0));
      }
      if (s || (!y && !l && !o && c == g(i) && c == h.length))
        for (n = 0; n < c; n++)
          null !== (d = f(i, n)) && (r[d].sWidth = vt(h.eq(n).width()));
      else {
        (c = t(a).clone().css("visibility", "hidden").removeAttr("id"))
          .find("tbody tr")
          .remove();
        var x = t("<tr/>").appendTo(c.find("tbody"));
        for (
          c.find("thead, tfoot").remove(),
            c.append(t(i.nTHead).clone()).append(t(i.nTFoot).clone()),
            c.find("tfoot th, tfoot td").css("width", ""),
            h = j(i, c.find("thead")[0]),
            n = 0;
          n < d.length;
          n++
        )
          (w = r[d[n]]),
            (h[n].style.width =
              null !== w.sWidthOrig && "" !== w.sWidthOrig
                ? vt(w.sWidthOrig)
                : ""),
            w.sWidthOrig &&
              l &&
              t(h[n]).append(
                t("<div/>").css({
                  width: w.sWidthOrig,
                  margin: 0,
                  padding: 0,
                  border: 0,
                  height: 1,
                })
              );
        if (i.aoData.length)
          for (n = 0; n < d.length; n++)
            (w = r[(y = d[n])]),
              t(mt(i, y)).clone(!1).append(w.sContentPadding).appendTo(x);
        for (
          t("[name]", c).removeAttr("name"),
            w = t("<div/>")
              .css(
                l || o
                  ? {
                      position: "absolute",
                      top: 0,
                      left: 0,
                      height: 1,
                      right: 0,
                      overflow: "hidden",
                    }
                  : {}
              )
              .append(c)
              .appendTo(_),
            l && u
              ? c.width(u)
              : l
              ? (c.css("width", "auto"),
                c.removeAttr("width"),
                c.width() < _.clientWidth && m && c.width(_.clientWidth))
              : o
              ? c.width(_.clientWidth)
              : m && c.width(m),
            n = o = 0;
          n < d.length;
          n++
        )
          (u = (_ = t(h[n])).outerWidth() - _.width()),
            (o += _ =
              b.bBounding
                ? Math.ceil(h[n].getBoundingClientRect().width)
                : _.outerWidth()),
            (r[d[n]].sWidth = vt(_ - u));
        (a.style.width = vt(o)), w.remove();
      }
      m && (a.style.width = vt(m)),
        (!m && !l) ||
          i._reszEvt ||
          ((a = function () {
            t(e).on(
              "resize.DT-" + i.sInstance,
              _e(function () {
                p(i);
              })
            );
          }),
          s ? setTimeout(a, 1e3) : a(),
          (i._reszEvt = !0));
    }
    function ft(e, n) {
      return e
        ? ((n = (e = t("<div/>")
            .css("width", vt(e))
            .appendTo(n || i.body))[0].offsetWidth),
          e.remove(),
          n)
        : 0;
    }
    function mt(e, i) {
      var n = gt(e, i);
      if (0 > n) return null;
      var a = e.aoData[n];
      return a.nTr ? a.anCells[i] : t("<td/>").html(x(e, n, i, "display"))[0];
    }
    function gt(t, e) {
      for (var i, n = -1, a = -1, r = 0, s = t.aoData.length; r < s; r++)
        (i = (i = (i = x(t, r, e, "display") + "").replace(ve, "")).replace(
          /&nbsp;/g,
          " "
        )).length > n && ((n = i.length), (a = r));
      return a;
    }
    function vt(t) {
      return null === t
        ? "0px"
        : "number" == typeof t
        ? 0 > t
          ? "0px"
          : t + "px"
        : t.match(/\d$/)
        ? t + "px"
        : t;
    }
    function _t(e) {
      var i = [],
        a = e.aoColumns,
        r = e.aaSortingFixed,
        s = t.isPlainObject(r),
        o = [],
        l = function (e) {
          e.length && !Array.isArray(e[0]) ? o.push(e) : t.merge(o, e);
        };
      for (
        Array.isArray(r) && l(r),
          s && r.pre && l(r.pre),
          l(e.aaSorting),
          s && r.post && l(r.post),
          e = 0;
        e < o.length;
        e++
      ) {
        var u = o[e][0];
        for (r = 0, s = (l = a[u].aDataSort).length; r < s; r++) {
          var c = l[r],
            d = a[c].sType || "string";
          o[e]._idx === n && (o[e]._idx = t.inArray(o[e][1], a[c].asSorting)),
            i.push({
              src: u,
              col: c,
              dir: o[e][1],
              index: o[e]._idx,
              type: d,
              formatter: Wt.ext.type.order[d + "-pre"],
            });
        }
      }
      return i;
    }
    function yt(t) {
      var e,
        i = [],
        n = Wt.ext.type.order,
        a = t.aoData,
        r = 0,
        s = t.aiDisplayMaster;
      _(t);
      var o = _t(t),
        l = 0;
      for (e = o.length; l < e; l++) {
        var u = o[l];
        u.formatter && r++, Tt(t, u.col);
      }
      if ("ssp" != Bt(t) && 0 !== o.length) {
        for (l = 0, e = s.length; l < e; l++) i[s[l]] = l;
        r === o.length
          ? s.sort(function (t, e) {
              var n,
                r = o.length,
                s = a[t]._aSortData,
                l = a[e]._aSortData;
              for (n = 0; n < r; n++) {
                var u = o[n],
                  c = s[u.col],
                  d = l[u.col];
                if (0 !== (c = c < d ? -1 : c > d ? 1 : 0))
                  return "asc" === u.dir ? c : -c;
              }
              return (c = i[t]) < (d = i[e]) ? -1 : c > d ? 1 : 0;
            })
          : s.sort(function (t, e) {
              var r,
                s = o.length,
                l = a[t]._aSortData,
                u = a[e]._aSortData;
              for (r = 0; r < s; r++) {
                var c = o[r],
                  d = l[c.col],
                  h = u[c.col];
                if (
                  0 !==
                  (d = (c = n[c.type + "-" + c.dir] || n["string-" + c.dir])(
                    d,
                    h
                  ))
                )
                  return d;
              }
              return (d = i[t]) < (h = i[e]) ? -1 : d > h ? 1 : 0;
            });
      }
      t.bSorted = !0;
    }
    function bt(t) {
      var e = t.aoColumns,
        i = _t(t);
      t = t.oLanguage.oAria;
      for (var n = 0, a = e.length; n < a; n++) {
        var r = e[n],
          s = r.asSorting,
          o = r.ariaTitle || r.sTitle.replace(/<.*?>/g, ""),
          l = r.nTh;
        l.removeAttribute("aria-sort"),
          r.bSortable &&
            (0 < i.length && i[0].col == n
              ? (l.setAttribute(
                  "aria-sort",
                  "asc" == i[0].dir ? "ascending" : "descending"
                ),
                (r = s[i[0].index + 1] || s[0]))
              : (r = s[0]),
            (o += "asc" === r ? t.sSortAscending : t.sSortDescending)),
          l.setAttribute("aria-label", o);
      }
    }
    function wt(e, i, a, r) {
      var s = e.aaSorting,
        o = e.aoColumns[i].asSorting,
        l = function (e, i) {
          var a = e._idx;
          return (
            a === n && (a = t.inArray(e[1], o)),
            a + 1 < o.length ? a + 1 : i ? null : 0
          );
        };
      "number" == typeof s[0] && (s = e.aaSorting = [s]),
        a && e.oFeatures.bSortMulti
          ? -1 !== (a = t.inArray(i, ie(s, "0")))
            ? (null === (i = l(s[a], !0)) && 1 === s.length && (i = 0),
              null === i ? s.splice(a, 1) : ((s[a][1] = o[i]), (s[a]._idx = i)))
            : (s.push([i, o[0], 0]), (s[s.length - 1]._idx = 0))
          : s.length && s[0][0] == i
          ? ((i = l(s[0])), (s.length = 1), (s[0][1] = o[i]), (s[0]._idx = i))
          : ((s.length = 0), s.push([i, o[0]]), (s[0]._idx = 0)),
        O(e),
        "function" == typeof r && r(e);
    }
    function xt(t, e, i, n) {
      var a = t.aoColumns[i];
      At(e, {}, function (e) {
        !1 !== a.bSortable &&
          (t.oFeatures.bProcessing
            ? (ut(t, !0),
              setTimeout(function () {
                wt(t, i, e.shiftKey, n), "ssp" !== Bt(t) && ut(t, !1);
              }, 0))
            : wt(t, i, e.shiftKey, n));
      });
    }
    function St(e) {
      var i,
        n = e.aLastSort,
        a = e.oClasses.sSortColumn,
        r = _t(e),
        s = e.oFeatures;
      if (s.bSort && s.bSortClasses) {
        for (s = 0, i = n.length; s < i; s++) {
          var o = n[s].src;
          t(ie(e.aoData, "anCells", o)).removeClass(a + (2 > s ? s + 1 : 3));
        }
        for (s = 0, i = r.length; s < i; s++)
          (o = r[s].src),
            t(ie(e.aoData, "anCells", o)).addClass(a + (2 > s ? s + 1 : 3));
      }
      e.aLastSort = r;
    }
    function Tt(t, e) {
      var i,
        n = t.aoColumns[e],
        a = Wt.ext.order[n.sSortDataType];
      a && (i = a.call(t.oInstance, t, e, m(t, e)));
      for (
        var r,
          s = Wt.ext.type.order[n.sType + "-pre"],
          o = 0,
          l = t.aoData.length;
        o < l;
        o++
      )
        (n = t.aoData[o])._aSortData || (n._aSortData = []),
          (!n._aSortData[e] || a) &&
            ((r = a ? i[o] : x(t, o, e, "sort")),
            (n._aSortData[e] = s ? s(r) : r));
    }
    function Ct(e) {
      if (!e._bLoadingState) {
        var i = {
          time: +new Date(),
          start: e._iDisplayStart,
          length: e._iDisplayLength,
          order: t.extend(!0, [], e.aaSorting),
          search: K(e.oPreviousSearch),
          columns: t.map(e.aoColumns, function (t, i) {
            return { visible: t.bVisible, search: K(e.aoPreSearchCols[i]) };
          }),
        };
        (e.oSavedState = i),
          zt(e, "aoStateSaveParams", "stateSaveParams", [e, i]),
          e.oFeatures.bStateSave &&
            !e.bDestroying &&
            e.fnStateSaveCallback.call(e.oInstance, e, i);
      }
    }
    function Et(t, e, i) {
      if (t.oFeatures.bStateSave)
        return (
          (e = t.fnStateLoadCallback.call(t.oInstance, t, function (e) {
            Pt(t, e, i);
          })) !== n && Pt(t, e, i),
          !0
        );
      i();
    }
    function Pt(e, i, a) {
      var r,
        s = e.aoColumns;
      e._bLoadingState = !0;
      var o = e._bInitComplete ? new Wt.Api(e) : null;
      if (i && i.time) {
        var l = zt(e, "aoStateLoadParams", "stateLoadParams", [e, i]);
        if (-1 !== t.inArray(!1, l)) e._bLoadingState = !1;
        else if (0 < (l = e.iStateDuration) && i.time < +new Date() - 1e3 * l)
          e._bLoadingState = !1;
        else if (i.columns && s.length !== i.columns.length)
          e._bLoadingState = !1;
        else {
          if (
            ((e.oLoadedState = t.extend(!0, {}, i)),
            i.start !== n &&
              ((e._iDisplayStart = i.start),
              null === o && (e.iInitDisplayStart = i.start)),
            i.length !== n && (e._iDisplayLength = i.length),
            i.order !== n &&
              ((e.aaSorting = []),
              t.each(i.order, function (t, i) {
                e.aaSorting.push(i[0] >= s.length ? [0, i[1]] : i);
              })),
            i.search !== n && t.extend(e.oPreviousSearch, J(i.search)),
            i.columns)
          ) {
            for (l = 0, r = i.columns.length; l < r; l++) {
              var u = i.columns[l];
              u.visible !== n &&
                (o
                  ? o.column(l).visible(u.visible, !1)
                  : (s[l].bVisible = u.visible)),
                u.search !== n && t.extend(e.aoPreSearchCols[l], J(u.search));
            }
            o && o.columns.adjust();
          }
          (e._bLoadingState = !1),
            zt(e, "aoStateLoaded", "stateLoaded", [e, i]);
        }
      } else e._bLoadingState = !1;
      a();
    }
    function Lt(e) {
      var i = Wt.settings;
      return -1 !== (e = t.inArray(e, ie(i, "nTable"))) ? i[e] : null;
    }
    function Dt(t, i, n, a) {
      if (
        ((n =
          "DataTables warning: " +
          (t ? "table id=" + t.sTableId + " - " : "") +
          n),
        a &&
          (n +=
            ". For more information about this error, please see http://datatables.net/tn/" +
            a),
        i)
      )
        e.console && console.log && console.log(n);
      else if (
        ((i = (i = Wt.ext).sErrMode || i.errMode),
        t && zt(t, null, "error", [t, a, n]),
        "alert" == i)
      )
        alert(n);
      else {
        if ("throw" == i) throw Error(n);
        "function" == typeof i && i(t, a, n);
      }
    }
    function Mt(e, i, a, r) {
      Array.isArray(a)
        ? t.each(a, function (t, n) {
            Array.isArray(n) ? Mt(e, i, n[0], n[1]) : Mt(e, i, n);
          })
        : (r === n && (r = a), i[a] !== n && (e[r] = i[a]));
    }
    function kt(e, i, n) {
      var a;
      for (a in i)
        if (i.hasOwnProperty(a)) {
          var r = i[a];
          t.isPlainObject(r)
            ? (t.isPlainObject(e[a]) || (e[a] = {}), t.extend(!0, e[a], r))
            : n && "data" !== a && "aaData" !== a && Array.isArray(r)
            ? (e[a] = r.slice())
            : (e[a] = r);
        }
      return e;
    }
    function At(e, i, n) {
      t(e)
        .on("click.DT", i, function (i) {
          t(e).trigger("blur"), n(i);
        })
        .on("keypress.DT", i, function (t) {
          13 === t.which && (t.preventDefault(), n(t));
        })
        .on("selectstart.DT", function () {
          return !1;
        });
    }
    function It(t, e, i, n) {
      i && t[e].push({ fn: i, sName: n });
    }
    function zt(e, i, n, a) {
      var r = [];
      return (
        i &&
          (r = t.map(e[i].slice().reverse(), function (t, i) {
            return t.fn.apply(e.oInstance, a);
          })),
        null !== n &&
          ((i = t.Event(n + ".dt")),
          t(e.nTable).trigger(i, a),
          r.push(i.result)),
        r
      );
    }
    function Ot(t) {
      var e = t._iDisplayStart,
        i = t.fnDisplayEnd(),
        n = t._iDisplayLength;
      e >= i && (e = i - n),
        (e -= e % n),
        (-1 === n || 0 > e) && (e = 0),
        (t._iDisplayStart = e);
    }
    function Nt(e, i) {
      e = e.renderer;
      var n = Wt.ext.renderer[i];
      return t.isPlainObject(e) && e[i]
        ? n[e[i]] || n._
        : ("string" == typeof e && n[e]) || n._;
    }
    function Bt(t) {
      return t.oFeatures.bServerSide
        ? "ssp"
        : t.ajax || t.sAjaxSource
        ? "ajax"
        : "dom";
    }
    function jt(t, e) {
      var i = Ie.numbers_length,
        n = Math.floor(i / 2);
      return (
        e <= i
          ? (t = ae(0, e))
          : t <= n
          ? ((t = ae(0, i - 2)).push("ellipsis"), t.push(e - 1))
          : (t >= e - 1 - n
              ? (t = ae(e - (i - 2), e))
              : ((t = ae(t - n + 2, t + n - 1)).push("ellipsis"),
                t.push(e - 1)),
            t.splice(0, 0, "ellipsis"),
            t.splice(0, 0, 0)),
        (t.DT_el = "span"),
        t
      );
    }
    function Ft(e) {
      t.each(
        {
          num: function (t) {
            return ze(t, e);
          },
          "num-fmt": function (t) {
            return ze(t, e, Xt);
          },
          "html-num": function (t) {
            return ze(t, e, Ut);
          },
          "html-num-fmt": function (t) {
            return ze(t, e, Ut, Xt);
          },
        },
        function (t, i) {
          ($t.type.order[t + e + "-pre"] = i),
            t.match(/^html\-/) && ($t.type.search[t + e] = $t.type.search.html);
        }
      );
    }
    function Rt(t) {
      return function () {
        var e = [Lt(this[Wt.ext.iApiIndex])].concat(
          Array.prototype.slice.call(arguments)
        );
        return Wt.ext.internal[t].apply(this, e);
      };
    }
    var $t,
      Ht,
      Vt,
      Wt = function (e, i) {
        if (this instanceof Wt) return t(e).DataTable(i);
        (i = e),
          (this.$ = function (t, e) {
            return this.api(!0).$(t, e);
          }),
          (this._ = function (t, e) {
            return this.api(!0).rows(t, e).data();
          }),
          (this.api = function (t) {
            return new we(t ? Lt(this[$t.iApiIndex]) : this);
          }),
          (this.fnAddData = function (e, i) {
            var a = this.api(!0);
            return (
              (e =
                Array.isArray(e) &&
                (Array.isArray(e[0]) || t.isPlainObject(e[0]))
                  ? a.rows.add(e)
                  : a.row.add(e)),
              (i === n || i) && a.draw(),
              e.flatten().toArray()
            );
          }),
          (this.fnAdjustColumnSizing = function (t) {
            var e = this.api(!0).columns.adjust(),
              i = e.settings()[0],
              a = i.oScroll;
            t === n || t ? e.draw(!1) : ("" !== a.sX || "" !== a.sY) && dt(i);
          }),
          (this.fnClearTable = function (t) {
            var e = this.api(!0).clear();
            (t === n || t) && e.draw();
          }),
          (this.fnClose = function (t) {
            this.api(!0).row(t).child.hide();
          }),
          (this.fnDeleteRow = function (t, e, i) {
            var a = this.api(!0),
              r = (t = a.rows(t)).settings()[0],
              s = r.aoData[t[0][0]];
            return (
              t.remove(), e && e.call(this, r, s), (i === n || i) && a.draw(), s
            );
          }),
          (this.fnDestroy = function (t) {
            this.api(!0).destroy(t);
          }),
          (this.fnDraw = function (t) {
            this.api(!0).draw(t);
          }),
          (this.fnFilter = function (t, e, i, a, r, s) {
            (r = this.api(!0)),
              null === e || e === n
                ? r.search(t, i, a, s)
                : r.column(e).search(t, i, a, s),
              r.draw();
          }),
          (this.fnGetData = function (t, e) {
            var i = this.api(!0);
            if (t !== n) {
              var a = t.nodeName ? t.nodeName.toLowerCase() : "";
              return e !== n || "td" == a || "th" == a
                ? i.cell(t, e).data()
                : i.row(t).data() || null;
            }
            return i.data().toArray();
          }),
          (this.fnGetNodes = function (t) {
            var e = this.api(!0);
            return t !== n
              ? e.row(t).node()
              : e.rows().nodes().flatten().toArray();
          }),
          (this.fnGetPosition = function (t) {
            var e = this.api(!0),
              i = t.nodeName.toUpperCase();
            return "TR" == i
              ? e.row(t).index()
              : "TD" == i || "TH" == i
              ? [(t = e.cell(t).index()).row, t.columnVisible, t.column]
              : null;
          }),
          (this.fnIsOpen = function (t) {
            return this.api(!0).row(t).child.isShown();
          }),
          (this.fnOpen = function (t, e, i) {
            return this.api(!0).row(t).child(e, i).show().child()[0];
          }),
          (this.fnPageChange = function (t, e) {
            (t = this.api(!0).page(t)), (e === n || e) && t.draw(!1);
          }),
          (this.fnSetColumnVis = function (t, e, i) {
            (t = this.api(!0).column(t).visible(e)),
              (i === n || i) && t.columns.adjust().draw();
          }),
          (this.fnSettings = function () {
            return Lt(this[$t.iApiIndex]);
          }),
          (this.fnSort = function (t) {
            this.api(!0).order(t).draw();
          }),
          (this.fnSortListener = function (t, e, i) {
            this.api(!0).order.listener(t, e, i);
          }),
          (this.fnUpdate = function (t, e, i, a, r) {
            var s = this.api(!0);
            return (
              i === n || null === i ? s.row(e).data(t) : s.cell(e, i).data(t),
              (r === n || r) && s.columns.adjust(),
              (a === n || a) && s.draw(),
              0
            );
          }),
          (this.fnVersionCheck = $t.fnVersionCheck);
        var a = this,
          c = i === n,
          p = this.length;
        for (var f in (c && (i = {}),
        (this.oApi = this.internal = $t.internal),
        Wt.ext.internal))
          f && (this[f] = Rt(f));
        return (
          this.each(function () {
            var e,
              f = {},
              m = 1 < p ? kt(f, i, !0) : i,
              g = 0;
            f = this.getAttribute("id");
            var v = !1,
              _ = Wt.defaults,
              x = t(this);
            if ("table" != this.nodeName.toLowerCase())
              Dt(
                null,
                0,
                "Non-table node initialisation (" + this.nodeName + ")",
                2
              );
            else {
              o(_),
                l(_.column),
                r(_, _, !0),
                r(_.column, _.column, !0),
                r(_, t.extend(m, x.data()), !0);
              var S = Wt.settings;
              for (g = 0, e = S.length; g < e; g++) {
                var T = S[g];
                if (
                  T.nTable == this ||
                  (T.nTHead && T.nTHead.parentNode == this) ||
                  (T.nTFoot && T.nTFoot.parentNode == this)
                ) {
                  var C = m.bRetrieve !== n ? m.bRetrieve : _.bRetrieve;
                  if (c || C) return T.oInstance;
                  if (m.bDestroy !== n ? m.bDestroy : _.bDestroy) {
                    T.oInstance.fnDestroy();
                    break;
                  }
                  return void Dt(T, 0, "Cannot reinitialise DataTable", 3);
                }
                if (T.sTableId == this.id) {
                  S.splice(g, 1);
                  break;
                }
              }
              (null !== f && "" !== f) ||
                (this.id = f = "DataTables_Table_" + Wt.ext._unique++);
              var E = t.extend(!0, {}, Wt.models.oSettings, {
                sDestroyWidth: x[0].style.width,
                sInstance: f,
                sTableId: f,
              });
              (E.nTable = this),
                (E.oApi = a.internal),
                (E.oInit = m),
                S.push(E),
                (E.oInstance = 1 === a.length ? a : x.dataTable()),
                o(m),
                s(m.oLanguage),
                m.aLengthMenu &&
                  !m.iDisplayLength &&
                  (m.iDisplayLength = Array.isArray(m.aLengthMenu[0])
                    ? m.aLengthMenu[0][0]
                    : m.aLengthMenu[0]),
                (m = kt(t.extend(!0, {}, _), m)),
                Mt(
                  E.oFeatures,
                  m,
                  "bPaginate bLengthChange bFilter bSort bSortMulti bInfo bProcessing bAutoWidth bSortClasses bServerSide bDeferRender".split(
                    " "
                  )
                ),
                Mt(E, m, [
                  "asStripeClasses",
                  "ajax",
                  "fnServerData",
                  "fnFormatNumber",
                  "sServerMethod",
                  "aaSorting",
                  "aaSortingFixed",
                  "aLengthMenu",
                  "sPaginationType",
                  "sAjaxSource",
                  "sAjaxDataProp",
                  "iStateDuration",
                  "sDom",
                  "bSortCellsTop",
                  "iTabIndex",
                  "fnStateLoadCallback",
                  "fnStateSaveCallback",
                  "renderer",
                  "searchDelay",
                  "rowId",
                  ["iCookieDuration", "iStateDuration"],
                  ["oSearch", "oPreviousSearch"],
                  ["aoSearchCols", "aoPreSearchCols"],
                  ["iDisplayLength", "_iDisplayLength"],
                ]),
                Mt(E.oScroll, m, [
                  ["sScrollX", "sX"],
                  ["sScrollXInner", "sXInner"],
                  ["sScrollY", "sY"],
                  ["bScrollCollapse", "bCollapse"],
                ]),
                Mt(E.oLanguage, m, "fnInfoCallback"),
                It(E, "aoDrawCallback", m.fnDrawCallback, "user"),
                It(E, "aoServerParams", m.fnServerParams, "user"),
                It(E, "aoStateSaveParams", m.fnStateSaveParams, "user"),
                It(E, "aoStateLoadParams", m.fnStateLoadParams, "user"),
                It(E, "aoStateLoaded", m.fnStateLoaded, "user"),
                It(E, "aoRowCallback", m.fnRowCallback, "user"),
                It(E, "aoRowCreatedCallback", m.fnCreatedRow, "user"),
                It(E, "aoHeaderCallback", m.fnHeaderCallback, "user"),
                It(E, "aoFooterCallback", m.fnFooterCallback, "user"),
                It(E, "aoInitComplete", m.fnInitComplete, "user"),
                It(E, "aoPreDrawCallback", m.fnPreDrawCallback, "user"),
                (E.rowIdFn = he(m.rowId)),
                u(E);
              var P = E.oClasses;
              t.extend(P, Wt.ext.classes, m.oClasses),
                x.addClass(P.sTable),
                E.iInitDisplayStart === n &&
                  ((E.iInitDisplayStart = m.iDisplayStart),
                  (E._iDisplayStart = m.iDisplayStart)),
                null !== m.iDeferLoading &&
                  ((E.bDeferLoading = !0),
                  (f = Array.isArray(m.iDeferLoading)),
                  (E._iRecordsDisplay = f
                    ? m.iDeferLoading[0]
                    : m.iDeferLoading),
                  (E._iRecordsTotal = f
                    ? m.iDeferLoading[1]
                    : m.iDeferLoading));
              var L = E.oLanguage;
              t.extend(!0, L, m.oLanguage),
                L.sUrl
                  ? (t.ajax({
                      dataType: "json",
                      url: L.sUrl,
                      success: function (e) {
                        r(_.oLanguage, e),
                          s(e),
                          t.extend(!0, L, e),
                          zt(E, null, "i18n", [E]),
                          it(E);
                      },
                      error: function () {
                        it(E);
                      },
                    }),
                    (v = !0))
                  : zt(E, null, "i18n", [E]),
                null === m.asStripeClasses &&
                  (E.asStripeClasses = [P.sStripeOdd, P.sStripeEven]),
                (f = E.asStripeClasses);
              var D = x.children("tbody").find("tr").eq(0);
              if (
                (-1 !==
                  t.inArray(
                    !0,
                    t.map(f, function (t, e) {
                      return D.hasClass(t);
                    })
                  ) &&
                  (t("tbody tr", this).removeClass(f.join(" ")),
                  (E.asDestroyStripes = f.slice())),
                (f = []),
                0 !== (S = this.getElementsByTagName("thead")).length &&
                  (B(E.aoHeader, S[0]), (f = j(E))),
                null === m.aoColumns)
              )
                for (S = [], g = 0, e = f.length; g < e; g++) S.push(null);
              else S = m.aoColumns;
              for (g = 0, e = S.length; g < e; g++) d(E, f ? f[g] : null);
              if (
                (y(E, m.aoColumnDefs, S, function (t, e) {
                  h(E, t, e);
                }),
                D.length)
              ) {
                var M = function (t, e) {
                  return null !== t.getAttribute("data-" + e) ? e : null;
                };
                t(D[0])
                  .children("th, td")
                  .each(function (t, e) {
                    var i = E.aoColumns[t];
                    if (i.mData === t) {
                      var a = M(e, "sort") || M(e, "order");
                      (e = M(e, "filter") || M(e, "search")),
                        (null === a && null === e) ||
                          ((i.mData = {
                            _: t + ".display",
                            sort: null !== a ? t + ".@data-" + a : n,
                            type: null !== a ? t + ".@data-" + a : n,
                            filter: null !== e ? t + ".@data-" + e : n,
                          }),
                          h(E, t));
                    }
                  });
              }
              var k = E.oFeatures;
              (f = function () {
                if (m.aaSorting === n) {
                  var i = E.aaSorting;
                  for (g = 0, e = i.length; g < e; g++)
                    i[g][1] = E.aoColumns[g].asSorting[0];
                }
                St(E),
                  k.bSort &&
                    It(E, "aoDrawCallback", function () {
                      if (E.bSorted) {
                        var e = _t(E),
                          i = {};
                        t.each(e, function (t, e) {
                          i[e.src] = e.dir;
                        }),
                          zt(E, null, "order", [E, e, i]),
                          bt(E);
                      }
                    }),
                  It(
                    E,
                    "aoDrawCallback",
                    function () {
                      (E.bSorted || "ssp" === Bt(E) || k.bDeferRender) && St(E);
                    },
                    "sc"
                  ),
                  (i = x.children("caption").each(function () {
                    this._captionSide = t(this).css("caption-side");
                  }));
                var a = x.children("thead");
                0 === a.length && (a = t("<thead/>").appendTo(x)),
                  (E.nTHead = a[0]);
                var r = x.children("tbody");
                if (
                  (0 === r.length && (r = t("<tbody/>").insertAfter(a)),
                  (E.nTBody = r[0]),
                  0 === (a = x.children("tfoot")).length &&
                    0 < i.length &&
                    ("" !== E.oScroll.sX || "" !== E.oScroll.sY) &&
                    (a = t("<tfoot/>").appendTo(x)),
                  0 === a.length || 0 === a.children().length
                    ? x.addClass(P.sNoFooter)
                    : 0 < a.length &&
                      ((E.nTFoot = a[0]), B(E.aoFooter, E.nTFoot)),
                  m.aaData)
                )
                  for (g = 0; g < m.aaData.length; g++) b(E, m.aaData[g]);
                else
                  (E.bDeferLoading || "dom" == Bt(E)) &&
                    w(E, t(E.nTBody).children("tr"));
                (E.aiDisplay = E.aiDisplayMaster.slice()),
                  (E.bInitialised = !0),
                  !1 === v && it(E);
              }),
                It(E, "aoDrawCallback", Ct, "state_save"),
                m.bStateSave ? ((k.bStateSave = !0), Et(E, m, f)) : f();
            }
          }),
          (a = null),
          this
        );
      },
      Zt = {},
      Gt = /[\r\n\u2028]/g,
      Ut = /<.*?>/g,
      Yt =
        /^\d{2,4}[\.\/\-]\d{1,2}[\.\/\-]\d{1,2}([T ]{1}\d{1,2}[:\.]\d{2}([\.:]\d{2})?)?$/,
      qt = /(\/|\.|\*|\+|\?|\||\(|\)|\[|\]|\{|\}|\\|\$|\^|\-)/g,
      Xt = /['\u00A0,$£€¥%\u2009\u202F\u20BD\u20a9\u20BArfkɃΞ]/gi,
      Kt = function (t) {
        return !t || !0 === t || "-" === t;
      },
      Jt = function (t) {
        var e = parseInt(t, 10);
        return !isNaN(e) && isFinite(t) ? e : null;
      },
      Qt = function (t, e) {
        return (
          Zt[e] || (Zt[e] = new RegExp(fe(e), "g")),
          "string" == typeof t && "." !== e
            ? t.replace(/\./g, "").replace(Zt[e], ".")
            : t
        );
      },
      te = function (t, e, i) {
        var n = "string" == typeof t;
        return (
          !!Kt(t) ||
          (e && n && (t = Qt(t, e)),
          i && n && (t = t.replace(Xt, "")),
          !isNaN(parseFloat(t)) && isFinite(t))
        );
      },
      ee = function (t, e, i) {
        return (
          !!Kt(t) ||
          ((Kt(t) || "string" == typeof t) && !!te(t.replace(Ut, ""), e, i)) ||
          null
        );
      },
      ie = function (t, e, i) {
        var a = [],
          r = 0,
          s = t.length;
        if (i !== n) for (; r < s; r++) t[r] && t[r][e] && a.push(t[r][e][i]);
        else for (; r < s; r++) t[r] && a.push(t[r][e]);
        return a;
      },
      ne = function (t, e, i, a) {
        var r = [],
          s = 0,
          o = e.length;
        if (a !== n) for (; s < o; s++) t[e[s]][i] && r.push(t[e[s]][i][a]);
        else for (; s < o; s++) r.push(t[e[s]][i]);
        return r;
      },
      ae = function (t, e) {
        var i = [];
        if (e === n) {
          e = 0;
          var a = t;
        } else (a = e), (e = t);
        for (t = e; t < a; t++) i.push(t);
        return i;
      },
      re = function (t) {
        for (var e = [], i = 0, n = t.length; i < n; i++) t[i] && e.push(t[i]);
        return e;
      },
      se = function (t) {
        t: {
          if (!(2 > t.length))
            for (
              var e = t.slice().sort(), i = e[0], n = 1, a = e.length;
              n < a;
              n++
            ) {
              if (e[n] === i) {
                e = !1;
                break t;
              }
              i = e[n];
            }
          e = !0;
        }
        if (e) return t.slice();
        (e = []), (a = t.length);
        var r,
          s = 0;
        n = 0;
        t: for (; n < a; n++) {
          for (i = t[n], r = 0; r < s; r++) if (e[r] === i) continue t;
          e.push(i), s++;
        }
        return e;
      },
      oe = function (t, e) {
        if (Array.isArray(e)) for (var i = 0; i < e.length; i++) oe(t, e[i]);
        else t.push(e);
        return t;
      },
      le = function (t, e) {
        return e === n && (e = 0), -1 !== this.indexOf(t, e);
      };
    Array.isArray ||
      (Array.isArray = function (t) {
        return "[object Array]" === Object.prototype.toString.call(t);
      }),
      Array.prototype.includes || (Array.prototype.includes = le),
      String.prototype.trim ||
        (String.prototype.trim = function () {
          return this.replace(/^[\s\uFEFF\xA0]+|[\s\uFEFF\xA0]+$/g, "");
        }),
      String.prototype.includes || (String.prototype.includes = le),
      (Wt.util = {
        throttle: function (t, e) {
          var i,
            a,
            r = e !== n ? e : 200;
          return function () {
            var e = this,
              s = +new Date(),
              o = arguments;
            i && s < i + r
              ? (clearTimeout(a),
                (a = setTimeout(function () {
                  (i = n), t.apply(e, o);
                }, r)))
              : ((i = s), t.apply(e, o));
          };
        },
        escapeRegex: function (t) {
          return t.replace(qt, "\\$1");
        },
        set: function (e) {
          if (t.isPlainObject(e)) return Wt.util.set(e._);
          if (null === e) return function () {};
          if ("function" == typeof e)
            return function (t, i, n) {
              e(t, "set", i, n);
            };
          if (
            "string" != typeof e ||
            (-1 === e.indexOf(".") &&
              -1 === e.indexOf("[") &&
              -1 === e.indexOf("("))
          )
            return function (t, i) {
              t[e] = i;
            };
          var i = function (t, e, a) {
            for (
              var r, s, o = (a = T(a))[a.length - 1], l = 0, u = a.length - 1;
              l < u;
              l++
            ) {
              if ("__proto__" === a[l] || "constructor" === a[l])
                throw Error("Cannot set prototype values");
              if (((r = a[l].match(ce)), (s = a[l].match(de)), r)) {
                if (
                  ((a[l] = a[l].replace(ce, "")),
                  (t[a[l]] = []),
                  (o = a.slice()).splice(0, l + 1),
                  (r = o.join(".")),
                  Array.isArray(e))
                )
                  for (s = 0, u = e.length; s < u; s++)
                    i((o = {}), e[s], r), t[a[l]].push(o);
                else t[a[l]] = e;
                return;
              }
              s && ((a[l] = a[l].replace(de, "")), (t = t[a[l]](e))),
                (null !== t[a[l]] && t[a[l]] !== n) || (t[a[l]] = {}),
                (t = t[a[l]]);
            }
            o.match(de) ? t[o.replace(de, "")](e) : (t[o.replace(ce, "")] = e);
          };
          return function (t, n) {
            return i(t, n, e);
          };
        },
        get: function (e) {
          if (t.isPlainObject(e)) {
            var i = {};
            return (
              t.each(e, function (t, e) {
                e && (i[t] = Wt.util.get(e));
              }),
              function (t, e, a, r) {
                var s = i[e] || i._;
                return s !== n ? s(t, e, a, r) : t;
              }
            );
          }
          if (null === e)
            return function (t) {
              return t;
            };
          if ("function" == typeof e)
            return function (t, i, n, a) {
              return e(t, i, n, a);
            };
          if (
            "string" != typeof e ||
            (-1 === e.indexOf(".") &&
              -1 === e.indexOf("[") &&
              -1 === e.indexOf("("))
          )
            return function (t, i) {
              return t[e];
            };
          var a = function (t, e, i) {
            if ("" !== i)
              for (var r = T(i), s = 0, o = r.length; s < o; s++) {
                i = r[s].match(ce);
                var l = r[s].match(de);
                if (i) {
                  if (
                    ((r[s] = r[s].replace(ce, "")),
                    "" !== r[s] && (t = t[r[s]]),
                    (l = []),
                    r.splice(0, s + 1),
                    (r = r.join(".")),
                    Array.isArray(t))
                  )
                    for (s = 0, o = t.length; s < o; s++) l.push(a(t[s], e, r));
                  t =
                    "" === (t = i[0].substring(1, i[0].length - 1))
                      ? l
                      : l.join(t);
                  break;
                }
                if (l) (r[s] = r[s].replace(de, "")), (t = t[r[s]]());
                else {
                  if (null === t || t[r[s]] === n) return n;
                  t = t[r[s]];
                }
              }
            return t;
          };
          return function (t, i) {
            return a(t, i, e);
          };
        },
      });
    var ue = function (t, e, i) {
        t[e] !== n && (t[i] = t[e]);
      },
      ce = /\[.*?\]$/,
      de = /\(\)$/,
      he = Wt.util.get,
      pe = Wt.util.set,
      fe = Wt.util.escapeRegex,
      me = t("<div>")[0],
      ge = me.textContent !== n,
      ve = /<.*?>/g,
      _e = Wt.util.throttle,
      ye = [],
      be = Array.prototype,
      we = function (e, i) {
        if (!(this instanceof we)) return new we(e, i);
        var n = [],
          a = function (e) {
            (e = (function (e) {
              var i,
                n = Wt.settings,
                a = t.map(n, function (t, e) {
                  return t.nTable;
                });
              if (!e) return [];
              if (e.nTable && e.oApi) return [e];
              if (e.nodeName && "table" === e.nodeName.toLowerCase()) {
                var r = t.inArray(e, a);
                return -1 !== r ? [n[r]] : null;
              }
              return e && "function" == typeof e.settings
                ? e.settings().toArray()
                : ("string" == typeof e
                    ? (i = t(e))
                    : e instanceof t && (i = e),
                  i
                    ? i
                        .map(function (e) {
                          return -1 !== (r = t.inArray(this, a)) ? n[r] : null;
                        })
                        .toArray()
                    : void 0);
            })(e)) && n.push.apply(n, e);
          };
        if (Array.isArray(e)) for (var r = 0, s = e.length; r < s; r++) a(e[r]);
        else a(e);
        (this.context = se(n)),
          i && t.merge(this, i),
          (this.selector = { rows: null, cols: null, opts: null }),
          we.extend(this, this, ye);
      };
    (Wt.Api = we),
      t.extend(we.prototype, {
        any: function () {
          return 0 !== this.count();
        },
        concat: be.concat,
        context: [],
        count: function () {
          return this.flatten().length;
        },
        each: function (t) {
          for (var e = 0, i = this.length; e < i; e++)
            t.call(this, this[e], e, this);
          return this;
        },
        eq: function (t) {
          var e = this.context;
          return e.length > t ? new we(e[t], this[t]) : null;
        },
        filter: function (t) {
          var e = [];
          if (be.filter) e = be.filter.call(this, t, this);
          else
            for (var i = 0, n = this.length; i < n; i++)
              t.call(this, this[i], i, this) && e.push(this[i]);
          return new we(this.context, e);
        },
        flatten: function () {
          var t = [];
          return new we(this.context, t.concat.apply(t, this.toArray()));
        },
        join: be.join,
        indexOf:
          be.indexOf ||
          function (t, e) {
            e = e || 0;
            for (var i = this.length; e < i; e++) if (this[e] === t) return e;
            return -1;
          },
        iterator: function (t, e, i, a) {
          var r,
            s,
            o,
            l = [],
            u = this.context,
            c = this.selector;
          "string" == typeof t && ((a = i), (i = e), (e = t), (t = !1));
          var d = 0;
          for (r = u.length; d < r; d++) {
            var h = new we(u[d]);
            if ("table" === e) {
              var p = i.call(h, u[d], d);
              p !== n && l.push(p);
            } else if ("columns" === e || "rows" === e)
              (p = i.call(h, u[d], this[d], d)) !== n && l.push(p);
            else if (
              "column" === e ||
              "column-rows" === e ||
              "row" === e ||
              "cell" === e
            ) {
              var f = this[d];
              "column-rows" === e && (o = Pe(u[d], c.opts));
              var m = 0;
              for (s = f.length; m < s; m++)
                (p = f[m]),
                  (p =
                    "cell" === e
                      ? i.call(h, u[d], p.row, p.column, d, m)
                      : i.call(h, u[d], p, d, m, o)) !== n && l.push(p);
            }
          }
          return l.length || a
            ? (((e = (t = new we(u, t ? l.concat.apply([], l) : l))
                .selector).rows = c.rows),
              (e.cols = c.cols),
              (e.opts = c.opts),
              t)
            : this;
        },
        lastIndexOf:
          be.lastIndexOf ||
          function (t, e) {
            return this.indexOf.apply(this.toArray.reverse(), arguments);
          },
        length: 0,
        map: function (t) {
          var e = [];
          if (be.map) e = be.map.call(this, t, this);
          else
            for (var i = 0, n = this.length; i < n; i++)
              e.push(t.call(this, this[i], i));
          return new we(this.context, e);
        },
        pluck: function (t) {
          return this.map(function (e) {
            return e[t];
          });
        },
        pop: be.pop,
        push: be.push,
        reduce:
          be.reduce ||
          function (t, e) {
            return c(this, t, e, 0, this.length, 1);
          },
        reduceRight:
          be.reduceRight ||
          function (t, e) {
            return c(this, t, e, this.length - 1, -1, -1);
          },
        reverse: be.reverse,
        selector: null,
        shift: be.shift,
        slice: function () {
          return new we(this.context, this);
        },
        sort: be.sort,
        splice: be.splice,
        toArray: function () {
          return be.slice.call(this);
        },
        to$: function () {
          return t(this);
        },
        toJQuery: function () {
          return t(this);
        },
        unique: function () {
          return new we(this.context, se(this));
        },
        unshift: be.unshift,
      }),
      (we.extend = function (t, e, i) {
        if (i.length && e && (e instanceof we || e.__dt_wrapper)) {
          var n,
            a = function (t, e, i) {
              return function () {
                var n = e.apply(t, arguments);
                return we.extend(n, n, i.methodExt), n;
              };
            },
            r = 0;
          for (n = i.length; r < n; r++) {
            var s = i[r];
            (e[s.name] =
              "function" === s.type
                ? a(t, s.val, s)
                : "object" === s.type
                ? {}
                : s.val),
              (e[s.name].__dt_wrapper = !0),
              we.extend(t, e[s.name], s.propExt);
          }
        }
      }),
      (we.register = Ht =
        function (e, i) {
          if (Array.isArray(e))
            for (var n = 0, a = e.length; n < a; n++) we.register(e[n], i);
          else {
            a = e.split(".");
            var r,
              s = ye;
            for (e = 0, n = a.length; e < n; e++) {
              var o = (r = -1 !== a[e].indexOf("()"))
                ? a[e].replace("()", "")
                : a[e];
              t: {
                for (var l = 0, u = s.length; l < u; l++)
                  if (s[l].name === o) {
                    l = s[l];
                    break t;
                  }
                l = null;
              }
              l ||
                ((l = {
                  name: o,
                  val: {},
                  methodExt: [],
                  propExt: [],
                  type: "object",
                }),
                s.push(l)),
                e === n - 1
                  ? ((l.val = i),
                    (l.type =
                      "function" == typeof i
                        ? "function"
                        : t.isPlainObject(i)
                        ? "object"
                        : "other"))
                  : (s = r ? l.methodExt : l.propExt);
            }
          }
        }),
      (we.registerPlural = Vt =
        function (t, e, i) {
          we.register(t, i),
            we.register(e, function () {
              var t = i.apply(this, arguments);
              return t === this
                ? this
                : t instanceof we
                ? t.length
                  ? Array.isArray(t[0])
                    ? new we(t.context, t[0])
                    : t[0]
                  : n
                : t;
            });
        });
    var xe = function (e, i) {
      if (Array.isArray(e))
        return t.map(e, function (t) {
          return xe(t, i);
        });
      if ("number" == typeof e) return [i[e]];
      var n = t.map(i, function (t, e) {
        return t.nTable;
      });
      return t(n)
        .filter(e)
        .map(function (e) {
          return (e = t.inArray(this, n)), i[e];
        })
        .toArray();
    };
    Ht("tables()", function (t) {
      return t !== n && null !== t ? new we(xe(t, this.context)) : this;
    }),
      Ht("table()", function (t) {
        var e = (t = this.tables(t)).context;
        return e.length ? new we(e[0]) : t;
      }),
      Vt("tables().nodes()", "table().node()", function () {
        return this.iterator(
          "table",
          function (t) {
            return t.nTable;
          },
          1
        );
      }),
      Vt("tables().body()", "table().body()", function () {
        return this.iterator(
          "table",
          function (t) {
            return t.nTBody;
          },
          1
        );
      }),
      Vt("tables().header()", "table().header()", function () {
        return this.iterator(
          "table",
          function (t) {
            return t.nTHead;
          },
          1
        );
      }),
      Vt("tables().footer()", "table().footer()", function () {
        return this.iterator(
          "table",
          function (t) {
            return t.nTFoot;
          },
          1
        );
      }),
      Vt("tables().containers()", "table().container()", function () {
        return this.iterator(
          "table",
          function (t) {
            return t.nTableWrapper;
          },
          1
        );
      }),
      Ht("draw()", function (t) {
        return this.iterator("table", function (e) {
          "page" === t
            ? z(e)
            : ("string" == typeof t && (t = "full-hold" !== t), O(e, !1 === t));
        });
      }),
      Ht("page()", function (t) {
        return t === n
          ? this.page.info().page
          : this.iterator("table", function (e) {
              ot(e, t);
            });
      }),
      Ht("page.info()", function (t) {
        if (0 === this.context.length) return n;
        var e = (t = this.context[0])._iDisplayStart,
          i = t.oFeatures.bPaginate ? t._iDisplayLength : -1,
          a = t.fnRecordsDisplay(),
          r = -1 === i;
        return {
          page: r ? 0 : Math.floor(e / i),
          pages: r ? 1 : Math.ceil(a / i),
          start: e,
          end: t.fnDisplayEnd(),
          length: i,
          recordsTotal: t.fnRecordsTotal(),
          recordsDisplay: a,
          serverSide: "ssp" === Bt(t),
        };
      }),
      Ht("page.len()", function (t) {
        return t === n
          ? 0 !== this.context.length
            ? this.context[0]._iDisplayLength
            : n
          : this.iterator("table", function (e) {
              at(e, t);
            });
      });
    var Se = function (t, e, i) {
      if (i) {
        var n = new we(t);
        n.one("draw", function () {
          i(n.ajax.json());
        });
      }
      if ("ssp" == Bt(t)) O(t, e);
      else {
        ut(t, !0);
        var a = t.jqXHR;
        a && 4 !== a.readyState && a.abort(),
          F(t, [], function (i) {
            E(t);
            for (var n = 0, a = (i = V(t, i)).length; n < a; n++) b(t, i[n]);
            O(t, e), ut(t, !1);
          });
      }
    };
    Ht("ajax.json()", function () {
      var t = this.context;
      if (0 < t.length) return t[0].json;
    }),
      Ht("ajax.params()", function () {
        var t = this.context;
        if (0 < t.length) return t[0].oAjaxData;
      }),
      Ht("ajax.reload()", function (t, e) {
        return this.iterator("table", function (i) {
          Se(i, !1 === e, t);
        });
      }),
      Ht("ajax.url()", function (e) {
        var i = this.context;
        return e === n
          ? 0 === i.length
            ? n
            : (i = i[0]).ajax
            ? t.isPlainObject(i.ajax)
              ? i.ajax.url
              : i.ajax
            : i.sAjaxSource
          : this.iterator("table", function (i) {
              t.isPlainObject(i.ajax) ? (i.ajax.url = e) : (i.ajax = e);
            });
      }),
      Ht("ajax.url().load()", function (t, e) {
        return this.iterator("table", function (i) {
          Se(i, !1 === e, t);
        });
      });
    var Te = function (t, e, i, a, r) {
        var s,
          o,
          l,
          u = [],
          c = typeof e;
        for (
          (e && "string" !== c && "function" !== c && e.length !== n) ||
            (e = [e]),
            c = 0,
            o = e.length;
          c < o;
          c++
        ) {
          var d =
              e[c] && e[c].split && !e[c].match(/[\[\(:]/)
                ? e[c].split(",")
                : [e[c]],
            h = 0;
          for (l = d.length; h < l; h++)
            (s = i("string" == typeof d[h] ? d[h].trim() : d[h])) &&
              s.length &&
              (u = u.concat(s));
        }
        if ((t = $t.selector[t]).length)
          for (c = 0, o = t.length; c < o; c++) u = t[c](a, r, u);
        return se(u);
      },
      Ce = function (e) {
        return (
          e || (e = {}),
          e.filter && e.search === n && (e.search = e.filter),
          t.extend({ search: "none", order: "current", page: "all" }, e)
        );
      },
      Ee = function (t) {
        for (var e = 0, i = t.length; e < i; e++)
          if (0 < t[e].length)
            return (
              (t[0] = t[e]),
              (t[0].length = 1),
              (t.length = 1),
              (t.context = [t.context[e]]),
              t
            );
        return (t.length = 0), t;
      },
      Pe = function (e, i) {
        var n = [],
          a = e.aiDisplay,
          r = e.aiDisplayMaster,
          s = i.search,
          o = i.order;
        if (((i = i.page), "ssp" == Bt(e)))
          return "removed" === s ? [] : ae(0, r.length);
        if ("current" == i)
          for (o = e._iDisplayStart, e = e.fnDisplayEnd(); o < e; o++)
            n.push(a[o]);
        else if ("current" == o || "applied" == o) {
          if ("none" == s) n = r.slice();
          else if ("applied" == s) n = a.slice();
          else if ("removed" == s) {
            var l = {};
            for (o = 0, e = a.length; o < e; o++) l[a[o]] = null;
            n = t.map(r, function (t) {
              return l.hasOwnProperty(t) ? null : t;
            });
          }
        } else if ("index" == o || "original" == o)
          for (o = 0, e = e.aoData.length; o < e; o++)
            "none" == s
              ? n.push(o)
              : ((-1 === (r = t.inArray(o, a)) && "removed" == s) ||
                  (0 <= r && "applied" == s)) &&
                n.push(o);
        return n;
      };
    Ht("rows()", function (e, i) {
      e === n ? (e = "") : t.isPlainObject(e) && ((i = e), (e = "")),
        (i = Ce(i));
      var a = this.iterator(
        "table",
        function (a) {
          return (function (e, i, a) {
            var r;
            return Te(
              "row",
              i,
              function (i) {
                var s = Jt(i),
                  o = e.aoData;
                if (null !== s && !a) return [s];
                if ((r || (r = Pe(e, a)), null !== s && -1 !== t.inArray(s, r)))
                  return [s];
                if (null === i || i === n || "" === i) return r;
                if ("function" == typeof i)
                  return t.map(r, function (t) {
                    var e = o[t];
                    return i(t, e._aData, e.nTr) ? t : null;
                  });
                if (i.nodeName) {
                  s = i._DT_RowIndex;
                  var l = i._DT_CellIndex;
                  return s !== n
                    ? o[s] && o[s].nTr === i
                      ? [s]
                      : []
                    : l
                    ? o[l.row] && o[l.row].nTr === i.parentNode
                      ? [l.row]
                      : []
                    : (s = t(i).closest("*[data-dt-row]")).length
                    ? [s.data("dt-row")]
                    : [];
                }
                return "string" == typeof i &&
                  "#" === i.charAt(0) &&
                  (s = e.aIds[i.replace(/^#/, "")]) !== n
                  ? [s.idx]
                  : ((s = re(ne(e.aoData, r, "nTr"))),
                    t(s)
                      .filter(i)
                      .map(function () {
                        return this._DT_RowIndex;
                      })
                      .toArray());
              },
              e,
              a
            );
          })(a, e, i);
        },
        1
      );
      return (a.selector.rows = e), (a.selector.opts = i), a;
    }),
      Ht("rows().nodes()", function () {
        return this.iterator(
          "row",
          function (t, e) {
            return t.aoData[e].nTr || n;
          },
          1
        );
      }),
      Ht("rows().data()", function () {
        return this.iterator(
          !0,
          "rows",
          function (t, e) {
            return ne(t.aoData, e, "_aData");
          },
          1
        );
      }),
      Vt("rows().cache()", "row().cache()", function (t) {
        return this.iterator(
          "row",
          function (e, i) {
            return (
              (e = e.aoData[i]), "search" === t ? e._aFilterData : e._aSortData
            );
          },
          1
        );
      }),
      Vt("rows().invalidate()", "row().invalidate()", function (t) {
        return this.iterator("row", function (e, i) {
          L(e, i, t);
        });
      }),
      Vt("rows().indexes()", "row().index()", function () {
        return this.iterator(
          "row",
          function (t, e) {
            return e;
          },
          1
        );
      }),
      Vt("rows().ids()", "row().id()", function (t) {
        for (var e = [], i = this.context, n = 0, a = i.length; n < a; n++)
          for (var r = 0, s = this[n].length; r < s; r++) {
            var o = i[n].rowIdFn(i[n].aoData[this[n][r]]._aData);
            e.push((!0 === t ? "#" : "") + o);
          }
        return new we(i, e);
      }),
      Vt("rows().remove()", "row().remove()", function () {
        var t = this;
        return (
          this.iterator("row", function (e, i, a) {
            var r,
              s,
              o = e.aoData,
              l = o[i];
            o.splice(i, 1);
            var u = 0;
            for (r = o.length; u < r; u++) {
              var c = o[u],
                d = c.anCells;
              if ((null !== c.nTr && (c.nTr._DT_RowIndex = u), null !== d))
                for (c = 0, s = d.length; c < s; c++)
                  d[c]._DT_CellIndex.row = u;
            }
            P(e.aiDisplayMaster, i),
              P(e.aiDisplay, i),
              P(t[a], i, !1),
              0 < e._iRecordsDisplay && e._iRecordsDisplay--,
              Ot(e),
              (i = e.rowIdFn(l._aData)) !== n && delete e.aIds[i];
          }),
          this.iterator("table", function (t) {
            for (var e = 0, i = t.aoData.length; e < i; e++)
              t.aoData[e].idx = e;
          }),
          this
        );
      }),
      Ht("rows.add()", function (e) {
        var i = this.iterator(
            "table",
            function (t) {
              var i,
                n = [],
                a = 0;
              for (i = e.length; a < i; a++) {
                var r = e[a];
                r.nodeName && "TR" === r.nodeName.toUpperCase()
                  ? n.push(w(t, r)[0])
                  : n.push(b(t, r));
              }
              return n;
            },
            1
          ),
          n = this.rows(-1);
        return n.pop(), t.merge(n, i), n;
      }),
      Ht("row()", function (t, e) {
        return Ee(this.rows(t, e));
      }),
      Ht("row().data()", function (t) {
        var e = this.context;
        if (t === n)
          return e.length && this.length ? e[0].aoData[this[0]]._aData : n;
        var i = e[0].aoData[this[0]];
        return (
          (i._aData = t),
          Array.isArray(t) && i.nTr && i.nTr.id && pe(e[0].rowId)(t, i.nTr.id),
          L(e[0], this[0], "data"),
          this
        );
      }),
      Ht("row().node()", function () {
        var t = this.context;
        return (t.length && this.length && t[0].aoData[this[0]].nTr) || null;
      }),
      Ht("row.add()", function (e) {
        e instanceof t && e.length && (e = e[0]);
        var i = this.iterator("table", function (t) {
          return e.nodeName && "TR" === e.nodeName.toUpperCase()
            ? w(t, e)[0]
            : b(t, e);
        });
        return this.row(i[0]);
      }),
      t(i).on("plugin-init.dt", function (t, e) {
        var i = new we(e);
        i.on("stateSaveParams", function (t, e, a) {
          (t = i.rows().iterator("row", function (t, e) {
            return t.aoData[e]._detailsShow ? e : n;
          })),
            (a.childRows = i.rows(t).ids(!0).toArray());
        }),
          (t = i.state.loaded()) &&
            t.childRows &&
            i.rows(t.childRows).every(function () {
              zt(e, null, "requestChild", [this]);
            });
      });
    var Le = function (e, i) {
        var a = e.context;
        a.length &&
          (e = a[0].aoData[i !== n ? i : e[0]]) &&
          e._details &&
          (e._details.remove(),
          (e._detailsShow = n),
          (e._details = n),
          t(e.nTr).removeClass("dt-hasChild"),
          Ct(a[0]));
      },
      De = function (e, i) {
        var n = e.context;
        if (n.length && e.length) {
          var a = n[0].aoData[e[0]];
          a._details &&
            ((a._detailsShow = i)
              ? (a._details.insertAfter(a.nTr),
                t(a.nTr).addClass("dt-hasChild"))
              : (a._details.detach(), t(a.nTr).removeClass("dt-hasChild")),
            zt(n[0], null, "childRow", [i, e.row(e[0])]),
            Me(n[0]),
            Ct(n[0]));
        }
      },
      Me = function (t) {
        var e = new we(t),
          i = t.aoData;
        e.off(
          "draw.dt.DT_details column-visibility.dt.DT_details destroy.dt.DT_details"
        ),
          0 < ie(i, "_details").length &&
            (e.on("draw.dt.DT_details", function (n, a) {
              t === a &&
                e
                  .rows({ page: "current" })
                  .eq(0)
                  .each(function (t) {
                    (t = i[t])._detailsShow && t._details.insertAfter(t.nTr);
                  });
            }),
            e.on("column-visibility.dt.DT_details", function (e, n, a, r) {
              if (t === n)
                for (n = g(n), a = 0, r = i.length; a < r; a++)
                  (e = i[a])._details &&
                    e._details.children("td[colspan]").attr("colspan", n);
            }),
            e.on("destroy.dt.DT_details", function (n, a) {
              if (t === a)
                for (n = 0, a = i.length; n < a; n++) i[n]._details && Le(e, n);
            }));
      };
    Ht("row().child()", function (e, i) {
      var a = this.context;
      return e === n
        ? a.length && this.length
          ? a[0].aoData[this[0]]._details
          : n
        : (!0 === e
            ? this.child.show()
            : !1 === e
            ? Le(this)
            : a.length &&
              this.length &&
              (function (e, i, n, a) {
                var r = [],
                  s = function (i, n) {
                    if (Array.isArray(i) || i instanceof t)
                      for (var a = 0, o = i.length; a < o; a++) s(i[a], n);
                    else
                      i.nodeName && "tr" === i.nodeName.toLowerCase()
                        ? r.push(i)
                        : ((a = t("<tr><td></td></tr>").addClass(n)),
                          (t("td", a).addClass(n).html(i)[0].colSpan = g(e)),
                          r.push(a[0]));
                  };
                s(n, a),
                  i._details && i._details.detach(),
                  (i._details = t(r)),
                  i._detailsShow && i._details.insertAfter(i.nTr);
              })(a[0], a[0].aoData[this[0]], e, i),
          this);
    }),
      Ht(["row().child.show()", "row().child().show()"], function (t) {
        return De(this, !0), this;
      }),
      Ht(["row().child.hide()", "row().child().hide()"], function () {
        return De(this, !1), this;
      }),
      Ht(["row().child.remove()", "row().child().remove()"], function () {
        return Le(this), this;
      }),
      Ht("row().child.isShown()", function () {
        var t = this.context;
        return (
          (t.length && this.length && t[0].aoData[this[0]]._detailsShow) || !1
        );
      });
    var ke = /^([^:]+):(name|visIdx|visible)$/,
      Ae = function (t, e, i, n, a) {
        (i = []), (n = 0);
        for (var r = a.length; n < r; n++) i.push(x(t, a[n], e));
        return i;
      };
    Ht("columns()", function (e, i) {
      e === n ? (e = "") : t.isPlainObject(e) && ((i = e), (e = "")),
        (i = Ce(i));
      var a = this.iterator(
        "table",
        function (n) {
          return (function (e, i, n) {
            var a = e.aoColumns,
              r = ie(a, "sName"),
              s = ie(a, "nTh");
            return Te(
              "column",
              i,
              function (i) {
                var o = Jt(i);
                if ("" === i) return ae(a.length);
                if (null !== o) return [0 <= o ? o : a.length + o];
                if ("function" == typeof i) {
                  var l = Pe(e, n);
                  return t.map(a, function (t, n) {
                    return i(n, Ae(e, n, 0, 0, l), s[n]) ? n : null;
                  });
                }
                var u = "string" == typeof i ? i.match(ke) : "";
                if (u)
                  switch (u[2]) {
                    case "visIdx":
                    case "visible":
                      if (0 > (o = parseInt(u[1], 10))) {
                        var c = t.map(a, function (t, e) {
                          return t.bVisible ? e : null;
                        });
                        return [c[c.length + o]];
                      }
                      return [f(e, o)];
                    case "name":
                      return t.map(r, function (t, e) {
                        return t === u[1] ? e : null;
                      });
                    default:
                      return [];
                  }
                return i.nodeName && i._DT_CellIndex
                  ? [i._DT_CellIndex.column]
                  : (o = t(s)
                      .filter(i)
                      .map(function () {
                        return t.inArray(this, s);
                      })
                      .toArray()).length || !i.nodeName
                  ? o
                  : (o = t(i).closest("*[data-dt-column]")).length
                  ? [o.data("dt-column")]
                  : [];
              },
              e,
              n
            );
          })(n, e, i);
        },
        1
      );
      return (a.selector.cols = e), (a.selector.opts = i), a;
    }),
      Vt("columns().header()", "column().header()", function (t, e) {
        return this.iterator(
          "column",
          function (t, e) {
            return t.aoColumns[e].nTh;
          },
          1
        );
      }),
      Vt("columns().footer()", "column().footer()", function (t, e) {
        return this.iterator(
          "column",
          function (t, e) {
            return t.aoColumns[e].nTf;
          },
          1
        );
      }),
      Vt("columns().data()", "column().data()", function () {
        return this.iterator("column-rows", Ae, 1);
      }),
      Vt("columns().dataSrc()", "column().dataSrc()", function () {
        return this.iterator(
          "column",
          function (t, e) {
            return t.aoColumns[e].mData;
          },
          1
        );
      }),
      Vt("columns().cache()", "column().cache()", function (t) {
        return this.iterator(
          "column-rows",
          function (e, i, n, a, r) {
            return ne(
              e.aoData,
              r,
              "search" === t ? "_aFilterData" : "_aSortData",
              i
            );
          },
          1
        );
      }),
      Vt("columns().nodes()", "column().nodes()", function () {
        return this.iterator(
          "column-rows",
          function (t, e, i, n, a) {
            return ne(t.aoData, a, "anCells", e);
          },
          1
        );
      }),
      Vt("columns().visible()", "column().visible()", function (e, i) {
        var a = this,
          r = this.iterator("column", function (i, a) {
            if (e === n) return i.aoColumns[a].bVisible;
            var r,
              s = i.aoColumns,
              o = s[a],
              l = i.aoData;
            if (e !== n && o.bVisible !== e) {
              if (e) {
                var u = t.inArray(!0, ie(s, "bVisible"), a + 1);
                for (s = 0, r = l.length; s < r; s++) {
                  var c = l[s].nTr;
                  (i = l[s].anCells), c && c.insertBefore(i[a], i[u] || null);
                }
              } else t(ie(i.aoData, "anCells", a)).detach();
              o.bVisible = e;
            }
          });
        return (
          e !== n &&
            this.iterator("table", function (r) {
              I(r, r.aoHeader),
                I(r, r.aoFooter),
                r.aiDisplay.length ||
                  t(r.nTBody).find("td[colspan]").attr("colspan", g(r)),
                Ct(r),
                a.iterator("column", function (t, n) {
                  zt(t, null, "column-visibility", [t, n, e, i]);
                }),
                (i === n || i) && a.columns.adjust();
            }),
          r
        );
      }),
      Vt("columns().indexes()", "column().index()", function (t) {
        return this.iterator(
          "column",
          function (e, i) {
            return "visible" === t ? m(e, i) : i;
          },
          1
        );
      }),
      Ht("columns.adjust()", function () {
        return this.iterator(
          "table",
          function (t) {
            p(t);
          },
          1
        );
      }),
      Ht("column.index()", function (t, e) {
        if (0 !== this.context.length) {
          var i = this.context[0];
          if ("fromVisible" === t || "toData" === t) return f(i, e);
          if ("fromData" === t || "toVisible" === t) return m(i, e);
        }
      }),
      Ht("column()", function (t, e) {
        return Ee(this.columns(t, e));
      });
    Ht("cells()", function (e, i, a) {
      if (
        (t.isPlainObject(e) &&
          (e.row === n ? ((a = e), (e = null)) : ((a = i), (i = null))),
        t.isPlainObject(i) && ((a = i), (i = null)),
        null === i || i === n)
      )
        return this.iterator("table", function (i) {
          return (function (e, i, a) {
            var r,
              s,
              o,
              l,
              u,
              c,
              d,
              h = e.aoData,
              p = Pe(e, a),
              f = re(ne(h, p, "anCells")),
              m = t(oe([], f)),
              g = e.aoColumns.length;
            return Te(
              "cell",
              i,
              function (i) {
                var a = "function" == typeof i;
                if (null === i || i === n || a) {
                  for (s = [], o = 0, l = p.length; o < l; o++)
                    for (r = p[o], u = 0; u < g; u++)
                      (c = { row: r, column: u }),
                        a
                          ? ((d = h[r]),
                            i(c, x(e, r, u), d.anCells ? d.anCells[u] : null) &&
                              s.push(c))
                          : s.push(c);
                  return s;
                }
                return t.isPlainObject(i)
                  ? i.column !== n && i.row !== n && -1 !== t.inArray(i.row, p)
                    ? [i]
                    : []
                  : (a = m
                      .filter(i)
                      .map(function (t, e) {
                        return {
                          row: e._DT_CellIndex.row,
                          column: e._DT_CellIndex.column,
                        };
                      })
                      .toArray()).length || !i.nodeName
                  ? a
                  : (d = t(i).closest("*[data-dt-row]")).length
                  ? [{ row: d.data("dt-row"), column: d.data("dt-column") }]
                  : [];
              },
              e,
              a
            );
          })(i, e, Ce(a));
        });
      var r,
        s,
        o,
        l,
        u = a ? { page: a.page, order: a.order, search: a.search } : {},
        c = this.columns(i, u),
        d = this.rows(e, u);
      return (
        (u = this.iterator(
          "table",
          function (t, e) {
            for (t = [], r = 0, s = d[e].length; r < s; r++)
              for (o = 0, l = c[e].length; o < l; o++)
                t.push({ row: d[e][r], column: c[e][o] });
            return t;
          },
          1
        )),
        (u = a && a.selected ? this.cells(u, a) : u),
        t.extend(u.selector, { cols: i, rows: e, opts: a }),
        u
      );
    }),
      Vt("cells().nodes()", "cell().node()", function () {
        return this.iterator(
          "cell",
          function (t, e, i) {
            return (t = t.aoData[e]) && t.anCells ? t.anCells[i] : n;
          },
          1
        );
      }),
      Ht("cells().data()", function () {
        return this.iterator(
          "cell",
          function (t, e, i) {
            return x(t, e, i);
          },
          1
        );
      }),
      Vt("cells().cache()", "cell().cache()", function (t) {
        return (
          (t = "search" === t ? "_aFilterData" : "_aSortData"),
          this.iterator(
            "cell",
            function (e, i, n) {
              return e.aoData[i][t][n];
            },
            1
          )
        );
      }),
      Vt("cells().render()", "cell().render()", function (t) {
        return this.iterator(
          "cell",
          function (e, i, n) {
            return x(e, i, n, t);
          },
          1
        );
      }),
      Vt("cells().indexes()", "cell().index()", function () {
        return this.iterator(
          "cell",
          function (t, e, i) {
            return { row: e, column: i, columnVisible: m(t, i) };
          },
          1
        );
      }),
      Vt("cells().invalidate()", "cell().invalidate()", function (t) {
        return this.iterator("cell", function (e, i, n) {
          L(e, i, t, n);
        });
      }),
      Ht("cell()", function (t, e, i) {
        return Ee(this.cells(t, e, i));
      }),
      Ht("cell().data()", function (t) {
        var e = this.context,
          i = this[0];
        return t === n
          ? e.length && i.length
            ? x(e[0], i[0].row, i[0].column)
            : n
          : (S(e[0], i[0].row, i[0].column, t),
            L(e[0], i[0].row, "data", i[0].column),
            this);
      }),
      Ht("order()", function (t, e) {
        var i = this.context;
        return t === n
          ? 0 !== i.length
            ? i[0].aaSorting
            : n
          : ("number" == typeof t
              ? (t = [[t, e]])
              : t.length &&
                !Array.isArray(t[0]) &&
                (t = Array.prototype.slice.call(arguments)),
            this.iterator("table", function (e) {
              e.aaSorting = t.slice();
            }));
      }),
      Ht("order.listener()", function (t, e, i) {
        return this.iterator("table", function (n) {
          xt(n, t, e, i);
        });
      }),
      Ht("order.fixed()", function (e) {
        if (!e) {
          var i = this.context;
          return (
            (i = i.length ? i[0].aaSortingFixed : n),
            Array.isArray(i) ? { pre: i } : i
          );
        }
        return this.iterator("table", function (i) {
          i.aaSortingFixed = t.extend(!0, {}, e);
        });
      }),
      Ht(["columns().order()", "column().order()"], function (e) {
        var i = this;
        return this.iterator("table", function (n, a) {
          var r = [];
          t.each(i[a], function (t, i) {
            r.push([i, e]);
          }),
            (n.aaSorting = r);
        });
      }),
      Ht("search()", function (e, i, a, r) {
        var s = this.context;
        return e === n
          ? 0 !== s.length
            ? s[0].oPreviousSearch.sSearch
            : n
          : this.iterator("table", function (n) {
              n.oFeatures.bFilter &&
                Z(
                  n,
                  t.extend({}, n.oPreviousSearch, {
                    sSearch: e + "",
                    bRegex: null !== i && i,
                    bSmart: null === a || a,
                    bCaseInsensitive: null === r || r,
                  }),
                  1
                );
            });
      }),
      Vt("columns().search()", "column().search()", function (e, i, a, r) {
        return this.iterator("column", function (s, o) {
          var l = s.aoPreSearchCols;
          if (e === n) return l[o].sSearch;
          s.oFeatures.bFilter &&
            (t.extend(l[o], {
              sSearch: e + "",
              bRegex: null !== i && i,
              bSmart: null === a || a,
              bCaseInsensitive: null === r || r,
            }),
            Z(s, s.oPreviousSearch, 1));
        });
      }),
      Ht("state()", function () {
        return this.context.length ? this.context[0].oSavedState : null;
      }),
      Ht("state.clear()", function () {
        return this.iterator("table", function (t) {
          t.fnStateSaveCallback.call(t.oInstance, t, {});
        });
      }),
      Ht("state.loaded()", function () {
        return this.context.length ? this.context[0].oLoadedState : null;
      }),
      Ht("state.save()", function () {
        return this.iterator("table", function (t) {
          Ct(t);
        });
      }),
      (Wt.versionCheck = Wt.fnVersionCheck =
        function (t) {
          for (
            var e,
              i,
              n = Wt.version.split("."),
              a = 0,
              r = (t = t.split(".")).length;
            a < r;
            a++
          )
            if ((e = parseInt(n[a], 10) || 0) !== (i = parseInt(t[a], 10) || 0))
              return e > i;
          return !0;
        }),
      (Wt.isDataTable = Wt.fnIsDataTable =
        function (e) {
          var i = t(e).get(0),
            n = !1;
          return (
            e instanceof Wt.Api ||
            (t.each(Wt.settings, function (e, a) {
              e = a.nScrollHead ? t("table", a.nScrollHead)[0] : null;
              var r = a.nScrollFoot ? t("table", a.nScrollFoot)[0] : null;
              (a.nTable !== i && e !== i && r !== i) || (n = !0);
            }),
            n)
          );
        }),
      (Wt.tables = Wt.fnTables =
        function (e) {
          var i = !1;
          t.isPlainObject(e) && ((i = e.api), (e = e.visible));
          var n = t.map(Wt.settings, function (i) {
            if (!e || (e && t(i.nTable).is(":visible"))) return i.nTable;
          });
          return i ? new we(n) : n;
        }),
      (Wt.camelToHungarian = r),
      Ht("$()", function (e, i) {
        return (
          (i = this.rows(i).nodes()),
          (i = t(i)),
          t([].concat(i.filter(e).toArray(), i.find(e).toArray()))
        );
      }),
      t.each(["on", "one", "off"], function (e, i) {
        Ht(i + "()", function () {
          var e = Array.prototype.slice.call(arguments);
          e[0] = t
            .map(e[0].split(/\s/), function (t) {
              return t.match(/\.dt\b/) ? t : t + ".dt";
            })
            .join(" ");
          var n = t(this.tables().nodes());
          return n[i].apply(n, e), this;
        });
      }),
      Ht("clear()", function () {
        return this.iterator("table", function (t) {
          E(t);
        });
      }),
      Ht("settings()", function () {
        return new we(this.context, this.context);
      }),
      Ht("init()", function () {
        var t = this.context;
        return t.length ? t[0].oInit : null;
      }),
      Ht("data()", function () {
        return this.iterator("table", function (t) {
          return ie(t.aoData, "_aData");
        }).flatten();
      }),
      Ht("destroy()", function (i) {
        return (
          (i = i || !1),
          this.iterator("table", function (n) {
            var a = n.nTableWrapper.parentNode,
              r = n.oClasses,
              s = n.nTable,
              o = n.nTBody,
              l = n.nTHead,
              u = n.nTFoot,
              c = t(s);
            o = t(o);
            var d,
              h = t(n.nTableWrapper),
              p = t.map(n.aoData, function (t) {
                return t.nTr;
              });
            (n.bDestroying = !0),
              zt(n, "aoDestroyCallback", "destroy", [n]),
              i || new we(n).columns().visible(!0),
              h.off(".DT").find(":not(tbody *)").off(".DT"),
              t(e).off(".DT-" + n.sInstance),
              s != l.parentNode && (c.children("thead").detach(), c.append(l)),
              u &&
                s != u.parentNode &&
                (c.children("tfoot").detach(), c.append(u)),
              (n.aaSorting = []),
              (n.aaSortingFixed = []),
              St(n),
              t(p).removeClass(n.asStripeClasses.join(" ")),
              t("th, td", l).removeClass(
                r.sSortable +
                  " " +
                  r.sSortableAsc +
                  " " +
                  r.sSortableDesc +
                  " " +
                  r.sSortableNone
              ),
              o.children().detach(),
              o.append(p),
              c[(l = i ? "remove" : "detach")](),
              h[l](),
              !i &&
                a &&
                (a.insertBefore(s, n.nTableReinsertBefore),
                c.css("width", n.sDestroyWidth).removeClass(r.sTable),
                (d = n.asDestroyStripes.length) &&
                  o.children().each(function (e) {
                    t(this).addClass(n.asDestroyStripes[e % d]);
                  })),
              -1 !== (a = t.inArray(n, Wt.settings)) &&
                Wt.settings.splice(a, 1);
          })
        );
      }),
      t.each(["column", "row", "cell"], function (t, e) {
        Ht(e + "s().every()", function (t) {
          var i = this.selector.opts,
            a = this;
          return this.iterator(e, function (r, s, o, l, u) {
            t.call(
              a[e](s, "cell" === e ? o : i, "cell" === e ? i : n),
              s,
              o,
              l,
              u
            );
          });
        });
      }),
      Ht("i18n()", function (e, i, a) {
        var r = this.context[0];
        return (
          (e = he(e)(r.oLanguage)) === n && (e = i),
          a !== n && t.isPlainObject(e) && (e = e[a] !== n ? e[a] : e._),
          e.replace("%d", a)
        );
      }),
      (Wt.version = "1.11.3"),
      (Wt.settings = []),
      (Wt.models = {}),
      (Wt.models.oSearch = {
        bCaseInsensitive: !0,
        sSearch: "",
        bRegex: !1,
        bSmart: !0,
        return: !1,
      }),
      (Wt.models.oRow = {
        nTr: null,
        anCells: null,
        _aData: [],
        _aSortData: null,
        _aFilterData: null,
        _sFilterRow: null,
        _sRowStripe: "",
        src: null,
        idx: -1,
      }),
      (Wt.models.oColumn = {
        idx: null,
        aDataSort: null,
        asSorting: null,
        bSearchable: null,
        bSortable: null,
        bVisible: null,
        _sManualType: null,
        _bAttrSrc: !1,
        fnCreatedCell: null,
        fnGetData: null,
        fnSetData: null,
        mData: null,
        mRender: null,
        nTh: null,
        nTf: null,
        sClass: null,
        sContentPadding: null,
        sDefaultContent: null,
        sName: null,
        sSortDataType: "std",
        sSortingClass: null,
        sSortingClassJUI: null,
        sTitle: null,
        sType: null,
        sWidth: null,
        sWidthOrig: null,
      }),
      (Wt.defaults = {
        aaData: null,
        aaSorting: [[0, "asc"]],
        aaSortingFixed: [],
        ajax: null,
        aLengthMenu: [10, 25, 50, 100],
        aoColumns: null,
        aoColumnDefs: null,
        aoSearchCols: [],
        asStripeClasses: null,
        bAutoWidth: !0,
        bDeferRender: !1,
        bDestroy: !1,
        bFilter: !0,
        bInfo: !0,
        bLengthChange: !0,
        bPaginate: !0,
        bProcessing: !1,
        bRetrieve: !1,
        bScrollCollapse: !1,
        bServerSide: !1,
        bSort: !0,
        bSortMulti: !0,
        bSortCellsTop: !1,
        bSortClasses: !0,
        bStateSave: !1,
        fnCreatedRow: null,
        fnDrawCallback: null,
        fnFooterCallback: null,
        fnFormatNumber: function (t) {
          return t
            .toString()
            .replace(/\B(?=(\d{3})+(?!\d))/g, this.oLanguage.sThousands);
        },
        fnHeaderCallback: null,
        fnInfoCallback: null,
        fnInitComplete: null,
        fnPreDrawCallback: null,
        fnRowCallback: null,
        fnServerData: null,
        fnServerParams: null,
        fnStateLoadCallback: function (t) {
          try {
            return JSON.parse(
              (-1 === t.iStateDuration ? sessionStorage : localStorage).getItem(
                "DataTables_" + t.sInstance + "_" + location.pathname
              )
            );
          } catch (t) {
            return {};
          }
        },
        fnStateLoadParams: null,
        fnStateLoaded: null,
        fnStateSaveCallback: function (t, e) {
          try {
            (-1 === t.iStateDuration ? sessionStorage : localStorage).setItem(
              "DataTables_" + t.sInstance + "_" + location.pathname,
              JSON.stringify(e)
            );
          } catch (t) {}
        },
        fnStateSaveParams: null,
        iStateDuration: 7200,
        iDeferLoading: null,
        iDisplayLength: 10,
        iDisplayStart: 0,
        iTabIndex: 0,
        oClasses: {},
        oLanguage: {
          oAria: {
            sSortAscending: ": activate to sort column ascending",
            sSortDescending: ": activate to sort column descending",
          },
          oPaginate: {
            sFirst: "First",
            sLast: "Last",
            sNext: "Next",
            sPrevious: "Previous",
          },
          sEmptyTable: "No data available in table",
          sInfo: "Showing _START_ to _END_ of _TOTAL_ entries",
          sInfoEmpty: "Showing 0 to 0 of 0 entries",
          sInfoFiltered: "(filtered from _MAX_ total entries)",
          sInfoPostFix: "",
          sDecimal: "",
          sThousands: ",",
          sLengthMenu: "Show _MENU_ entries",
          sLoadingRecords: "Loading...",
          sProcessing: "Processing...",
          sSearch: "Search:",
          sSearchPlaceholder: "",
          sUrl: "",
          sZeroRecords: "No matching records found",
        },
        oSearch: t.extend({}, Wt.models.oSearch),
        sAjaxDataProp: "data",
        sAjaxSource: null,
        sDom: "lfrtip",
        searchDelay: null,
        sPaginationType: "simple_numbers",
        sScrollX: "",
        sScrollXInner: "",
        sScrollY: "",
        sServerMethod: "GET",
        renderer: null,
        rowId: "DT_RowId",
      }),
      a(Wt.defaults),
      (Wt.defaults.column = {
        aDataSort: null,
        iDataSort: -1,
        asSorting: ["asc", "desc"],
        bSearchable: !0,
        bSortable: !0,
        bVisible: !0,
        fnCreatedCell: null,
        mData: null,
        mRender: null,
        sCellType: "td",
        sClass: "",
        sContentPadding: "",
        sDefaultContent: null,
        sName: "",
        sSortDataType: "std",
        sTitle: null,
        sType: null,
        sWidth: null,
      }),
      a(Wt.defaults.column),
      (Wt.models.oSettings = {
        oFeatures: {
          bAutoWidth: null,
          bDeferRender: null,
          bFilter: null,
          bInfo: null,
          bLengthChange: null,
          bPaginate: null,
          bProcessing: null,
          bServerSide: null,
          bSort: null,
          bSortMulti: null,
          bSortClasses: null,
          bStateSave: null,
        },
        oScroll: {
          bCollapse: null,
          iBarWidth: 0,
          sX: null,
          sXInner: null,
          sY: null,
        },
        oLanguage: { fnInfoCallback: null },
        oBrowser: {
          bScrollOversize: !1,
          bScrollbarLeft: !1,
          bBounding: !1,
          barWidth: 0,
        },
        ajax: null,
        aanFeatures: [],
        aoData: [],
        aiDisplay: [],
        aiDisplayMaster: [],
        aIds: {},
        aoColumns: [],
        aoHeader: [],
        aoFooter: [],
        oPreviousSearch: {},
        aoPreSearchCols: [],
        aaSorting: null,
        aaSortingFixed: [],
        asStripeClasses: null,
        asDestroyStripes: [],
        sDestroyWidth: 0,
        aoRowCallback: [],
        aoHeaderCallback: [],
        aoFooterCallback: [],
        aoDrawCallback: [],
        aoRowCreatedCallback: [],
        aoPreDrawCallback: [],
        aoInitComplete: [],
        aoStateSaveParams: [],
        aoStateLoadParams: [],
        aoStateLoaded: [],
        sTableId: "",
        nTable: null,
        nTHead: null,
        nTFoot: null,
        nTBody: null,
        nTableWrapper: null,
        bDeferLoading: !1,
        bInitialised: !1,
        aoOpenRows: [],
        sDom: null,
        searchDelay: null,
        sPaginationType: "two_button",
        iStateDuration: 0,
        aoStateSave: [],
        aoStateLoad: [],
        oSavedState: null,
        oLoadedState: null,
        sAjaxSource: null,
        sAjaxDataProp: null,
        jqXHR: null,
        json: n,
        oAjaxData: n,
        fnServerData: null,
        aoServerParams: [],
        sServerMethod: null,
        fnFormatNumber: null,
        aLengthMenu: null,
        iDraw: 0,
        bDrawing: !1,
        iDrawError: -1,
        _iDisplayLength: 10,
        _iDisplayStart: 0,
        _iRecordsTotal: 0,
        _iRecordsDisplay: 0,
        oClasses: {},
        bFiltered: !1,
        bSorted: !1,
        bSortCellsTop: null,
        oInit: null,
        aoDestroyCallback: [],
        fnRecordsTotal: function () {
          return "ssp" == Bt(this)
            ? 1 * this._iRecordsTotal
            : this.aiDisplayMaster.length;
        },
        fnRecordsDisplay: function () {
          return "ssp" == Bt(this)
            ? 1 * this._iRecordsDisplay
            : this.aiDisplay.length;
        },
        fnDisplayEnd: function () {
          var t = this._iDisplayLength,
            e = this._iDisplayStart,
            i = e + t,
            n = this.aiDisplay.length,
            a = this.oFeatures,
            r = a.bPaginate;
          return a.bServerSide
            ? !1 === r || -1 === t
              ? e + n
              : Math.min(e + t, this._iRecordsDisplay)
            : !r || i > n || -1 === t
            ? n
            : i;
        },
        oInstance: null,
        sInstance: null,
        iTabIndex: 0,
        nScrollHead: null,
        nScrollFoot: null,
        aLastSort: [],
        oPlugins: {},
        rowIdFn: null,
        rowId: null,
      }),
      (Wt.ext = $t =
        {
          buttons: {},
          classes: {},
          builder: "-source-",
          errMode: "alert",
          feature: [],
          search: [],
          selector: { cell: [], column: [], row: [] },
          internal: {},
          legacy: { ajax: null },
          pager: {},
          renderer: { pageButton: {}, header: {} },
          order: {},
          type: { detect: [], search: {}, order: {} },
          _unique: 0,
          fnVersionCheck: Wt.fnVersionCheck,
          iApiIndex: 0,
          oJUIClasses: {},
          sVersion: Wt.version,
        }),
      t.extend($t, {
        afnFiltering: $t.search,
        aTypes: $t.type.detect,
        ofnSearch: $t.type.search,
        oSort: $t.type.order,
        afnSortData: $t.order,
        aoFeatures: $t.feature,
        oApi: $t.internal,
        oStdClasses: $t.classes,
        oPagination: $t.pager,
      }),
      t.extend(Wt.ext.classes, {
        sTable: "dataTable",
        sNoFooter: "no-footer",
        sPageButton: "paginate_button",
        sPageButtonActive: "current",
        sPageButtonDisabled: "disabled",
        sStripeOdd: "odd",
        sStripeEven: "even",
        sRowEmpty: "dataTables_empty",
        sWrapper: "dataTables_wrapper",
        sFilter: "dataTables_filter",
        sInfo: "dataTables_info",
        sPaging: "dataTables_paginate paging_",
        sLength: "dataTables_length",
        sProcessing: "dataTables_processing",
        sSortAsc: "sorting_asc",
        sSortDesc: "sorting_desc",
        sSortable: "sorting",
        sSortableAsc: "sorting_desc_disabled",
        sSortableDesc: "sorting_asc_disabled",
        sSortableNone: "sorting_disabled",
        sSortColumn: "sorting_",
        sFilterInput: "",
        sLengthSelect: "",
        sScrollWrapper: "dataTables_scroll",
        sScrollHead: "dataTables_scrollHead",
        sScrollHeadInner: "dataTables_scrollHeadInner",
        sScrollBody: "dataTables_scrollBody",
        sScrollFoot: "dataTables_scrollFoot",
        sScrollFootInner: "dataTables_scrollFootInner",
        sHeaderTH: "",
        sFooterTH: "",
        sSortJUIAsc: "",
        sSortJUIDesc: "",
        sSortJUI: "",
        sSortJUIAscAllowed: "",
        sSortJUIDescAllowed: "",
        sSortJUIWrapper: "",
        sSortIcon: "",
        sJUIHeader: "",
        sJUIFooter: "",
      });
    var Ie = Wt.ext.pager;
    t.extend(Ie, {
      simple: function (t, e) {
        return ["previous", "next"];
      },
      full: function (t, e) {
        return ["first", "previous", "next", "last"];
      },
      numbers: function (t, e) {
        return [jt(t, e)];
      },
      simple_numbers: function (t, e) {
        return ["previous", jt(t, e), "next"];
      },
      full_numbers: function (t, e) {
        return ["first", "previous", jt(t, e), "next", "last"];
      },
      first_last_numbers: function (t, e) {
        return ["first", jt(t, e), "last"];
      },
      _numbers: jt,
      numbers_length: 7,
    }),
      t.extend(!0, Wt.ext.renderer, {
        pageButton: {
          _: function (e, a, r, s, o, l) {
            var u,
              c,
              d = e.oClasses,
              h = e.oLanguage.oPaginate,
              p = e.oLanguage.oAria.paginate || {},
              f = 0,
              m = function (i, n) {
                var a,
                  s = d.sPageButtonDisabled,
                  g = function (t) {
                    ot(e, t.data.action, !0);
                  },
                  v = 0;
                for (a = n.length; v < a; v++) {
                  var _ = n[v];
                  if (Array.isArray(_)) {
                    var y = t("<" + (_.DT_el || "div") + "/>").appendTo(i);
                    m(y, _);
                  } else {
                    switch (((u = null), (c = _), (y = e.iTabIndex), _)) {
                      case "ellipsis":
                        i.append('<span class="ellipsis">&#x2026;</span>');
                        break;
                      case "first":
                        (u = h.sFirst), 0 === o && ((y = -1), (c += " " + s));
                        break;
                      case "previous":
                        (u = h.sPrevious),
                          0 === o && ((y = -1), (c += " " + s));
                        break;
                      case "next":
                        (u = h.sNext),
                          (0 !== l && o !== l - 1) ||
                            ((y = -1), (c += " " + s));
                        break;
                      case "last":
                        (u = h.sLast),
                          (0 !== l && o !== l - 1) ||
                            ((y = -1), (c += " " + s));
                        break;
                      default:
                        (u = e.fnFormatNumber(_ + 1)),
                          (c = o === _ ? d.sPageButtonActive : "");
                    }
                    null !== u &&
                      (At(
                        (y = t("<a>", {
                          class: d.sPageButton + " " + c,
                          "aria-controls": e.sTableId,
                          "aria-label": p[_],
                          "data-dt-idx": f,
                          tabindex: y,
                          id:
                            0 === r && "string" == typeof _
                              ? e.sTableId + "_" + _
                              : null,
                        })
                          .html(u)
                          .appendTo(i)),
                        { action: _ },
                        g
                      ),
                      f++);
                  }
                }
              };
            try {
              var g = t(a).find(i.activeElement).data("dt-idx");
            } catch (t) {}
            m(t(a).empty(), s),
              g !== n &&
                t(a)
                  .find("[data-dt-idx=" + g + "]")
                  .trigger("focus");
          },
        },
      }),
      t.extend(Wt.ext.type.detect, [
        function (t, e) {
          return (e = e.oLanguage.sDecimal), te(t, e) ? "num" + e : null;
        },
        function (t, e) {
          return (!t || t instanceof Date || Yt.test(t)) &&
            ((null !== (e = Date.parse(t)) && !isNaN(e)) || Kt(t))
            ? "date"
            : null;
        },
        function (t, e) {
          return (
            (e = e.oLanguage.sDecimal), te(t, e, !0) ? "num-fmt" + e : null
          );
        },
        function (t, e) {
          return (e = e.oLanguage.sDecimal), ee(t, e) ? "html-num" + e : null;
        },
        function (t, e) {
          return (
            (e = e.oLanguage.sDecimal), ee(t, e, !0) ? "html-num-fmt" + e : null
          );
        },
        function (t, e) {
          return Kt(t) || ("string" == typeof t && -1 !== t.indexOf("<"))
            ? "html"
            : null;
        },
      ]),
      t.extend(Wt.ext.type.search, {
        html: function (t) {
          return Kt(t)
            ? t
            : "string" == typeof t
            ? t.replace(Gt, " ").replace(Ut, "")
            : "";
        },
        string: function (t) {
          return Kt(t) ? t : "string" == typeof t ? t.replace(Gt, " ") : t;
        },
      });
    var ze = function (t, e, i, n) {
      return 0 === t || (t && "-" !== t)
        ? (e && (t = Qt(t, e)),
          t.replace &&
            (i && (t = t.replace(i, "")), n && (t = t.replace(n, ""))),
          1 * t)
        : -1 / 0;
    };
    t.extend($t.type.order, {
      "date-pre": function (t) {
        return (t = Date.parse(t)), isNaN(t) ? -1 / 0 : t;
      },
      "html-pre": function (t) {
        return Kt(t)
          ? ""
          : t.replace
          ? t.replace(/<.*?>/g, "").toLowerCase()
          : t + "";
      },
      "string-pre": function (t) {
        return Kt(t)
          ? ""
          : "string" == typeof t
          ? t.toLowerCase()
          : t.toString
          ? t.toString()
          : "";
      },
      "string-asc": function (t, e) {
        return t < e ? -1 : t > e ? 1 : 0;
      },
      "string-desc": function (t, e) {
        return t < e ? 1 : t > e ? -1 : 0;
      },
    }),
      Ft(""),
      t.extend(!0, Wt.ext.renderer, {
        header: {
          _: function (e, i, n, a) {
            t(e.nTable).on("order.dt.DT", function (t, r, s, o) {
              e === r &&
                ((t = n.idx),
                i
                  .removeClass(a.sSortAsc + " " + a.sSortDesc)
                  .addClass(
                    "asc" == o[t]
                      ? a.sSortAsc
                      : "desc" == o[t]
                      ? a.sSortDesc
                      : n.sSortingClass
                  ));
            });
          },
          jqueryui: function (e, i, n, a) {
            t("<div/>")
              .addClass(a.sSortJUIWrapper)
              .append(i.contents())
              .append(
                t("<span/>").addClass(a.sSortIcon + " " + n.sSortingClassJUI)
              )
              .appendTo(i),
              t(e.nTable).on("order.dt.DT", function (t, r, s, o) {
                e === r &&
                  ((t = n.idx),
                  i
                    .removeClass(a.sSortAsc + " " + a.sSortDesc)
                    .addClass(
                      "asc" == o[t]
                        ? a.sSortAsc
                        : "desc" == o[t]
                        ? a.sSortDesc
                        : n.sSortingClass
                    ),
                  i
                    .find("span." + a.sSortIcon)
                    .removeClass(
                      a.sSortJUIAsc +
                        " " +
                        a.sSortJUIDesc +
                        " " +
                        a.sSortJUI +
                        " " +
                        a.sSortJUIAscAllowed +
                        " " +
                        a.sSortJUIDescAllowed
                    )
                    .addClass(
                      "asc" == o[t]
                        ? a.sSortJUIAsc
                        : "desc" == o[t]
                        ? a.sSortJUIDesc
                        : n.sSortingClassJUI
                    ));
              });
          },
        },
      });
    var Oe = function (t) {
      return (
        Array.isArray(t) && (t = t.join(",")),
        "string" == typeof t
          ? t
              .replace(/&/g, "&amp;")
              .replace(/</g, "&lt;")
              .replace(/>/g, "&gt;")
              .replace(/"/g, "&quot;")
          : t
      );
    };
    return (
      (Wt.render = {
        number: function (t, e, i, n, a) {
          return {
            display: function (r) {
              if ("number" != typeof r && "string" != typeof r) return r;
              var s = 0 > r ? "-" : "",
                o = parseFloat(r);
              return isNaN(o)
                ? Oe(r)
                : ((o = o.toFixed(i)),
                  (r = Math.abs(o)),
                  (o = parseInt(r, 10)),
                  (r = i ? e + (r - o).toFixed(i).substring(2) : ""),
                  0 === o && 0 === parseFloat(r) && (s = ""),
                  s +
                    (n || "") +
                    o.toString().replace(/\B(?=(\d{3})+(?!\d))/g, t) +
                    r +
                    (a || ""));
            },
          };
        },
        text: function () {
          return { display: Oe, filter: Oe };
        },
      }),
      t.extend(Wt.ext.internal, {
        _fnExternApiFunc: Rt,
        _fnBuildAjax: F,
        _fnAjaxUpdate: R,
        _fnAjaxParameters: $,
        _fnAjaxUpdateDraw: H,
        _fnAjaxDataSrc: V,
        _fnAddColumn: d,
        _fnColumnOptions: h,
        _fnAdjustColumnSizing: p,
        _fnVisibleToColumnIndex: f,
        _fnColumnIndexToVisible: m,
        _fnVisbleColumns: g,
        _fnGetColumns: v,
        _fnColumnTypes: _,
        _fnApplyColumnDefs: y,
        _fnHungarianMap: a,
        _fnCamelToHungarian: r,
        _fnLanguageCompat: s,
        _fnBrowserDetect: u,
        _fnAddData: b,
        _fnAddTr: w,
        _fnNodeToDataIndex: function (t, e) {
          return e._DT_RowIndex !== n ? e._DT_RowIndex : null;
        },
        _fnNodeToColumnIndex: function (e, i, n) {
          return t.inArray(n, e.aoData[i].anCells);
        },
        _fnGetCellData: x,
        _fnSetCellData: S,
        _fnSplitObjNotation: T,
        _fnGetObjectDataFn: he,
        _fnSetObjectDataFn: pe,
        _fnGetDataMaster: C,
        _fnClearTable: E,
        _fnDeleteIndex: P,
        _fnInvalidate: L,
        _fnGetRowElements: D,
        _fnCreateTr: M,
        _fnBuildHead: A,
        _fnDrawHead: I,
        _fnDraw: z,
        _fnReDraw: O,
        _fnAddOptionsHtml: N,
        _fnDetectHeader: B,
        _fnGetUniqueThs: j,
        _fnFeatureHtmlFilter: W,
        _fnFilterComplete: Z,
        _fnFilterCustom: G,
        _fnFilterColumn: U,
        _fnFilter: Y,
        _fnFilterCreateSearch: q,
        _fnEscapeRegex: fe,
        _fnFilterData: X,
        _fnFeatureHtmlInfo: Q,
        _fnUpdateInfo: tt,
        _fnInfoMacros: et,
        _fnInitialise: it,
        _fnInitComplete: nt,
        _fnLengthChange: at,
        _fnFeatureHtmlLength: rt,
        _fnFeatureHtmlPaginate: st,
        _fnPageChange: ot,
        _fnFeatureHtmlProcessing: lt,
        _fnProcessingDisplay: ut,
        _fnFeatureHtmlTable: ct,
        _fnScrollDraw: dt,
        _fnApplyToChildren: ht,
        _fnCalculateColumnWidths: pt,
        _fnThrottle: _e,
        _fnConvertToWidth: ft,
        _fnGetWidestNode: mt,
        _fnGetMaxLenString: gt,
        _fnStringToCss: vt,
        _fnSortFlatten: _t,
        _fnSort: yt,
        _fnSortAria: bt,
        _fnSortListener: wt,
        _fnSortAttachListener: xt,
        _fnSortingClasses: St,
        _fnSortData: Tt,
        _fnSaveState: Ct,
        _fnLoadState: Et,
        _fnImplementState: Pt,
        _fnSettingsFromNode: Lt,
        _fnLog: Dt,
        _fnMap: Mt,
        _fnBindAction: At,
        _fnCallbackReg: It,
        _fnCallbackFire: zt,
        _fnLengthOverflow: Ot,
        _fnRenderer: Nt,
        _fnDataSource: Bt,
        _fnRowAttributes: k,
        _fnExtend: kt,
        _fnCalculateEnd: function () {},
      }),
      (t.fn.dataTable = Wt),
      (Wt.$ = t),
      (t.fn.dataTableSettings = Wt.settings),
      (t.fn.dataTableExt = Wt.ext),
      (t.fn.DataTable = function (e) {
        return t(this).dataTable(e).api();
      }),
      t.each(Wt, function (e, i) {
        t.fn.DataTable[e] = i;
      }),
      Wt
    );
  }),
  (($jscomp = $jscomp || {}).scope = {}),
  ($jscomp.findInternal = function (t, e, i) {
    t instanceof String && (t = String(t));
    for (var n = t.length, a = 0; a < n; a++) {
      var r = t[a];
      if (e.call(i, r, a, t)) return { i: a, v: r };
    }
    return { i: -1, v: void 0 };
  }),
  ($jscomp.ASSUME_ES5 = !1),
  ($jscomp.ASSUME_NO_NATIVE_MAP = !1),
  ($jscomp.ASSUME_NO_NATIVE_SET = !1),
  ($jscomp.SIMPLE_FROUND_POLYFILL = !1),
  ($jscomp.ISOLATE_POLYFILLS = !1),
  ($jscomp.defineProperty =
    $jscomp.ASSUME_ES5 || "function" == typeof Object.defineProperties
      ? Object.defineProperty
      : function (t, e, i) {
          return (
            t == Array.prototype || t == Object.prototype || (t[e] = i.value), t
          );
        }),
  ($jscomp.getGlobal = function (t) {
    t = [
      "object" == typeof globalThis && globalThis,
      t,
      "object" == typeof window && window,
      "object" == typeof self && self,
      "object" == typeof global && global,
    ];
    for (var e = 0; e < t.length; ++e) {
      var i = t[e];
      if (i && i.Math == Math) return i;
    }
    throw Error("Cannot find global object");
  }),
  ($jscomp.global = $jscomp.getGlobal(this)),
  ($jscomp.IS_SYMBOL_NATIVE =
    "function" == typeof Symbol && "symbol" == typeof Symbol("x")),
  ($jscomp.TRUST_ES6_POLYFILLS =
    !$jscomp.ISOLATE_POLYFILLS || $jscomp.IS_SYMBOL_NATIVE),
  ($jscomp.polyfills = {}),
  ($jscomp.propertyToPolyfillSymbol = {}),
  ($jscomp.POLYFILL_PREFIX = "$jscp$");
$jscomp$lookupPolyfilledValue = function (t, e) {
  var i = $jscomp.propertyToPolyfillSymbol[e];
  return null == i ? t[e] : void 0 !== (i = t[i]) ? i : t[e];
};
function ownKeys(t, e) {
  var i,
    n = Object.keys(t);
  return (
    Object.getOwnPropertySymbols &&
      ((i = Object.getOwnPropertySymbols(t)),
      e &&
        (i = i.filter(function (e) {
          return Object.getOwnPropertyDescriptor(t, e).enumerable;
        })),
      n.push.apply(n, i)),
    n
  );
}
function _objectSpread(t) {
  for (var e = 1; e < arguments.length; e++) {
    var i = null != arguments[e] ? arguments[e] : {};
    e % 2
      ? ownKeys(Object(i), !0).forEach(function (e) {
          _defineProperty(t, e, i[e]);
        })
      : Object.getOwnPropertyDescriptors
      ? Object.defineProperties(t, Object.getOwnPropertyDescriptors(i))
      : ownKeys(Object(i)).forEach(function (e) {
          Object.defineProperty(t, e, Object.getOwnPropertyDescriptor(i, e));
        });
  }
  return t;
}
function _defineProperty(t, e, i) {
  return (
    e in t
      ? Object.defineProperty(t, e, {
          value: i,
          enumerable: !0,
          configurable: !0,
          writable: !0,
        })
      : (t[e] = i),
    t
  );
}
function _classCallCheck(t, e) {
  if (!(t instanceof e))
    throw new TypeError("Cannot call a class as a function");
}
function _defineProperties(t, e) {
  for (var i = 0; i < e.length; i++) {
    var n = e[i];
    (n.enumerable = n.enumerable || !1),
      (n.configurable = !0),
      "value" in n && (n.writable = !0),
      Object.defineProperty(t, n.key, n);
  }
}
function _createClass(t, e, i) {
  return (
    e && _defineProperties(t.prototype, e), i && _defineProperties(t, i), t
  );
}
function _typeof(t) {
  return (_typeof =
    "function" == typeof Symbol && "symbol" == typeof Symbol.iterator
      ? function (t) {
          return typeof t;
        }
      : function (t) {
          return t &&
            "function" == typeof Symbol &&
            t.constructor === Symbol &&
            t !== Symbol.prototype
            ? "symbol"
            : typeof t;
        })(t);
}
($jscomp.polyfill = function (t, e, i, n) {
  e &&
    ($jscomp.ISOLATE_POLYFILLS
      ? $jscomp.polyfillIsolated(t, e, i, n)
      : $jscomp.polyfillUnisolated(t, e, i, n));
}),
  ($jscomp.polyfillUnisolated = function (t, e, i, n) {
    for (i = $jscomp.global, t = t.split("."), n = 0; n < t.length - 1; n++) {
      var a = t[n];
      if (!(a in i)) return;
      i = i[a];
    }
    (e = e((n = i[(t = t[t.length - 1])]))) != n &&
      null != e &&
      $jscomp.defineProperty(i, t, {
        configurable: !0,
        writable: !0,
        value: e,
      });
  }),
  ($jscomp.polyfillIsolated = function (t, e, i, n) {
    var a = t.split(".");
    (t = 1 === a.length),
      (n = a[0]),
      (n = !t && n in $jscomp.polyfills ? $jscomp.polyfills : $jscomp.global);
    for (var r = 0; r < a.length - 1; r++) {
      var s = a[r];
      if (!(s in n)) return;
      n = n[s];
    }
    (a = a[a.length - 1]),
      null !=
        (e = e((i = $jscomp.IS_SYMBOL_NATIVE && "es6" === i ? n[a] : null))) &&
        (t
          ? $jscomp.defineProperty($jscomp.polyfills, a, {
              configurable: !0,
              writable: !0,
              value: e,
            })
          : e !== i &&
            (($jscomp.propertyToPolyfillSymbol[a] = $jscomp.IS_SYMBOL_NATIVE
              ? $jscomp.global.Symbol(a)
              : $jscomp.POLYFILL_PREFIX + a),
            (a = $jscomp.propertyToPolyfillSymbol[a]),
            $jscomp.defineProperty(n, a, {
              configurable: !0,
              writable: !0,
              value: e,
            })));
  }),
  $jscomp.polyfill(
    "Array.prototype.find",
    function (t) {
      return (
        t ||
        function (t, e) {
          return $jscomp.findInternal(this, t, e).v;
        }
      );
    },
    "es6",
    "es3"
  ),
  (function (t) {
    "function" == typeof define && define.amd
      ? define(["jquery", "datatables.net"], function (e) {
          return t(e, window, document);
        })
      : "object" == typeof exports
      ? (module.exports = function (e, i) {
          return (
            e || (e = window),
            (i && i.fn.dataTable) || (i = require("datatables.net")(e, i).$),
            t(i, e, e.document)
          );
        })
      : t(jQuery, window, document);
  })(function (t, e, i, n) {
    var a = t.fn.dataTable;
    return (
      t.extend(!0, a.defaults, {
        dom: "<'row'<'col-sm-12 col-md-6'l><'col-sm-12 col-md-6'f>><'row'<'col-sm-12'tr>><'row'<'col-sm-12 col-md-5'i><'col-sm-12 col-md-7'p>>",
        renderer: "bootstrap",
      }),
      t.extend(a.ext.classes, {
        sWrapper: "dataTables_wrapper dt-bootstrap4",
        sFilterInput: "form-control form-control-sm",
        sLengthSelect:
          "custom-select custom-select-sm form-control form-control-sm",
        sProcessing: "dataTables_processing card",
        sPageButton: "paginate_button page-item",
      }),
      (a.ext.renderer.pageButton.bootstrap = function (e, r, s, o, l, u) {
        var c,
          d,
          h = new a.Api(e),
          p = e.oClasses,
          f = e.oLanguage.oPaginate,
          m = e.oLanguage.oAria.paginate || {},
          g = 0,
          v = function (i, n) {
            var a,
              r = function (e) {
                e.preventDefault(),
                  t(e.currentTarget).hasClass("disabled") ||
                    h.page() == e.data.action ||
                    h.page(e.data.action).draw("page");
              },
              o = 0;
            for (a = n.length; o < a; o++) {
              var _ = n[o];
              if (Array.isArray(_)) v(i, _);
              else {
                switch (((d = c = ""), _)) {
                  case "ellipsis":
                    (c = "&#x2026;"), (d = "disabled");
                    break;
                  case "first":
                    (c = f.sFirst), (d = _ + (0 < l ? "" : " disabled"));
                    break;
                  case "previous":
                    (c = f.sPrevious), (d = _ + (0 < l ? "" : " disabled"));
                    break;
                  case "next":
                    (c = f.sNext), (d = _ + (l < u - 1 ? "" : " disabled"));
                    break;
                  case "last":
                    (c = f.sLast), (d = _ + (l < u - 1 ? "" : " disabled"));
                    break;
                  default:
                    (c = _ + 1), (d = l === _ ? "active" : "");
                }
                if (c) {
                  var y = t("<li>", {
                    class: p.sPageButton + " " + d,
                    id:
                      0 === s && "string" == typeof _
                        ? e.sTableId + "_" + _
                        : null,
                  })
                    .append(
                      t("<a>", {
                        href: "#",
                        "aria-controls": e.sTableId,
                        "aria-label": m[_],
                        "data-dt-idx": g,
                        tabindex: e.iTabIndex,
                        class: "page-link",
                      }).html(c)
                    )
                    .appendTo(i);
                  e.oApi._fnBindAction(y, { action: _ }, r), g++;
                }
              }
            }
          };
        try {
          var _ = t(r).find(i.activeElement).data("dt-idx");
        } catch (t) {}
        v(t(r).empty().html('<ul class="pagination"/>').children("ul"), o),
          _ !== n &&
            t(r)
              .find("[data-dt-idx=" + _ + "]")
              .trigger("focus");
      }),
      a
    );
  }),
  /*!
Waypoints - 4.0.1
Copyright © 2011-2016 Caleb Troughton
Licensed under the MIT license.
https://github.com/imakewebthings/waypoints/blob/master/licenses.txt
*/
  (function () {
    "use strict";
    var t = 0,
      e = {};
    function i(n) {
      if (!n) throw new Error("No options passed to Waypoint constructor");
      if (!n.element)
        throw new Error("No element option passed to Waypoint constructor");
      if (!n.handler)
        throw new Error("No handler option passed to Waypoint constructor");
      (this.key = "waypoint-" + t),
        (this.options = i.Adapter.extend({}, i.defaults, n)),
        (this.element = this.options.element),
        (this.adapter = new i.Adapter(this.element)),
        (this.callback = n.handler),
        (this.axis = this.options.horizontal ? "horizontal" : "vertical"),
        (this.enabled = this.options.enabled),
        (this.triggerPoint = null),
        (this.group = i.Group.findOrCreate({
          name: this.options.group,
          axis: this.axis,
        })),
        (this.context = i.Context.findOrCreateByElement(this.options.context)),
        i.offsetAliases[this.options.offset] &&
          (this.options.offset = i.offsetAliases[this.options.offset]),
        this.group.add(this),
        this.context.add(this),
        (e[this.key] = this),
        (t += 1);
    }
    (i.prototype.queueTrigger = function (t) {
      this.group.queueTrigger(this, t);
    }),
      (i.prototype.trigger = function (t) {
        this.enabled && this.callback && this.callback.apply(this, t);
      }),
      (i.prototype.destroy = function () {
        this.context.remove(this), this.group.remove(this), delete e[this.key];
      }),
      (i.prototype.disable = function () {
        return (this.enabled = !1), this;
      }),
      (i.prototype.enable = function () {
        return this.context.refresh(), (this.enabled = !0), this;
      }),
      (i.prototype.next = function () {
        return this.group.next(this);
      }),
      (i.prototype.previous = function () {
        return this.group.previous(this);
      }),
      (i.invokeAll = function (t) {
        var i = [];
        for (var n in e) i.push(e[n]);
        for (var a = 0, r = i.length; a < r; a++) i[a][t]();
      }),
      (i.destroyAll = function () {
        i.invokeAll("destroy");
      }),
      (i.disableAll = function () {
        i.invokeAll("disable");
      }),
      (i.enableAll = function () {
        for (var t in (i.Context.refreshAll(), e)) e[t].enabled = !0;
        return this;
      }),
      (i.refreshAll = function () {
        i.Context.refreshAll();
      }),
      (i.viewportHeight = function () {
        return window.innerHeight || document.documentElement.clientHeight;
      }),
      (i.viewportWidth = function () {
        return document.documentElement.clientWidth;
      }),
      (i.adapters = []),
      (i.defaults = {
        context: window,
        continuous: !0,
        enabled: !0,
        group: "default",
        horizontal: !1,
        offset: 0,
      }),
      (i.offsetAliases = {
        "bottom-in-view": function () {
          return this.context.innerHeight() - this.adapter.outerHeight();
        },
        "right-in-view": function () {
          return this.context.innerWidth() - this.adapter.outerWidth();
        },
      }),
      (window.Waypoint = i);
  })(),
  (function () {
    "use strict";
    function t(t) {
      window.setTimeout(t, 1e3 / 60);
    }
    var e = 0,
      i = {},
      n = window.Waypoint,
      a = window.onload;
    function r(t) {
      (this.element = t),
        (this.Adapter = n.Adapter),
        (this.adapter = new this.Adapter(t)),
        (this.key = "waypoint-context-" + e),
        (this.didScroll = !1),
        (this.didResize = !1),
        (this.oldScroll = {
          x: this.adapter.scrollLeft(),
          y: this.adapter.scrollTop(),
        }),
        (this.waypoints = { vertical: {}, horizontal: {} }),
        (t.waypointContextKey = this.key),
        (i[t.waypointContextKey] = this),
        (e += 1),
        n.windowContext ||
          ((n.windowContext = !0), (n.windowContext = new r(window))),
        this.createThrottledScrollHandler(),
        this.createThrottledResizeHandler();
    }
    (r.prototype.add = function (t) {
      var e = t.options.horizontal ? "horizontal" : "vertical";
      (this.waypoints[e][t.key] = t), this.refresh();
    }),
      (r.prototype.checkEmpty = function () {
        var t = this.Adapter.isEmptyObject(this.waypoints.horizontal),
          e = this.Adapter.isEmptyObject(this.waypoints.vertical),
          n = this.element == this.element.window;
        t && e && !n && (this.adapter.off(".waypoints"), delete i[this.key]);
      }),
      (r.prototype.createThrottledResizeHandler = function () {
        var t = this;
        function e() {
          t.handleResize(), (t.didResize = !1);
        }
        this.adapter.on("resize.waypoints", function () {
          t.didResize || ((t.didResize = !0), n.requestAnimationFrame(e));
        });
      }),
      (r.prototype.createThrottledScrollHandler = function () {
        var t = this;
        function e() {
          t.handleScroll(), (t.didScroll = !1);
        }
        this.adapter.on("scroll.waypoints", function () {
          (t.didScroll && !n.isTouch) ||
            ((t.didScroll = !0), n.requestAnimationFrame(e));
        });
      }),
      (r.prototype.handleResize = function () {
        n.Context.refreshAll();
      }),
      (r.prototype.handleScroll = function () {
        var t = {},
          e = {
            horizontal: {
              newScroll: this.adapter.scrollLeft(),
              oldScroll: this.oldScroll.x,
              forward: "right",
              backward: "left",
            },
            vertical: {
              newScroll: this.adapter.scrollTop(),
              oldScroll: this.oldScroll.y,
              forward: "down",
              backward: "up",
            },
          };
        for (var i in e) {
          var n = e[i],
            a = n.newScroll > n.oldScroll ? n.forward : n.backward;
          for (var r in this.waypoints[i]) {
            var s = this.waypoints[i][r];
            if (null !== s.triggerPoint) {
              var o = n.oldScroll < s.triggerPoint,
                l = n.newScroll >= s.triggerPoint;
              ((o && l) || (!o && !l)) &&
                (s.queueTrigger(a), (t[s.group.id] = s.group));
            }
          }
        }
        for (var u in t) t[u].flushTriggers();
        this.oldScroll = { x: e.horizontal.newScroll, y: e.vertical.newScroll };
      }),
      (r.prototype.innerHeight = function () {
        return this.element == this.element.window
          ? n.viewportHeight()
          : this.adapter.innerHeight();
      }),
      (r.prototype.remove = function (t) {
        delete this.waypoints[t.axis][t.key], this.checkEmpty();
      }),
      (r.prototype.innerWidth = function () {
        return this.element == this.element.window
          ? n.viewportWidth()
          : this.adapter.innerWidth();
      }),
      (r.prototype.destroy = function () {
        var t = [];
        for (var e in this.waypoints)
          for (var i in this.waypoints[e]) t.push(this.waypoints[e][i]);
        for (var n = 0, a = t.length; n < a; n++) t[n].destroy();
      }),
      (r.prototype.refresh = function () {
        var t,
          e = this.element == this.element.window,
          i = e ? void 0 : this.adapter.offset(),
          a = {};
        for (var r in (this.handleScroll(),
        (t = {
          horizontal: {
            contextOffset: e ? 0 : i.left,
            contextScroll: e ? 0 : this.oldScroll.x,
            contextDimension: this.innerWidth(),
            oldScroll: this.oldScroll.x,
            forward: "right",
            backward: "left",
            offsetProp: "left",
          },
          vertical: {
            contextOffset: e ? 0 : i.top,
            contextScroll: e ? 0 : this.oldScroll.y,
            contextDimension: this.innerHeight(),
            oldScroll: this.oldScroll.y,
            forward: "down",
            backward: "up",
            offsetProp: "top",
          },
        }))) {
          var s = t[r];
          for (var o in this.waypoints[r]) {
            var l,
              u,
              c,
              d,
              h = this.waypoints[r][o],
              p = h.options.offset,
              f = h.triggerPoint,
              m = 0,
              g = null == f;
            h.element !== h.element.window &&
              (m = h.adapter.offset()[s.offsetProp]),
              "function" == typeof p
                ? (p = p.apply(h))
                : "string" == typeof p &&
                  ((p = parseFloat(p)),
                  h.options.offset.indexOf("%") > -1 &&
                    (p = Math.ceil((s.contextDimension * p) / 100))),
              (l = s.contextScroll - s.contextOffset),
              (h.triggerPoint = Math.floor(m + l - p)),
              (u = f < s.oldScroll),
              (c = h.triggerPoint >= s.oldScroll),
              (d = !u && !c),
              !g && u && c
                ? (h.queueTrigger(s.backward), (a[h.group.id] = h.group))
                : ((!g && d) || (g && s.oldScroll >= h.triggerPoint)) &&
                  (h.queueTrigger(s.forward), (a[h.group.id] = h.group));
          }
        }
        return (
          n.requestAnimationFrame(function () {
            for (var t in a) a[t].flushTriggers();
          }),
          this
        );
      }),
      (r.findOrCreateByElement = function (t) {
        return r.findByElement(t) || new r(t);
      }),
      (r.refreshAll = function () {
        for (var t in i) i[t].refresh();
      }),
      (r.findByElement = function (t) {
        return i[t.waypointContextKey];
      }),
      (window.onload = function () {
        a && a(), r.refreshAll();
      }),
      (n.requestAnimationFrame = function (e) {
        (
          window.requestAnimationFrame ||
          window.mozRequestAnimationFrame ||
          window.webkitRequestAnimationFrame ||
          t
        ).call(window, e);
      }),
      (n.Context = r);
  })(),
  (function () {
    "use strict";
    function t(t, e) {
      return t.triggerPoint - e.triggerPoint;
    }
    function e(t, e) {
      return e.triggerPoint - t.triggerPoint;
    }
    var i = { vertical: {}, horizontal: {} },
      n = window.Waypoint;
    function a(t) {
      (this.name = t.name),
        (this.axis = t.axis),
        (this.id = this.name + "-" + this.axis),
        (this.waypoints = []),
        this.clearTriggerQueues(),
        (i[this.axis][this.name] = this);
    }
    (a.prototype.add = function (t) {
      this.waypoints.push(t);
    }),
      (a.prototype.clearTriggerQueues = function () {
        this.triggerQueues = { up: [], down: [], left: [], right: [] };
      }),
      (a.prototype.flushTriggers = function () {
        for (var i in this.triggerQueues) {
          var n = this.triggerQueues[i],
            a = "up" === i || "left" === i;
          n.sort(a ? e : t);
          for (var r = 0, s = n.length; r < s; r += 1) {
            var o = n[r];
            (o.options.continuous || r === n.length - 1) && o.trigger([i]);
          }
        }
        this.clearTriggerQueues();
      }),
      (a.prototype.next = function (e) {
        this.waypoints.sort(t);
        var i = n.Adapter.inArray(e, this.waypoints);
        return i === this.waypoints.length - 1 ? null : this.waypoints[i + 1];
      }),
      (a.prototype.previous = function (e) {
        this.waypoints.sort(t);
        var i = n.Adapter.inArray(e, this.waypoints);
        return i ? this.waypoints[i - 1] : null;
      }),
      (a.prototype.queueTrigger = function (t, e) {
        this.triggerQueues[e].push(t);
      }),
      (a.prototype.remove = function (t) {
        var e = n.Adapter.inArray(t, this.waypoints);
        e > -1 && this.waypoints.splice(e, 1);
      }),
      (a.prototype.first = function () {
        return this.waypoints[0];
      }),
      (a.prototype.last = function () {
        return this.waypoints[this.waypoints.length - 1];
      }),
      (a.findOrCreate = function (t) {
        return i[t.axis][t.name] || new a(t);
      }),
      (n.Group = a);
  })(),
  (function () {
    "use strict";
    var t = window.jQuery,
      e = window.Waypoint;
    function i(e) {
      this.$element = t(e);
    }
    t.each(
      [
        "innerHeight",
        "innerWidth",
        "off",
        "offset",
        "on",
        "outerHeight",
        "outerWidth",
        "scrollLeft",
        "scrollTop",
      ],
      function (t, e) {
        i.prototype[e] = function () {
          var t = Array.prototype.slice.call(arguments);
          return this.$element[e].apply(this.$element, t);
        };
      }
    ),
      t.each(["extend", "inArray", "isEmptyObject"], function (e, n) {
        i[n] = t[n];
      }),
      e.adapters.push({ name: "jquery", Adapter: i }),
      (e.Adapter = i);
  })(),
  (function () {
    "use strict";
    var t = window.Waypoint;
    function e(e) {
      return function () {
        var i = [],
          n = arguments[0];
        return (
          e.isFunction(arguments[0]) &&
            ((n = e.extend({}, arguments[1])).handler = arguments[0]),
          this.each(function () {
            var a = e.extend({}, n, { element: this });
            "string" == typeof a.context &&
              (a.context = e(this).closest(a.context)[0]),
              i.push(new t(a));
          }),
          i
        );
      };
    }
    window.jQuery && (window.jQuery.fn.waypoint = e(window.jQuery)),
      window.Zepto && (window.Zepto.fn.waypoint = e(window.Zepto));
  })(),
  (function () {
    "use strict";
    function t(t, e) {
      return Object.prototype.hasOwnProperty.call(t, e);
    }
    function e(t) {
      return t[t.length - 1];
    }
    function i(t, ...e) {
      return (
        e.forEach((e) => {
          t.includes(e) || t.push(e);
        }),
        t
      );
    }
    function n(t, e) {
      return t ? t.split(e) : [];
    }
    function a(t, e, i) {
      return (void 0 === e || t >= e) && (void 0 === i || t <= i);
    }
    function r(t, e, i) {
      return t < e ? e : t > i ? i : t;
    }
    function s(t, e, i = {}, n = 0, a = "") {
      a += `<${Object.keys(i).reduce((t, e) => {
        let a = i[e];
        return "function" == typeof a && (a = a(n)), `${t} ${e}="${a}"`;
      }, t)}></${t}>`;
      const r = n + 1;
      return r < e ? s(t, e, i, r, a) : a;
    }
    function o(t) {
      return t.replace(/>\s+/g, ">").replace(/\s+</, "<");
    }
    function l(t) {
      return new Date(t).setHours(0, 0, 0, 0);
    }
    function u() {
      return new Date().setHours(0, 0, 0, 0);
    }
    function c(...t) {
      switch (t.length) {
        case 0:
          return u();
        case 1:
          return l(t[0]);
      }
      const e = new Date(0);
      return e.setFullYear(...t), e.setHours(0, 0, 0, 0);
    }
    function d(t, e) {
      const i = new Date(t);
      return i.setDate(i.getDate() + e);
    }
    function h(t, e) {
      const i = new Date(t),
        n = i.getMonth() + e;
      let a = n % 12;
      a < 0 && (a += 12);
      const r = i.setMonth(n);
      return i.getMonth() !== a ? i.setDate(0) : r;
    }
    function p(t, e) {
      const i = new Date(t),
        n = i.getMonth(),
        a = i.setFullYear(i.getFullYear() + e);
      return 1 === n && 2 === i.getMonth() ? i.setDate(0) : a;
    }
    function f(t, e) {
      return (t - e + 7) % 7;
    }
    function m(t, e, i = 0) {
      const n = new Date(t).getDay();
      return d(t, f(e, i) - f(n, i));
    }
    function g(t, e) {
      const i = new Date(t).getFullYear();
      return Math.floor(i / e) * e;
    }
    const v = /dd?|DD?|mm?|MM?|yy?(?:yy)?/,
      _ = /[\s!-/:-@[-`{-~年月日]+/;
    let y = {};
    const b = {
        y: (t, e) => new Date(t).setFullYear(parseInt(e, 10)),
        m(t, e, i) {
          const n = new Date(t);
          let a = parseInt(e, 10) - 1;
          if (isNaN(a)) {
            if (!e) return NaN;
            const t = e.toLowerCase(),
              n = (e) => e.toLowerCase().startsWith(t);
            if (
              ((a = i.monthsShort.findIndex(n)),
              a < 0 && (a = i.months.findIndex(n)),
              a < 0)
            )
              return NaN;
          }
          return (
            n.setMonth(a), n.getMonth() !== x(a) ? n.setDate(0) : n.getTime()
          );
        },
        d: (t, e) => new Date(t).setDate(parseInt(e, 10)),
      },
      w = {
        d: (t) => t.getDate(),
        dd: (t) => S(t.getDate(), 2),
        D: (t, e) => e.daysShort[t.getDay()],
        DD: (t, e) => e.days[t.getDay()],
        m: (t) => t.getMonth() + 1,
        mm: (t) => S(t.getMonth() + 1, 2),
        M: (t, e) => e.monthsShort[t.getMonth()],
        MM: (t, e) => e.months[t.getMonth()],
        y: (t) => t.getFullYear(),
        yy: (t) => S(t.getFullYear(), 2).slice(-2),
        yyyy: (t) => S(t.getFullYear(), 4),
      };
    function x(t) {
      return t > -1 ? t % 12 : x(t + 12);
    }
    function S(t, e) {
      return t.toString().padStart(e, "0");
    }
    function T(t) {
      if ("string" != typeof t) throw new Error("Invalid date format.");
      if (t in y) return y[t];
      const i = t.split(v),
        n = t.match(new RegExp(v, "g"));
      if (0 === i.length || !n) throw new Error("Invalid date format.");
      const a = n.map((t) => w[t]),
        r = Object.keys(b).reduce(
          (t, e) => (
            n.find((t) => "D" !== t[0] && t[0].toLowerCase() === e) &&
              t.push(e),
            t
          ),
          []
        );
      return (y[t] = {
        parser(t, e) {
          const i = t.split(_).reduce((t, e, i) => {
            if (e.length > 0 && n[i]) {
              const a = n[i][0];
              "M" === a ? (t.m = e) : "D" !== a && (t[a] = e);
            }
            return t;
          }, {});
          return r.reduce((t, n) => {
            const a = b[n](t, i[n], e);
            return isNaN(a) ? t : a;
          }, u());
        },
        formatter: (t, n) =>
          a.reduce((e, a, r) => e + `${i[r]}${a(t, n)}`, "") + e(i),
      });
    }
    function C(t, e, i) {
      if (t instanceof Date || "number" == typeof t) {
        const e = l(t);
        return isNaN(e) ? void 0 : e;
      }
      if (t) {
        if ("today" === t) return u();
        if (e && e.toValue) {
          const n = e.toValue(t, e, i);
          return isNaN(n) ? void 0 : l(n);
        }
        return T(e).parser(t, i);
      }
    }
    function E(t, e, i) {
      if (isNaN(t) || (!t && 0 !== t)) return "";
      const n = "number" == typeof t ? new Date(t) : t;
      return e.toDisplay ? e.toDisplay(n, e, i) : T(e).formatter(n, i);
    }
    const P = new WeakMap(),
      { addEventListener: L, removeEventListener: D } = EventTarget.prototype;
    function M(t, e) {
      let i = P.get(t);
      i || ((i = []), P.set(t, i)),
        e.forEach((t) => {
          L.call(...t), i.push(t);
        });
    }
    function k(t) {
      let e = P.get(t);
      e &&
        (e.forEach((t) => {
          D.call(...t);
        }),
        P.delete(t));
    }
    if (!Event.prototype.composedPath) {
      const t = (e, i = []) => {
        let n;
        return (
          i.push(e),
          e.parentNode
            ? (n = e.parentNode)
            : e.host
            ? (n = e.host)
            : e.defaultView && (n = e.defaultView),
          n ? t(n, i) : i
        );
      };
      Event.prototype.composedPath = function () {
        return t(this.target);
      };
    }
    function A(t, e, i, n = 0) {
      const a = t[n];
      return e(a) ? a : a !== i && a.parentElement ? A(t, e, i, n + 1) : void 0;
    }
    function I(t, e) {
      const i = "function" == typeof e ? e : (t) => t.matches(e);
      return A(t.composedPath(), i, t.currentTarget);
    }
    const z = {
        en: {
          days: [
            "Sunday",
            "Monday",
            "Tuesday",
            "Wednesday",
            "Thursday",
            "Friday",
            "Saturday",
          ],
          daysShort: ["Sun", "Mon", "Tue", "Wed", "Thu", "Fri", "Sat"],
          daysMin: ["Su", "Mo", "Tu", "We", "Th", "Fr", "Sa"],
          months: [
            "January",
            "February",
            "March",
            "April",
            "May",
            "June",
            "July",
            "August",
            "September",
            "October",
            "November",
            "December",
          ],
          monthsShort: [
            "Jan",
            "Feb",
            "Mar",
            "Apr",
            "May",
            "Jun",
            "Jul",
            "Aug",
            "Sep",
            "Oct",
            "Nov",
            "Dec",
          ],
          today: "Today",
          clear: "Clear",
          titleFormat: "MM y",
        },
      },
      O = {
        autohide: !1,
        beforeShowDay: null,
        beforeShowDecade: null,
        beforeShowMonth: null,
        beforeShowYear: null,
        calendarWeeks: !1,
        clearBtn: !1,
        dateDelimiter: ",",
        datesDisabled: [],
        daysOfWeekDisabled: [],
        daysOfWeekHighlighted: [],
        defaultViewDate: void 0,
        disableTouchKeyboard: !1,
        format: "mm/dd/yyyy",
        language: "en",
        maxDate: null,
        maxNumberOfDates: 1,
        maxView: 3,
        minDate: null,
        nextArrow: "»",
        orientation: "auto",
        pickLevel: 0,
        prevArrow: "«",
        showDaysOfWeek: !0,
        showOnClick: !0,
        showOnFocus: !0,
        startView: 0,
        title: "",
        todayBtn: !1,
        todayBtnMode: 0,
        todayHighlight: !1,
        updateOnBlur: !0,
        weekStart: 0,
      },
      N = document.createRange();
    function B(t) {
      return N.createContextualFragment(t);
    }
    function j(t) {
      "none" !== t.style.display &&
        (t.style.display && (t.dataset.styleDisplay = t.style.display),
        (t.style.display = "none"));
    }
    function F(t) {
      "none" === t.style.display &&
        (t.dataset.styleDisplay
          ? ((t.style.display = t.dataset.styleDisplay),
            delete t.dataset.styleDisplay)
          : (t.style.display = ""));
    }
    function R(t) {
      t.firstChild && (t.removeChild(t.firstChild), R(t));
    }
    const { language: $, format: H, weekStart: V } = O;
    function W(t, e) {
      return t.length < 6 && e >= 0 && e < 7 ? i(t, e) : t;
    }
    function Z(t) {
      return (t + 6) % 7;
    }
    function G(t, e, i, n) {
      const a = C(t, e, i);
      return void 0 !== a ? a : n;
    }
    function U(t, e, i = 3) {
      const n = parseInt(t, 10);
      return n >= 0 && n <= i ? n : e;
    }
    function Y(e, n) {
      const a = Object.assign({}, e),
        r = {},
        s = n.constructor.locales;
      let {
        format: o,
        language: l,
        locale: u,
        maxDate: d,
        maxView: h,
        minDate: p,
        pickLevel: f,
        startView: m,
        weekStart: g,
      } = n.config || {};
      if (a.language) {
        let t;
        if (
          (a.language !== l &&
            (s[a.language]
              ? (t = a.language)
              : ((t = a.language.split("-")[0]), void 0 === s[t] && (t = !1))),
          delete a.language,
          t)
        ) {
          l = r.language = t;
          const e = u || s[$];
          (u = Object.assign({ format: H, weekStart: V }, s[$])),
            l !== $ && Object.assign(u, s[l]),
            (r.locale = u),
            o === e.format && (o = r.format = u.format),
            g === e.weekStart &&
              ((g = r.weekStart = u.weekStart), (r.weekEnd = Z(u.weekStart)));
        }
      }
      if (a.format) {
        const t = "function" == typeof a.format.toDisplay,
          e = "function" == typeof a.format.toValue,
          i = v.test(a.format);
        ((t && e) || i) && (o = r.format = a.format), delete a.format;
      }
      let _ = p,
        y = d;
      if (
        (void 0 !== a.minDate &&
          ((_ = null === a.minDate ? c(0, 0, 1) : G(a.minDate, o, u, _)),
          delete a.minDate),
        void 0 !== a.maxDate &&
          ((y = null === a.maxDate ? void 0 : G(a.maxDate, o, u, y)),
          delete a.maxDate),
        y < _
          ? ((p = r.minDate = y), (d = r.maxDate = _))
          : (p !== _ && (p = r.minDate = _), d !== y && (d = r.maxDate = y)),
        a.datesDisabled &&
          ((r.datesDisabled = a.datesDisabled.reduce((t, e) => {
            const n = C(e, o, u);
            return void 0 !== n ? i(t, n) : t;
          }, [])),
          delete a.datesDisabled),
        void 0 !== a.defaultViewDate)
      ) {
        const t = C(a.defaultViewDate, o, u);
        void 0 !== t && (r.defaultViewDate = t), delete a.defaultViewDate;
      }
      if (void 0 !== a.weekStart) {
        const t = Number(a.weekStart) % 7;
        isNaN(t) || ((g = r.weekStart = t), (r.weekEnd = Z(t))),
          delete a.weekStart;
      }
      if (
        (a.daysOfWeekDisabled &&
          ((r.daysOfWeekDisabled = a.daysOfWeekDisabled.reduce(W, [])),
          delete a.daysOfWeekDisabled),
        a.daysOfWeekHighlighted &&
          ((r.daysOfWeekHighlighted = a.daysOfWeekHighlighted.reduce(W, [])),
          delete a.daysOfWeekHighlighted),
        void 0 !== a.maxNumberOfDates)
      ) {
        const t = parseInt(a.maxNumberOfDates, 10);
        t >= 0 && ((r.maxNumberOfDates = t), (r.multidate = 1 !== t)),
          delete a.maxNumberOfDates;
      }
      a.dateDelimiter &&
        ((r.dateDelimiter = String(a.dateDelimiter)), delete a.dateDelimiter);
      let b = f;
      void 0 !== a.pickLevel && ((b = U(a.pickLevel, 2)), delete a.pickLevel),
        b !== f && (f = r.pickLevel = b);
      let w = h;
      void 0 !== a.maxView && ((w = U(a.maxView, h)), delete a.maxView),
        (w = f > w ? f : w),
        w !== h && (h = r.maxView = w);
      let x = m;
      if (
        (void 0 !== a.startView &&
          ((x = U(a.startView, x)), delete a.startView),
        x < f ? (x = f) : x > h && (x = h),
        x !== m && (r.startView = x),
        a.prevArrow)
      ) {
        const t = B(a.prevArrow);
        t.childNodes.length > 0 && (r.prevArrow = t.childNodes),
          delete a.prevArrow;
      }
      if (a.nextArrow) {
        const t = B(a.nextArrow);
        t.childNodes.length > 0 && (r.nextArrow = t.childNodes),
          delete a.nextArrow;
      }
      if (
        (void 0 !== a.disableTouchKeyboard &&
          ((r.disableTouchKeyboard =
            "ontouchstart" in document && !!a.disableTouchKeyboard),
          delete a.disableTouchKeyboard),
        a.orientation)
      ) {
        const t = a.orientation.toLowerCase().split(/\s+/g);
        (r.orientation = {
          x: t.find((t) => "left" === t || "right" === t) || "auto",
          y: t.find((t) => "top" === t || "bottom" === t) || "auto",
        }),
          delete a.orientation;
      }
      if (void 0 !== a.todayBtnMode) {
        switch (a.todayBtnMode) {
          case 0:
          case 1:
            r.todayBtnMode = a.todayBtnMode;
        }
        delete a.todayBtnMode;
      }
      return (
        Object.keys(a).forEach((e) => {
          void 0 !== a[e] && t(O, e) && (r[e] = a[e]);
        }),
        r
      );
    }
    const q = o(
        '<div class="datepicker">\n  <div class="datepicker-picker">\n    <div class="datepicker-header">\n      <div class="datepicker-title"></div>\n      <div class="datepicker-controls">\n        <button type="button" class="%buttonClass% prev-btn"></button>\n        <button type="button" class="%buttonClass% view-switch"></button>\n        <button type="button" class="%buttonClass% next-btn"></button>\n      </div>\n    </div>\n    <div class="datepicker-main"></div>\n    <div class="datepicker-footer">\n      <div class="datepicker-controls">\n        <button type="button" class="%buttonClass% today-btn"></button>\n        <button type="button" class="%buttonClass% clear-btn"></button>\n      </div>\n    </div>\n  </div>\n</div>'
      ),
      X = o(
        `<div class="days">\n  <div class="days-of-week">${s("span", 7, {
          class: "dow",
        })}</div>\n  <div class="datepicker-grid">${s(
          "span",
          42
        )}</div>\n</div>`
      ),
      K = o(
        `<div class="calendar-weeks">\n  <div class="days-of-week"><span class="dow"></span></div>\n  <div class="weeks">${s(
          "span",
          6,
          { class: "week" }
        )}</div>\n</div>`
      );
    class J {
      constructor(t, e) {
        Object.assign(this, e, {
          picker: t,
          element: B('<div class="datepicker-view"></div>').firstChild,
          selected: [],
        }),
          this.init(this.picker.datepicker.config);
      }
      init(t) {
        void 0 !== t.pickLevel && (this.isMinView = this.id === t.pickLevel),
          this.setOptions(t),
          this.updateFocus(),
          this.updateSelection();
      }
      performBeforeHook(t, e, n) {
        let a = this.beforeShow(new Date(n));
        switch (typeof a) {
          case "boolean":
            a = { enabled: a };
            break;
          case "string":
            a = { classes: a };
        }
        if (a) {
          if (
            (!1 === a.enabled &&
              (t.classList.add("disabled"), i(this.disabled, e)),
            a.classes)
          ) {
            const n = a.classes.split(/\s+/);
            t.classList.add(...n),
              n.includes("disabled") && i(this.disabled, e);
          }
          a.content &&
            (function (t, e) {
              R(t),
                e instanceof DocumentFragment
                  ? t.appendChild(e)
                  : "string" == typeof e
                  ? t.appendChild(B(e))
                  : "function" == typeof e.forEach &&
                    e.forEach((e) => {
                      t.appendChild(e);
                    });
            })(t, a.content);
        }
      }
    }
    class Q extends J {
      constructor(t) {
        super(t, { id: 0, name: "days", cellClass: "day" });
      }
      init(t, e = !0) {
        if (e) {
          const t = B(X).firstChild;
          (this.dow = t.firstChild),
            (this.grid = t.lastChild),
            this.element.appendChild(t);
        }
        super.init(t);
      }
      setOptions(e) {
        let i;
        if (
          (t(e, "minDate") && (this.minDate = e.minDate),
          t(e, "maxDate") && (this.maxDate = e.maxDate),
          e.datesDisabled && (this.datesDisabled = e.datesDisabled),
          e.daysOfWeekDisabled &&
            ((this.daysOfWeekDisabled = e.daysOfWeekDisabled), (i = !0)),
          e.daysOfWeekHighlighted &&
            (this.daysOfWeekHighlighted = e.daysOfWeekHighlighted),
          void 0 !== e.todayHighlight &&
            (this.todayHighlight = e.todayHighlight),
          void 0 !== e.weekStart &&
            ((this.weekStart = e.weekStart),
            (this.weekEnd = e.weekEnd),
            (i = !0)),
          e.locale)
        ) {
          const t = (this.locale = e.locale);
          (this.dayNames = t.daysMin),
            (this.switchLabelFormat = t.titleFormat),
            (i = !0);
        }
        if (
          (void 0 !== e.beforeShowDay &&
            (this.beforeShow =
              "function" == typeof e.beforeShowDay ? e.beforeShowDay : void 0),
          void 0 !== e.calendarWeeks)
        )
          if (e.calendarWeeks && !this.calendarWeeks) {
            const t = B(K).firstChild;
            (this.calendarWeeks = {
              element: t,
              dow: t.firstChild,
              weeks: t.lastChild,
            }),
              this.element.insertBefore(t, this.element.firstChild);
          } else
            this.calendarWeeks &&
              !e.calendarWeeks &&
              (this.element.removeChild(this.calendarWeeks.element),
              (this.calendarWeeks = null));
        void 0 !== e.showDaysOfWeek &&
          (e.showDaysOfWeek
            ? (F(this.dow), this.calendarWeeks && F(this.calendarWeeks.dow))
            : (j(this.dow), this.calendarWeeks && j(this.calendarWeeks.dow))),
          i &&
            Array.from(this.dow.children).forEach((t, e) => {
              const i = (this.weekStart + e) % 7;
              (t.textContent = this.dayNames[i]),
                (t.className = this.daysOfWeekDisabled.includes(i)
                  ? "dow disabled"
                  : "dow");
            });
      }
      updateFocus() {
        const t = new Date(this.picker.viewDate),
          e = t.getFullYear(),
          i = t.getMonth(),
          n = c(e, i, 1),
          a = m(n, this.weekStart, this.weekStart);
        (this.first = n),
          (this.last = c(e, i + 1, 0)),
          (this.start = a),
          (this.focused = this.picker.viewDate);
      }
      updateSelection() {
        const { dates: t, rangepicker: e } = this.picker.datepicker;
        (this.selected = t), e && (this.range = e.dates);
      }
      render() {
        (this.today = this.todayHighlight ? u() : void 0),
          (this.disabled = [...this.datesDisabled]);
        const t = E(this.focused, this.switchLabelFormat, this.locale);
        if (
          (this.picker.setViewSwitchLabel(t),
          this.picker.setPrevBtnDisabled(this.first <= this.minDate),
          this.picker.setNextBtnDisabled(this.last >= this.maxDate),
          this.calendarWeeks)
        ) {
          const t = m(this.first, 1, 1);
          Array.from(this.calendarWeeks.weeks.children).forEach((e, i) => {
            e.textContent = (function (t) {
              const e = m(t, 4, 1),
                i = m(new Date(e).setMonth(0, 4), 4, 1);
              return Math.round((e - i) / 6048e5) + 1;
            })(d(t, 7 * i));
          });
        }
        Array.from(this.grid.children).forEach((t, e) => {
          const n = t.classList,
            a = d(this.start, e),
            r = new Date(a),
            s = r.getDay();
          if (
            ((t.className = `datepicker-cell ${this.cellClass}`),
            (t.dataset.date = a),
            (t.textContent = r.getDate()),
            a < this.first ? n.add("prev") : a > this.last && n.add("next"),
            this.today === a && n.add("today"),
            (a < this.minDate ||
              a > this.maxDate ||
              this.disabled.includes(a)) &&
              n.add("disabled"),
            this.daysOfWeekDisabled.includes(s) &&
              (n.add("disabled"), i(this.disabled, a)),
            this.daysOfWeekHighlighted.includes(s) && n.add("highlighted"),
            this.range)
          ) {
            const [t, e] = this.range;
            a > t && a < e && n.add("range"),
              a === t && n.add("range-start"),
              a === e && n.add("range-end");
          }
          this.selected.includes(a) && n.add("selected"),
            a === this.focused && n.add("focused"),
            this.beforeShow && this.performBeforeHook(t, a, a);
        });
      }
      refresh() {
        const [t, e] = this.range || [];
        this.grid
          .querySelectorAll(
            ".range, .range-start, .range-end, .selected, .focused"
          )
          .forEach((t) => {
            t.classList.remove(
              "range",
              "range-start",
              "range-end",
              "selected",
              "focused"
            );
          }),
          Array.from(this.grid.children).forEach((i) => {
            const n = Number(i.dataset.date),
              a = i.classList;
            n > t && n < e && a.add("range"),
              n === t && a.add("range-start"),
              n === e && a.add("range-end"),
              this.selected.includes(n) && a.add("selected"),
              n === this.focused && a.add("focused");
          });
      }
      refreshFocus() {
        const t = Math.round((this.focused - this.start) / 864e5);
        this.grid.querySelectorAll(".focused").forEach((t) => {
          t.classList.remove("focused");
        }),
          this.grid.children[t].classList.add("focused");
      }
    }
    function tt(t, e) {
      if (!t || !t[0] || !t[1]) return;
      const [[i, n], [a, r]] = t;
      return i > e || a < e ? void 0 : [i === e ? n : -1, a === e ? r : 12];
    }
    class et extends J {
      constructor(t) {
        super(t, { id: 1, name: "months", cellClass: "month" });
      }
      init(t, e = !0) {
        e &&
          ((this.grid = this.element),
          this.element.classList.add("months", "datepicker-grid"),
          this.grid.appendChild(B(s("span", 12, { "data-month": (t) => t })))),
          super.init(t);
      }
      setOptions(e) {
        if (
          (e.locale && (this.monthNames = e.locale.monthsShort),
          t(e, "minDate"))
        )
          if (void 0 === e.minDate)
            this.minYear = this.minMonth = this.minDate = void 0;
          else {
            const t = new Date(e.minDate);
            (this.minYear = t.getFullYear()),
              (this.minMonth = t.getMonth()),
              (this.minDate = t.setDate(1));
          }
        if (t(e, "maxDate"))
          if (void 0 === e.maxDate)
            this.maxYear = this.maxMonth = this.maxDate = void 0;
          else {
            const t = new Date(e.maxDate);
            (this.maxYear = t.getFullYear()),
              (this.maxMonth = t.getMonth()),
              (this.maxDate = c(this.maxYear, this.maxMonth + 1, 0));
          }
        void 0 !== e.beforeShowMonth &&
          (this.beforeShow =
            "function" == typeof e.beforeShowMonth
              ? e.beforeShowMonth
              : void 0);
      }
      updateFocus() {
        const t = new Date(this.picker.viewDate);
        (this.year = t.getFullYear()), (this.focused = t.getMonth());
      }
      updateSelection() {
        const { dates: t, rangepicker: e } = this.picker.datepicker;
        (this.selected = t.reduce((t, e) => {
          const n = new Date(e),
            a = n.getFullYear(),
            r = n.getMonth();
          return void 0 === t[a] ? (t[a] = [r]) : i(t[a], r), t;
        }, {})),
          e &&
            e.dates &&
            (this.range = e.dates.map((t) => {
              const e = new Date(t);
              return isNaN(e) ? void 0 : [e.getFullYear(), e.getMonth()];
            }));
      }
      render() {
        (this.disabled = []),
          this.picker.setViewSwitchLabel(this.year),
          this.picker.setPrevBtnDisabled(this.year <= this.minYear),
          this.picker.setNextBtnDisabled(this.year >= this.maxYear);
        const t = this.selected[this.year] || [],
          e = this.year < this.minYear || this.year > this.maxYear,
          i = this.year === this.minYear,
          n = this.year === this.maxYear,
          a = tt(this.range, this.year);
        Array.from(this.grid.children).forEach((r, s) => {
          const o = r.classList,
            l = c(this.year, s, 1);
          if (
            ((r.className = `datepicker-cell ${this.cellClass}`),
            this.isMinView && (r.dataset.date = l),
            (r.textContent = this.monthNames[s]),
            (e || (i && s < this.minMonth) || (n && s > this.maxMonth)) &&
              o.add("disabled"),
            a)
          ) {
            const [t, e] = a;
            s > t && s < e && o.add("range"),
              s === t && o.add("range-start"),
              s === e && o.add("range-end");
          }
          t.includes(s) && o.add("selected"),
            s === this.focused && o.add("focused"),
            this.beforeShow && this.performBeforeHook(r, s, l);
        });
      }
      refresh() {
        const t = this.selected[this.year] || [],
          [e, i] = tt(this.range, this.year) || [];
        this.grid
          .querySelectorAll(
            ".range, .range-start, .range-end, .selected, .focused"
          )
          .forEach((t) => {
            t.classList.remove(
              "range",
              "range-start",
              "range-end",
              "selected",
              "focused"
            );
          }),
          Array.from(this.grid.children).forEach((n, a) => {
            const r = n.classList;
            a > e && a < i && r.add("range"),
              a === e && r.add("range-start"),
              a === i && r.add("range-end"),
              t.includes(a) && r.add("selected"),
              a === this.focused && r.add("focused");
          });
      }
      refreshFocus() {
        this.grid.querySelectorAll(".focused").forEach((t) => {
          t.classList.remove("focused");
        }),
          this.grid.children[this.focused].classList.add("focused");
      }
    }
    class it extends J {
      constructor(t, e) {
        super(t, e);
      }
      init(t, e = !0) {
        var i;
        e &&
          ((this.navStep = 10 * this.step),
          (this.beforeShowOption = `beforeShow${
            ((i = this.cellClass),
            [...i].reduce((t, e, i) => t + (i ? e : e.toUpperCase()), ""))
          }`),
          (this.grid = this.element),
          this.element.classList.add(this.name, "datepicker-grid"),
          this.grid.appendChild(B(s("span", 12)))),
          super.init(t);
      }
      setOptions(e) {
        if (
          (t(e, "minDate") &&
            (void 0 === e.minDate
              ? (this.minYear = this.minDate = void 0)
              : ((this.minYear = g(e.minDate, this.step)),
                (this.minDate = c(this.minYear, 0, 1)))),
          t(e, "maxDate") &&
            (void 0 === e.maxDate
              ? (this.maxYear = this.maxDate = void 0)
              : ((this.maxYear = g(e.maxDate, this.step)),
                (this.maxDate = c(this.maxYear, 11, 31)))),
          void 0 !== e[this.beforeShowOption])
        ) {
          const t = e[this.beforeShowOption];
          this.beforeShow = "function" == typeof t ? t : void 0;
        }
      }
      updateFocus() {
        const t = new Date(this.picker.viewDate),
          e = g(t, this.navStep),
          i = e + 9 * this.step;
        (this.first = e),
          (this.last = i),
          (this.start = e - this.step),
          (this.focused = g(t, this.step));
      }
      updateSelection() {
        const { dates: t, rangepicker: e } = this.picker.datepicker;
        (this.selected = t.reduce((t, e) => i(t, g(e, this.step)), [])),
          e &&
            e.dates &&
            (this.range = e.dates.map((t) => {
              if (void 0 !== t) return g(t, this.step);
            }));
      }
      render() {
        (this.disabled = []),
          this.picker.setViewSwitchLabel(`${this.first}-${this.last}`),
          this.picker.setPrevBtnDisabled(this.first <= this.minYear),
          this.picker.setNextBtnDisabled(this.last >= this.maxYear),
          Array.from(this.grid.children).forEach((t, e) => {
            const i = t.classList,
              n = this.start + e * this.step,
              a = c(n, 0, 1);
            if (
              ((t.className = `datepicker-cell ${this.cellClass}`),
              this.isMinView && (t.dataset.date = a),
              (t.textContent = t.dataset.year = n),
              0 === e ? i.add("prev") : 11 === e && i.add("next"),
              (n < this.minYear || n > this.maxYear) && i.add("disabled"),
              this.range)
            ) {
              const [t, e] = this.range;
              n > t && n < e && i.add("range"),
                n === t && i.add("range-start"),
                n === e && i.add("range-end");
            }
            this.selected.includes(n) && i.add("selected"),
              n === this.focused && i.add("focused"),
              this.beforeShow && this.performBeforeHook(t, n, a);
          });
      }
      refresh() {
        const [t, e] = this.range || [];
        this.grid
          .querySelectorAll(
            ".range, .range-start, .range-end, .selected, .focused"
          )
          .forEach((t) => {
            t.classList.remove(
              "range",
              "range-start",
              "range-end",
              "selected",
              "focused"
            );
          }),
          Array.from(this.grid.children).forEach((i) => {
            const n = Number(i.textContent),
              a = i.classList;
            n > t && n < e && a.add("range"),
              n === t && a.add("range-start"),
              n === e && a.add("range-end"),
              this.selected.includes(n) && a.add("selected"),
              n === this.focused && a.add("focused");
          });
      }
      refreshFocus() {
        const t = Math.round((this.focused - this.start) / this.step);
        this.grid.querySelectorAll(".focused").forEach((t) => {
          t.classList.remove("focused");
        }),
          this.grid.children[t].classList.add("focused");
      }
    }
    function nt(t, e) {
      const i = {
        date: t.getDate(),
        viewDate: new Date(t.picker.viewDate),
        viewId: t.picker.currentView.id,
        datepicker: t,
      };
      t.element.dispatchEvent(new CustomEvent(e, { detail: i }));
    }
    function at(t, e) {
      const { minDate: i, maxDate: n } = t.config,
        { currentView: a, viewDate: s } = t.picker;
      let o;
      switch (a.id) {
        case 0:
          o = h(s, e);
          break;
        case 1:
          o = p(s, e);
          break;
        default:
          o = p(s, e * a.navStep);
      }
      (o = r(o, i, n)), t.picker.changeFocus(o).render();
    }
    function rt(t) {
      const e = t.picker.currentView.id;
      e !== t.config.maxView && t.picker.changeView(e + 1).render();
    }
    function st(t) {
      t.config.updateOnBlur
        ? t.update({ autohide: !0 })
        : (t.refresh("input"), t.hide());
    }
    function ot(t, e) {
      const i = t.picker,
        n = new Date(i.viewDate),
        a = i.currentView.id,
        r = 1 === a ? h(n, e - n.getMonth()) : p(n, e - n.getFullYear());
      i.changeFocus(r)
        .changeView(a - 1)
        .render();
    }
    function lt(t) {
      const e = t.picker,
        i = u();
      if (1 === t.config.todayBtnMode) {
        if (t.config.autohide) return void t.setDate(i);
        t.setDate(i, { render: !1 }), e.update();
      }
      e.viewDate !== i && e.changeFocus(i), e.changeView(0).render();
    }
    function ut(t) {
      t.setDate({ clear: !0 });
    }
    function ct(t) {
      rt(t);
    }
    function dt(t) {
      at(t, -1);
    }
    function ht(t) {
      at(t, 1);
    }
    function pt(t, e) {
      const i = I(e, ".datepicker-cell");
      if (!i || i.classList.contains("disabled")) return;
      const { id: n, isMinView: a } = t.picker.currentView;
      a
        ? t.setDate(Number(i.dataset.date))
        : ot(t, Number(1 === n ? i.dataset.month : i.dataset.year));
    }
    function ft(t) {
      t.inline || t.config.disableTouchKeyboard || t.inputField.focus();
    }
    function mt(e, i) {
      if (
        (void 0 !== i.title &&
          (i.title
            ? ((e.controls.title.textContent = i.title), F(e.controls.title))
            : ((e.controls.title.textContent = ""), j(e.controls.title))),
        i.prevArrow)
      ) {
        const t = e.controls.prevBtn;
        R(t),
          i.prevArrow.forEach((e) => {
            t.appendChild(e.cloneNode(!0));
          });
      }
      if (i.nextArrow) {
        const t = e.controls.nextBtn;
        R(t),
          i.nextArrow.forEach((e) => {
            t.appendChild(e.cloneNode(!0));
          });
      }
      if (
        (i.locale &&
          ((e.controls.todayBtn.textContent = i.locale.today),
          (e.controls.clearBtn.textContent = i.locale.clear)),
        void 0 !== i.todayBtn &&
          (i.todayBtn ? F(e.controls.todayBtn) : j(e.controls.todayBtn)),
        t(i, "minDate") || t(i, "maxDate"))
      ) {
        const { minDate: t, maxDate: i } = e.datepicker.config;
        e.controls.todayBtn.disabled = !a(u(), t, i);
      }
      void 0 !== i.clearBtn &&
        (i.clearBtn ? F(e.controls.clearBtn) : j(e.controls.clearBtn));
    }
    function gt(t) {
      const { dates: i, config: n } = t;
      return r(i.length > 0 ? e(i) : n.defaultViewDate, n.minDate, n.maxDate);
    }
    function vt(t, e) {
      const i = new Date(t.viewDate),
        n = new Date(e),
        { id: a, year: r, first: s, last: o } = t.currentView,
        l = n.getFullYear();
      switch (
        ((t.viewDate = e),
        l !== i.getFullYear() && nt(t.datepicker, "changeYear"),
        n.getMonth() !== i.getMonth() && nt(t.datepicker, "changeMonth"),
        a)
      ) {
        case 0:
          return e < s || e > o;
        case 1:
          return l !== r;
        default:
          return l < s || l > o;
      }
    }
    function _t(t) {
      return window.getComputedStyle(t).direction;
    }
    class yt {
      constructor(t) {
        this.datepicker = t;
        const e = q.replace(/%buttonClass%/g, t.config.buttonClass),
          i = (this.element = B(e).firstChild),
          [n, a, r] = i.firstChild.children,
          s = n.firstElementChild,
          [o, l, u] = n.lastElementChild.children,
          [c, d] = r.firstChild.children,
          h = {
            title: s,
            prevBtn: o,
            viewSwitch: l,
            nextBtn: u,
            todayBtn: c,
            clearBtn: d,
          };
        (this.main = a), (this.controls = h);
        const p = t.inline ? "inline" : "dropdown";
        i.classList.add(`datepicker-${p}`),
          mt(this, t.config),
          (this.viewDate = gt(t)),
          M(t, [
            [i, "click", ft.bind(null, t), { capture: !0 }],
            [a, "click", pt.bind(null, t)],
            [h.viewSwitch, "click", ct.bind(null, t)],
            [h.prevBtn, "click", dt.bind(null, t)],
            [h.nextBtn, "click", ht.bind(null, t)],
            [h.todayBtn, "click", lt.bind(null, t)],
            [h.clearBtn, "click", ut.bind(null, t)],
          ]),
          (this.views = [
            new Q(this),
            new et(this),
            new it(this, { id: 2, name: "years", cellClass: "year", step: 1 }),
            new it(this, {
              id: 3,
              name: "decades",
              cellClass: "decade",
              step: 10,
            }),
          ]),
          (this.currentView = this.views[t.config.startView]),
          this.currentView.render(),
          this.main.appendChild(this.currentView.element),
          t.config.container.appendChild(this.element);
      }
      setOptions(t) {
        mt(this, t),
          this.views.forEach((e) => {
            e.init(t, !1);
          }),
          this.currentView.render();
      }
      detach() {
        this.datepicker.config.container.removeChild(this.element);
      }
      show() {
        if (this.active) return;
        this.element.classList.add("active"), (this.active = !0);
        const t = this.datepicker;
        if (!t.inline) {
          const e = _t(t.inputField);
          e !== _t(t.config.container)
            ? (this.element.dir = e)
            : this.element.dir && this.element.removeAttribute("dir"),
            this.place(),
            t.config.disableTouchKeyboard && t.inputField.blur();
        }
        nt(t, "show");
      }
      hide() {
        this.active &&
          (this.datepicker.exitEditMode(),
          this.element.classList.remove("active"),
          (this.active = !1),
          nt(this.datepicker, "hide"));
      }
      place() {
        const { classList: t, style: e } = this.element,
          { config: i, inputField: n } = this.datepicker,
          a = i.container,
          { width: r, height: s } = this.element.getBoundingClientRect(),
          { left: o, top: l, width: u } = a.getBoundingClientRect(),
          { left: c, top: d, width: h, height: p } = n.getBoundingClientRect();
        let f,
          m,
          g,
          { x: v, y: _ } = i.orientation;
        a === document.body
          ? ((f = window.scrollY), (m = c + window.scrollX), (g = d + f))
          : ((f = a.scrollTop), (m = c - o), (g = d - l + f)),
          "auto" === v &&
            (m < 0
              ? ((v = "left"), (m = 10))
              : (v = m + r > u || "rtl" === _t(n) ? "right" : "left")),
          "right" === v && (m -= r - h),
          "auto" === _ && (_ = g - s < f ? "bottom" : "top"),
          "top" === _ ? (g -= s) : (g += p),
          t.remove(
            "datepicker-orient-top",
            "datepicker-orient-bottom",
            "datepicker-orient-right",
            "datepicker-orient-left"
          ),
          t.add(`datepicker-orient-${_}`, `datepicker-orient-${v}`),
          (e.top = g ? `${g}px` : g),
          (e.left = m ? `${m}px` : m);
      }
      setViewSwitchLabel(t) {
        this.controls.viewSwitch.textContent = t;
      }
      setPrevBtnDisabled(t) {
        this.controls.prevBtn.disabled = t;
      }
      setNextBtnDisabled(t) {
        this.controls.nextBtn.disabled = t;
      }
      changeView(t) {
        const e = this.currentView,
          i = this.views[t];
        return (
          i.id !== e.id &&
            ((this.currentView = i),
            (this._renderMethod = "render"),
            nt(this.datepicker, "changeView"),
            this.main.replaceChild(i.element, e.element)),
          this
        );
      }
      changeFocus(t) {
        return (
          (this._renderMethod = vt(this, t) ? "render" : "refreshFocus"),
          this.views.forEach((t) => {
            t.updateFocus();
          }),
          this
        );
      }
      update() {
        const t = gt(this.datepicker);
        return (
          (this._renderMethod = vt(this, t) ? "render" : "refresh"),
          this.views.forEach((t) => {
            t.updateFocus(), t.updateSelection();
          }),
          this
        );
      }
      render(t = !0) {
        const e = (t && this._renderMethod) || "render";
        delete this._renderMethod, this.currentView[e]();
      }
    }
    function bt(t, e, i, n, r, s) {
      if (a(t, r, s)) {
        if (n(t)) {
          return bt(e(t, i), e, i, n, r, s);
        }
        return t;
      }
    }
    function wt(t, e, i, n) {
      const a = t.picker,
        r = a.currentView,
        s = r.step || 1;
      let o,
        l,
        u = a.viewDate;
      switch (r.id) {
        case 0:
          (u = n ? d(u, 7 * i) : e.ctrlKey || e.metaKey ? p(u, i) : d(u, i)),
            (o = d),
            (l = (t) => r.disabled.includes(t));
          break;
        case 1:
          (u = h(u, n ? 4 * i : i)),
            (o = h),
            (l = (t) => {
              const e = new Date(t),
                { year: i, disabled: n } = r;
              return e.getFullYear() === i && n.includes(e.getMonth());
            });
          break;
        default:
          (u = p(u, i * (n ? 4 : 1) * s)),
            (o = p),
            (l = (t) => r.disabled.includes(g(t, s)));
      }
      (u = bt(u, o, i < 0 ? -s : s, l, r.minDate, r.maxDate)),
        void 0 !== u && a.changeFocus(u).render();
    }
    function xt(t, e) {
      if ("Tab" === e.key) return void st(t);
      const i = t.picker,
        { id: n, isMinView: a } = i.currentView;
      if (i.active)
        if (t.editMode)
          switch (e.key) {
            case "Escape":
              i.hide();
              break;
            case "Enter":
              t.exitEditMode({ update: !0, autohide: t.config.autohide });
              break;
            default:
              return;
          }
        else
          switch (e.key) {
            case "Escape":
              i.hide();
              break;
            case "ArrowLeft":
              if (e.ctrlKey || e.metaKey) at(t, -1);
              else {
                if (e.shiftKey) return void t.enterEditMode();
                wt(t, e, -1, !1);
              }
              break;
            case "ArrowRight":
              if (e.ctrlKey || e.metaKey) at(t, 1);
              else {
                if (e.shiftKey) return void t.enterEditMode();
                wt(t, e, 1, !1);
              }
              break;
            case "ArrowUp":
              if (e.ctrlKey || e.metaKey) rt(t);
              else {
                if (e.shiftKey) return void t.enterEditMode();
                wt(t, e, -1, !0);
              }
              break;
            case "ArrowDown":
              if (e.shiftKey && !e.ctrlKey && !e.metaKey)
                return void t.enterEditMode();
              wt(t, e, 1, !0);
              break;
            case "Enter":
              a ? t.setDate(i.viewDate) : i.changeView(n - 1).render();
              break;
            case "Backspace":
            case "Delete":
              return void t.enterEditMode();
            default:
              return void (
                1 !== e.key.length ||
                e.ctrlKey ||
                e.metaKey ||
                t.enterEditMode()
              );
          }
      else
        switch (e.key) {
          case "ArrowDown":
          case "Escape":
            i.show();
            break;
          case "Enter":
            t.update();
            break;
          default:
            return;
        }
      e.preventDefault(), e.stopPropagation();
    }
    function St(t) {
      t.config.showOnFocus && !t._showing && t.show();
    }
    function Tt(t, e) {
      const i = e.target;
      (t.picker.active || t.config.showOnClick) &&
        ((i._active = i === document.activeElement),
        (i._clicking = setTimeout(() => {
          delete i._active, delete i._clicking;
        }, 2e3)));
    }
    function Ct(t, e) {
      const i = e.target;
      i._clicking &&
        (clearTimeout(i._clicking),
        delete i._clicking,
        i._active && t.enterEditMode(),
        delete i._active,
        t.config.showOnClick && t.show());
    }
    function Et(t, e) {
      e.clipboardData.types.includes("text/plain") && t.enterEditMode();
    }
    function Pt(t, e) {
      const i = t.element;
      if (i !== document.activeElement) return;
      const n = t.picker.element;
      I(e, (t) => t === i || t === n) || st(t);
    }
    function Lt(t, e) {
      return t.map((t) => E(t, e.format, e.locale)).join(e.dateDelimiter);
    }
    function Dt(t, e, i = !1) {
      const { config: n, dates: r, rangepicker: s } = t;
      if (0 === e.length) return i ? [] : void 0;
      const o = s && t === s.datepickers[1];
      let l = e.reduce((t, e) => {
        let i = C(e, n.format, n.locale);
        if (void 0 === i) return t;
        if (n.pickLevel > 0) {
          const t = new Date(i);
          i =
            1 === n.pickLevel
              ? o
                ? t.setMonth(t.getMonth() + 1, 0)
                : t.setDate(1)
              : o
              ? t.setFullYear(t.getFullYear() + 1, 0, 0)
              : t.setMonth(0, 1);
        }
        return (
          !a(i, n.minDate, n.maxDate) ||
            t.includes(i) ||
            n.datesDisabled.includes(i) ||
            n.daysOfWeekDisabled.includes(new Date(i).getDay()) ||
            t.push(i),
          t
        );
      }, []);
      return 0 !== l.length
        ? (n.multidate &&
            !i &&
            (l = l.reduce(
              (t, e) => (r.includes(e) || t.push(e), t),
              r.filter((t) => !l.includes(t))
            )),
          n.maxNumberOfDates && l.length > n.maxNumberOfDates
            ? l.slice(-1 * n.maxNumberOfDates)
            : l)
        : void 0;
    }
    function Mt(t, e = 3, i = !0) {
      const { config: n, picker: a, inputField: r } = t;
      if (2 & e) {
        const t = a.active ? n.pickLevel : n.startView;
        a.update().changeView(t).render(i);
      }
      1 & e && r && (r.value = Lt(t.dates, n));
    }
    function kt(t, e, i) {
      let { clear: n, render: a, autohide: r } = i;
      void 0 === a && (a = !0),
        a ? void 0 === r && (r = t.config.autohide) : (r = !1);
      const s = Dt(t, e, n);
      s &&
        (s.toString() !== t.dates.toString()
          ? ((t.dates = s), Mt(t, a ? 3 : 1), nt(t, "changeDate"))
          : Mt(t, 1),
        r && t.hide());
    }
    class At {
      constructor(t, e = {}, i) {
        (t.datepicker = this), (this.element = t);
        const a = (this.config = Object.assign(
          {
            buttonClass: (e.buttonClass && String(e.buttonClass)) || "button",
            container: document.body,
            defaultViewDate: u(),
            maxDate: void 0,
            minDate: void 0,
          },
          Y(O, this)
        ));
        (this._options = e), Object.assign(a, Y(e, this));
        const r = (this.inline = "INPUT" !== t.tagName);
        let s, o;
        if (r)
          (a.container = t),
            (o = n(t.dataset.date, a.dateDelimiter)),
            delete t.dataset.date;
        else {
          const i = e.container ? document.querySelector(e.container) : null;
          i && (a.container = i),
            (s = this.inputField = t),
            s.classList.add("datepicker-input"),
            (o = n(s.value, a.dateDelimiter));
        }
        if (i) {
          const t = i.inputs.indexOf(s),
            e = i.datepickers;
          if (t < 0 || t > 1 || !Array.isArray(e))
            throw Error("Invalid rangepicker object.");
          (e[t] = this),
            Object.defineProperty(this, "rangepicker", { get: () => i });
        }
        this.dates = [];
        const l = Dt(this, o);
        l && l.length > 0 && (this.dates = l),
          s && (s.value = Lt(this.dates, a));
        const c = (this.picker = new yt(this));
        if (r) this.show();
        else {
          const t = Pt.bind(null, this);
          M(this, [
            [s, "keydown", xt.bind(null, this)],
            [s, "focus", St.bind(null, this)],
            [s, "mousedown", Tt.bind(null, this)],
            [s, "click", Ct.bind(null, this)],
            [s, "paste", Et.bind(null, this)],
            [document, "mousedown", t],
            [document, "touchstart", t],
            [window, "resize", c.place.bind(c)],
          ]);
        }
      }
      static formatDate(t, e, i) {
        return E(t, e, (i && z[i]) || z.en);
      }
      static parseDate(t, e, i) {
        return C(t, e, (i && z[i]) || z.en);
      }
      static get locales() {
        return z;
      }
      get active() {
        return !(!this.picker || !this.picker.active);
      }
      get pickerElement() {
        return this.picker ? this.picker.element : void 0;
      }
      setOptions(t) {
        const e = this.picker,
          i = Y(t, this);
        Object.assign(this._options, t),
          Object.assign(this.config, i),
          e.setOptions(i),
          Mt(this, 3);
      }
      show() {
        if (this.inputField) {
          if (this.inputField.disabled) return;
          this.inputField !== document.activeElement &&
            ((this._showing = !0),
            this.inputField.focus(),
            delete this._showing);
        }
        this.picker.show();
      }
      hide() {
        this.inline ||
          (this.picker.hide(),
          this.picker.update().changeView(this.config.startView).render());
      }
      destroy() {
        return (
          this.hide(),
          k(this),
          this.picker.detach(),
          this.inline || this.inputField.classList.remove("datepicker-input"),
          delete this.element.datepicker,
          this
        );
      }
      getDate(t) {
        const e = t ? (e) => E(e, t, this.config.locale) : (t) => new Date(t);
        return this.config.multidate
          ? this.dates.map(e)
          : this.dates.length > 0
          ? e(this.dates[0])
          : void 0;
      }
      setDate(...t) {
        const i = [...t],
          n = {},
          a = e(t);
        "object" != typeof a ||
          Array.isArray(a) ||
          a instanceof Date ||
          !a ||
          Object.assign(n, i.pop());
        kt(this, Array.isArray(i[0]) ? i[0] : i, n);
      }
      update(t) {
        if (this.inline) return;
        const e = { clear: !0, autohide: !(!t || !t.autohide) };
        kt(this, n(this.inputField.value, this.config.dateDelimiter), e);
      }
      refresh(t, e = !1) {
        let i;
        t && "string" != typeof t && ((e = t), (t = void 0)),
          (i = "picker" === t ? 2 : "input" === t ? 1 : 3),
          Mt(this, i, !e);
      }
      enterEditMode() {
        this.inline ||
          !this.picker.active ||
          this.editMode ||
          ((this.editMode = !0), this.inputField.classList.add("in-edit"));
      }
      exitEditMode(t) {
        if (this.inline || !this.editMode) return;
        const e = Object.assign({ update: !1 }, t);
        delete this.editMode,
          this.inputField.classList.remove("in-edit"),
          e.update && this.update(e);
      }
    }
    function It(t) {
      const e = Object.assign({}, t);
      return (
        delete e.inputs,
        delete e.allowOneSidedRange,
        delete e.maxNumberOfDates,
        e
      );
    }
    function zt(t, e, i, n) {
      M(t, [[i, "changeDate", e]]), new At(i, n, t);
    }
    function Ot(t, e) {
      if (t._updating) return;
      t._updating = !0;
      const i = e.target;
      if (void 0 === i.datepicker) return;
      const n = t.datepickers,
        a = { render: !1 },
        r = t.inputs.indexOf(i),
        s = 0 === r ? 1 : 0,
        o = n[r].dates[0],
        l = n[s].dates[0];
      void 0 !== o && void 0 !== l
        ? 0 === r && o > l
          ? (n[0].setDate(l, a), n[1].setDate(o, a))
          : 1 === r && o < l && (n[0].setDate(o, a), n[1].setDate(l, a))
        : t.allowOneSidedRange ||
          (void 0 === o && void 0 === l) ||
          ((a.clear = !0), n[s].setDate(n[r].dates, a)),
        n[0].picker.update().render(),
        n[1].picker.update().render(),
        delete t._updating;
    }
    (window.Datepicker = At),
      (window.DateRangePicker = class {
        constructor(t, e = {}) {
          const i = Array.isArray(e.inputs)
            ? e.inputs
            : Array.from(t.querySelectorAll("input"));
          if (i.length < 2) return;
          (t.rangepicker = this),
            (this.element = t),
            (this.inputs = i.slice(0, 2)),
            (this.allowOneSidedRange = !!e.allowOneSidedRange);
          const n = Ot.bind(null, this),
            a = It(e),
            r = [];
          Object.defineProperty(this, "datepickers", { get: () => r }),
            zt(this, n, this.inputs[0], a),
            zt(this, n, this.inputs[1], a),
            Object.freeze(r),
            r[0].dates.length > 0
              ? Ot(this, { target: this.inputs[0] })
              : r[1].dates.length > 0 && Ot(this, { target: this.inputs[1] });
        }
        get dates() {
          return 2 === this.datepickers.length
            ? [this.datepickers[0].dates[0], this.datepickers[1].dates[0]]
            : void 0;
        }
        setOptions(t) {
          this.allowOneSidedRange = !!t.allowOneSidedRange;
          const e = It(t);
          this.datepickers[0].setOptions(e), this.datepickers[1].setOptions(e);
        }
        destroy() {
          this.datepickers[0].destroy(),
            this.datepickers[1].destroy(),
            k(this),
            delete this.element.rangepicker;
        }
        getDates(t) {
          const e = t
            ? (e) => E(e, t, this.datepickers[0].config.locale)
            : (t) => new Date(t);
          return this.dates.map((t) => (void 0 === t ? t : e(t)));
        }
        setDates(t, e) {
          const [i, n] = this.datepickers,
            a = this.dates;
          (this._updating = !0),
            i.setDate(t),
            n.setDate(e),
            delete this._updating,
            n.dates[0] !== a[1]
              ? Ot(this, { target: this.inputs[1] })
              : i.dates[0] !== a[0] && Ot(this, { target: this.inputs[0] });
        }
      });
  })(),
  (function (t) {
    "function" == typeof define && define.amd
      ? define([], t)
      : "object" == typeof exports
      ? (module.exports = t())
      : (window.noUiSlider = t());
  })(function () {
    "use strict";
    var t = "14.7.0";
    function e(t) {
      t.parentElement.removeChild(t);
    }
    function i(t) {
      return null != t;
    }
    function n(t) {
      t.preventDefault();
    }
    function a(t) {
      return "number" == typeof t && !isNaN(t) && isFinite(t);
    }
    function r(t, e, i) {
      0 < i &&
        (u(t, e),
        setTimeout(function () {
          c(t, e);
        }, i));
    }
    function s(t) {
      return Math.max(Math.min(t, 100), 0);
    }
    function o(t) {
      return Array.isArray(t) ? t : [t];
    }
    function l(t) {
      var e = (t = String(t)).split(".");
      return 1 < e.length ? e[1].length : 0;
    }
    function u(t, e) {
      t.classList && !/\s/.test(e)
        ? t.classList.add(e)
        : (t.className += " " + e);
    }
    function c(t, e) {
      t.classList && !/\s/.test(e)
        ? t.classList.remove(e)
        : (t.className = t.className.replace(
            new RegExp("(^|\\b)" + e.split(" ").join("|") + "(\\b|$)", "gi"),
            " "
          ));
    }
    function d(t) {
      var e = void 0 !== window.pageXOffset,
        i = "CSS1Compat" === (t.compatMode || "");
      return {
        x: e
          ? window.pageXOffset
          : i
          ? t.documentElement.scrollLeft
          : t.body.scrollLeft,
        y: e
          ? window.pageYOffset
          : i
          ? t.documentElement.scrollTop
          : t.body.scrollTop,
      };
    }
    function h(t, e) {
      return 100 / (e - t);
    }
    function p(t, e, i) {
      return (100 * e) / (t[i + 1] - t[i]);
    }
    function f(t, e) {
      for (var i = 1; t >= e[i]; ) i += 1;
      return i;
    }
    function m(t, e, i) {
      var n;
      if (("number" == typeof e && (e = [e]), !Array.isArray(e)))
        throw new Error("noUiSlider (14.7.0): 'range' contains invalid value.");
      if (
        !a((n = "min" === t ? 0 : "max" === t ? 100 : parseFloat(t))) ||
        !a(e[0])
      )
        throw new Error("noUiSlider (14.7.0): 'range' value isn't numeric.");
      i.xPct.push(n),
        i.xVal.push(e[0]),
        n
          ? i.xSteps.push(!isNaN(e[1]) && e[1])
          : isNaN(e[1]) || (i.xSteps[0] = e[1]),
        i.xHighestCompleteStep.push(0);
    }
    function g(t, e, i) {
      if (e)
        if (i.xVal[t] !== i.xVal[t + 1]) {
          i.xSteps[t] =
            p([i.xVal[t], i.xVal[t + 1]], e, 0) / h(i.xPct[t], i.xPct[t + 1]);
          var n = (i.xVal[t + 1] - i.xVal[t]) / i.xNumSteps[t],
            a = Math.ceil(Number(n.toFixed(3)) - 1),
            r = i.xVal[t] + i.xNumSteps[t] * a;
          i.xHighestCompleteStep[t] = r;
        } else i.xSteps[t] = i.xHighestCompleteStep[t] = i.xVal[t];
    }
    function v(t, e, i) {
      var n;
      (this.xPct = []),
        (this.xVal = []),
        (this.xSteps = [i || !1]),
        (this.xNumSteps = [!1]),
        (this.xHighestCompleteStep = []),
        (this.snap = e);
      var a = [];
      for (n in t) t.hasOwnProperty(n) && a.push([t[n], n]);
      for (
        a.length && "object" == typeof a[0][0]
          ? a.sort(function (t, e) {
              return t[0][0] - e[0][0];
            })
          : a.sort(function (t, e) {
              return t[0] - e[0];
            }),
          n = 0;
        n < a.length;
        n++
      )
        m(a[n][1], a[n][0], this);
      for (
        this.xNumSteps = this.xSteps.slice(0), n = 0;
        n < this.xNumSteps.length;
        n++
      )
        g(n, this.xNumSteps[n], this);
    }
    (v.prototype.getDistance = function (t) {
      var e,
        i = [];
      for (e = 0; e < this.xNumSteps.length - 1; e++) {
        var n = this.xNumSteps[e];
        if (n && (t / n) % 1 != 0)
          throw new Error(
            "noUiSlider (14.7.0): 'limit', 'margin' and 'padding' of " +
              this.xPct[e] +
              "% range must be divisible by step."
          );
        i[e] = p(this.xVal, t, e);
      }
      return i;
    }),
      (v.prototype.getAbsoluteDistance = function (t, e, i) {
        var n,
          a = 0;
        if (t < this.xPct[this.xPct.length - 1])
          for (; t > this.xPct[a + 1]; ) a++;
        else
          t === this.xPct[this.xPct.length - 1] && (a = this.xPct.length - 2);
        i || t !== this.xPct[a + 1] || a++;
        var r = 1,
          s = e[a],
          o = 0,
          l = 0,
          u = 0,
          c = 0;
        for (
          n = i
            ? (t - this.xPct[a]) / (this.xPct[a + 1] - this.xPct[a])
            : (this.xPct[a + 1] - t) / (this.xPct[a + 1] - this.xPct[a]);
          0 < s;

        )
          (o = this.xPct[a + 1 + c] - this.xPct[a + c]),
            100 < e[a + c] * r + 100 - 100 * n
              ? ((l = o * n), (r = (s - 100 * n) / e[a + c]), (n = 1))
              : ((l = ((e[a + c] * o) / 100) * r), (r = 0)),
            i
              ? ((u -= l), 1 <= this.xPct.length + c && c--)
              : ((u += l), 1 <= this.xPct.length - c && c++),
            (s = e[a + c] * r);
        return t + u;
      }),
      (v.prototype.toStepping = function (t) {
        return (function (t, e, i) {
          if (i >= t.slice(-1)[0]) return 100;
          var n,
            a,
            r = f(i, t),
            s = t[r - 1],
            o = t[r],
            l = e[r - 1],
            u = e[r];
          return (
            l +
            ((a = i),
            p((n = [s, o]), n[0] < 0 ? a + Math.abs(n[0]) : a - n[0], 0) /
              h(l, u))
          );
        })(this.xVal, this.xPct, t);
      }),
      (v.prototype.fromStepping = function (t) {
        return (function (t, e, i) {
          if (100 <= i) return t.slice(-1)[0];
          var n,
            a = f(i, e),
            r = t[a - 1],
            s = t[a],
            o = e[a - 1];
          return (
            (n = [r, s]), ((i - o) * h(o, e[a]) * (n[1] - n[0])) / 100 + n[0]
          );
        })(this.xVal, this.xPct, t);
      }),
      (v.prototype.getStep = function (t) {
        return (function (t, e, i, n) {
          if (100 === n) return n;
          var a,
            r,
            s = f(n, t),
            o = t[s - 1],
            l = t[s];
          return i
            ? (l - o) / 2 < n - o
              ? l
              : o
            : e[s - 1]
            ? t[s - 1] +
              ((a = n - t[s - 1]), (r = e[s - 1]), Math.round(a / r) * r)
            : n;
        })(this.xPct, this.xSteps, this.snap, t);
      }),
      (v.prototype.getDefaultStep = function (t, e, i) {
        var n = f(t, this.xPct);
        return (
          (100 === t || (e && t === this.xPct[n - 1])) &&
            (n = Math.max(n - 1, 1)),
          (this.xVal[n] - this.xVal[n - 1]) / i
        );
      }),
      (v.prototype.getNearbySteps = function (t) {
        var e = f(t, this.xPct);
        return {
          stepBefore: {
            startValue: this.xVal[e - 2],
            step: this.xNumSteps[e - 2],
            highestStep: this.xHighestCompleteStep[e - 2],
          },
          thisStep: {
            startValue: this.xVal[e - 1],
            step: this.xNumSteps[e - 1],
            highestStep: this.xHighestCompleteStep[e - 1],
          },
          stepAfter: {
            startValue: this.xVal[e],
            step: this.xNumSteps[e],
            highestStep: this.xHighestCompleteStep[e],
          },
        };
      }),
      (v.prototype.countStepDecimals = function () {
        var t = this.xNumSteps.map(l);
        return Math.max.apply(null, t);
      }),
      (v.prototype.convert = function (t) {
        return this.getStep(this.toStepping(t));
      });
    var _ = {
        to: function (t) {
          return void 0 !== t && t.toFixed(2);
        },
        from: Number,
      },
      y = {
        target: "target",
        base: "base",
        origin: "origin",
        handle: "handle",
        handleLower: "handle-lower",
        handleUpper: "handle-upper",
        touchArea: "touch-area",
        horizontal: "horizontal",
        vertical: "vertical",
        background: "background",
        connect: "connect",
        connects: "connects",
        ltr: "ltr",
        rtl: "rtl",
        textDirectionLtr: "txt-dir-ltr",
        textDirectionRtl: "txt-dir-rtl",
        draggable: "draggable",
        drag: "state-drag",
        tap: "state-tap",
        active: "active",
        tooltip: "tooltip",
        pips: "pips",
        pipsHorizontal: "pips-horizontal",
        pipsVertical: "pips-vertical",
        marker: "marker",
        markerHorizontal: "marker-horizontal",
        markerVertical: "marker-vertical",
        markerNormal: "marker-normal",
        markerLarge: "marker-large",
        markerSub: "marker-sub",
        value: "value",
        valueHorizontal: "value-horizontal",
        valueVertical: "value-vertical",
        valueNormal: "value-normal",
        valueLarge: "value-large",
        valueSub: "value-sub",
      },
      b = ".__tooltips",
      w = ".__aria";
    function x(t) {
      if (
        "object" == typeof (e = t) &&
        "function" == typeof e.to &&
        "function" == typeof e.from
      )
        return !0;
      var e;
      throw new Error(
        "noUiSlider (14.7.0): 'format' requires 'to' and 'from' methods."
      );
    }
    function S(t, e) {
      if (!a(e)) throw new Error("noUiSlider (14.7.0): 'step' is not numeric.");
      t.singleStep = e;
    }
    function T(t, e) {
      if (!a(e))
        throw new Error(
          "noUiSlider (14.7.0): 'keyboardPageMultiplier' is not numeric."
        );
      t.keyboardPageMultiplier = e;
    }
    function C(t, e) {
      if (!a(e))
        throw new Error(
          "noUiSlider (14.7.0): 'keyboardDefaultStep' is not numeric."
        );
      t.keyboardDefaultStep = e;
    }
    function E(t, e) {
      if ("object" != typeof e || Array.isArray(e))
        throw new Error("noUiSlider (14.7.0): 'range' is not an object.");
      if (void 0 === e.min || void 0 === e.max)
        throw new Error(
          "noUiSlider (14.7.0): Missing 'min' or 'max' in 'range'."
        );
      if (e.min === e.max)
        throw new Error(
          "noUiSlider (14.7.0): 'range' 'min' and 'max' cannot be equal."
        );
      t.spectrum = new v(e, t.snap, t.singleStep);
    }
    function P(t, e) {
      if (((e = o(e)), !Array.isArray(e) || !e.length))
        throw new Error("noUiSlider (14.7.0): 'start' option is incorrect.");
      (t.handles = e.length), (t.start = e);
    }
    function L(t, e) {
      if ("boolean" != typeof (t.snap = e))
        throw new Error(
          "noUiSlider (14.7.0): 'snap' option must be a boolean."
        );
    }
    function D(t, e) {
      if ("boolean" != typeof (t.animate = e))
        throw new Error(
          "noUiSlider (14.7.0): 'animate' option must be a boolean."
        );
    }
    function M(t, e) {
      if ("number" != typeof (t.animationDuration = e))
        throw new Error(
          "noUiSlider (14.7.0): 'animationDuration' option must be a number."
        );
    }
    function k(t, e) {
      var i,
        n = [!1];
      if (
        ("lower" === e ? (e = [!0, !1]) : "upper" === e && (e = [!1, !0]),
        !0 === e || !1 === e)
      ) {
        for (i = 1; i < t.handles; i++) n.push(e);
        n.push(!1);
      } else {
        if (!Array.isArray(e) || !e.length || e.length !== t.handles + 1)
          throw new Error(
            "noUiSlider (14.7.0): 'connect' option doesn't match handle count."
          );
        n = e;
      }
      t.connect = n;
    }
    function A(t, e) {
      switch (e) {
        case "horizontal":
          t.ort = 0;
          break;
        case "vertical":
          t.ort = 1;
          break;
        default:
          throw new Error(
            "noUiSlider (14.7.0): 'orientation' option is invalid."
          );
      }
    }
    function I(t, e) {
      if (!a(e))
        throw new Error(
          "noUiSlider (14.7.0): 'margin' option must be numeric."
        );
      0 !== e && (t.margin = t.spectrum.getDistance(e));
    }
    function z(t, e) {
      if (!a(e))
        throw new Error("noUiSlider (14.7.0): 'limit' option must be numeric.");
      if (((t.limit = t.spectrum.getDistance(e)), !t.limit || t.handles < 2))
        throw new Error(
          "noUiSlider (14.7.0): 'limit' option is only supported on linear sliders with 2 or more handles."
        );
    }
    function O(t, e) {
      var i;
      if (!a(e) && !Array.isArray(e))
        throw new Error(
          "noUiSlider (14.7.0): 'padding' option must be numeric or array of exactly 2 numbers."
        );
      if (Array.isArray(e) && 2 !== e.length && !a(e[0]) && !a(e[1]))
        throw new Error(
          "noUiSlider (14.7.0): 'padding' option must be numeric or array of exactly 2 numbers."
        );
      if (0 !== e) {
        for (
          Array.isArray(e) || (e = [e, e]),
            t.padding = [
              t.spectrum.getDistance(e[0]),
              t.spectrum.getDistance(e[1]),
            ],
            i = 0;
          i < t.spectrum.xNumSteps.length - 1;
          i++
        )
          if (t.padding[0][i] < 0 || t.padding[1][i] < 0)
            throw new Error(
              "noUiSlider (14.7.0): 'padding' option must be a positive number(s)."
            );
        var n = e[0] + e[1],
          r = t.spectrum.xVal[0];
        if (1 < n / (t.spectrum.xVal[t.spectrum.xVal.length - 1] - r))
          throw new Error(
            "noUiSlider (14.7.0): 'padding' option must not exceed 100% of the range."
          );
      }
    }
    function N(t, e) {
      switch (e) {
        case "ltr":
          t.dir = 0;
          break;
        case "rtl":
          t.dir = 1;
          break;
        default:
          throw new Error(
            "noUiSlider (14.7.0): 'direction' option was not recognized."
          );
      }
    }
    function B(t, e) {
      if ("string" != typeof e)
        throw new Error(
          "noUiSlider (14.7.0): 'behaviour' must be a string containing options."
        );
      var i = 0 <= e.indexOf("tap"),
        n = 0 <= e.indexOf("drag"),
        a = 0 <= e.indexOf("fixed"),
        r = 0 <= e.indexOf("snap"),
        s = 0 <= e.indexOf("hover"),
        o = 0 <= e.indexOf("unconstrained");
      if (a) {
        if (2 !== t.handles)
          throw new Error(
            "noUiSlider (14.7.0): 'fixed' behaviour must be used with 2 handles"
          );
        I(t, t.start[1] - t.start[0]);
      }
      if (o && (t.margin || t.limit))
        throw new Error(
          "noUiSlider (14.7.0): 'unconstrained' behaviour cannot be used with margin or limit"
        );
      t.events = {
        tap: i || r,
        drag: n,
        fixed: a,
        snap: r,
        hover: s,
        unconstrained: o,
      };
    }
    function j(t, e) {
      if (!1 !== e)
        if (!0 === e) {
          t.tooltips = [];
          for (var i = 0; i < t.handles; i++) t.tooltips.push(!0);
        } else {
          if (((t.tooltips = o(e)), t.tooltips.length !== t.handles))
            throw new Error(
              "noUiSlider (14.7.0): must pass a formatter for all handles."
            );
          t.tooltips.forEach(function (t) {
            if (
              "boolean" != typeof t &&
              ("object" != typeof t || "function" != typeof t.to)
            )
              throw new Error(
                "noUiSlider (14.7.0): 'tooltips' must be passed a formatter or 'false'."
              );
          });
        }
    }
    function F(t, e) {
      x((t.ariaFormat = e));
    }
    function R(t, e) {
      x((t.format = e));
    }
    function $(t, e) {
      if ("boolean" != typeof (t.keyboardSupport = e))
        throw new Error(
          "noUiSlider (14.7.0): 'keyboardSupport' option must be a boolean."
        );
    }
    function H(t, e) {
      t.documentElement = e;
    }
    function V(t, e) {
      if ("string" != typeof e && !1 !== e)
        throw new Error(
          "noUiSlider (14.7.0): 'cssPrefix' must be a string or `false`."
        );
      t.cssPrefix = e;
    }
    function W(t, e) {
      if ("object" != typeof e)
        throw new Error("noUiSlider (14.7.0): 'cssClasses' must be an object.");
      if ("string" == typeof t.cssPrefix)
        for (var i in ((t.cssClasses = {}), e))
          e.hasOwnProperty(i) && (t.cssClasses[i] = t.cssPrefix + e[i]);
      else t.cssClasses = e;
    }
    function Z(t) {
      var e = {
          margin: 0,
          limit: 0,
          padding: 0,
          animate: !0,
          animationDuration: 300,
          ariaFormat: _,
          format: _,
        },
        n = {
          step: { r: !1, t: S },
          keyboardPageMultiplier: { r: !1, t: T },
          keyboardDefaultStep: { r: !1, t: C },
          start: { r: !0, t: P },
          connect: { r: !0, t: k },
          direction: { r: !0, t: N },
          snap: { r: !1, t: L },
          animate: { r: !1, t: D },
          animationDuration: { r: !1, t: M },
          range: { r: !0, t: E },
          orientation: { r: !1, t: A },
          margin: { r: !1, t: I },
          limit: { r: !1, t: z },
          padding: { r: !1, t: O },
          behaviour: { r: !0, t: B },
          ariaFormat: { r: !1, t: F },
          format: { r: !1, t: R },
          tooltips: { r: !1, t: j },
          keyboardSupport: { r: !0, t: $ },
          documentElement: { r: !1, t: H },
          cssPrefix: { r: !0, t: V },
          cssClasses: { r: !0, t: W },
        },
        a = {
          connect: !1,
          direction: "ltr",
          behaviour: "tap",
          orientation: "horizontal",
          keyboardSupport: !0,
          cssPrefix: "noUi-",
          cssClasses: y,
          keyboardPageMultiplier: 5,
          keyboardDefaultStep: 10,
        };
      t.format && !t.ariaFormat && (t.ariaFormat = t.format),
        Object.keys(n).forEach(function (r) {
          if (!i(t[r]) && void 0 === a[r]) {
            if (n[r].r)
              throw new Error("noUiSlider (14.7.0): '" + r + "' is required.");
            return !0;
          }
          n[r].t(e, i(t[r]) ? t[r] : a[r]);
        }),
        (e.pips = t.pips);
      var r = document.createElement("div"),
        s = void 0 !== r.style.msTransform,
        o = void 0 !== r.style.transform;
      return (
        (e.transformRule = o
          ? "transform"
          : s
          ? "msTransform"
          : "webkitTransform"),
        (e.style = [
          ["left", "top"],
          ["right", "bottom"],
        ][e.dir][e.ort]),
        e
      );
    }
    return {
      __spectrum: v,
      version: t,
      cssClasses: y,
      create: function (t, a) {
        if (!t || !t.nodeName)
          throw new Error(
            "noUiSlider (14.7.0): create requires a single element, got: " + t
          );
        if (t.noUiSlider)
          throw new Error(
            "noUiSlider (14.7.0): Slider was already initialized."
          );
        var l = (function (t, a, l) {
          var h,
            p,
            f,
            m,
            g,
            v,
            _,
            y,
            x = window.navigator.pointerEnabled
              ? { start: "pointerdown", move: "pointermove", end: "pointerup" }
              : window.navigator.msPointerEnabled
              ? {
                  start: "MSPointerDown",
                  move: "MSPointerMove",
                  end: "MSPointerUp",
                }
              : {
                  start: "mousedown touchstart",
                  move: "mousemove touchmove",
                  end: "mouseup touchend",
                },
            S =
              window.CSS &&
              CSS.supports &&
              CSS.supports("touch-action", "none") &&
              (function () {
                var t = !1;
                try {
                  var e = Object.defineProperty({}, "passive", {
                    get: function () {
                      t = !0;
                    },
                  });
                  window.addEventListener("test", null, e);
                } catch (t) {}
                return t;
              })(),
            T = t,
            C = a.spectrum,
            E = [],
            P = [],
            L = [],
            D = 0,
            M = {},
            k = t.ownerDocument,
            A = a.documentElement || k.documentElement,
            I = k.body,
            z = "rtl" === k.dir || 1 === a.ort ? 0 : 100;
          function O(t, e) {
            var i = k.createElement("div");
            return e && u(i, e), t.appendChild(i), i;
          }
          function N(t, e) {
            var i = O(t, a.cssClasses.origin),
              n = O(i, a.cssClasses.handle);
            return (
              O(n, a.cssClasses.touchArea),
              n.setAttribute("data-handle", e),
              a.keyboardSupport &&
                (n.setAttribute("tabindex", "0"),
                n.addEventListener("keydown", function (t) {
                  return (function (t, e) {
                    if (F() || R(e)) return !1;
                    var i = ["Left", "Right"],
                      n = ["Down", "Up"],
                      r = ["PageDown", "PageUp"],
                      s = ["Home", "End"];
                    a.dir && !a.ort
                      ? i.reverse()
                      : a.ort && !a.dir && (n.reverse(), r.reverse());
                    var o,
                      l = t.key.replace("Arrow", ""),
                      u = l === r[0],
                      c = l === r[1],
                      d = l === n[0] || l === i[0] || u,
                      h = l === n[1] || l === i[1] || c,
                      p = l === s[1];
                    if (!(d || h || l === s[0] || p)) return !0;
                    if ((t.preventDefault(), h || d)) {
                      var f = a.keyboardPageMultiplier,
                        m = d ? 0 : 1,
                        g = pt(e)[m];
                      if (null === g) return !1;
                      !1 === g &&
                        (g = C.getDefaultStep(P[e], d, a.keyboardDefaultStep)),
                        (c || u) && (g *= f),
                        (g = Math.max(g, 1e-7)),
                        (g *= d ? -1 : 1),
                        (o = E[e] + g);
                    } else o = p ? a.spectrum.xVal[a.spectrum.xVal.length - 1] : a.spectrum.xVal[0];
                    return (
                      lt(e, C.toStepping(o), !0, !0),
                      it("slide", e),
                      it("update", e),
                      it("change", e),
                      it("set", e),
                      !1
                    );
                  })(t, e);
                })),
              n.setAttribute("role", "slider"),
              n.setAttribute(
                "aria-orientation",
                a.ort ? "vertical" : "horizontal"
              ),
              0 === e
                ? u(n, a.cssClasses.handleLower)
                : e === a.handles - 1 && u(n, a.cssClasses.handleUpper),
              i
            );
          }
          function B(t, e) {
            return !!e && O(t, a.cssClasses.connect);
          }
          function j(t, e) {
            return !!a.tooltips[e] && O(t.firstChild, a.cssClasses.tooltip);
          }
          function F() {
            return T.hasAttribute("disabled");
          }
          function R(t) {
            return p[t].hasAttribute("disabled");
          }
          function $() {
            g &&
              (et("update" + b),
              g.forEach(function (t) {
                t && e(t);
              }),
              (g = null));
          }
          function H() {
            $(),
              (g = p.map(j)),
              tt("update" + b, function (t, e, i) {
                if (g[e]) {
                  var n = t[e];
                  !0 !== a.tooltips[e] && (n = a.tooltips[e].to(i[e])),
                    (g[e].innerHTML = n);
                }
              });
          }
          function V(t, e, i) {
            var n = k.createElement("div"),
              r = [];
            (r[0] = a.cssClasses.valueNormal),
              (r[1] = a.cssClasses.valueLarge),
              (r[2] = a.cssClasses.valueSub);
            var s = [];
            (s[0] = a.cssClasses.markerNormal),
              (s[1] = a.cssClasses.markerLarge),
              (s[2] = a.cssClasses.markerSub);
            var o = [a.cssClasses.valueHorizontal, a.cssClasses.valueVertical],
              l = [a.cssClasses.markerHorizontal, a.cssClasses.markerVertical];
            function c(t, e) {
              var i = e === a.cssClasses.value,
                n = i ? r : s;
              return e + " " + (i ? o : l)[a.ort] + " " + n[t];
            }
            return (
              u(n, a.cssClasses.pips),
              u(
                n,
                0 === a.ort
                  ? a.cssClasses.pipsHorizontal
                  : a.cssClasses.pipsVertical
              ),
              Object.keys(t).forEach(function (r) {
                !(function (t, r, s) {
                  if (-1 !== (s = e ? e(r, s) : s)) {
                    var o = O(n, !1);
                    (o.className = c(s, a.cssClasses.marker)),
                      (o.style[a.style] = t + "%"),
                      0 < s &&
                        (((o = O(n, !1)).className = c(s, a.cssClasses.value)),
                        o.setAttribute("data-value", r),
                        (o.style[a.style] = t + "%"),
                        (o.innerHTML = i.to(r)));
                  }
                })(r, t[r][0], t[r][1]);
              }),
              n
            );
          }
          function W() {
            m && (e(m), (m = null));
          }
          function G(t) {
            W();
            var e,
              i,
              n,
              a,
              r,
              s,
              o,
              l,
              u,
              c = t.mode,
              d = t.density || 1,
              h = t.filter || !1,
              p = (function (t, e, i) {
                if ("range" === t || "steps" === t) return C.xVal;
                if ("count" === t) {
                  if (e < 2)
                    throw new Error(
                      "noUiSlider (14.7.0): 'values' (>= 2) required for mode 'count'."
                    );
                  var n = e - 1,
                    a = 100 / n;
                  for (e = []; n--; ) e[n] = n * a;
                  e.push(100), (t = "positions");
                }
                return "positions" === t
                  ? e.map(function (t) {
                      return C.fromStepping(i ? C.getStep(t) : t);
                    })
                  : "values" === t
                  ? i
                    ? e.map(function (t) {
                        return C.fromStepping(C.getStep(C.toStepping(t)));
                      })
                    : e
                  : void 0;
              })(c, t.values || !1, t.stepped || !1),
              f =
                ((e = d),
                (i = c),
                (n = p),
                (a = {}),
                (r = C.xVal[0]),
                (s = C.xVal[C.xVal.length - 1]),
                (l = o = !1),
                (u = 0),
                (n = n
                  .slice()
                  .sort(function (t, e) {
                    return t - e;
                  })
                  .filter(function (t) {
                    return !this[t] && (this[t] = !0);
                  }, {}))[0] !== r && (n.unshift(r), (o = !0)),
                n[n.length - 1] !== s && (n.push(s), (l = !0)),
                n.forEach(function (t, r) {
                  var s,
                    c,
                    d,
                    h,
                    p,
                    f,
                    m,
                    g,
                    v,
                    _,
                    y = t,
                    b = n[r + 1],
                    w = "steps" === i;
                  if ((w && (s = C.xNumSteps[r]), s || (s = b - y), !1 !== y))
                    for (
                      void 0 === b && (b = y), s = Math.max(s, 1e-7), c = y;
                      c <= b;
                      c = (c + s).toFixed(7) / 1
                    ) {
                      for (
                        g = (p = (h = C.toStepping(c)) - u) / e,
                          _ = p / (v = Math.round(g)),
                          d = 1;
                        d <= v;
                        d += 1
                      )
                        a[(f = u + d * _).toFixed(5)] = [C.fromStepping(f), 0];
                      (m = -1 < n.indexOf(c) ? 1 : w ? 2 : 0),
                        !r && o && c !== b && (m = 0),
                        (c === b && l) || (a[h.toFixed(5)] = [c, m]),
                        (u = h);
                    }
                }),
                a),
              g = t.format || { to: Math.round };
            return (m = T.appendChild(V(f, h, g)));
          }
          function U() {
            var t = h.getBoundingClientRect(),
              e = "offset" + ["Width", "Height"][a.ort];
            return 0 === a.ort ? t.width || h[e] : t.height || h[e];
          }
          function Y(t, e, i, n) {
            var r = function (r) {
                return (
                  !!(r = (function (t, e, i) {
                    var n,
                      a,
                      r = 0 === t.type.indexOf("touch"),
                      s = 0 === t.type.indexOf("mouse"),
                      o = 0 === t.type.indexOf("pointer");
                    if (
                      (0 === t.type.indexOf("MSPointer") && (o = !0),
                      "mousedown" === t.type && !t.buttons && !t.touches)
                    )
                      return !1;
                    if (r) {
                      var l = function (t) {
                        return (
                          t.target === i ||
                          i.contains(t.target) ||
                          (t.target.shadowRoot &&
                            t.target.shadowRoot.contains(i))
                        );
                      };
                      if ("touchstart" === t.type) {
                        var u = Array.prototype.filter.call(t.touches, l);
                        if (1 < u.length) return !1;
                        (n = u[0].pageX), (a = u[0].pageY);
                      } else {
                        var c = Array.prototype.find.call(t.changedTouches, l);
                        if (!c) return !1;
                        (n = c.pageX), (a = c.pageY);
                      }
                    }
                    return (
                      (e = e || d(k)),
                      (s || o) &&
                        ((n = t.clientX + e.x), (a = t.clientY + e.y)),
                      (t.pageOffset = e),
                      (t.points = [n, a]),
                      (t.cursor = s || o),
                      t
                    );
                  })(r, n.pageOffset, n.target || e)) &&
                  !(F() && !n.doNotReject) &&
                  ((s = T),
                  (o = a.cssClasses.tap),
                  !(
                    (s.classList
                      ? s.classList.contains(o)
                      : new RegExp("\\b" + o + "\\b").test(s.className)) &&
                    !n.doNotReject
                  ) &&
                    !(t === x.start && void 0 !== r.buttons && 1 < r.buttons) &&
                    (!n.hover || !r.buttons) &&
                    (S || r.preventDefault(),
                    (r.calcPoint = r.points[a.ort]),
                    void i(r, n)))
                );
                var s, o;
              },
              s = [];
            return (
              t.split(" ").forEach(function (t) {
                e.addEventListener(t, r, !!S && { passive: !0 }),
                  s.push([t, r]);
              }),
              s
            );
          }
          function q(t) {
            var e,
              i,
              n,
              r,
              o,
              l,
              u =
                (100 *
                  (t -
                    ((e = h),
                    (i = a.ort),
                    (n = e.getBoundingClientRect()),
                    (o = (r = e.ownerDocument).documentElement),
                    (l = d(r)),
                    /webkit.*Chrome.*Mobile/i.test(navigator.userAgent) &&
                      (l.x = 0),
                    i
                      ? n.top + l.y - o.clientTop
                      : n.left + l.x - o.clientLeft))) /
                U();
            return (u = s(u)), a.dir ? 100 - u : u;
          }
          function X(t, e) {
            "mouseout" === t.type &&
              "HTML" === t.target.nodeName &&
              null === t.relatedTarget &&
              J(t, e);
          }
          function K(t, e) {
            if (
              -1 === navigator.appVersion.indexOf("MSIE 9") &&
              0 === t.buttons &&
              0 !== e.buttonsProperty
            )
              return J(t, e);
            var i = (a.dir ? -1 : 1) * (t.calcPoint - e.startCalcPoint);
            rt(0 < i, (100 * i) / e.baseSize, e.locations, e.handleNumbers);
          }
          function J(t, e) {
            e.handle && (c(e.handle, a.cssClasses.active), (D -= 1)),
              e.listeners.forEach(function (t) {
                A.removeEventListener(t[0], t[1]);
              }),
              0 === D &&
                (c(T, a.cssClasses.drag),
                ot(),
                t.cursor &&
                  ((I.style.cursor = ""),
                  I.removeEventListener("selectstart", n))),
              e.handleNumbers.forEach(function (t) {
                it("change", t), it("set", t), it("end", t);
              });
          }
          function Q(t, e) {
            if (e.handleNumbers.some(R)) return !1;
            var i;
            1 === e.handleNumbers.length &&
              ((i = p[e.handleNumbers[0]].children[0]),
              (D += 1),
              u(i, a.cssClasses.active)),
              t.stopPropagation();
            var r = [],
              s = Y(x.move, A, K, {
                target: t.target,
                handle: i,
                listeners: r,
                startCalcPoint: t.calcPoint,
                baseSize: U(),
                pageOffset: t.pageOffset,
                handleNumbers: e.handleNumbers,
                buttonsProperty: t.buttons,
                locations: P.slice(),
              }),
              o = Y(x.end, A, J, {
                target: t.target,
                handle: i,
                listeners: r,
                doNotReject: !0,
                handleNumbers: e.handleNumbers,
              }),
              l = Y("mouseout", A, X, {
                target: t.target,
                handle: i,
                listeners: r,
                doNotReject: !0,
                handleNumbers: e.handleNumbers,
              });
            r.push.apply(r, s.concat(o, l)),
              t.cursor &&
                ((I.style.cursor = getComputedStyle(t.target).cursor),
                1 < p.length && u(T, a.cssClasses.drag),
                I.addEventListener("selectstart", n, !1)),
              e.handleNumbers.forEach(function (t) {
                it("start", t);
              });
          }
          function tt(t, e) {
            (M[t] = M[t] || []),
              M[t].push(e),
              "update" === t.split(".")[0] &&
                p.forEach(function (t, e) {
                  it("update", e);
                });
          }
          function et(t) {
            var e = t && t.split(".")[0],
              i = e ? t.substring(e.length) : t;
            Object.keys(M).forEach(function (t) {
              var n,
                a = t.split(".")[0],
                r = t.substring(a.length);
              (e && e !== a) ||
                (i && i !== r) ||
                ((((n = r) !== w && n !== b) || i === r) && delete M[t]);
            });
          }
          function it(t, e, i) {
            Object.keys(M).forEach(function (n) {
              var r = n.split(".")[0];
              t === r &&
                M[n].forEach(function (t) {
                  t.call(
                    v,
                    E.map(a.format.to),
                    e,
                    E.slice(),
                    i || !1,
                    P.slice(),
                    v
                  );
                });
            });
          }
          function nt(t, e, i, n, r, o) {
            var l;
            return (
              1 < p.length &&
                !a.events.unconstrained &&
                (n &&
                  0 < e &&
                  ((l = C.getAbsoluteDistance(t[e - 1], a.margin, 0)),
                  (i = Math.max(i, l))),
                r &&
                  e < p.length - 1 &&
                  ((l = C.getAbsoluteDistance(t[e + 1], a.margin, 1)),
                  (i = Math.min(i, l)))),
              1 < p.length &&
                a.limit &&
                (n &&
                  0 < e &&
                  ((l = C.getAbsoluteDistance(t[e - 1], a.limit, 0)),
                  (i = Math.min(i, l))),
                r &&
                  e < p.length - 1 &&
                  ((l = C.getAbsoluteDistance(t[e + 1], a.limit, 1)),
                  (i = Math.max(i, l)))),
              a.padding &&
                (0 === e &&
                  ((l = C.getAbsoluteDistance(0, a.padding[0], 0)),
                  (i = Math.max(i, l))),
                e === p.length - 1 &&
                  ((l = C.getAbsoluteDistance(100, a.padding[1], 1)),
                  (i = Math.min(i, l)))),
              !((i = s((i = C.getStep(i)))) === t[e] && !o) && i
            );
          }
          function at(t, e) {
            var i = a.ort;
            return (i ? e : t) + ", " + (i ? t : e);
          }
          function rt(t, e, i, n) {
            var a = i.slice(),
              r = [!t, t],
              s = [t, !t];
            (n = n.slice()),
              t && n.reverse(),
              1 < n.length
                ? n.forEach(function (t, i) {
                    var n = nt(a, t, a[t] + e, r[i], s[i], !1);
                    !1 === n ? (e = 0) : ((e = n - a[t]), (a[t] = n));
                  })
                : (r = s = [!0]);
            var o = !1;
            n.forEach(function (t, n) {
              o = lt(t, i[t] + e, r[n], s[n]) || o;
            }),
              o &&
                n.forEach(function (t) {
                  it("update", t), it("slide", t);
                });
          }
          function st(t, e) {
            return a.dir ? 100 - t - e : t;
          }
          function ot() {
            L.forEach(function (t) {
              var e = 50 < P[t] ? -1 : 1,
                i = 3 + (p.length + e * t);
              p[t].style.zIndex = i;
            });
          }
          function lt(t, e, i, n, r) {
            return (
              r || (e = nt(P, t, e, i, n, !1)),
              !1 !== e &&
                ((function (t, e) {
                  (P[t] = e), (E[t] = C.fromStepping(e));
                  var i =
                    "translate(" + at(10 * (st(e, 0) - z) + "%", "0") + ")";
                  (p[t].style[a.transformRule] = i), ut(t), ut(t + 1);
                })(t, e),
                !0)
            );
          }
          function ut(t) {
            if (f[t]) {
              var e = 0,
                i = 100;
              0 !== t && (e = P[t - 1]), t !== f.length - 1 && (i = P[t]);
              var n = i - e,
                r = "translate(" + at(st(e, n) + "%", "0") + ")",
                s = "scale(" + at(n / 100, "1") + ")";
              f[t].style[a.transformRule] = r + " " + s;
            }
          }
          function ct(t, e) {
            return null === t || !1 === t || void 0 === t
              ? P[e]
              : ("number" == typeof t && (t = String(t)),
                (t = a.format.from(t)),
                !1 === (t = C.toStepping(t)) || isNaN(t) ? P[e] : t);
          }
          function dt(t, e, i) {
            var n = o(t),
              s = void 0 === P[0];
            (e = void 0 === e || !!e),
              a.animate && !s && r(T, a.cssClasses.tap, a.animationDuration),
              L.forEach(function (t) {
                lt(t, ct(n[t], t), !0, !1, i);
              });
            for (var l = 1 === L.length ? 0 : 1; l < L.length; ++l)
              L.forEach(function (t) {
                lt(t, P[t], !0, !0, i);
              });
            ot(),
              L.forEach(function (t) {
                it("update", t), null !== n[t] && e && it("set", t);
              });
          }
          function ht() {
            var t = E.map(a.format.to);
            return 1 === t.length ? t[0] : t;
          }
          function pt(t) {
            var e = P[t],
              i = C.getNearbySteps(e),
              n = E[t],
              r = i.thisStep.step,
              s = null;
            if (a.snap)
              return [
                n - i.stepBefore.startValue || null,
                i.stepAfter.startValue - n || null,
              ];
            !1 !== r &&
              n + r > i.stepAfter.startValue &&
              (r = i.stepAfter.startValue - n),
              (s =
                n > i.thisStep.startValue
                  ? i.thisStep.step
                  : !1 !== i.stepBefore.step && n - i.stepBefore.highestStep),
              100 === e ? (r = null) : 0 === e && (s = null);
            var o = C.countStepDecimals();
            return (
              null !== r && !1 !== r && (r = Number(r.toFixed(o))),
              null !== s && !1 !== s && (s = Number(s.toFixed(o))),
              [s, r]
            );
          }
          return (
            u((_ = T), a.cssClasses.target),
            0 === a.dir ? u(_, a.cssClasses.ltr) : u(_, a.cssClasses.rtl),
            0 === a.ort
              ? u(_, a.cssClasses.horizontal)
              : u(_, a.cssClasses.vertical),
            u(
              _,
              "rtl" === getComputedStyle(_).direction
                ? a.cssClasses.textDirectionRtl
                : a.cssClasses.textDirectionLtr
            ),
            (h = O(_, a.cssClasses.base)),
            (function (t, e) {
              var i = O(e, a.cssClasses.connects);
              (p = []), (f = []).push(B(i, t[0]));
              for (var n = 0; n < a.handles; n++)
                p.push(N(e, n)), (L[n] = n), f.push(B(i, t[n + 1]));
            })(a.connect, h),
            (y = a.events).fixed ||
              p.forEach(function (t, e) {
                Y(x.start, t.children[0], Q, { handleNumbers: [e] });
              }),
            y.tap &&
              Y(
                x.start,
                h,
                function (t) {
                  t.stopPropagation();
                  var e,
                    i,
                    n,
                    s = q(t.calcPoint),
                    o =
                      ((e = s),
                      (n = !(i = 100)),
                      p.forEach(function (t, a) {
                        if (!R(a)) {
                          var r = P[a],
                            s = Math.abs(r - e);
                          (s < i ||
                            (s <= i && r < e) ||
                            (100 === s && 100 === i)) &&
                            ((n = a), (i = s));
                        }
                      }),
                      n);
                  if (!1 === o) return !1;
                  a.events.snap || r(T, a.cssClasses.tap, a.animationDuration),
                    lt(o, s, !0, !0),
                    ot(),
                    it("slide", o, !0),
                    it("update", o, !0),
                    it("change", o, !0),
                    it("set", o, !0),
                    a.events.snap && Q(t, { handleNumbers: [o] });
                },
                {}
              ),
            y.hover &&
              Y(
                x.move,
                h,
                function (t) {
                  var e = q(t.calcPoint),
                    i = C.getStep(e),
                    n = C.fromStepping(i);
                  Object.keys(M).forEach(function (t) {
                    "hover" === t.split(".")[0] &&
                      M[t].forEach(function (t) {
                        t.call(v, n);
                      });
                  });
                },
                { hover: !0 }
              ),
            y.drag &&
              f.forEach(function (t, e) {
                if (!1 !== t && 0 !== e && e !== f.length - 1) {
                  var i = p[e - 1],
                    n = p[e],
                    r = [t];
                  u(t, a.cssClasses.draggable),
                    y.fixed && (r.push(i.children[0]), r.push(n.children[0])),
                    r.forEach(function (t) {
                      Y(x.start, t, Q, {
                        handles: [i, n],
                        handleNumbers: [e - 1, e],
                      });
                    });
                }
              }),
            dt(a.start),
            a.pips && G(a.pips),
            a.tooltips && H(),
            et("update" + w),
            tt("update" + w, function (t, e, i, n, r) {
              L.forEach(function (t) {
                var e = p[t],
                  n = nt(P, t, 0, !0, !0, !0),
                  s = nt(P, t, 100, !0, !0, !0),
                  o = r[t],
                  l = a.ariaFormat.to(i[t]);
                (n = C.fromStepping(n).toFixed(1)),
                  (s = C.fromStepping(s).toFixed(1)),
                  (o = C.fromStepping(o).toFixed(1)),
                  e.children[0].setAttribute("aria-valuemin", n),
                  e.children[0].setAttribute("aria-valuemax", s),
                  e.children[0].setAttribute("aria-valuenow", o),
                  e.children[0].setAttribute("aria-valuetext", l);
              });
            }),
            (v = {
              destroy: function () {
                for (var t in (et(w), et(b), a.cssClasses))
                  a.cssClasses.hasOwnProperty(t) && c(T, a.cssClasses[t]);
                for (; T.firstChild; ) T.removeChild(T.firstChild);
                delete T.noUiSlider;
              },
              steps: function () {
                return L.map(pt);
              },
              on: tt,
              off: et,
              get: ht,
              set: dt,
              setHandle: function (t, e, i, n) {
                if (!(0 <= (t = Number(t)) && t < L.length))
                  throw new Error(
                    "noUiSlider (14.7.0): invalid handle number, got: " + t
                  );
                lt(t, ct(e, t), !0, !0, n), it("update", t), i && it("set", t);
              },
              reset: function (t) {
                dt(a.start, t);
              },
              __moveHandles: function (t, e, i) {
                rt(t, e, P, i);
              },
              options: l,
              updateOptions: function (t, e) {
                var n = ht(),
                  r = [
                    "margin",
                    "limit",
                    "padding",
                    "range",
                    "animate",
                    "snap",
                    "step",
                    "format",
                    "pips",
                    "tooltips",
                  ];
                r.forEach(function (e) {
                  void 0 !== t[e] && (l[e] = t[e]);
                });
                var s = Z(l);
                r.forEach(function (e) {
                  void 0 !== t[e] && (a[e] = s[e]);
                }),
                  (C = s.spectrum),
                  (a.margin = s.margin),
                  (a.limit = s.limit),
                  (a.padding = s.padding),
                  a.pips ? G(a.pips) : W(),
                  a.tooltips ? H() : $(),
                  (P = []),
                  dt(i(t.start) ? t.start : n, e);
              },
              target: T,
              removePips: W,
              removeTooltips: $,
              getTooltips: function () {
                return g;
              },
              getOrigins: function () {
                return p;
              },
              pips: G,
            })
          );
        })(t, Z(a), a);
        return (t.noUiSlider = l);
      },
    };
  }),
  (function (t, e) {
    "object" == typeof exports && "object" == typeof module
      ? (module.exports = e())
      : "function" == typeof define && define.amd
      ? define([], e)
      : "object" == typeof exports
      ? (exports.counterUp = e())
      : (t.counterUp = e());
  })(window, function () {
    return (function (t) {
      var e = {};
      function i(n) {
        if (e[n]) return e[n].exports;
        var a = (e[n] = { i: n, l: !1, exports: {} });
        return t[n].call(a.exports, a, a.exports, i), (a.l = !0), a.exports;
      }
      return (
        (i.m = t),
        (i.c = e),
        (i.d = function (t, e, n) {
          i.o(t, e) || Object.defineProperty(t, e, { enumerable: !0, get: n });
        }),
        (i.r = function (t) {
          "undefined" != typeof Symbol &&
            Symbol.toStringTag &&
            Object.defineProperty(t, Symbol.toStringTag, { value: "Module" }),
            Object.defineProperty(t, "__esModule", { value: !0 });
        }),
        (i.t = function (t, e) {
          if ((1 & e && (t = i(t)), 8 & e)) return t;
          if (4 & e && "object" == typeof t && t && t.__esModule) return t;
          var n = Object.create(null);
          if (
            (i.r(n),
            Object.defineProperty(n, "default", { enumerable: !0, value: t }),
            2 & e && "string" != typeof t)
          )
            for (var a in t)
              i.d(
                n,
                a,
                function (e) {
                  return t[e];
                }.bind(null, a)
              );
          return n;
        }),
        (i.n = function (t) {
          var e =
            t && t.__esModule
              ? function () {
                  return t.default;
                }
              : function () {
                  return t;
                };
          return i.d(e, "a", e), e;
        }),
        (i.o = function (t, e) {
          return Object.prototype.hasOwnProperty.call(t, e);
        }),
        (i.p = ""),
        i((i.s = 0))
      );
    })([
      function (t, e, i) {
        "use strict";
        i.r(e),
          i.d(e, "divideNumbers", function () {
            return a;
          }),
          i.d(e, "hasComma", function () {
            return r;
          }),
          i.d(e, "isFloat", function () {
            return s;
          }),
          i.d(e, "decimalPlaces", function () {
            return o;
          }),
          (e.default = function (t) {
            var e =
                arguments.length > 1 && void 0 !== arguments[1]
                  ? arguments[1]
                  : {},
              i = e.action,
              r = void 0 === i ? "start" : i,
              s = e.duration,
              o = void 0 === s ? 1e3 : s,
              l = e.delay,
              u = void 0 === l ? 16 : l,
              c = e.lang,
              d = void 0 === c ? void 0 : c;
            if ("stop" !== r) {
              if ((n(t), /[0-9]/.test(t.innerHTML))) {
                var h = a(t.innerHTML, {
                  duration: o || t.getAttribute("data-duration"),
                  lang:
                    d ||
                    document.querySelector("html").getAttribute("lang") ||
                    void 0,
                  delay: u || t.getAttribute("data-delay"),
                });
                (t._countUpOrigInnerHTML = t.innerHTML),
                  (t.innerHTML = h[0]),
                  (t.style.visibility = "visible"),
                  (t.countUpTimeout = setTimeout(function e() {
                    (t.innerHTML = h.shift()),
                      h.length
                        ? (clearTimeout(t.countUpTimeout),
                          (t.countUpTimeout = setTimeout(e, u)))
                        : (t._countUpOrigInnerHTML = void 0);
                  }, u));
              }
            } else n(t);
          });
        var n = function (t) {
            clearTimeout(t.countUpTimeout),
              t._countUpOrigInnerHTML &&
                ((t.innerHTML = t._countUpOrigInnerHTML),
                (t._countUpOrigInnerHTML = void 0)),
              (t.style.visibility = "");
          },
          a = function (t) {
            for (
              var e =
                  arguments.length > 1 && void 0 !== arguments[1]
                    ? arguments[1]
                    : {},
                i = e.duration,
                n = void 0 === i ? 1e3 : i,
                a = e.delay,
                r = void 0 === a ? 16 : a,
                s = e.lang,
                o = void 0 === s ? void 0 : s,
                l = n / r,
                u = t.toString().split(/(<[^>]+>|[0-9.][,.0-9]*[0-9]*)/),
                c = [],
                d = 0;
              d < l;
              d++
            )
              c.push("");
            for (var h = 0; h < u.length; h++)
              if (
                /([0-9.][,.0-9]*[0-9]*)/.test(u[h]) &&
                !/<[^>]+>/.test(u[h])
              ) {
                var p = u[h],
                  f = /[0-9]+,[0-9]+/.test(p);
                p = p.replace(/,/g, "");
                for (
                  var m = /^[0-9]+\.[0-9]+$/.test(p),
                    g = m ? (p.split(".")[1] || []).length : 0,
                    v = c.length - 1,
                    _ = l;
                  _ >= 1;
                  _--
                ) {
                  var y = parseInt((p / l) * _, 10);
                  m &&
                    ((y = parseFloat((p / l) * _).toFixed(g)),
                    (y = parseFloat(y).toLocaleString(o))),
                    f && (y = y.toLocaleString(o)),
                    (c[v--] += y);
                }
              } else for (var b = 0; b < l; b++) c[b] += u[h];
            return (c[c.length] = t.toString()), c;
          },
          r = function (t) {
            return /[0-9]+,[0-9]+/.test(t);
          },
          s = function (t) {
            return /^[0-9]+\.[0-9]+$/.test(t);
          },
          o = function (t) {
            return s(t) ? (t.split(".")[1] || []).length : 0;
          };
      },
    ]);
  }),
  (function (t, e) {
    "object" == typeof exports && "undefined" != typeof module
      ? (module.exports = e())
      : "function" == typeof define && define.amd
      ? define(e)
      : ((t =
          "undefined" != typeof globalThis ? globalThis : t || self).Swiper =
          e());
  })(this, function () {
    "use strict";
    function t(t, e) {
      for (var i = 0; i < e.length; i++) {
        var n = e[i];
        (n.enumerable = n.enumerable || !1),
          (n.configurable = !0),
          "value" in n && (n.writable = !0),
          Object.defineProperty(t, n.key, n);
      }
    }
    function e() {
      return (e =
        Object.assign ||
        function (t) {
          for (var e = 1; e < arguments.length; e++) {
            var i = arguments[e];
            for (var n in i)
              Object.prototype.hasOwnProperty.call(i, n) && (t[n] = i[n]);
          }
          return t;
        }).apply(this, arguments);
    }
    function i(t) {
      return (
        null !== t &&
        "object" == typeof t &&
        "constructor" in t &&
        t.constructor === Object
      );
    }
    function n(t, e) {
      void 0 === t && (t = {}),
        void 0 === e && (e = {}),
        Object.keys(e).forEach(function (a) {
          void 0 === t[a]
            ? (t[a] = e[a])
            : i(e[a]) &&
              i(t[a]) &&
              Object.keys(e[a]).length > 0 &&
              n(t[a], e[a]);
        });
    }
    var a = {
      body: {},
      addEventListener: function () {},
      removeEventListener: function () {},
      activeElement: { blur: function () {}, nodeName: "" },
      querySelector: function () {
        return null;
      },
      querySelectorAll: function () {
        return [];
      },
      getElementById: function () {
        return null;
      },
      createEvent: function () {
        return { initEvent: function () {} };
      },
      createElement: function () {
        return {
          children: [],
          childNodes: [],
          style: {},
          setAttribute: function () {},
          getElementsByTagName: function () {
            return [];
          },
        };
      },
      createElementNS: function () {
        return {};
      },
      importNode: function () {
        return null;
      },
      location: {
        hash: "",
        host: "",
        hostname: "",
        href: "",
        origin: "",
        pathname: "",
        protocol: "",
        search: "",
      },
    };
    function r() {
      var t = "undefined" != typeof document ? document : {};
      return n(t, a), t;
    }
    var s = {
      document: a,
      navigator: { userAgent: "" },
      location: {
        hash: "",
        host: "",
        hostname: "",
        href: "",
        origin: "",
        pathname: "",
        protocol: "",
        search: "",
      },
      history: {
        replaceState: function () {},
        pushState: function () {},
        go: function () {},
        back: function () {},
      },
      CustomEvent: function () {
        return this;
      },
      addEventListener: function () {},
      removeEventListener: function () {},
      getComputedStyle: function () {
        return {
          getPropertyValue: function () {
            return "";
          },
        };
      },
      Image: function () {},
      Date: function () {},
      screen: {},
      setTimeout: function () {},
      clearTimeout: function () {},
      matchMedia: function () {
        return {};
      },
      requestAnimationFrame: function (t) {
        return "undefined" == typeof setTimeout
          ? (t(), null)
          : setTimeout(t, 0);
      },
      cancelAnimationFrame: function (t) {
        "undefined" != typeof setTimeout && clearTimeout(t);
      },
    };
    function o() {
      var t = "undefined" != typeof window ? window : {};
      return n(t, s), t;
    }
    function l(t) {
      return (l = Object.setPrototypeOf
        ? Object.getPrototypeOf
        : function (t) {
            return t.__proto__ || Object.getPrototypeOf(t);
          })(t);
    }
    function u(t, e) {
      return (u =
        Object.setPrototypeOf ||
        function (t, e) {
          return (t.__proto__ = e), t;
        })(t, e);
    }
    function c() {
      if ("undefined" == typeof Reflect || !Reflect.construct) return !1;
      if (Reflect.construct.sham) return !1;
      if ("function" == typeof Proxy) return !0;
      try {
        return (
          Date.prototype.toString.call(
            Reflect.construct(Date, [], function () {})
          ),
          !0
        );
      } catch (t) {
        return !1;
      }
    }
    function d(t, e, i) {
      return (d = c()
        ? Reflect.construct
        : function (t, e, i) {
            var n = [null];
            n.push.apply(n, e);
            var a = new (Function.bind.apply(t, n))();
            return i && u(a, i.prototype), a;
          }).apply(null, arguments);
    }
    function h(t) {
      var e = "function" == typeof Map ? new Map() : void 0;
      return (h = function (t) {
        if (
          null === t ||
          ((i = t), -1 === Function.toString.call(i).indexOf("[native code]"))
        )
          return t;
        var i;
        if ("function" != typeof t)
          throw new TypeError(
            "Super expression must either be null or a function"
          );
        if (void 0 !== e) {
          if (e.has(t)) return e.get(t);
          e.set(t, n);
        }
        function n() {
          return d(t, arguments, l(this).constructor);
        }
        return (
          (n.prototype = Object.create(t.prototype, {
            constructor: {
              value: n,
              enumerable: !1,
              writable: !0,
              configurable: !0,
            },
          })),
          u(n, t)
        );
      })(t);
    }
    var p = (function (t) {
      var e, i;
      function n(e) {
        var i, n, a;
        return (
          (n = (function (t) {
            if (void 0 === t)
              throw new ReferenceError(
                "this hasn't been initialised - super() hasn't been called"
              );
            return t;
          })((i = t.call.apply(t, [this].concat(e)) || this))),
          (a = n.__proto__),
          Object.defineProperty(n, "__proto__", {
            get: function () {
              return a;
            },
            set: function (t) {
              a.__proto__ = t;
            },
          }),
          i
        );
      }
      return (
        (i = t),
        ((e = n).prototype = Object.create(i.prototype)),
        (e.prototype.constructor = e),
        (e.__proto__ = i),
        n
      );
    })(h(Array));
    function f(t) {
      void 0 === t && (t = []);
      var e = [];
      return (
        t.forEach(function (t) {
          Array.isArray(t) ? e.push.apply(e, f(t)) : e.push(t);
        }),
        e
      );
    }
    function m(t, e) {
      return Array.prototype.filter.call(t, e);
    }
    function g(t, e) {
      var i = o(),
        n = r(),
        a = [];
      if (!e && t instanceof p) return t;
      if (!t) return new p(a);
      if ("string" == typeof t) {
        var s = t.trim();
        if (s.indexOf("<") >= 0 && s.indexOf(">") >= 0) {
          var l = "div";
          0 === s.indexOf("<li") && (l = "ul"),
            0 === s.indexOf("<tr") && (l = "tbody"),
            (0 !== s.indexOf("<td") && 0 !== s.indexOf("<th")) || (l = "tr"),
            0 === s.indexOf("<tbody") && (l = "table"),
            0 === s.indexOf("<option") && (l = "select");
          var u = n.createElement(l);
          u.innerHTML = s;
          for (var c = 0; c < u.childNodes.length; c += 1)
            a.push(u.childNodes[c]);
        } else
          a = (function (t, e) {
            if ("string" != typeof t) return [t];
            for (
              var i = [], n = e.querySelectorAll(t), a = 0;
              a < n.length;
              a += 1
            )
              i.push(n[a]);
            return i;
          })(t.trim(), e || n);
      } else if (t.nodeType || t === i || t === n) a.push(t);
      else if (Array.isArray(t)) {
        if (t instanceof p) return t;
        a = t;
      }
      return new p(
        (function (t) {
          for (var e = [], i = 0; i < t.length; i += 1)
            -1 === e.indexOf(t[i]) && e.push(t[i]);
          return e;
        })(a)
      );
    }
    g.fn = p.prototype;
    var v,
      _,
      y,
      b = {
        addClass: function () {
          for (var t = arguments.length, e = new Array(t), i = 0; i < t; i++)
            e[i] = arguments[i];
          var n = f(
            e.map(function (t) {
              return t.split(" ");
            })
          );
          return (
            this.forEach(function (t) {
              var e;
              (e = t.classList).add.apply(e, n);
            }),
            this
          );
        },
        removeClass: function () {
          for (var t = arguments.length, e = new Array(t), i = 0; i < t; i++)
            e[i] = arguments[i];
          var n = f(
            e.map(function (t) {
              return t.split(" ");
            })
          );
          return (
            this.forEach(function (t) {
              var e;
              (e = t.classList).remove.apply(e, n);
            }),
            this
          );
        },
        hasClass: function () {
          for (var t = arguments.length, e = new Array(t), i = 0; i < t; i++)
            e[i] = arguments[i];
          var n = f(
            e.map(function (t) {
              return t.split(" ");
            })
          );
          return (
            m(this, function (t) {
              return (
                n.filter(function (e) {
                  return t.classList.contains(e);
                }).length > 0
              );
            }).length > 0
          );
        },
        toggleClass: function () {
          for (var t = arguments.length, e = new Array(t), i = 0; i < t; i++)
            e[i] = arguments[i];
          var n = f(
            e.map(function (t) {
              return t.split(" ");
            })
          );
          this.forEach(function (t) {
            n.forEach(function (e) {
              t.classList.toggle(e);
            });
          });
        },
        attr: function (t, e) {
          if (1 === arguments.length && "string" == typeof t)
            return this[0] ? this[0].getAttribute(t) : void 0;
          for (var i = 0; i < this.length; i += 1)
            if (2 === arguments.length) this[i].setAttribute(t, e);
            else
              for (var n in t)
                (this[i][n] = t[n]), this[i].setAttribute(n, t[n]);
          return this;
        },
        removeAttr: function (t) {
          for (var e = 0; e < this.length; e += 1) this[e].removeAttribute(t);
          return this;
        },
        transform: function (t) {
          for (var e = 0; e < this.length; e += 1) this[e].style.transform = t;
          return this;
        },
        transition: function (t) {
          for (var e = 0; e < this.length; e += 1)
            this[e].style.transitionDuration =
              "string" != typeof t ? t + "ms" : t;
          return this;
        },
        on: function () {
          for (var t = arguments.length, e = new Array(t), i = 0; i < t; i++)
            e[i] = arguments[i];
          var n = e[0],
            a = e[1],
            r = e[2],
            s = e[3];
          function o(t) {
            var e = t.target;
            if (e) {
              var i = t.target.dom7EventData || [];
              if ((i.indexOf(t) < 0 && i.unshift(t), g(e).is(a))) r.apply(e, i);
              else
                for (var n = g(e).parents(), s = 0; s < n.length; s += 1)
                  g(n[s]).is(a) && r.apply(n[s], i);
            }
          }
          function l(t) {
            var e = (t && t.target && t.target.dom7EventData) || [];
            e.indexOf(t) < 0 && e.unshift(t), r.apply(this, e);
          }
          "function" == typeof e[1] &&
            ((n = e[0]), (r = e[1]), (s = e[2]), (a = void 0)),
            s || (s = !1);
          for (var u, c = n.split(" "), d = 0; d < this.length; d += 1) {
            var h = this[d];
            if (a)
              for (u = 0; u < c.length; u += 1) {
                var p = c[u];
                h.dom7LiveListeners || (h.dom7LiveListeners = {}),
                  h.dom7LiveListeners[p] || (h.dom7LiveListeners[p] = []),
                  h.dom7LiveListeners[p].push({
                    listener: r,
                    proxyListener: o,
                  }),
                  h.addEventListener(p, o, s);
              }
            else
              for (u = 0; u < c.length; u += 1) {
                var f = c[u];
                h.dom7Listeners || (h.dom7Listeners = {}),
                  h.dom7Listeners[f] || (h.dom7Listeners[f] = []),
                  h.dom7Listeners[f].push({ listener: r, proxyListener: l }),
                  h.addEventListener(f, l, s);
              }
          }
          return this;
        },
        off: function () {
          for (var t = arguments.length, e = new Array(t), i = 0; i < t; i++)
            e[i] = arguments[i];
          var n = e[0],
            a = e[1],
            r = e[2],
            s = e[3];
          "function" == typeof e[1] &&
            ((n = e[0]), (r = e[1]), (s = e[2]), (a = void 0)),
            s || (s = !1);
          for (var o = n.split(" "), l = 0; l < o.length; l += 1)
            for (var u = o[l], c = 0; c < this.length; c += 1) {
              var d = this[c],
                h = void 0;
              if (
                (!a && d.dom7Listeners
                  ? (h = d.dom7Listeners[u])
                  : a && d.dom7LiveListeners && (h = d.dom7LiveListeners[u]),
                h && h.length)
              )
                for (var p = h.length - 1; p >= 0; p -= 1) {
                  var f = h[p];
                  (r && f.listener === r) ||
                  (r &&
                    f.listener &&
                    f.listener.dom7proxy &&
                    f.listener.dom7proxy === r)
                    ? (d.removeEventListener(u, f.proxyListener, s),
                      h.splice(p, 1))
                    : r ||
                      (d.removeEventListener(u, f.proxyListener, s),
                      h.splice(p, 1));
                }
            }
          return this;
        },
        trigger: function () {
          for (
            var t = o(), e = arguments.length, i = new Array(e), n = 0;
            n < e;
            n++
          )
            i[n] = arguments[n];
          for (var a = i[0].split(" "), r = i[1], s = 0; s < a.length; s += 1)
            for (var l = a[s], u = 0; u < this.length; u += 1) {
              var c = this[u];
              if (t.CustomEvent) {
                var d = new t.CustomEvent(l, {
                  detail: r,
                  bubbles: !0,
                  cancelable: !0,
                });
                (c.dom7EventData = i.filter(function (t, e) {
                  return e > 0;
                })),
                  c.dispatchEvent(d),
                  (c.dom7EventData = []),
                  delete c.dom7EventData;
              }
            }
          return this;
        },
        transitionEnd: function (t) {
          var e = this;
          return (
            t &&
              e.on("transitionend", function i(n) {
                n.target === this &&
                  (t.call(this, n), e.off("transitionend", i));
              }),
            this
          );
        },
        outerWidth: function (t) {
          if (this.length > 0) {
            if (t) {
              var e = this.styles();
              return (
                this[0].offsetWidth +
                parseFloat(e.getPropertyValue("margin-right")) +
                parseFloat(e.getPropertyValue("margin-left"))
              );
            }
            return this[0].offsetWidth;
          }
          return null;
        },
        outerHeight: function (t) {
          if (this.length > 0) {
            if (t) {
              var e = this.styles();
              return (
                this[0].offsetHeight +
                parseFloat(e.getPropertyValue("margin-top")) +
                parseFloat(e.getPropertyValue("margin-bottom"))
              );
            }
            return this[0].offsetHeight;
          }
          return null;
        },
        styles: function () {
          var t = o();
          return this[0] ? t.getComputedStyle(this[0], null) : {};
        },
        offset: function () {
          if (this.length > 0) {
            var t = o(),
              e = r(),
              i = this[0],
              n = i.getBoundingClientRect(),
              a = e.body,
              s = i.clientTop || a.clientTop || 0,
              l = i.clientLeft || a.clientLeft || 0,
              u = i === t ? t.scrollY : i.scrollTop,
              c = i === t ? t.scrollX : i.scrollLeft;
            return { top: n.top + u - s, left: n.left + c - l };
          }
          return null;
        },
        css: function (t, e) {
          var i,
            n = o();
          if (1 === arguments.length) {
            if ("string" != typeof t) {
              for (i = 0; i < this.length; i += 1)
                for (var a in t) this[i].style[a] = t[a];
              return this;
            }
            if (this[0])
              return n.getComputedStyle(this[0], null).getPropertyValue(t);
          }
          if (2 === arguments.length && "string" == typeof t) {
            for (i = 0; i < this.length; i += 1) this[i].style[t] = e;
            return this;
          }
          return this;
        },
        each: function (t) {
          return t
            ? (this.forEach(function (e, i) {
                t.apply(e, [e, i]);
              }),
              this)
            : this;
        },
        html: function (t) {
          if (void 0 === t) return this[0] ? this[0].innerHTML : null;
          for (var e = 0; e < this.length; e += 1) this[e].innerHTML = t;
          return this;
        },
        text: function (t) {
          if (void 0 === t) return this[0] ? this[0].textContent.trim() : null;
          for (var e = 0; e < this.length; e += 1) this[e].textContent = t;
          return this;
        },
        is: function (t) {
          var e,
            i,
            n = o(),
            a = r(),
            s = this[0];
          if (!s || void 0 === t) return !1;
          if ("string" == typeof t) {
            if (s.matches) return s.matches(t);
            if (s.webkitMatchesSelector) return s.webkitMatchesSelector(t);
            if (s.msMatchesSelector) return s.msMatchesSelector(t);
            for (e = g(t), i = 0; i < e.length; i += 1)
              if (e[i] === s) return !0;
            return !1;
          }
          if (t === a) return s === a;
          if (t === n) return s === n;
          if (t.nodeType || t instanceof p) {
            for (e = t.nodeType ? [t] : t, i = 0; i < e.length; i += 1)
              if (e[i] === s) return !0;
            return !1;
          }
          return !1;
        },
        index: function () {
          var t,
            e = this[0];
          if (e) {
            for (t = 0; null !== (e = e.previousSibling); )
              1 === e.nodeType && (t += 1);
            return t;
          }
        },
        eq: function (t) {
          if (void 0 === t) return this;
          var e = this.length;
          if (t > e - 1) return g([]);
          if (t < 0) {
            var i = e + t;
            return g(i < 0 ? [] : [this[i]]);
          }
          return g([this[t]]);
        },
        append: function () {
          for (var t, e = r(), i = 0; i < arguments.length; i += 1) {
            t = i < 0 || arguments.length <= i ? void 0 : arguments[i];
            for (var n = 0; n < this.length; n += 1)
              if ("string" == typeof t) {
                var a = e.createElement("div");
                for (a.innerHTML = t; a.firstChild; )
                  this[n].appendChild(a.firstChild);
              } else if (t instanceof p)
                for (var s = 0; s < t.length; s += 1) this[n].appendChild(t[s]);
              else this[n].appendChild(t);
          }
          return this;
        },
        prepend: function (t) {
          var e,
            i,
            n = r();
          for (e = 0; e < this.length; e += 1)
            if ("string" == typeof t) {
              var a = n.createElement("div");
              for (a.innerHTML = t, i = a.childNodes.length - 1; i >= 0; i -= 1)
                this[e].insertBefore(a.childNodes[i], this[e].childNodes[0]);
            } else if (t instanceof p)
              for (i = 0; i < t.length; i += 1)
                this[e].insertBefore(t[i], this[e].childNodes[0]);
            else this[e].insertBefore(t, this[e].childNodes[0]);
          return this;
        },
        next: function (t) {
          return this.length > 0
            ? t
              ? this[0].nextElementSibling &&
                g(this[0].nextElementSibling).is(t)
                ? g([this[0].nextElementSibling])
                : g([])
              : this[0].nextElementSibling
              ? g([this[0].nextElementSibling])
              : g([])
            : g([]);
        },
        nextAll: function (t) {
          var e = [],
            i = this[0];
          if (!i) return g([]);
          for (; i.nextElementSibling; ) {
            var n = i.nextElementSibling;
            t ? g(n).is(t) && e.push(n) : e.push(n), (i = n);
          }
          return g(e);
        },
        prev: function (t) {
          if (this.length > 0) {
            var e = this[0];
            return t
              ? e.previousElementSibling && g(e.previousElementSibling).is(t)
                ? g([e.previousElementSibling])
                : g([])
              : e.previousElementSibling
              ? g([e.previousElementSibling])
              : g([]);
          }
          return g([]);
        },
        prevAll: function (t) {
          var e = [],
            i = this[0];
          if (!i) return g([]);
          for (; i.previousElementSibling; ) {
            var n = i.previousElementSibling;
            t ? g(n).is(t) && e.push(n) : e.push(n), (i = n);
          }
          return g(e);
        },
        parent: function (t) {
          for (var e = [], i = 0; i < this.length; i += 1)
            null !== this[i].parentNode &&
              (t
                ? g(this[i].parentNode).is(t) && e.push(this[i].parentNode)
                : e.push(this[i].parentNode));
          return g(e);
        },
        parents: function (t) {
          for (var e = [], i = 0; i < this.length; i += 1)
            for (var n = this[i].parentNode; n; )
              t ? g(n).is(t) && e.push(n) : e.push(n), (n = n.parentNode);
          return g(e);
        },
        closest: function (t) {
          var e = this;
          return void 0 === t
            ? g([])
            : (e.is(t) || (e = e.parents(t).eq(0)), e);
        },
        find: function (t) {
          for (var e = [], i = 0; i < this.length; i += 1)
            for (
              var n = this[i].querySelectorAll(t), a = 0;
              a < n.length;
              a += 1
            )
              e.push(n[a]);
          return g(e);
        },
        children: function (t) {
          for (var e = [], i = 0; i < this.length; i += 1)
            for (var n = this[i].children, a = 0; a < n.length; a += 1)
              (t && !g(n[a]).is(t)) || e.push(n[a]);
          return g(e);
        },
        filter: function (t) {
          return g(m(this, t));
        },
        remove: function () {
          for (var t = 0; t < this.length; t += 1)
            this[t].parentNode && this[t].parentNode.removeChild(this[t]);
          return this;
        },
      };
    function w(t, e) {
      return void 0 === e && (e = 0), setTimeout(t, e);
    }
    function x() {
      return Date.now();
    }
    function S(t, e) {
      void 0 === e && (e = "x");
      var i,
        n,
        a,
        r = o(),
        s = (function (t) {
          var e,
            i = o();
          return (
            i.getComputedStyle && (e = i.getComputedStyle(t, null)),
            !e && t.currentStyle && (e = t.currentStyle),
            e || (e = t.style),
            e
          );
        })(t);
      return (
        r.WebKitCSSMatrix
          ? ((n = s.transform || s.webkitTransform).split(",").length > 6 &&
              (n = n
                .split(", ")
                .map(function (t) {
                  return t.replace(",", ".");
                })
                .join(", ")),
            (a = new r.WebKitCSSMatrix("none" === n ? "" : n)))
          : (i = (a =
              s.MozTransform ||
              s.OTransform ||
              s.MsTransform ||
              s.msTransform ||
              s.transform ||
              s
                .getPropertyValue("transform")
                .replace("translate(", "matrix(1, 0, 0, 1,"))
              .toString()
              .split(",")),
        "x" === e &&
          (n = r.WebKitCSSMatrix
            ? a.m41
            : 16 === i.length
            ? parseFloat(i[12])
            : parseFloat(i[4])),
        "y" === e &&
          (n = r.WebKitCSSMatrix
            ? a.m42
            : 16 === i.length
            ? parseFloat(i[13])
            : parseFloat(i[5])),
        n || 0
      );
    }
    function T(t) {
      return (
        "object" == typeof t &&
        null !== t &&
        t.constructor &&
        "Object" === Object.prototype.toString.call(t).slice(8, -1)
      );
    }
    function C(t) {
      return "undefined" != typeof window && void 0 !== window.HTMLElement
        ? t instanceof HTMLElement
        : t && (1 === t.nodeType || 11 === t.nodeType);
    }
    function E() {
      for (
        var t = Object(arguments.length <= 0 ? void 0 : arguments[0]),
          e = ["__proto__", "constructor", "prototype"],
          i = 1;
        i < arguments.length;
        i += 1
      ) {
        var n = i < 0 || arguments.length <= i ? void 0 : arguments[i];
        if (null != n && !C(n))
          for (
            var a = Object.keys(Object(n)).filter(function (t) {
                return e.indexOf(t) < 0;
              }),
              r = 0,
              s = a.length;
            r < s;
            r += 1
          ) {
            var o = a[r],
              l = Object.getOwnPropertyDescriptor(n, o);
            void 0 !== l &&
              l.enumerable &&
              (T(t[o]) && T(n[o])
                ? n[o].__swiper__
                  ? (t[o] = n[o])
                  : E(t[o], n[o])
                : !T(t[o]) && T(n[o])
                ? ((t[o] = {}), n[o].__swiper__ ? (t[o] = n[o]) : E(t[o], n[o]))
                : (t[o] = n[o]));
          }
      }
      return t;
    }
    function P(t, e) {
      Object.keys(e).forEach(function (i) {
        T(e[i]) &&
          Object.keys(e[i]).forEach(function (n) {
            "function" == typeof e[i][n] && (e[i][n] = e[i][n].bind(t));
          }),
          (t[i] = e[i]);
      });
    }
    function L(t) {
      return (
        void 0 === t && (t = ""),
        "." +
          t
            .trim()
            .replace(/([\.:!\/])/g, "\\$1")
            .replace(/ /g, ".")
      );
    }
    function D(t, e, i, n) {
      var a = r();
      return (
        i &&
          Object.keys(n).forEach(function (i) {
            if (!e[i] && !0 === e.auto) {
              var r = a.createElement("div");
              (r.className = n[i]), t.append(r), (e[i] = r);
            }
          }),
        e
      );
    }
    function M() {
      return (
        v ||
          (v = (function () {
            var t = o(),
              e = r();
            return {
              touch: !!(
                "ontouchstart" in t ||
                (t.DocumentTouch && e instanceof t.DocumentTouch)
              ),
              pointerEvents:
                !!t.PointerEvent &&
                "maxTouchPoints" in t.navigator &&
                t.navigator.maxTouchPoints >= 0,
              observer:
                "MutationObserver" in t || "WebkitMutationObserver" in t,
              passiveListener: (function () {
                var e = !1;
                try {
                  var i = Object.defineProperty({}, "passive", {
                    get: function () {
                      e = !0;
                    },
                  });
                  t.addEventListener("testPassiveListener", null, i);
                } catch (t) {}
                return e;
              })(),
              gestures: "ongesturestart" in t,
            };
          })()),
        v
      );
    }
    function k(t) {
      return (
        void 0 === t && (t = {}),
        _ ||
          (_ = (function (t) {
            var e = (void 0 === t ? {} : t).userAgent,
              i = M(),
              n = o(),
              a = n.navigator.platform,
              r = e || n.navigator.userAgent,
              s = { ios: !1, android: !1 },
              l = n.screen.width,
              u = n.screen.height,
              c = r.match(/(Android);?[\s\/]+([\d.]+)?/),
              d = r.match(/(iPad).*OS\s([\d_]+)/),
              h = r.match(/(iPod)(.*OS\s([\d_]+))?/),
              p = !d && r.match(/(iPhone\sOS|iOS)\s([\d_]+)/),
              f = "Win32" === a,
              m = "MacIntel" === a;
            return (
              !d &&
                m &&
                i.touch &&
                [
                  "1024x1366",
                  "1366x1024",
                  "834x1194",
                  "1194x834",
                  "834x1112",
                  "1112x834",
                  "768x1024",
                  "1024x768",
                  "820x1180",
                  "1180x820",
                  "810x1080",
                  "1080x810",
                ].indexOf(l + "x" + u) >= 0 &&
                ((d = r.match(/(Version)\/([\d.]+)/)) || (d = [0, 1, "13_0_0"]),
                (m = !1)),
              c && !f && ((s.os = "android"), (s.android = !0)),
              (d || p || h) && ((s.os = "ios"), (s.ios = !0)),
              s
            );
          })(t)),
        _
      );
    }
    function A() {
      return (
        y ||
          (y = (function () {
            var t,
              e = o();
            return {
              isEdge: !!e.navigator.userAgent.match(/Edge/g),
              isSafari:
                ((t = e.navigator.userAgent.toLowerCase()),
                t.indexOf("safari") >= 0 &&
                  t.indexOf("chrome") < 0 &&
                  t.indexOf("android") < 0),
              isWebView: /(iPhone|iPod|iPad).*AppleWebKit(?!.*Safari)/i.test(
                e.navigator.userAgent
              ),
            };
          })()),
        y
      );
    }
    Object.keys(b).forEach(function (t) {
      Object.defineProperty(g.fn, t, { value: b[t], writable: !0 });
    });
    var I = {
        name: "resize",
        create: function () {
          var t = this;
          E(t, {
            resize: {
              observer: null,
              createObserver: function () {
                t &&
                  !t.destroyed &&
                  t.initialized &&
                  ((t.resize.observer = new ResizeObserver(function (e) {
                    var i = t.width,
                      n = t.height,
                      a = i,
                      r = n;
                    e.forEach(function (e) {
                      var i = e.contentBoxSize,
                        n = e.contentRect,
                        s = e.target;
                      (s && s !== t.el) ||
                        ((a = n ? n.width : (i[0] || i).inlineSize),
                        (r = n ? n.height : (i[0] || i).blockSize));
                    }),
                      (a === i && r === n) || t.resize.resizeHandler();
                  })),
                  t.resize.observer.observe(t.el));
              },
              removeObserver: function () {
                t.resize.observer &&
                  t.resize.observer.unobserve &&
                  t.el &&
                  (t.resize.observer.unobserve(t.el),
                  (t.resize.observer = null));
              },
              resizeHandler: function () {
                t &&
                  !t.destroyed &&
                  t.initialized &&
                  (t.emit("beforeResize"), t.emit("resize"));
              },
              orientationChangeHandler: function () {
                t &&
                  !t.destroyed &&
                  t.initialized &&
                  t.emit("orientationchange");
              },
            },
          });
        },
        on: {
          init: function (t) {
            var e = o();
            t.params.resizeObserver && void 0 !== o().ResizeObserver
              ? t.resize.createObserver()
              : (e.addEventListener("resize", t.resize.resizeHandler),
                e.addEventListener(
                  "orientationchange",
                  t.resize.orientationChangeHandler
                ));
          },
          destroy: function (t) {
            var e = o();
            t.resize.removeObserver(),
              e.removeEventListener("resize", t.resize.resizeHandler),
              e.removeEventListener(
                "orientationchange",
                t.resize.orientationChangeHandler
              );
          },
        },
      },
      z = {
        attach: function (t, e) {
          void 0 === e && (e = {});
          var i = o(),
            n = this,
            a = new (i.MutationObserver || i.WebkitMutationObserver)(function (
              t
            ) {
              if (1 !== t.length) {
                var e = function () {
                  n.emit("observerUpdate", t[0]);
                };
                i.requestAnimationFrame
                  ? i.requestAnimationFrame(e)
                  : i.setTimeout(e, 0);
              } else n.emit("observerUpdate", t[0]);
            });
          a.observe(t, {
            attributes: void 0 === e.attributes || e.attributes,
            childList: void 0 === e.childList || e.childList,
            characterData: void 0 === e.characterData || e.characterData,
          }),
            n.observer.observers.push(a);
        },
        init: function () {
          var t = this;
          if (t.support.observer && t.params.observer) {
            if (t.params.observeParents)
              for (var e = t.$el.parents(), i = 0; i < e.length; i += 1)
                t.observer.attach(e[i]);
            t.observer.attach(t.$el[0], {
              childList: t.params.observeSlideChildren,
            }),
              t.observer.attach(t.$wrapperEl[0], { attributes: !1 });
          }
        },
        destroy: function () {
          this.observer.observers.forEach(function (t) {
            t.disconnect();
          }),
            (this.observer.observers = []);
        },
      },
      O = {
        name: "observer",
        params: { observer: !1, observeParents: !1, observeSlideChildren: !1 },
        create: function () {
          P(this, { observer: e({}, z, { observers: [] }) });
        },
        on: {
          init: function (t) {
            t.observer.init();
          },
          destroy: function (t) {
            t.observer.destroy();
          },
        },
      };
    function N(t) {
      var e = this,
        i = r(),
        n = o(),
        a = e.touchEventsData,
        s = e.params,
        l = e.touches;
      if (e.enabled && (!e.animating || !s.preventInteractionOnTransition)) {
        var u = t;
        u.originalEvent && (u = u.originalEvent);
        var c = g(u.target);
        if (
          ("wrapper" !== s.touchEventsTarget ||
            c.closest(e.wrapperEl).length) &&
          ((a.isTouchEvent = "touchstart" === u.type),
          (a.isTouchEvent || !("which" in u) || 3 !== u.which) &&
            !(
              (!a.isTouchEvent && "button" in u && u.button > 0) ||
              (a.isTouched && a.isMoved)
            ))
        ) {
          s.noSwipingClass &&
            "" !== s.noSwipingClass &&
            u.target &&
            u.target.shadowRoot &&
            t.path &&
            t.path[0] &&
            (c = g(t.path[0]));
          var d = s.noSwipingSelector
              ? s.noSwipingSelector
              : "." + s.noSwipingClass,
            h = !(!u.target || !u.target.shadowRoot);
          if (
            s.noSwiping &&
            (h
              ? (function (t, e) {
                  return (
                    void 0 === e && (e = this),
                    (function e(i) {
                      return i && i !== r() && i !== o()
                        ? (i.assignedSlot && (i = i.assignedSlot),
                          i.closest(t) || e(i.getRootNode().host))
                        : null;
                    })(e)
                  );
                })(d, u.target)
              : c.closest(d)[0])
          )
            e.allowClick = !0;
          else if (!s.swipeHandler || c.closest(s.swipeHandler)[0]) {
            (l.currentX =
              "touchstart" === u.type ? u.targetTouches[0].pageX : u.pageX),
              (l.currentY =
                "touchstart" === u.type ? u.targetTouches[0].pageY : u.pageY);
            var p = l.currentX,
              f = l.currentY,
              m = s.edgeSwipeDetection || s.iOSEdgeSwipeDetection,
              v = s.edgeSwipeThreshold || s.iOSEdgeSwipeThreshold;
            if (m && (p <= v || p >= n.innerWidth - v)) {
              if ("prevent" !== m) return;
              t.preventDefault();
            }
            if (
              (E(a, {
                isTouched: !0,
                isMoved: !1,
                allowTouchCallbacks: !0,
                isScrolling: void 0,
                startMoving: void 0,
              }),
              (l.startX = p),
              (l.startY = f),
              (a.touchStartTime = x()),
              (e.allowClick = !0),
              e.updateSize(),
              (e.swipeDirection = void 0),
              s.threshold > 0 && (a.allowThresholdMove = !1),
              "touchstart" !== u.type)
            ) {
              var _ = !0;
              c.is(a.focusableElements) && (_ = !1),
                i.activeElement &&
                  g(i.activeElement).is(a.focusableElements) &&
                  i.activeElement !== c[0] &&
                  i.activeElement.blur();
              var y = _ && e.allowTouchMove && s.touchStartPreventDefault;
              (!s.touchStartForcePreventDefault && !y) ||
                c[0].isContentEditable ||
                u.preventDefault();
            }
            e.emit("touchStart", u);
          }
        }
      }
    }
    function B(t) {
      var e = r(),
        i = this,
        n = i.touchEventsData,
        a = i.params,
        s = i.touches,
        o = i.rtlTranslate;
      if (i.enabled) {
        var l = t;
        if ((l.originalEvent && (l = l.originalEvent), n.isTouched)) {
          if (!n.isTouchEvent || "touchmove" === l.type) {
            var u =
                "touchmove" === l.type &&
                l.targetTouches &&
                (l.targetTouches[0] || l.changedTouches[0]),
              c = "touchmove" === l.type ? u.pageX : l.pageX,
              d = "touchmove" === l.type ? u.pageY : l.pageY;
            if (l.preventedByNestedSwiper)
              return (s.startX = c), void (s.startY = d);
            if (!i.allowTouchMove)
              return (
                (i.allowClick = !1),
                void (
                  n.isTouched &&
                  (E(s, { startX: c, startY: d, currentX: c, currentY: d }),
                  (n.touchStartTime = x()))
                )
              );
            if (n.isTouchEvent && a.touchReleaseOnEdges && !a.loop)
              if (i.isVertical()) {
                if (
                  (d < s.startY && i.translate <= i.maxTranslate()) ||
                  (d > s.startY && i.translate >= i.minTranslate())
                )
                  return (n.isTouched = !1), void (n.isMoved = !1);
              } else if (
                (c < s.startX && i.translate <= i.maxTranslate()) ||
                (c > s.startX && i.translate >= i.minTranslate())
              )
                return;
            if (
              n.isTouchEvent &&
              e.activeElement &&
              l.target === e.activeElement &&
              g(l.target).is(n.focusableElements)
            )
              return (n.isMoved = !0), void (i.allowClick = !1);
            if (
              (n.allowTouchCallbacks && i.emit("touchMove", l),
              !(l.targetTouches && l.targetTouches.length > 1))
            ) {
              (s.currentX = c), (s.currentY = d);
              var h,
                p = s.currentX - s.startX,
                f = s.currentY - s.startY;
              if (
                !(
                  i.params.threshold &&
                  Math.sqrt(Math.pow(p, 2) + Math.pow(f, 2)) <
                    i.params.threshold
                )
              )
                if (
                  (void 0 === n.isScrolling &&
                    ((i.isHorizontal() && s.currentY === s.startY) ||
                    (i.isVertical() && s.currentX === s.startX)
                      ? (n.isScrolling = !1)
                      : p * p + f * f >= 25 &&
                        ((h =
                          (180 * Math.atan2(Math.abs(f), Math.abs(p))) /
                          Math.PI),
                        (n.isScrolling = i.isHorizontal()
                          ? h > a.touchAngle
                          : 90 - h > a.touchAngle))),
                  n.isScrolling && i.emit("touchMoveOpposite", l),
                  void 0 === n.startMoving &&
                    ((s.currentX === s.startX && s.currentY === s.startY) ||
                      (n.startMoving = !0)),
                  n.isScrolling)
                )
                  n.isTouched = !1;
                else if (n.startMoving) {
                  (i.allowClick = !1),
                    !a.cssMode && l.cancelable && l.preventDefault(),
                    a.touchMoveStopPropagation &&
                      !a.nested &&
                      l.stopPropagation(),
                    n.isMoved ||
                      (a.loop && i.loopFix(),
                      (n.startTranslate = i.getTranslate()),
                      i.setTransition(0),
                      i.animating &&
                        i.$wrapperEl.trigger(
                          "webkitTransitionEnd transitionend"
                        ),
                      (n.allowMomentumBounce = !1),
                      !a.grabCursor ||
                        (!0 !== i.allowSlideNext && !0 !== i.allowSlidePrev) ||
                        i.setGrabCursor(!0),
                      i.emit("sliderFirstMove", l)),
                    i.emit("sliderMove", l),
                    (n.isMoved = !0);
                  var m = i.isHorizontal() ? p : f;
                  (s.diff = m),
                    (m *= a.touchRatio),
                    o && (m = -m),
                    (i.swipeDirection = m > 0 ? "prev" : "next"),
                    (n.currentTranslate = m + n.startTranslate);
                  var v = !0,
                    _ = a.resistanceRatio;
                  if (
                    (a.touchReleaseOnEdges && (_ = 0),
                    m > 0 && n.currentTranslate > i.minTranslate()
                      ? ((v = !1),
                        a.resistance &&
                          (n.currentTranslate =
                            i.minTranslate() -
                            1 +
                            Math.pow(
                              -i.minTranslate() + n.startTranslate + m,
                              _
                            )))
                      : m < 0 &&
                        n.currentTranslate < i.maxTranslate() &&
                        ((v = !1),
                        a.resistance &&
                          (n.currentTranslate =
                            i.maxTranslate() +
                            1 -
                            Math.pow(
                              i.maxTranslate() - n.startTranslate - m,
                              _
                            ))),
                    v && (l.preventedByNestedSwiper = !0),
                    !i.allowSlideNext &&
                      "next" === i.swipeDirection &&
                      n.currentTranslate < n.startTranslate &&
                      (n.currentTranslate = n.startTranslate),
                    !i.allowSlidePrev &&
                      "prev" === i.swipeDirection &&
                      n.currentTranslate > n.startTranslate &&
                      (n.currentTranslate = n.startTranslate),
                    i.allowSlidePrev ||
                      i.allowSlideNext ||
                      (n.currentTranslate = n.startTranslate),
                    a.threshold > 0)
                  ) {
                    if (!(Math.abs(m) > a.threshold || n.allowThresholdMove))
                      return void (n.currentTranslate = n.startTranslate);
                    if (!n.allowThresholdMove)
                      return (
                        (n.allowThresholdMove = !0),
                        (s.startX = s.currentX),
                        (s.startY = s.currentY),
                        (n.currentTranslate = n.startTranslate),
                        void (s.diff = i.isHorizontal()
                          ? s.currentX - s.startX
                          : s.currentY - s.startY)
                      );
                  }
                  a.followFinger &&
                    !a.cssMode &&
                    ((a.freeMode ||
                      a.watchSlidesProgress ||
                      a.watchSlidesVisibility) &&
                      (i.updateActiveIndex(), i.updateSlidesClasses()),
                    a.freeMode &&
                      (0 === n.velocities.length &&
                        n.velocities.push({
                          position: s[i.isHorizontal() ? "startX" : "startY"],
                          time: n.touchStartTime,
                        }),
                      n.velocities.push({
                        position: s[i.isHorizontal() ? "currentX" : "currentY"],
                        time: x(),
                      })),
                    i.updateProgress(n.currentTranslate),
                    i.setTranslate(n.currentTranslate));
                }
            }
          }
        } else n.startMoving && n.isScrolling && i.emit("touchMoveOpposite", l);
      }
    }
    function j(t) {
      var e = this,
        i = e.touchEventsData,
        n = e.params,
        a = e.touches,
        r = e.rtlTranslate,
        s = e.$wrapperEl,
        o = e.slidesGrid,
        l = e.snapGrid;
      if (e.enabled) {
        var u = t;
        if (
          (u.originalEvent && (u = u.originalEvent),
          i.allowTouchCallbacks && e.emit("touchEnd", u),
          (i.allowTouchCallbacks = !1),
          !i.isTouched)
        )
          return (
            i.isMoved && n.grabCursor && e.setGrabCursor(!1),
            (i.isMoved = !1),
            void (i.startMoving = !1)
          );
        n.grabCursor &&
          i.isMoved &&
          i.isTouched &&
          (!0 === e.allowSlideNext || !0 === e.allowSlidePrev) &&
          e.setGrabCursor(!1);
        var c,
          d = x(),
          h = d - i.touchStartTime;
        if (
          (e.allowClick &&
            (e.updateClickedSlide(u),
            e.emit("tap click", u),
            h < 300 &&
              d - i.lastClickTime < 300 &&
              e.emit("doubleTap doubleClick", u)),
          (i.lastClickTime = x()),
          w(function () {
            e.destroyed || (e.allowClick = !0);
          }),
          !i.isTouched ||
            !i.isMoved ||
            !e.swipeDirection ||
            0 === a.diff ||
            i.currentTranslate === i.startTranslate)
        )
          return (
            (i.isTouched = !1), (i.isMoved = !1), void (i.startMoving = !1)
          );
        if (
          ((i.isTouched = !1),
          (i.isMoved = !1),
          (i.startMoving = !1),
          (c = n.followFinger
            ? r
              ? e.translate
              : -e.translate
            : -i.currentTranslate),
          !n.cssMode)
        )
          if (n.freeMode) {
            if (c < -e.minTranslate()) return void e.slideTo(e.activeIndex);
            if (c > -e.maxTranslate())
              return void (e.slides.length < l.length
                ? e.slideTo(l.length - 1)
                : e.slideTo(e.slides.length - 1));
            if (n.freeModeMomentum) {
              if (i.velocities.length > 1) {
                var p = i.velocities.pop(),
                  f = i.velocities.pop(),
                  m = p.position - f.position,
                  g = p.time - f.time;
                (e.velocity = m / g),
                  (e.velocity /= 2),
                  Math.abs(e.velocity) < n.freeModeMinimumVelocity &&
                    (e.velocity = 0),
                  (g > 150 || x() - p.time > 300) && (e.velocity = 0);
              } else e.velocity = 0;
              (e.velocity *= n.freeModeMomentumVelocityRatio),
                (i.velocities.length = 0);
              var v = 1e3 * n.freeModeMomentumRatio,
                _ = e.velocity * v,
                y = e.translate + _;
              r && (y = -y);
              var b,
                S,
                T = !1,
                C = 20 * Math.abs(e.velocity) * n.freeModeMomentumBounceRatio;
              if (y < e.maxTranslate())
                n.freeModeMomentumBounce
                  ? (y + e.maxTranslate() < -C && (y = e.maxTranslate() - C),
                    (b = e.maxTranslate()),
                    (T = !0),
                    (i.allowMomentumBounce = !0))
                  : (y = e.maxTranslate()),
                  n.loop && n.centeredSlides && (S = !0);
              else if (y > e.minTranslate())
                n.freeModeMomentumBounce
                  ? (y - e.minTranslate() > C && (y = e.minTranslate() + C),
                    (b = e.minTranslate()),
                    (T = !0),
                    (i.allowMomentumBounce = !0))
                  : (y = e.minTranslate()),
                  n.loop && n.centeredSlides && (S = !0);
              else if (n.freeModeSticky) {
                for (var E, P = 0; P < l.length; P += 1)
                  if (l[P] > -y) {
                    E = P;
                    break;
                  }
                y = -(y =
                  Math.abs(l[E] - y) < Math.abs(l[E - 1] - y) ||
                  "next" === e.swipeDirection
                    ? l[E]
                    : l[E - 1]);
              }
              if (
                (S &&
                  e.once("transitionEnd", function () {
                    e.loopFix();
                  }),
                0 !== e.velocity)
              ) {
                if (
                  ((v = r
                    ? Math.abs((-y - e.translate) / e.velocity)
                    : Math.abs((y - e.translate) / e.velocity)),
                  n.freeModeSticky)
                ) {
                  var L = Math.abs((r ? -y : y) - e.translate),
                    D = e.slidesSizesGrid[e.activeIndex];
                  v =
                    L < D ? n.speed : L < 2 * D ? 1.5 * n.speed : 2.5 * n.speed;
                }
              } else if (n.freeModeSticky) return void e.slideToClosest();
              n.freeModeMomentumBounce && T
                ? (e.updateProgress(b),
                  e.setTransition(v),
                  e.setTranslate(y),
                  e.transitionStart(!0, e.swipeDirection),
                  (e.animating = !0),
                  s.transitionEnd(function () {
                    e &&
                      !e.destroyed &&
                      i.allowMomentumBounce &&
                      (e.emit("momentumBounce"),
                      e.setTransition(n.speed),
                      setTimeout(function () {
                        e.setTranslate(b),
                          s.transitionEnd(function () {
                            e && !e.destroyed && e.transitionEnd();
                          });
                      }, 0));
                  }))
                : e.velocity
                ? (e.updateProgress(y),
                  e.setTransition(v),
                  e.setTranslate(y),
                  e.transitionStart(!0, e.swipeDirection),
                  e.animating ||
                    ((e.animating = !0),
                    s.transitionEnd(function () {
                      e && !e.destroyed && e.transitionEnd();
                    })))
                : (e.emit("_freeModeNoMomentumRelease"), e.updateProgress(y)),
                e.updateActiveIndex(),
                e.updateSlidesClasses();
            } else {
              if (n.freeModeSticky) return void e.slideToClosest();
              n.freeMode && e.emit("_freeModeNoMomentumRelease");
            }
            (!n.freeModeMomentum || h >= n.longSwipesMs) &&
              (e.updateProgress(),
              e.updateActiveIndex(),
              e.updateSlidesClasses());
          } else {
            for (
              var M = 0, k = e.slidesSizesGrid[0], A = 0;
              A < o.length;
              A += A < n.slidesPerGroupSkip ? 1 : n.slidesPerGroup
            ) {
              var I = A < n.slidesPerGroupSkip - 1 ? 1 : n.slidesPerGroup;
              void 0 !== o[A + I]
                ? c >= o[A] && c < o[A + I] && ((M = A), (k = o[A + I] - o[A]))
                : c >= o[A] &&
                  ((M = A), (k = o[o.length - 1] - o[o.length - 2]));
            }
            var z = (c - o[M]) / k,
              O = M < n.slidesPerGroupSkip - 1 ? 1 : n.slidesPerGroup;
            if (h > n.longSwipesMs) {
              if (!n.longSwipes) return void e.slideTo(e.activeIndex);
              "next" === e.swipeDirection &&
                (z >= n.longSwipesRatio ? e.slideTo(M + O) : e.slideTo(M)),
                "prev" === e.swipeDirection &&
                  (z > 1 - n.longSwipesRatio ? e.slideTo(M + O) : e.slideTo(M));
            } else {
              if (!n.shortSwipes) return void e.slideTo(e.activeIndex);
              !e.navigation ||
              (u.target !== e.navigation.nextEl &&
                u.target !== e.navigation.prevEl)
                ? ("next" === e.swipeDirection && e.slideTo(M + O),
                  "prev" === e.swipeDirection && e.slideTo(M))
                : u.target === e.navigation.nextEl
                ? e.slideTo(M + O)
                : e.slideTo(M);
            }
          }
      }
    }
    function F() {
      var t = this,
        e = t.params,
        i = t.el;
      if (!i || 0 !== i.offsetWidth) {
        e.breakpoints && t.setBreakpoint();
        var n = t.allowSlideNext,
          a = t.allowSlidePrev,
          r = t.snapGrid;
        (t.allowSlideNext = !0),
          (t.allowSlidePrev = !0),
          t.updateSize(),
          t.updateSlides(),
          t.updateSlidesClasses(),
          ("auto" === e.slidesPerView || e.slidesPerView > 1) &&
          t.isEnd &&
          !t.isBeginning &&
          !t.params.centeredSlides
            ? t.slideTo(t.slides.length - 1, 0, !1, !0)
            : t.slideTo(t.activeIndex, 0, !1, !0),
          t.autoplay &&
            t.autoplay.running &&
            t.autoplay.paused &&
            t.autoplay.run(),
          (t.allowSlidePrev = a),
          (t.allowSlideNext = n),
          t.params.watchOverflow && r !== t.snapGrid && t.checkOverflow();
      }
    }
    function R(t) {
      var e = this;
      e.enabled &&
        (e.allowClick ||
          (e.params.preventClicks && t.preventDefault(),
          e.params.preventClicksPropagation &&
            e.animating &&
            (t.stopPropagation(), t.stopImmediatePropagation())));
    }
    function $() {
      var t = this,
        e = t.wrapperEl,
        i = t.rtlTranslate;
      if (t.enabled) {
        (t.previousTranslate = t.translate),
          t.isHorizontal()
            ? (t.translate = i
                ? e.scrollWidth - e.offsetWidth - e.scrollLeft
                : -e.scrollLeft)
            : (t.translate = -e.scrollTop),
          -0 === t.translate && (t.translate = 0),
          t.updateActiveIndex(),
          t.updateSlidesClasses();
        var n = t.maxTranslate() - t.minTranslate();
        (0 === n ? 0 : (t.translate - t.minTranslate()) / n) !== t.progress &&
          t.updateProgress(i ? -t.translate : t.translate),
          t.emit("setTranslate", t.translate, !1);
      }
    }
    var H = !1;
    function V() {}
    var W = {
        init: !0,
        direction: "horizontal",
        touchEventsTarget: "container",
        initialSlide: 0,
        speed: 300,
        cssMode: !1,
        updateOnWindowResize: !0,
        resizeObserver: !1,
        nested: !1,
        createElements: !1,
        enabled: !0,
        focusableElements:
          "input, select, option, textarea, button, video, label",
        width: null,
        height: null,
        preventInteractionOnTransition: !1,
        userAgent: null,
        url: null,
        edgeSwipeDetection: !1,
        edgeSwipeThreshold: 20,
        freeMode: !1,
        freeModeMomentum: !0,
        freeModeMomentumRatio: 1,
        freeModeMomentumBounce: !0,
        freeModeMomentumBounceRatio: 1,
        freeModeMomentumVelocityRatio: 1,
        freeModeSticky: !1,
        freeModeMinimumVelocity: 0.02,
        autoHeight: !1,
        setWrapperSize: !1,
        virtualTranslate: !1,
        effect: "slide",
        breakpoints: void 0,
        breakpointsBase: "window",
        spaceBetween: 0,
        slidesPerView: 1,
        slidesPerColumn: 1,
        slidesPerColumnFill: "column",
        slidesPerGroup: 1,
        slidesPerGroupSkip: 0,
        centeredSlides: !1,
        centeredSlidesBounds: !1,
        slidesOffsetBefore: 0,
        slidesOffsetAfter: 0,
        normalizeSlideIndex: !0,
        centerInsufficientSlides: !1,
        watchOverflow: !1,
        roundLengths: !1,
        touchRatio: 1,
        touchAngle: 45,
        simulateTouch: !0,
        shortSwipes: !0,
        longSwipes: !0,
        longSwipesRatio: 0.5,
        longSwipesMs: 300,
        followFinger: !0,
        allowTouchMove: !0,
        threshold: 0,
        touchMoveStopPropagation: !1,
        touchStartPreventDefault: !0,
        touchStartForcePreventDefault: !1,
        touchReleaseOnEdges: !1,
        uniqueNavElements: !0,
        resistance: !0,
        resistanceRatio: 0.85,
        watchSlidesProgress: !1,
        watchSlidesVisibility: !1,
        grabCursor: !1,
        preventClicks: !0,
        preventClicksPropagation: !0,
        slideToClickedSlide: !1,
        preloadImages: !0,
        updateOnImagesReady: !0,
        loop: !1,
        loopAdditionalSlides: 0,
        loopedSlides: null,
        loopFillGroupWithBlank: !1,
        loopPreventsSlide: !0,
        allowSlidePrev: !0,
        allowSlideNext: !0,
        swipeHandler: null,
        noSwiping: !0,
        noSwipingClass: "swiper-no-swiping",
        noSwipingSelector: null,
        passiveListeners: !0,
        containerModifierClass: "swiper-container-",
        slideClass: "swiper-slide",
        slideBlankClass: "swiper-slide-invisible-blank",
        slideActiveClass: "swiper-slide-active",
        slideDuplicateActiveClass: "swiper-slide-duplicate-active",
        slideVisibleClass: "swiper-slide-visible",
        slideDuplicateClass: "swiper-slide-duplicate",
        slideNextClass: "swiper-slide-next",
        slideDuplicateNextClass: "swiper-slide-duplicate-next",
        slidePrevClass: "swiper-slide-prev",
        slideDuplicatePrevClass: "swiper-slide-duplicate-prev",
        wrapperClass: "swiper-wrapper",
        runCallbacksOnInit: !0,
        _emitClasses: !1,
      },
      Z = {
        modular: {
          useParams: function (t) {
            var e = this;
            e.modules &&
              Object.keys(e.modules).forEach(function (i) {
                var n = e.modules[i];
                n.params && E(t, n.params);
              });
          },
          useModules: function (t) {
            void 0 === t && (t = {});
            var e = this;
            e.modules &&
              Object.keys(e.modules).forEach(function (i) {
                var n = e.modules[i],
                  a = t[i] || {};
                n.on &&
                  e.on &&
                  Object.keys(n.on).forEach(function (t) {
                    e.on(t, n.on[t]);
                  }),
                  n.create && n.create.bind(e)(a);
              });
          },
        },
        eventsEmitter: {
          on: function (t, e, i) {
            var n = this;
            if ("function" != typeof e) return n;
            var a = i ? "unshift" : "push";
            return (
              t.split(" ").forEach(function (t) {
                n.eventsListeners[t] || (n.eventsListeners[t] = []),
                  n.eventsListeners[t][a](e);
              }),
              n
            );
          },
          once: function (t, e, i) {
            var n = this;
            if ("function" != typeof e) return n;
            function a() {
              n.off(t, a), a.__emitterProxy && delete a.__emitterProxy;
              for (
                var i = arguments.length, r = new Array(i), s = 0;
                s < i;
                s++
              )
                r[s] = arguments[s];
              e.apply(n, r);
            }
            return (a.__emitterProxy = e), n.on(t, a, i);
          },
          onAny: function (t, e) {
            var i = this;
            if ("function" != typeof t) return i;
            var n = e ? "unshift" : "push";
            return (
              i.eventsAnyListeners.indexOf(t) < 0 && i.eventsAnyListeners[n](t),
              i
            );
          },
          offAny: function (t) {
            var e = this;
            if (!e.eventsAnyListeners) return e;
            var i = e.eventsAnyListeners.indexOf(t);
            return i >= 0 && e.eventsAnyListeners.splice(i, 1), e;
          },
          off: function (t, e) {
            var i = this;
            return i.eventsListeners
              ? (t.split(" ").forEach(function (t) {
                  void 0 === e
                    ? (i.eventsListeners[t] = [])
                    : i.eventsListeners[t] &&
                      i.eventsListeners[t].forEach(function (n, a) {
                        (n === e ||
                          (n.__emitterProxy && n.__emitterProxy === e)) &&
                          i.eventsListeners[t].splice(a, 1);
                      });
                }),
                i)
              : i;
          },
          emit: function () {
            var t,
              e,
              i,
              n = this;
            if (!n.eventsListeners) return n;
            for (var a = arguments.length, r = new Array(a), s = 0; s < a; s++)
              r[s] = arguments[s];
            "string" == typeof r[0] || Array.isArray(r[0])
              ? ((t = r[0]), (e = r.slice(1, r.length)), (i = n))
              : ((t = r[0].events), (e = r[0].data), (i = r[0].context || n)),
              e.unshift(i);
            var o = Array.isArray(t) ? t : t.split(" ");
            return (
              o.forEach(function (t) {
                n.eventsAnyListeners &&
                  n.eventsAnyListeners.length &&
                  n.eventsAnyListeners.forEach(function (n) {
                    n.apply(i, [t].concat(e));
                  }),
                  n.eventsListeners &&
                    n.eventsListeners[t] &&
                    n.eventsListeners[t].forEach(function (t) {
                      t.apply(i, e);
                    });
              }),
              n
            );
          },
        },
        update: {
          updateSize: function () {
            var t,
              e,
              i = this,
              n = i.$el;
            (t =
              void 0 !== i.params.width && null !== i.params.width
                ? i.params.width
                : n[0].clientWidth),
              (e =
                void 0 !== i.params.height && null !== i.params.height
                  ? i.params.height
                  : n[0].clientHeight),
              (0 === t && i.isHorizontal()) ||
                (0 === e && i.isVertical()) ||
                ((t =
                  t -
                  parseInt(n.css("padding-left") || 0, 10) -
                  parseInt(n.css("padding-right") || 0, 10)),
                (e =
                  e -
                  parseInt(n.css("padding-top") || 0, 10) -
                  parseInt(n.css("padding-bottom") || 0, 10)),
                Number.isNaN(t) && (t = 0),
                Number.isNaN(e) && (e = 0),
                E(i, { width: t, height: e, size: i.isHorizontal() ? t : e }));
          },
          updateSlides: function () {
            var t = this;
            function e(e) {
              return t.isHorizontal()
                ? e
                : {
                    width: "height",
                    "margin-top": "margin-left",
                    "margin-bottom ": "margin-right",
                    "margin-left": "margin-top",
                    "margin-right": "margin-bottom",
                    "padding-left": "padding-top",
                    "padding-right": "padding-bottom",
                    marginRight: "marginBottom",
                  }[e];
            }
            function i(t, i) {
              return parseFloat(t.getPropertyValue(e(i)) || 0);
            }
            var n = t.params,
              a = t.$wrapperEl,
              r = t.size,
              s = t.rtlTranslate,
              o = t.wrongRTL,
              l = t.virtual && n.virtual.enabled,
              u = l ? t.virtual.slides.length : t.slides.length,
              c = a.children("." + t.params.slideClass),
              d = l ? t.virtual.slides.length : c.length,
              h = [],
              p = [],
              f = [],
              m = n.slidesOffsetBefore;
            "function" == typeof m && (m = n.slidesOffsetBefore.call(t));
            var g = n.slidesOffsetAfter;
            "function" == typeof g && (g = n.slidesOffsetAfter.call(t));
            var v = t.snapGrid.length,
              _ = t.slidesGrid.length,
              y = n.spaceBetween,
              b = -m,
              w = 0,
              x = 0;
            if (void 0 !== r) {
              var S, T;
              "string" == typeof y &&
                y.indexOf("%") >= 0 &&
                (y = (parseFloat(y.replace("%", "")) / 100) * r),
                (t.virtualSize = -y),
                s
                  ? c.css({ marginLeft: "", marginBottom: "", marginTop: "" })
                  : c.css({ marginRight: "", marginBottom: "", marginTop: "" }),
                n.slidesPerColumn > 1 &&
                  ((S =
                    Math.floor(d / n.slidesPerColumn) ===
                    d / t.params.slidesPerColumn
                      ? d
                      : Math.ceil(d / n.slidesPerColumn) * n.slidesPerColumn),
                  "auto" !== n.slidesPerView &&
                    "row" === n.slidesPerColumnFill &&
                    (S = Math.max(S, n.slidesPerView * n.slidesPerColumn)));
              for (
                var C,
                  P,
                  L,
                  D = n.slidesPerColumn,
                  M = S / D,
                  k = Math.floor(d / n.slidesPerColumn),
                  A = 0;
                A < d;
                A += 1
              ) {
                T = 0;
                var I = c.eq(A);
                if (n.slidesPerColumn > 1) {
                  var z = void 0,
                    O = void 0,
                    N = void 0;
                  if ("row" === n.slidesPerColumnFill && n.slidesPerGroup > 1) {
                    var B = Math.floor(
                        A / (n.slidesPerGroup * n.slidesPerColumn)
                      ),
                      j = A - n.slidesPerColumn * n.slidesPerGroup * B,
                      F =
                        0 === B
                          ? n.slidesPerGroup
                          : Math.min(
                              Math.ceil((d - B * D * n.slidesPerGroup) / D),
                              n.slidesPerGroup
                            );
                    (z =
                      (O =
                        j -
                        (N = Math.floor(j / F)) * F +
                        B * n.slidesPerGroup) +
                      (N * S) / D),
                      I.css({
                        "-webkit-box-ordinal-group": z,
                        "-moz-box-ordinal-group": z,
                        "-ms-flex-order": z,
                        "-webkit-order": z,
                        order: z,
                      });
                  } else
                    "column" === n.slidesPerColumnFill
                      ? ((N = A - (O = Math.floor(A / D)) * D),
                        (O > k || (O === k && N === D - 1)) &&
                          (N += 1) >= D &&
                          ((N = 0), (O += 1)))
                      : (O = A - (N = Math.floor(A / M)) * M);
                  I.css(
                    e("margin-top"),
                    0 !== N ? n.spaceBetween && n.spaceBetween + "px" : ""
                  );
                }
                if ("none" !== I.css("display")) {
                  if ("auto" === n.slidesPerView) {
                    var R = getComputedStyle(I[0]),
                      $ = I[0].style.transform,
                      H = I[0].style.webkitTransform;
                    if (
                      ($ && (I[0].style.transform = "none"),
                      H && (I[0].style.webkitTransform = "none"),
                      n.roundLengths)
                    )
                      T = t.isHorizontal()
                        ? I.outerWidth(!0)
                        : I.outerHeight(!0);
                    else {
                      var V = i(R, "width"),
                        W = i(R, "padding-left"),
                        Z = i(R, "padding-right"),
                        G = i(R, "margin-left"),
                        U = i(R, "margin-right"),
                        Y = R.getPropertyValue("box-sizing");
                      if (Y && "border-box" === Y) T = V + G + U;
                      else {
                        var q = I[0],
                          X = q.clientWidth;
                        T = V + W + Z + G + U + (q.offsetWidth - X);
                      }
                    }
                    $ && (I[0].style.transform = $),
                      H && (I[0].style.webkitTransform = H),
                      n.roundLengths && (T = Math.floor(T));
                  } else
                    (T = (r - (n.slidesPerView - 1) * y) / n.slidesPerView),
                      n.roundLengths && (T = Math.floor(T)),
                      c[A] && (c[A].style[e("width")] = T + "px");
                  c[A] && (c[A].swiperSlideSize = T),
                    f.push(T),
                    n.centeredSlides
                      ? ((b = b + T / 2 + w / 2 + y),
                        0 === w && 0 !== A && (b = b - r / 2 - y),
                        0 === A && (b = b - r / 2 - y),
                        Math.abs(b) < 0.001 && (b = 0),
                        n.roundLengths && (b = Math.floor(b)),
                        x % n.slidesPerGroup == 0 && h.push(b),
                        p.push(b))
                      : (n.roundLengths && (b = Math.floor(b)),
                        (x - Math.min(t.params.slidesPerGroupSkip, x)) %
                          t.params.slidesPerGroup ==
                          0 && h.push(b),
                        p.push(b),
                        (b = b + T + y)),
                    (t.virtualSize += T + y),
                    (w = T),
                    (x += 1);
                }
              }
              if (
                ((t.virtualSize = Math.max(t.virtualSize, r) + g),
                s &&
                  o &&
                  ("slide" === n.effect || "coverflow" === n.effect) &&
                  a.css({ width: t.virtualSize + n.spaceBetween + "px" }),
                n.setWrapperSize &&
                  a.css(
                    (((P = {})[e("width")] =
                      t.virtualSize + n.spaceBetween + "px"),
                    P)
                  ),
                n.slidesPerColumn > 1 &&
                  ((t.virtualSize = (T + n.spaceBetween) * S),
                  (t.virtualSize =
                    Math.ceil(t.virtualSize / n.slidesPerColumn) -
                    n.spaceBetween),
                  a.css(
                    (((L = {})[e("width")] =
                      t.virtualSize + n.spaceBetween + "px"),
                    L)
                  ),
                  n.centeredSlides))
              ) {
                C = [];
                for (var K = 0; K < h.length; K += 1) {
                  var J = h[K];
                  n.roundLengths && (J = Math.floor(J)),
                    h[K] < t.virtualSize + h[0] && C.push(J);
                }
                h = C;
              }
              if (!n.centeredSlides) {
                C = [];
                for (var Q = 0; Q < h.length; Q += 1) {
                  var tt = h[Q];
                  n.roundLengths && (tt = Math.floor(tt)),
                    h[Q] <= t.virtualSize - r && C.push(tt);
                }
                (h = C),
                  Math.floor(t.virtualSize - r) - Math.floor(h[h.length - 1]) >
                    1 && h.push(t.virtualSize - r);
              }
              if ((0 === h.length && (h = [0]), 0 !== n.spaceBetween)) {
                var et,
                  it = t.isHorizontal() && s ? "marginLeft" : e("marginRight");
                c.filter(function (t, e) {
                  return !n.cssMode || e !== c.length - 1;
                }).css((((et = {})[it] = y + "px"), et));
              }
              if (n.centeredSlides && n.centeredSlidesBounds) {
                var nt = 0;
                f.forEach(function (t) {
                  nt += t + (n.spaceBetween ? n.spaceBetween : 0);
                });
                var at = (nt -= n.spaceBetween) - r;
                h = h.map(function (t) {
                  return t < 0 ? -m : t > at ? at + g : t;
                });
              }
              if (n.centerInsufficientSlides) {
                var rt = 0;
                if (
                  (f.forEach(function (t) {
                    rt += t + (n.spaceBetween ? n.spaceBetween : 0);
                  }),
                  (rt -= n.spaceBetween) < r)
                ) {
                  var st = (r - rt) / 2;
                  h.forEach(function (t, e) {
                    h[e] = t - st;
                  }),
                    p.forEach(function (t, e) {
                      p[e] = t + st;
                    });
                }
              }
              E(t, {
                slides: c,
                snapGrid: h,
                slidesGrid: p,
                slidesSizesGrid: f,
              }),
                d !== u && t.emit("slidesLengthChange"),
                h.length !== v &&
                  (t.params.watchOverflow && t.checkOverflow(),
                  t.emit("snapGridLengthChange")),
                p.length !== _ && t.emit("slidesGridLengthChange"),
                (n.watchSlidesProgress || n.watchSlidesVisibility) &&
                  t.updateSlidesOffset();
            }
          },
          updateAutoHeight: function (t) {
            var e,
              i = this,
              n = [],
              a = i.virtual && i.params.virtual.enabled,
              r = 0;
            "number" == typeof t
              ? i.setTransition(t)
              : !0 === t && i.setTransition(i.params.speed);
            var s = function (t) {
              return a
                ? i.slides.filter(function (e) {
                    return (
                      parseInt(
                        e.getAttribute("data-swiper-slide-index"),
                        10
                      ) === t
                    );
                  })[0]
                : i.slides.eq(t)[0];
            };
            if ("auto" !== i.params.slidesPerView && i.params.slidesPerView > 1)
              if (i.params.centeredSlides)
                i.visibleSlides.each(function (t) {
                  n.push(t);
                });
              else
                for (e = 0; e < Math.ceil(i.params.slidesPerView); e += 1) {
                  var o = i.activeIndex + e;
                  if (o > i.slides.length && !a) break;
                  n.push(s(o));
                }
            else n.push(s(i.activeIndex));
            for (e = 0; e < n.length; e += 1)
              if (void 0 !== n[e]) {
                var l = n[e].offsetHeight;
                r = l > r ? l : r;
              }
            r && i.$wrapperEl.css("height", r + "px");
          },
          updateSlidesOffset: function () {
            for (var t = this.slides, e = 0; e < t.length; e += 1)
              t[e].swiperSlideOffset = this.isHorizontal()
                ? t[e].offsetLeft
                : t[e].offsetTop;
          },
          updateSlidesProgress: function (t) {
            void 0 === t && (t = (this && this.translate) || 0);
            var e = this,
              i = e.params,
              n = e.slides,
              a = e.rtlTranslate;
            if (0 !== n.length) {
              void 0 === n[0].swiperSlideOffset && e.updateSlidesOffset();
              var r = -t;
              a && (r = t),
                n.removeClass(i.slideVisibleClass),
                (e.visibleSlidesIndexes = []),
                (e.visibleSlides = []);
              for (var s = 0; s < n.length; s += 1) {
                var o = n[s],
                  l =
                    (r +
                      (i.centeredSlides ? e.minTranslate() : 0) -
                      o.swiperSlideOffset) /
                    (o.swiperSlideSize + i.spaceBetween);
                if (
                  i.watchSlidesVisibility ||
                  (i.centeredSlides && i.autoHeight)
                ) {
                  var u = -(r - o.swiperSlideOffset),
                    c = u + e.slidesSizesGrid[s];
                  ((u >= 0 && u < e.size - 1) ||
                    (c > 1 && c <= e.size) ||
                    (u <= 0 && c >= e.size)) &&
                    (e.visibleSlides.push(o),
                    e.visibleSlidesIndexes.push(s),
                    n.eq(s).addClass(i.slideVisibleClass));
                }
                o.progress = a ? -l : l;
              }
              e.visibleSlides = g(e.visibleSlides);
            }
          },
          updateProgress: function (t) {
            var e = this;
            if (void 0 === t) {
              var i = e.rtlTranslate ? -1 : 1;
              t = (e && e.translate && e.translate * i) || 0;
            }
            var n = e.params,
              a = e.maxTranslate() - e.minTranslate(),
              r = e.progress,
              s = e.isBeginning,
              o = e.isEnd,
              l = s,
              u = o;
            0 === a
              ? ((r = 0), (s = !0), (o = !0))
              : ((s = (r = (t - e.minTranslate()) / a) <= 0), (o = r >= 1)),
              E(e, { progress: r, isBeginning: s, isEnd: o }),
              (n.watchSlidesProgress ||
                n.watchSlidesVisibility ||
                (n.centeredSlides && n.autoHeight)) &&
                e.updateSlidesProgress(t),
              s && !l && e.emit("reachBeginning toEdge"),
              o && !u && e.emit("reachEnd toEdge"),
              ((l && !s) || (u && !o)) && e.emit("fromEdge"),
              e.emit("progress", r);
          },
          updateSlidesClasses: function () {
            var t,
              e = this,
              i = e.slides,
              n = e.params,
              a = e.$wrapperEl,
              r = e.activeIndex,
              s = e.realIndex,
              o = e.virtual && n.virtual.enabled;
            i.removeClass(
              n.slideActiveClass +
                " " +
                n.slideNextClass +
                " " +
                n.slidePrevClass +
                " " +
                n.slideDuplicateActiveClass +
                " " +
                n.slideDuplicateNextClass +
                " " +
                n.slideDuplicatePrevClass
            ),
              (t = o
                ? e.$wrapperEl.find(
                    "." + n.slideClass + '[data-swiper-slide-index="' + r + '"]'
                  )
                : i.eq(r)).addClass(n.slideActiveClass),
              n.loop &&
                (t.hasClass(n.slideDuplicateClass)
                  ? a
                      .children(
                        "." +
                          n.slideClass +
                          ":not(." +
                          n.slideDuplicateClass +
                          ')[data-swiper-slide-index="' +
                          s +
                          '"]'
                      )
                      .addClass(n.slideDuplicateActiveClass)
                  : a
                      .children(
                        "." +
                          n.slideClass +
                          "." +
                          n.slideDuplicateClass +
                          '[data-swiper-slide-index="' +
                          s +
                          '"]'
                      )
                      .addClass(n.slideDuplicateActiveClass));
            var l = t
              .nextAll("." + n.slideClass)
              .eq(0)
              .addClass(n.slideNextClass);
            n.loop &&
              0 === l.length &&
              (l = i.eq(0)).addClass(n.slideNextClass);
            var u = t
              .prevAll("." + n.slideClass)
              .eq(0)
              .addClass(n.slidePrevClass);
            n.loop &&
              0 === u.length &&
              (u = i.eq(-1)).addClass(n.slidePrevClass),
              n.loop &&
                (l.hasClass(n.slideDuplicateClass)
                  ? a
                      .children(
                        "." +
                          n.slideClass +
                          ":not(." +
                          n.slideDuplicateClass +
                          ')[data-swiper-slide-index="' +
                          l.attr("data-swiper-slide-index") +
                          '"]'
                      )
                      .addClass(n.slideDuplicateNextClass)
                  : a
                      .children(
                        "." +
                          n.slideClass +
                          "." +
                          n.slideDuplicateClass +
                          '[data-swiper-slide-index="' +
                          l.attr("data-swiper-slide-index") +
                          '"]'
                      )
                      .addClass(n.slideDuplicateNextClass),
                u.hasClass(n.slideDuplicateClass)
                  ? a
                      .children(
                        "." +
                          n.slideClass +
                          ":not(." +
                          n.slideDuplicateClass +
                          ')[data-swiper-slide-index="' +
                          u.attr("data-swiper-slide-index") +
                          '"]'
                      )
                      .addClass(n.slideDuplicatePrevClass)
                  : a
                      .children(
                        "." +
                          n.slideClass +
                          "." +
                          n.slideDuplicateClass +
                          '[data-swiper-slide-index="' +
                          u.attr("data-swiper-slide-index") +
                          '"]'
                      )
                      .addClass(n.slideDuplicatePrevClass)),
              e.emitSlidesClasses();
          },
          updateActiveIndex: function (t) {
            var e,
              i = this,
              n = i.rtlTranslate ? i.translate : -i.translate,
              a = i.slidesGrid,
              r = i.snapGrid,
              s = i.params,
              o = i.activeIndex,
              l = i.realIndex,
              u = i.snapIndex,
              c = t;
            if (void 0 === c) {
              for (var d = 0; d < a.length; d += 1)
                void 0 !== a[d + 1]
                  ? n >= a[d] && n < a[d + 1] - (a[d + 1] - a[d]) / 2
                    ? (c = d)
                    : n >= a[d] && n < a[d + 1] && (c = d + 1)
                  : n >= a[d] && (c = d);
              s.normalizeSlideIndex && (c < 0 || void 0 === c) && (c = 0);
            }
            if (r.indexOf(n) >= 0) e = r.indexOf(n);
            else {
              var h = Math.min(s.slidesPerGroupSkip, c);
              e = h + Math.floor((c - h) / s.slidesPerGroup);
            }
            if ((e >= r.length && (e = r.length - 1), c !== o)) {
              var p = parseInt(
                i.slides.eq(c).attr("data-swiper-slide-index") || c,
                10
              );
              E(i, {
                snapIndex: e,
                realIndex: p,
                previousIndex: o,
                activeIndex: c,
              }),
                i.emit("activeIndexChange"),
                i.emit("snapIndexChange"),
                l !== p && i.emit("realIndexChange"),
                (i.initialized || i.params.runCallbacksOnInit) &&
                  i.emit("slideChange");
            } else e !== u && ((i.snapIndex = e), i.emit("snapIndexChange"));
          },
          updateClickedSlide: function (t) {
            var e,
              i = this,
              n = i.params,
              a = g(t.target).closest("." + n.slideClass)[0],
              r = !1;
            if (a)
              for (var s = 0; s < i.slides.length; s += 1)
                if (i.slides[s] === a) {
                  (r = !0), (e = s);
                  break;
                }
            if (!a || !r)
              return (i.clickedSlide = void 0), void (i.clickedIndex = void 0);
            (i.clickedSlide = a),
              i.virtual && i.params.virtual.enabled
                ? (i.clickedIndex = parseInt(
                    g(a).attr("data-swiper-slide-index"),
                    10
                  ))
                : (i.clickedIndex = e),
              n.slideToClickedSlide &&
                void 0 !== i.clickedIndex &&
                i.clickedIndex !== i.activeIndex &&
                i.slideToClickedSlide();
          },
        },
        translate: {
          getTranslate: function (t) {
            void 0 === t && (t = this.isHorizontal() ? "x" : "y");
            var e = this,
              i = e.params,
              n = e.rtlTranslate,
              a = e.translate,
              r = e.$wrapperEl;
            if (i.virtualTranslate) return n ? -a : a;
            if (i.cssMode) return a;
            var s = S(r[0], t);
            return n && (s = -s), s || 0;
          },
          setTranslate: function (t, e) {
            var i = this,
              n = i.rtlTranslate,
              a = i.params,
              r = i.$wrapperEl,
              s = i.wrapperEl,
              o = i.progress,
              l = 0,
              u = 0;
            i.isHorizontal() ? (l = n ? -t : t) : (u = t),
              a.roundLengths && ((l = Math.floor(l)), (u = Math.floor(u))),
              a.cssMode
                ? (s[i.isHorizontal() ? "scrollLeft" : "scrollTop"] =
                    i.isHorizontal() ? -l : -u)
                : a.virtualTranslate ||
                  r.transform("translate3d(" + l + "px, " + u + "px, 0px)"),
              (i.previousTranslate = i.translate),
              (i.translate = i.isHorizontal() ? l : u);
            var c = i.maxTranslate() - i.minTranslate();
            (0 === c ? 0 : (t - i.minTranslate()) / c) !== o &&
              i.updateProgress(t),
              i.emit("setTranslate", i.translate, e);
          },
          minTranslate: function () {
            return -this.snapGrid[0];
          },
          maxTranslate: function () {
            return -this.snapGrid[this.snapGrid.length - 1];
          },
          translateTo: function (t, e, i, n, a) {
            void 0 === t && (t = 0),
              void 0 === e && (e = this.params.speed),
              void 0 === i && (i = !0),
              void 0 === n && (n = !0);
            var r = this,
              s = r.params,
              o = r.wrapperEl;
            if (r.animating && s.preventInteractionOnTransition) return !1;
            var l,
              u = r.minTranslate(),
              c = r.maxTranslate();
            if (
              ((l = n && t > u ? u : n && t < c ? c : t),
              r.updateProgress(l),
              s.cssMode)
            ) {
              var d,
                h = r.isHorizontal();
              return (
                0 === e
                  ? (o[h ? "scrollLeft" : "scrollTop"] = -l)
                  : o.scrollTo
                  ? o.scrollTo(
                      (((d = {})[h ? "left" : "top"] = -l),
                      (d.behavior = "smooth"),
                      d)
                    )
                  : (o[h ? "scrollLeft" : "scrollTop"] = -l),
                !0
              );
            }
            return (
              0 === e
                ? (r.setTransition(0),
                  r.setTranslate(l),
                  i &&
                    (r.emit("beforeTransitionStart", e, a),
                    r.emit("transitionEnd")))
                : (r.setTransition(e),
                  r.setTranslate(l),
                  i &&
                    (r.emit("beforeTransitionStart", e, a),
                    r.emit("transitionStart")),
                  r.animating ||
                    ((r.animating = !0),
                    r.onTranslateToWrapperTransitionEnd ||
                      (r.onTranslateToWrapperTransitionEnd = function (t) {
                        r &&
                          !r.destroyed &&
                          t.target === this &&
                          (r.$wrapperEl[0].removeEventListener(
                            "transitionend",
                            r.onTranslateToWrapperTransitionEnd
                          ),
                          r.$wrapperEl[0].removeEventListener(
                            "webkitTransitionEnd",
                            r.onTranslateToWrapperTransitionEnd
                          ),
                          (r.onTranslateToWrapperTransitionEnd = null),
                          delete r.onTranslateToWrapperTransitionEnd,
                          i && r.emit("transitionEnd"));
                      }),
                    r.$wrapperEl[0].addEventListener(
                      "transitionend",
                      r.onTranslateToWrapperTransitionEnd
                    ),
                    r.$wrapperEl[0].addEventListener(
                      "webkitTransitionEnd",
                      r.onTranslateToWrapperTransitionEnd
                    ))),
              !0
            );
          },
        },
        transition: {
          setTransition: function (t, e) {
            var i = this;
            i.params.cssMode || i.$wrapperEl.transition(t),
              i.emit("setTransition", t, e);
          },
          transitionStart: function (t, e) {
            void 0 === t && (t = !0);
            var i = this,
              n = i.activeIndex,
              a = i.params,
              r = i.previousIndex;
            if (!a.cssMode) {
              a.autoHeight && i.updateAutoHeight();
              var s = e;
              if (
                (s || (s = n > r ? "next" : n < r ? "prev" : "reset"),
                i.emit("transitionStart"),
                t && n !== r)
              ) {
                if ("reset" === s)
                  return void i.emit("slideResetTransitionStart");
                i.emit("slideChangeTransitionStart"),
                  "next" === s
                    ? i.emit("slideNextTransitionStart")
                    : i.emit("slidePrevTransitionStart");
              }
            }
          },
          transitionEnd: function (t, e) {
            void 0 === t && (t = !0);
            var i = this,
              n = i.activeIndex,
              a = i.previousIndex,
              r = i.params;
            if (((i.animating = !1), !r.cssMode)) {
              i.setTransition(0);
              var s = e;
              if (
                (s || (s = n > a ? "next" : n < a ? "prev" : "reset"),
                i.emit("transitionEnd"),
                t && n !== a)
              ) {
                if ("reset" === s)
                  return void i.emit("slideResetTransitionEnd");
                i.emit("slideChangeTransitionEnd"),
                  "next" === s
                    ? i.emit("slideNextTransitionEnd")
                    : i.emit("slidePrevTransitionEnd");
              }
            }
          },
        },
        slide: {
          slideTo: function (t, e, i, n, a) {
            if (
              (void 0 === t && (t = 0),
              void 0 === e && (e = this.params.speed),
              void 0 === i && (i = !0),
              "number" != typeof t && "string" != typeof t)
            )
              throw new Error(
                "The 'index' argument cannot have type other than 'number' or 'string'. [" +
                  typeof t +
                  "] given."
              );
            if ("string" == typeof t) {
              var r = parseInt(t, 10);
              if (!isFinite(r))
                throw new Error(
                  "The passed-in 'index' (string) couldn't be converted to 'number'. [" +
                    t +
                    "] given."
                );
              t = r;
            }
            var s = this,
              o = t;
            o < 0 && (o = 0);
            var l = s.params,
              u = s.snapGrid,
              c = s.slidesGrid,
              d = s.previousIndex,
              h = s.activeIndex,
              p = s.rtlTranslate,
              f = s.wrapperEl,
              m = s.enabled;
            if (
              (s.animating && l.preventInteractionOnTransition) ||
              (!m && !n && !a)
            )
              return !1;
            var g = Math.min(s.params.slidesPerGroupSkip, o),
              v = g + Math.floor((o - g) / s.params.slidesPerGroup);
            v >= u.length && (v = u.length - 1),
              (h || l.initialSlide || 0) === (d || 0) &&
                i &&
                s.emit("beforeSlideChangeStart");
            var _,
              y = -u[v];
            if ((s.updateProgress(y), l.normalizeSlideIndex))
              for (var b = 0; b < c.length; b += 1) {
                var w = -Math.floor(100 * y),
                  x = Math.floor(100 * c[b]),
                  S = Math.floor(100 * c[b + 1]);
                void 0 !== c[b + 1]
                  ? w >= x && w < S - (S - x) / 2
                    ? (o = b)
                    : w >= x && w < S && (o = b + 1)
                  : w >= x && (o = b);
              }
            if (s.initialized && o !== h) {
              if (!s.allowSlideNext && y < s.translate && y < s.minTranslate())
                return !1;
              if (
                !s.allowSlidePrev &&
                y > s.translate &&
                y > s.maxTranslate() &&
                (h || 0) !== o
              )
                return !1;
            }
            if (
              ((_ = o > h ? "next" : o < h ? "prev" : "reset"),
              (p && -y === s.translate) || (!p && y === s.translate))
            )
              return (
                s.updateActiveIndex(o),
                l.autoHeight && s.updateAutoHeight(),
                s.updateSlidesClasses(),
                "slide" !== l.effect && s.setTranslate(y),
                "reset" !== _ &&
                  (s.transitionStart(i, _), s.transitionEnd(i, _)),
                !1
              );
            if (l.cssMode) {
              var T,
                C = s.isHorizontal(),
                E = -y;
              return (
                p && (E = f.scrollWidth - f.offsetWidth - E),
                0 === e
                  ? (f[C ? "scrollLeft" : "scrollTop"] = E)
                  : f.scrollTo
                  ? f.scrollTo(
                      (((T = {})[C ? "left" : "top"] = E),
                      (T.behavior = "smooth"),
                      T)
                    )
                  : (f[C ? "scrollLeft" : "scrollTop"] = E),
                !0
              );
            }
            return (
              0 === e
                ? (s.setTransition(0),
                  s.setTranslate(y),
                  s.updateActiveIndex(o),
                  s.updateSlidesClasses(),
                  s.emit("beforeTransitionStart", e, n),
                  s.transitionStart(i, _),
                  s.transitionEnd(i, _))
                : (s.setTransition(e),
                  s.setTranslate(y),
                  s.updateActiveIndex(o),
                  s.updateSlidesClasses(),
                  s.emit("beforeTransitionStart", e, n),
                  s.transitionStart(i, _),
                  s.animating ||
                    ((s.animating = !0),
                    s.onSlideToWrapperTransitionEnd ||
                      (s.onSlideToWrapperTransitionEnd = function (t) {
                        s &&
                          !s.destroyed &&
                          t.target === this &&
                          (s.$wrapperEl[0].removeEventListener(
                            "transitionend",
                            s.onSlideToWrapperTransitionEnd
                          ),
                          s.$wrapperEl[0].removeEventListener(
                            "webkitTransitionEnd",
                            s.onSlideToWrapperTransitionEnd
                          ),
                          (s.onSlideToWrapperTransitionEnd = null),
                          delete s.onSlideToWrapperTransitionEnd,
                          s.transitionEnd(i, _));
                      }),
                    s.$wrapperEl[0].addEventListener(
                      "transitionend",
                      s.onSlideToWrapperTransitionEnd
                    ),
                    s.$wrapperEl[0].addEventListener(
                      "webkitTransitionEnd",
                      s.onSlideToWrapperTransitionEnd
                    ))),
              !0
            );
          },
          slideToLoop: function (t, e, i, n) {
            void 0 === t && (t = 0),
              void 0 === e && (e = this.params.speed),
              void 0 === i && (i = !0);
            var a = this,
              r = t;
            return (
              a.params.loop && (r += a.loopedSlides), a.slideTo(r, e, i, n)
            );
          },
          slideNext: function (t, e, i) {
            void 0 === t && (t = this.params.speed), void 0 === e && (e = !0);
            var n = this,
              a = n.params,
              r = n.animating;
            if (!n.enabled) return n;
            var s = n.activeIndex < a.slidesPerGroupSkip ? 1 : a.slidesPerGroup;
            if (a.loop) {
              if (r && a.loopPreventsSlide) return !1;
              n.loopFix(), (n._clientLeft = n.$wrapperEl[0].clientLeft);
            }
            return n.slideTo(n.activeIndex + s, t, e, i);
          },
          slidePrev: function (t, e, i) {
            void 0 === t && (t = this.params.speed), void 0 === e && (e = !0);
            var n = this,
              a = n.params,
              r = n.animating,
              s = n.snapGrid,
              o = n.slidesGrid,
              l = n.rtlTranslate;
            if (!n.enabled) return n;
            if (a.loop) {
              if (r && a.loopPreventsSlide) return !1;
              n.loopFix(), (n._clientLeft = n.$wrapperEl[0].clientLeft);
            }
            function u(t) {
              return t < 0 ? -Math.floor(Math.abs(t)) : Math.floor(t);
            }
            var c,
              d = u(l ? n.translate : -n.translate),
              h = s.map(function (t) {
                return u(t);
              }),
              p = s[h.indexOf(d) - 1];
            return (
              void 0 === p &&
                a.cssMode &&
                s.forEach(function (t) {
                  !p && d >= t && (p = t);
                }),
              void 0 !== p && (c = o.indexOf(p)) < 0 && (c = n.activeIndex - 1),
              n.slideTo(c, t, e, i)
            );
          },
          slideReset: function (t, e, i) {
            return (
              void 0 === t && (t = this.params.speed),
              void 0 === e && (e = !0),
              this.slideTo(this.activeIndex, t, e, i)
            );
          },
          slideToClosest: function (t, e, i, n) {
            void 0 === t && (t = this.params.speed),
              void 0 === e && (e = !0),
              void 0 === n && (n = 0.5);
            var a = this,
              r = a.activeIndex,
              s = Math.min(a.params.slidesPerGroupSkip, r),
              o = s + Math.floor((r - s) / a.params.slidesPerGroup),
              l = a.rtlTranslate ? a.translate : -a.translate;
            if (l >= a.snapGrid[o]) {
              var u = a.snapGrid[o];
              l - u > (a.snapGrid[o + 1] - u) * n &&
                (r += a.params.slidesPerGroup);
            } else {
              var c = a.snapGrid[o - 1];
              l - c <= (a.snapGrid[o] - c) * n &&
                (r -= a.params.slidesPerGroup);
            }
            return (
              (r = Math.max(r, 0)),
              (r = Math.min(r, a.slidesGrid.length - 1)),
              a.slideTo(r, t, e, i)
            );
          },
          slideToClickedSlide: function () {
            var t,
              e = this,
              i = e.params,
              n = e.$wrapperEl,
              a =
                "auto" === i.slidesPerView
                  ? e.slidesPerViewDynamic()
                  : i.slidesPerView,
              r = e.clickedIndex;
            if (i.loop) {
              if (e.animating) return;
              (t = parseInt(
                g(e.clickedSlide).attr("data-swiper-slide-index"),
                10
              )),
                i.centeredSlides
                  ? r < e.loopedSlides - a / 2 ||
                    r > e.slides.length - e.loopedSlides + a / 2
                    ? (e.loopFix(),
                      (r = n
                        .children(
                          "." +
                            i.slideClass +
                            '[data-swiper-slide-index="' +
                            t +
                            '"]:not(.' +
                            i.slideDuplicateClass +
                            ")"
                        )
                        .eq(0)
                        .index()),
                      w(function () {
                        e.slideTo(r);
                      }))
                    : e.slideTo(r)
                  : r > e.slides.length - a
                  ? (e.loopFix(),
                    (r = n
                      .children(
                        "." +
                          i.slideClass +
                          '[data-swiper-slide-index="' +
                          t +
                          '"]:not(.' +
                          i.slideDuplicateClass +
                          ")"
                      )
                      .eq(0)
                      .index()),
                    w(function () {
                      e.slideTo(r);
                    }))
                  : e.slideTo(r);
            } else e.slideTo(r);
          },
        },
        loop: {
          loopCreate: function () {
            var t = this,
              e = r(),
              i = t.params,
              n = t.$wrapperEl;
            n.children(
              "." + i.slideClass + "." + i.slideDuplicateClass
            ).remove();
            var a = n.children("." + i.slideClass);
            if (i.loopFillGroupWithBlank) {
              var s = i.slidesPerGroup - (a.length % i.slidesPerGroup);
              if (s !== i.slidesPerGroup) {
                for (var o = 0; o < s; o += 1) {
                  var l = g(e.createElement("div")).addClass(
                    i.slideClass + " " + i.slideBlankClass
                  );
                  n.append(l);
                }
                a = n.children("." + i.slideClass);
              }
            }
            "auto" !== i.slidesPerView ||
              i.loopedSlides ||
              (i.loopedSlides = a.length),
              (t.loopedSlides = Math.ceil(
                parseFloat(i.loopedSlides || i.slidesPerView, 10)
              )),
              (t.loopedSlides += i.loopAdditionalSlides),
              t.loopedSlides > a.length && (t.loopedSlides = a.length);
            var u = [],
              c = [];
            a.each(function (e, i) {
              var n = g(e);
              i < t.loopedSlides && c.push(e),
                i < a.length && i >= a.length - t.loopedSlides && u.push(e),
                n.attr("data-swiper-slide-index", i);
            });
            for (var d = 0; d < c.length; d += 1)
              n.append(g(c[d].cloneNode(!0)).addClass(i.slideDuplicateClass));
            for (var h = u.length - 1; h >= 0; h -= 1)
              n.prepend(g(u[h].cloneNode(!0)).addClass(i.slideDuplicateClass));
          },
          loopFix: function () {
            var t = this;
            t.emit("beforeLoopFix");
            var e,
              i = t.activeIndex,
              n = t.slides,
              a = t.loopedSlides,
              r = t.allowSlidePrev,
              s = t.allowSlideNext,
              o = t.snapGrid,
              l = t.rtlTranslate;
            (t.allowSlidePrev = !0), (t.allowSlideNext = !0);
            var u = -o[i] - t.getTranslate();
            i < a
              ? ((e = n.length - 3 * a + i),
                (e += a),
                t.slideTo(e, 0, !1, !0) &&
                  0 !== u &&
                  t.setTranslate((l ? -t.translate : t.translate) - u))
              : i >= n.length - a &&
                ((e = -n.length + i + a),
                (e += a),
                t.slideTo(e, 0, !1, !0) &&
                  0 !== u &&
                  t.setTranslate((l ? -t.translate : t.translate) - u)),
              (t.allowSlidePrev = r),
              (t.allowSlideNext = s),
              t.emit("loopFix");
          },
          loopDestroy: function () {
            var t = this,
              e = t.$wrapperEl,
              i = t.params,
              n = t.slides;
            e
              .children(
                "." +
                  i.slideClass +
                  "." +
                  i.slideDuplicateClass +
                  ",." +
                  i.slideClass +
                  "." +
                  i.slideBlankClass
              )
              .remove(),
              n.removeAttr("data-swiper-slide-index");
          },
        },
        grabCursor: {
          setGrabCursor: function (t) {
            var e = this;
            if (
              !(
                e.support.touch ||
                !e.params.simulateTouch ||
                (e.params.watchOverflow && e.isLocked) ||
                e.params.cssMode
              )
            ) {
              var i = e.el;
              (i.style.cursor = "move"),
                (i.style.cursor = t ? "-webkit-grabbing" : "-webkit-grab"),
                (i.style.cursor = t ? "-moz-grabbin" : "-moz-grab"),
                (i.style.cursor = t ? "grabbing" : "grab");
            }
          },
          unsetGrabCursor: function () {
            var t = this;
            t.support.touch ||
              (t.params.watchOverflow && t.isLocked) ||
              t.params.cssMode ||
              (t.el.style.cursor = "");
          },
        },
        manipulation: {
          appendSlide: function (t) {
            var e = this,
              i = e.$wrapperEl,
              n = e.params;
            if (
              (n.loop && e.loopDestroy(), "object" == typeof t && "length" in t)
            )
              for (var a = 0; a < t.length; a += 1) t[a] && i.append(t[a]);
            else i.append(t);
            n.loop && e.loopCreate(),
              (n.observer && e.support.observer) || e.update();
          },
          prependSlide: function (t) {
            var e = this,
              i = e.params,
              n = e.$wrapperEl,
              a = e.activeIndex;
            i.loop && e.loopDestroy();
            var r = a + 1;
            if ("object" == typeof t && "length" in t) {
              for (var s = 0; s < t.length; s += 1) t[s] && n.prepend(t[s]);
              r = a + t.length;
            } else n.prepend(t);
            i.loop && e.loopCreate(),
              (i.observer && e.support.observer) || e.update(),
              e.slideTo(r, 0, !1);
          },
          addSlide: function (t, e) {
            var i = this,
              n = i.$wrapperEl,
              a = i.params,
              r = i.activeIndex;
            a.loop &&
              ((r -= i.loopedSlides),
              i.loopDestroy(),
              (i.slides = n.children("." + a.slideClass)));
            var s = i.slides.length;
            if (t <= 0) i.prependSlide(e);
            else if (t >= s) i.appendSlide(e);
            else {
              for (
                var o = r > t ? r + 1 : r, l = [], u = s - 1;
                u >= t;
                u -= 1
              ) {
                var c = i.slides.eq(u);
                c.remove(), l.unshift(c);
              }
              if ("object" == typeof e && "length" in e) {
                for (var d = 0; d < e.length; d += 1) e[d] && n.append(e[d]);
                o = r > t ? r + e.length : r;
              } else n.append(e);
              for (var h = 0; h < l.length; h += 1) n.append(l[h]);
              a.loop && i.loopCreate(),
                (a.observer && i.support.observer) || i.update(),
                a.loop
                  ? i.slideTo(o + i.loopedSlides, 0, !1)
                  : i.slideTo(o, 0, !1);
            }
          },
          removeSlide: function (t) {
            var e = this,
              i = e.params,
              n = e.$wrapperEl,
              a = e.activeIndex;
            i.loop &&
              ((a -= e.loopedSlides),
              e.loopDestroy(),
              (e.slides = n.children("." + i.slideClass)));
            var r,
              s = a;
            if ("object" == typeof t && "length" in t) {
              for (var o = 0; o < t.length; o += 1)
                (r = t[o]),
                  e.slides[r] && e.slides.eq(r).remove(),
                  r < s && (s -= 1);
              s = Math.max(s, 0);
            } else
              (r = t),
                e.slides[r] && e.slides.eq(r).remove(),
                r < s && (s -= 1),
                (s = Math.max(s, 0));
            i.loop && e.loopCreate(),
              (i.observer && e.support.observer) || e.update(),
              i.loop
                ? e.slideTo(s + e.loopedSlides, 0, !1)
                : e.slideTo(s, 0, !1);
          },
          removeAllSlides: function () {
            for (var t = [], e = 0; e < this.slides.length; e += 1) t.push(e);
            this.removeSlide(t);
          },
        },
        events: {
          attachEvents: function () {
            var t = this,
              e = r(),
              i = t.params,
              n = t.touchEvents,
              a = t.el,
              s = t.wrapperEl,
              o = t.device,
              l = t.support;
            (t.onTouchStart = N.bind(t)),
              (t.onTouchMove = B.bind(t)),
              (t.onTouchEnd = j.bind(t)),
              i.cssMode && (t.onScroll = $.bind(t)),
              (t.onClick = R.bind(t));
            var u = !!i.nested;
            if (!l.touch && l.pointerEvents)
              a.addEventListener(n.start, t.onTouchStart, !1),
                e.addEventListener(n.move, t.onTouchMove, u),
                e.addEventListener(n.end, t.onTouchEnd, !1);
            else {
              if (l.touch) {
                var c = !(
                  "touchstart" !== n.start ||
                  !l.passiveListener ||
                  !i.passiveListeners
                ) && { passive: !0, capture: !1 };
                a.addEventListener(n.start, t.onTouchStart, c),
                  a.addEventListener(
                    n.move,
                    t.onTouchMove,
                    l.passiveListener ? { passive: !1, capture: u } : u
                  ),
                  a.addEventListener(n.end, t.onTouchEnd, c),
                  n.cancel && a.addEventListener(n.cancel, t.onTouchEnd, c),
                  H || (e.addEventListener("touchstart", V), (H = !0));
              }
              ((i.simulateTouch && !o.ios && !o.android) ||
                (i.simulateTouch && !l.touch && o.ios)) &&
                (a.addEventListener("mousedown", t.onTouchStart, !1),
                e.addEventListener("mousemove", t.onTouchMove, u),
                e.addEventListener("mouseup", t.onTouchEnd, !1));
            }
            (i.preventClicks || i.preventClicksPropagation) &&
              a.addEventListener("click", t.onClick, !0),
              i.cssMode && s.addEventListener("scroll", t.onScroll),
              i.updateOnWindowResize
                ? t.on(
                    o.ios || o.android
                      ? "resize orientationchange observerUpdate"
                      : "resize observerUpdate",
                    F,
                    !0
                  )
                : t.on("observerUpdate", F, !0);
          },
          detachEvents: function () {
            var t = this,
              e = r(),
              i = t.params,
              n = t.touchEvents,
              a = t.el,
              s = t.wrapperEl,
              o = t.device,
              l = t.support,
              u = !!i.nested;
            if (!l.touch && l.pointerEvents)
              a.removeEventListener(n.start, t.onTouchStart, !1),
                e.removeEventListener(n.move, t.onTouchMove, u),
                e.removeEventListener(n.end, t.onTouchEnd, !1);
            else {
              if (l.touch) {
                var c = !(
                  "onTouchStart" !== n.start ||
                  !l.passiveListener ||
                  !i.passiveListeners
                ) && { passive: !0, capture: !1 };
                a.removeEventListener(n.start, t.onTouchStart, c),
                  a.removeEventListener(n.move, t.onTouchMove, u),
                  a.removeEventListener(n.end, t.onTouchEnd, c),
                  n.cancel && a.removeEventListener(n.cancel, t.onTouchEnd, c);
              }
              ((i.simulateTouch && !o.ios && !o.android) ||
                (i.simulateTouch && !l.touch && o.ios)) &&
                (a.removeEventListener("mousedown", t.onTouchStart, !1),
                e.removeEventListener("mousemove", t.onTouchMove, u),
                e.removeEventListener("mouseup", t.onTouchEnd, !1));
            }
            (i.preventClicks || i.preventClicksPropagation) &&
              a.removeEventListener("click", t.onClick, !0),
              i.cssMode && s.removeEventListener("scroll", t.onScroll),
              t.off(
                o.ios || o.android
                  ? "resize orientationchange observerUpdate"
                  : "resize observerUpdate",
                F
              );
          },
        },
        breakpoints: {
          setBreakpoint: function () {
            var t = this,
              e = t.activeIndex,
              i = t.initialized,
              n = t.loopedSlides,
              a = void 0 === n ? 0 : n,
              r = t.params,
              s = t.$el,
              o = r.breakpoints;
            if (o && (!o || 0 !== Object.keys(o).length)) {
              var l = t.getBreakpoint(o, t.params.breakpointsBase, t.el);
              if (l && t.currentBreakpoint !== l) {
                var u = l in o ? o[l] : void 0;
                u &&
                  [
                    "slidesPerView",
                    "spaceBetween",
                    "slidesPerGroup",
                    "slidesPerGroupSkip",
                    "slidesPerColumn",
                  ].forEach(function (t) {
                    var e = u[t];
                    void 0 !== e &&
                      (u[t] =
                        "slidesPerView" !== t || ("AUTO" !== e && "auto" !== e)
                          ? "slidesPerView" === t
                            ? parseFloat(e)
                            : parseInt(e, 10)
                          : "auto");
                  });
                var c = u || t.originalParams,
                  d = r.slidesPerColumn > 1,
                  h = c.slidesPerColumn > 1,
                  p = r.enabled;
                d && !h
                  ? (s.removeClass(
                      r.containerModifierClass +
                        "multirow " +
                        r.containerModifierClass +
                        "multirow-column"
                    ),
                    t.emitContainerClasses())
                  : !d &&
                    h &&
                    (s.addClass(r.containerModifierClass + "multirow"),
                    ((c.slidesPerColumnFill &&
                      "column" === c.slidesPerColumnFill) ||
                      (!c.slidesPerColumnFill &&
                        "column" === r.slidesPerColumnFill)) &&
                      s.addClass(r.containerModifierClass + "multirow-column"),
                    t.emitContainerClasses());
                var f = c.direction && c.direction !== r.direction,
                  m = r.loop && (c.slidesPerView !== r.slidesPerView || f);
                f && i && t.changeDirection(), E(t.params, c);
                var g = t.params.enabled;
                E(t, {
                  allowTouchMove: t.params.allowTouchMove,
                  allowSlideNext: t.params.allowSlideNext,
                  allowSlidePrev: t.params.allowSlidePrev,
                }),
                  p && !g ? t.disable() : !p && g && t.enable(),
                  (t.currentBreakpoint = l),
                  t.emit("_beforeBreakpoint", c),
                  m &&
                    i &&
                    (t.loopDestroy(),
                    t.loopCreate(),
                    t.updateSlides(),
                    t.slideTo(e - a + t.loopedSlides, 0, !1)),
                  t.emit("breakpoint", c);
              }
            }
          },
          getBreakpoint: function (t, e, i) {
            if (
              (void 0 === e && (e = "window"), t && ("container" !== e || i))
            ) {
              var n = !1,
                a = o(),
                r = "window" === e ? a.innerHeight : i.clientHeight,
                s = Object.keys(t).map(function (t) {
                  if ("string" == typeof t && 0 === t.indexOf("@")) {
                    var e = parseFloat(t.substr(1));
                    return { value: r * e, point: t };
                  }
                  return { value: t, point: t };
                });
              s.sort(function (t, e) {
                return parseInt(t.value, 10) - parseInt(e.value, 10);
              });
              for (var l = 0; l < s.length; l += 1) {
                var u = s[l],
                  c = u.point,
                  d = u.value;
                "window" === e
                  ? a.matchMedia("(min-width: " + d + "px)").matches && (n = c)
                  : d <= i.clientWidth && (n = c);
              }
              return n || "max";
            }
          },
        },
        checkOverflow: {
          checkOverflow: function () {
            var t = this,
              e = t.params,
              i = t.isLocked,
              n =
                t.slides.length > 0 &&
                e.slidesOffsetBefore +
                  e.spaceBetween * (t.slides.length - 1) +
                  t.slides[0].offsetWidth * t.slides.length;
            e.slidesOffsetBefore && e.slidesOffsetAfter && n
              ? (t.isLocked = n <= t.size)
              : (t.isLocked = 1 === t.snapGrid.length),
              (t.allowSlideNext = !t.isLocked),
              (t.allowSlidePrev = !t.isLocked),
              i !== t.isLocked && t.emit(t.isLocked ? "lock" : "unlock"),
              i &&
                i !== t.isLocked &&
                ((t.isEnd = !1), t.navigation && t.navigation.update());
          },
        },
        classes: {
          addClasses: function () {
            var t,
              e,
              i,
              n = this,
              a = n.classNames,
              r = n.params,
              s = n.rtl,
              o = n.$el,
              l = n.device,
              u = n.support,
              c =
                ((t = [
                  "initialized",
                  r.direction,
                  { "pointer-events": u.pointerEvents && !u.touch },
                  { "free-mode": r.freeMode },
                  { autoheight: r.autoHeight },
                  { rtl: s },
                  { multirow: r.slidesPerColumn > 1 },
                  {
                    "multirow-column":
                      r.slidesPerColumn > 1 &&
                      "column" === r.slidesPerColumnFill,
                  },
                  { android: l.android },
                  { ios: l.ios },
                  { "css-mode": r.cssMode },
                ]),
                (e = r.containerModifierClass),
                (i = []),
                t.forEach(function (t) {
                  "object" == typeof t
                    ? Object.keys(t).forEach(function (n) {
                        t[n] && i.push(e + n);
                      })
                    : "string" == typeof t && i.push(e + t);
                }),
                i);
            a.push.apply(a, c),
              o.addClass([].concat(a).join(" ")),
              n.emitContainerClasses();
          },
          removeClasses: function () {
            var t = this,
              e = t.$el,
              i = t.classNames;
            e.removeClass(i.join(" ")), t.emitContainerClasses();
          },
        },
        images: {
          loadImage: function (t, e, i, n, a, r) {
            var s,
              l = o();
            function u() {
              r && r();
            }
            g(t).parent("picture")[0] || (t.complete && a)
              ? u()
              : e
              ? (((s = new l.Image()).onload = u),
                (s.onerror = u),
                n && (s.sizes = n),
                i && (s.srcset = i),
                e && (s.src = e))
              : u();
          },
          preloadImages: function () {
            var t = this;
            function e() {
              null != t &&
                t &&
                !t.destroyed &&
                (void 0 !== t.imagesLoaded && (t.imagesLoaded += 1),
                t.imagesLoaded === t.imagesToLoad.length &&
                  (t.params.updateOnImagesReady && t.update(),
                  t.emit("imagesReady")));
            }
            t.imagesToLoad = t.$el.find("img");
            for (var i = 0; i < t.imagesToLoad.length; i += 1) {
              var n = t.imagesToLoad[i];
              t.loadImage(
                n,
                n.currentSrc || n.getAttribute("src"),
                n.srcset || n.getAttribute("srcset"),
                n.sizes || n.getAttribute("sizes"),
                !0,
                e
              );
            }
          },
        },
      },
      G = {},
      U = (function () {
        function e() {
          for (
            var t, i, n = arguments.length, a = new Array(n), r = 0;
            r < n;
            r++
          )
            a[r] = arguments[r];
          if (
            (1 === a.length &&
            a[0].constructor &&
            "Object" === Object.prototype.toString.call(a[0]).slice(8, -1)
              ? (i = a[0])
              : ((t = a[0]), (i = a[1])),
            i || (i = {}),
            (i = E({}, i)),
            t && !i.el && (i.el = t),
            i.el && g(i.el).length > 1)
          ) {
            var s = [];
            return (
              g(i.el).each(function (t) {
                var n = E({}, i, { el: t });
                s.push(new e(n));
              }),
              s
            );
          }
          var o = this;
          (o.__swiper__ = !0),
            (o.support = M()),
            (o.device = k({ userAgent: i.userAgent })),
            (o.browser = A()),
            (o.eventsListeners = {}),
            (o.eventsAnyListeners = []),
            void 0 === o.modules && (o.modules = {}),
            Object.keys(o.modules).forEach(function (t) {
              var e = o.modules[t];
              if (e.params) {
                var n = Object.keys(e.params)[0],
                  a = e.params[n];
                if ("object" != typeof a || null === a) return;
                if (
                  (["navigation", "pagination", "scrollbar"].indexOf(n) >= 0 &&
                    !0 === i[n] &&
                    (i[n] = { auto: !0 }),
                  !(n in i) || !("enabled" in a))
                )
                  return;
                !0 === i[n] && (i[n] = { enabled: !0 }),
                  "object" != typeof i[n] ||
                    "enabled" in i[n] ||
                    (i[n].enabled = !0),
                  i[n] || (i[n] = { enabled: !1 });
              }
            });
          var l,
            u,
            c = E({}, W);
          return (
            o.useParams(c),
            (o.params = E({}, c, G, i)),
            (o.originalParams = E({}, o.params)),
            (o.passedParams = E({}, i)),
            o.params &&
              o.params.on &&
              Object.keys(o.params.on).forEach(function (t) {
                o.on(t, o.params.on[t]);
              }),
            o.params && o.params.onAny && o.onAny(o.params.onAny),
            (o.$ = g),
            E(o, {
              enabled: o.params.enabled,
              el: t,
              classNames: [],
              slides: g(),
              slidesGrid: [],
              snapGrid: [],
              slidesSizesGrid: [],
              isHorizontal: function () {
                return "horizontal" === o.params.direction;
              },
              isVertical: function () {
                return "vertical" === o.params.direction;
              },
              activeIndex: 0,
              realIndex: 0,
              isBeginning: !0,
              isEnd: !1,
              translate: 0,
              previousTranslate: 0,
              progress: 0,
              velocity: 0,
              animating: !1,
              allowSlideNext: o.params.allowSlideNext,
              allowSlidePrev: o.params.allowSlidePrev,
              touchEvents:
                ((l = ["touchstart", "touchmove", "touchend", "touchcancel"]),
                (u = ["mousedown", "mousemove", "mouseup"]),
                o.support.pointerEvents &&
                  (u = ["pointerdown", "pointermove", "pointerup"]),
                (o.touchEventsTouch = {
                  start: l[0],
                  move: l[1],
                  end: l[2],
                  cancel: l[3],
                }),
                (o.touchEventsDesktop = { start: u[0], move: u[1], end: u[2] }),
                o.support.touch || !o.params.simulateTouch
                  ? o.touchEventsTouch
                  : o.touchEventsDesktop),
              touchEventsData: {
                isTouched: void 0,
                isMoved: void 0,
                allowTouchCallbacks: void 0,
                touchStartTime: void 0,
                isScrolling: void 0,
                currentTranslate: void 0,
                startTranslate: void 0,
                allowThresholdMove: void 0,
                focusableElements: o.params.focusableElements,
                lastClickTime: x(),
                clickTimeout: void 0,
                velocities: [],
                allowMomentumBounce: void 0,
                isTouchEvent: void 0,
                startMoving: void 0,
              },
              allowClick: !0,
              allowTouchMove: o.params.allowTouchMove,
              touches: {
                startX: 0,
                startY: 0,
                currentX: 0,
                currentY: 0,
                diff: 0,
              },
              imagesToLoad: [],
              imagesLoaded: 0,
            }),
            o.useModules(),
            o.emit("_swiper"),
            o.params.init && o.init(),
            o
          );
        }
        var i,
          n,
          a = e.prototype;
        return (
          (a.enable = function () {
            var t = this;
            t.enabled ||
              ((t.enabled = !0),
              t.params.grabCursor && t.setGrabCursor(),
              t.emit("enable"));
          }),
          (a.disable = function () {
            var t = this;
            t.enabled &&
              ((t.enabled = !1),
              t.params.grabCursor && t.unsetGrabCursor(),
              t.emit("disable"));
          }),
          (a.setProgress = function (t, e) {
            var i = this;
            t = Math.min(Math.max(t, 0), 1);
            var n = i.minTranslate(),
              a = (i.maxTranslate() - n) * t + n;
            i.translateTo(a, void 0 === e ? 0 : e),
              i.updateActiveIndex(),
              i.updateSlidesClasses();
          }),
          (a.emitContainerClasses = function () {
            var t = this;
            if (t.params._emitClasses && t.el) {
              var e = t.el.className.split(" ").filter(function (e) {
                return (
                  0 === e.indexOf("swiper-container") ||
                  0 === e.indexOf(t.params.containerModifierClass)
                );
              });
              t.emit("_containerClasses", e.join(" "));
            }
          }),
          (a.getSlideClasses = function (t) {
            var e = this;
            return t.className
              .split(" ")
              .filter(function (t) {
                return (
                  0 === t.indexOf("swiper-slide") ||
                  0 === t.indexOf(e.params.slideClass)
                );
              })
              .join(" ");
          }),
          (a.emitSlidesClasses = function () {
            var t = this;
            if (t.params._emitClasses && t.el) {
              var e = [];
              t.slides.each(function (i) {
                var n = t.getSlideClasses(i);
                e.push({ slideEl: i, classNames: n }),
                  t.emit("_slideClass", i, n);
              }),
                t.emit("_slideClasses", e);
            }
          }),
          (a.slidesPerViewDynamic = function () {
            var t = this,
              e = t.params,
              i = t.slides,
              n = t.slidesGrid,
              a = t.size,
              r = t.activeIndex,
              s = 1;
            if (e.centeredSlides) {
              for (
                var o, l = i[r].swiperSlideSize, u = r + 1;
                u < i.length;
                u += 1
              )
                i[u] &&
                  !o &&
                  ((s += 1), (l += i[u].swiperSlideSize) > a && (o = !0));
              for (var c = r - 1; c >= 0; c -= 1)
                i[c] &&
                  !o &&
                  ((s += 1), (l += i[c].swiperSlideSize) > a && (o = !0));
            } else
              for (var d = r + 1; d < i.length; d += 1)
                n[d] - n[r] < a && (s += 1);
            return s;
          }),
          (a.update = function () {
            var t = this;
            if (t && !t.destroyed) {
              var e = t.snapGrid,
                i = t.params;
              i.breakpoints && t.setBreakpoint(),
                t.updateSize(),
                t.updateSlides(),
                t.updateProgress(),
                t.updateSlidesClasses(),
                t.params.freeMode
                  ? (n(), t.params.autoHeight && t.updateAutoHeight())
                  : (("auto" === t.params.slidesPerView ||
                      t.params.slidesPerView > 1) &&
                    t.isEnd &&
                    !t.params.centeredSlides
                      ? t.slideTo(t.slides.length - 1, 0, !1, !0)
                      : t.slideTo(t.activeIndex, 0, !1, !0)) || n(),
                i.watchOverflow && e !== t.snapGrid && t.checkOverflow(),
                t.emit("update");
            }
            function n() {
              var e = t.rtlTranslate ? -1 * t.translate : t.translate,
                i = Math.min(Math.max(e, t.maxTranslate()), t.minTranslate());
              t.setTranslate(i), t.updateActiveIndex(), t.updateSlidesClasses();
            }
          }),
          (a.changeDirection = function (t, e) {
            void 0 === e && (e = !0);
            var i = this,
              n = i.params.direction;
            return (
              t || (t = "horizontal" === n ? "vertical" : "horizontal"),
              t === n ||
                ("horizontal" !== t && "vertical" !== t) ||
                (i.$el
                  .removeClass("" + i.params.containerModifierClass + n)
                  .addClass("" + i.params.containerModifierClass + t),
                i.emitContainerClasses(),
                (i.params.direction = t),
                i.slides.each(function (e) {
                  "vertical" === t
                    ? (e.style.width = "")
                    : (e.style.height = "");
                }),
                i.emit("changeDirection"),
                e && i.update()),
              i
            );
          }),
          (a.mount = function (t) {
            var e = this;
            if (e.mounted) return !0;
            var i = g(t || e.params.el);
            if (!(t = i[0])) return !1;
            t.swiper = e;
            var n = function () {
                return (
                  "." +
                  (e.params.wrapperClass || "").trim().split(" ").join(".")
                );
              },
              a = (function () {
                if (t && t.shadowRoot && t.shadowRoot.querySelector) {
                  var e = g(t.shadowRoot.querySelector(n()));
                  return (
                    (e.children = function (t) {
                      return i.children(t);
                    }),
                    e
                  );
                }
                return i.children(n());
              })();
            if (0 === a.length && e.params.createElements) {
              var s = r().createElement("div");
              (a = g(s)),
                (s.className = e.params.wrapperClass),
                i.append(s),
                i.children("." + e.params.slideClass).each(function (t) {
                  a.append(t);
                });
            }
            return (
              E(e, {
                $el: i,
                el: t,
                $wrapperEl: a,
                wrapperEl: a[0],
                mounted: !0,
                rtl:
                  "rtl" === t.dir.toLowerCase() || "rtl" === i.css("direction"),
                rtlTranslate:
                  "horizontal" === e.params.direction &&
                  ("rtl" === t.dir.toLowerCase() ||
                    "rtl" === i.css("direction")),
                wrongRTL: "-webkit-box" === a.css("display"),
              }),
              !0
            );
          }),
          (a.init = function (t) {
            var e = this;
            return (
              e.initialized ||
                !1 === e.mount(t) ||
                (e.emit("beforeInit"),
                e.params.breakpoints && e.setBreakpoint(),
                e.addClasses(),
                e.params.loop && e.loopCreate(),
                e.updateSize(),
                e.updateSlides(),
                e.params.watchOverflow && e.checkOverflow(),
                e.params.grabCursor && e.enabled && e.setGrabCursor(),
                e.params.preloadImages && e.preloadImages(),
                e.params.loop
                  ? e.slideTo(
                      e.params.initialSlide + e.loopedSlides,
                      0,
                      e.params.runCallbacksOnInit,
                      !1,
                      !0
                    )
                  : e.slideTo(
                      e.params.initialSlide,
                      0,
                      e.params.runCallbacksOnInit,
                      !1,
                      !0
                    ),
                e.attachEvents(),
                (e.initialized = !0),
                e.emit("init"),
                e.emit("afterInit")),
              e
            );
          }),
          (a.destroy = function (t, e) {
            void 0 === t && (t = !0), void 0 === e && (e = !0);
            var i,
              n = this,
              a = n.params,
              r = n.$el,
              s = n.$wrapperEl,
              o = n.slides;
            return (
              void 0 === n.params ||
                n.destroyed ||
                (n.emit("beforeDestroy"),
                (n.initialized = !1),
                n.detachEvents(),
                a.loop && n.loopDestroy(),
                e &&
                  (n.removeClasses(),
                  r.removeAttr("style"),
                  s.removeAttr("style"),
                  o &&
                    o.length &&
                    o
                      .removeClass(
                        [
                          a.slideVisibleClass,
                          a.slideActiveClass,
                          a.slideNextClass,
                          a.slidePrevClass,
                        ].join(" ")
                      )
                      .removeAttr("style")
                      .removeAttr("data-swiper-slide-index")),
                n.emit("destroy"),
                Object.keys(n.eventsListeners).forEach(function (t) {
                  n.off(t);
                }),
                !1 !== t &&
                  ((n.$el[0].swiper = null),
                  (i = n),
                  Object.keys(i).forEach(function (t) {
                    try {
                      i[t] = null;
                    } catch (t) {}
                    try {
                      delete i[t];
                    } catch (t) {}
                  })),
                (n.destroyed = !0)),
              null
            );
          }),
          (e.extendDefaults = function (t) {
            E(G, t);
          }),
          (e.installModule = function (t) {
            e.prototype.modules || (e.prototype.modules = {});
            var i =
              t.name || Object.keys(e.prototype.modules).length + "_" + x();
            e.prototype.modules[i] = t;
          }),
          (e.use = function (t) {
            return Array.isArray(t)
              ? (t.forEach(function (t) {
                  return e.installModule(t);
                }),
                e)
              : (e.installModule(t), e);
          }),
          (i = e),
          (n = [
            {
              key: "extendedDefaults",
              get: function () {
                return G;
              },
            },
            {
              key: "defaults",
              get: function () {
                return W;
              },
            },
          ]),
          null && t(i.prototype, null),
          n && t(i, n),
          e
        );
      })();
    Object.keys(Z).forEach(function (t) {
      Object.keys(Z[t]).forEach(function (e) {
        U.prototype[e] = Z[t][e];
      });
    }),
      U.use([I, O]);
    var Y = {
        update: function (t) {
          var e = this,
            i = e.params,
            n = i.slidesPerView,
            a = i.slidesPerGroup,
            r = i.centeredSlides,
            s = e.params.virtual,
            o = s.addSlidesBefore,
            l = s.addSlidesAfter,
            u = e.virtual,
            c = u.from,
            d = u.to,
            h = u.slides,
            p = u.slidesGrid,
            f = u.renderSlide,
            m = u.offset;
          e.updateActiveIndex();
          var g,
            v,
            _,
            y = e.activeIndex || 0;
          (g = e.rtlTranslate ? "right" : e.isHorizontal() ? "left" : "top"),
            r
              ? ((v = Math.floor(n / 2) + a + l),
                (_ = Math.floor(n / 2) + a + o))
              : ((v = n + (a - 1) + l), (_ = a + o));
          var b = Math.max((y || 0) - _, 0),
            w = Math.min((y || 0) + v, h.length - 1),
            x = (e.slidesGrid[b] || 0) - (e.slidesGrid[0] || 0);
          function S() {
            e.updateSlides(),
              e.updateProgress(),
              e.updateSlidesClasses(),
              e.lazy && e.params.lazy.enabled && e.lazy.load();
          }
          if (
            (E(e.virtual, {
              from: b,
              to: w,
              offset: x,
              slidesGrid: e.slidesGrid,
            }),
            c === b && d === w && !t)
          )
            return (
              e.slidesGrid !== p && x !== m && e.slides.css(g, x + "px"),
              void e.updateProgress()
            );
          if (e.params.virtual.renderExternal)
            return (
              e.params.virtual.renderExternal.call(e, {
                offset: x,
                from: b,
                to: w,
                slides: (function () {
                  for (var t = [], e = b; e <= w; e += 1) t.push(h[e]);
                  return t;
                })(),
              }),
              void (e.params.virtual.renderExternalUpdate && S())
            );
          var T = [],
            C = [];
          if (t) e.$wrapperEl.find("." + e.params.slideClass).remove();
          else
            for (var P = c; P <= d; P += 1)
              (P < b || P > w) &&
                e.$wrapperEl
                  .find(
                    "." +
                      e.params.slideClass +
                      '[data-swiper-slide-index="' +
                      P +
                      '"]'
                  )
                  .remove();
          for (var L = 0; L < h.length; L += 1)
            L >= b &&
              L <= w &&
              (void 0 === d || t
                ? C.push(L)
                : (L > d && C.push(L), L < c && T.push(L)));
          C.forEach(function (t) {
            e.$wrapperEl.append(f(h[t], t));
          }),
            T.sort(function (t, e) {
              return e - t;
            }).forEach(function (t) {
              e.$wrapperEl.prepend(f(h[t], t));
            }),
            e.$wrapperEl.children(".swiper-slide").css(g, x + "px"),
            S();
        },
        renderSlide: function (t, e) {
          var i = this,
            n = i.params.virtual;
          if (n.cache && i.virtual.cache[e]) return i.virtual.cache[e];
          var a = n.renderSlide
            ? g(n.renderSlide.call(i, t, e))
            : g(
                '<div class="' +
                  i.params.slideClass +
                  '" data-swiper-slide-index="' +
                  e +
                  '">' +
                  t +
                  "</div>"
              );
          return (
            a.attr("data-swiper-slide-index") ||
              a.attr("data-swiper-slide-index", e),
            n.cache && (i.virtual.cache[e] = a),
            a
          );
        },
        appendSlide: function (t) {
          var e = this;
          if ("object" == typeof t && "length" in t)
            for (var i = 0; i < t.length; i += 1)
              t[i] && e.virtual.slides.push(t[i]);
          else e.virtual.slides.push(t);
          e.virtual.update(!0);
        },
        prependSlide: function (t) {
          var e = this,
            i = e.activeIndex,
            n = i + 1,
            a = 1;
          if (Array.isArray(t)) {
            for (var r = 0; r < t.length; r += 1)
              t[r] && e.virtual.slides.unshift(t[r]);
            (n = i + t.length), (a = t.length);
          } else e.virtual.slides.unshift(t);
          if (e.params.virtual.cache) {
            var s = e.virtual.cache,
              o = {};
            Object.keys(s).forEach(function (t) {
              var e = s[t],
                i = e.attr("data-swiper-slide-index");
              i && e.attr("data-swiper-slide-index", parseInt(i, 10) + 1),
                (o[parseInt(t, 10) + a] = e);
            }),
              (e.virtual.cache = o);
          }
          e.virtual.update(!0), e.slideTo(n, 0);
        },
        removeSlide: function (t) {
          var e = this;
          if (null != t) {
            var i = e.activeIndex;
            if (Array.isArray(t))
              for (var n = t.length - 1; n >= 0; n -= 1)
                e.virtual.slides.splice(t[n], 1),
                  e.params.virtual.cache && delete e.virtual.cache[t[n]],
                  t[n] < i && (i -= 1),
                  (i = Math.max(i, 0));
            else
              e.virtual.slides.splice(t, 1),
                e.params.virtual.cache && delete e.virtual.cache[t],
                t < i && (i -= 1),
                (i = Math.max(i, 0));
            e.virtual.update(!0), e.slideTo(i, 0);
          }
        },
        removeAllSlides: function () {
          var t = this;
          (t.virtual.slides = []),
            t.params.virtual.cache && (t.virtual.cache = {}),
            t.virtual.update(!0),
            t.slideTo(0, 0);
        },
      },
      q = {
        name: "virtual",
        params: {
          virtual: {
            enabled: !1,
            slides: [],
            cache: !0,
            renderSlide: null,
            renderExternal: null,
            renderExternalUpdate: !0,
            addSlidesBefore: 0,
            addSlidesAfter: 0,
          },
        },
        create: function () {
          P(this, {
            virtual: e({}, Y, {
              slides: this.params.virtual.slides,
              cache: {},
            }),
          });
        },
        on: {
          beforeInit: function (t) {
            if (t.params.virtual.enabled) {
              t.classNames.push(t.params.containerModifierClass + "virtual");
              var e = { watchSlidesProgress: !0 };
              E(t.params, e),
                E(t.originalParams, e),
                t.params.initialSlide || t.virtual.update();
            }
          },
          setTranslate: function (t) {
            t.params.virtual.enabled && t.virtual.update();
          },
        },
      },
      X = {
        handle: function (t) {
          var e = this;
          if (e.enabled) {
            var i = o(),
              n = r(),
              a = e.rtlTranslate,
              s = t;
            s.originalEvent && (s = s.originalEvent);
            var l = s.keyCode || s.charCode,
              u = e.params.keyboard.pageUpDown,
              c = u && 33 === l,
              d = u && 34 === l,
              h = 37 === l,
              p = 39 === l,
              f = 38 === l,
              m = 40 === l;
            if (
              !e.allowSlideNext &&
              ((e.isHorizontal() && p) || (e.isVertical() && m) || d)
            )
              return !1;
            if (
              !e.allowSlidePrev &&
              ((e.isHorizontal() && h) || (e.isVertical() && f) || c)
            )
              return !1;
            if (
              !(
                s.shiftKey ||
                s.altKey ||
                s.ctrlKey ||
                s.metaKey ||
                (n.activeElement &&
                  n.activeElement.nodeName &&
                  ("input" === n.activeElement.nodeName.toLowerCase() ||
                    "textarea" === n.activeElement.nodeName.toLowerCase()))
              )
            ) {
              if (
                e.params.keyboard.onlyInViewport &&
                (c || d || h || p || f || m)
              ) {
                var g = !1;
                if (
                  e.$el.parents("." + e.params.slideClass).length > 0 &&
                  0 === e.$el.parents("." + e.params.slideActiveClass).length
                )
                  return;
                var v = e.$el,
                  _ = v[0].clientWidth,
                  y = v[0].clientHeight,
                  b = i.innerWidth,
                  w = i.innerHeight,
                  x = e.$el.offset();
                a && (x.left -= e.$el[0].scrollLeft);
                for (
                  var S = [
                      [x.left, x.top],
                      [x.left + _, x.top],
                      [x.left, x.top + y],
                      [x.left + _, x.top + y],
                    ],
                    T = 0;
                  T < S.length;
                  T += 1
                ) {
                  var C = S[T];
                  if (C[0] >= 0 && C[0] <= b && C[1] >= 0 && C[1] <= w) {
                    if (0 === C[0] && 0 === C[1]) continue;
                    g = !0;
                  }
                }
                if (!g) return;
              }
              e.isHorizontal()
                ? ((c || d || h || p) &&
                    (s.preventDefault
                      ? s.preventDefault()
                      : (s.returnValue = !1)),
                  (((d || p) && !a) || ((c || h) && a)) && e.slideNext(),
                  (((c || h) && !a) || ((d || p) && a)) && e.slidePrev())
                : ((c || d || f || m) &&
                    (s.preventDefault
                      ? s.preventDefault()
                      : (s.returnValue = !1)),
                  (d || m) && e.slideNext(),
                  (c || f) && e.slidePrev()),
                e.emit("keyPress", l);
            }
          }
        },
        enable: function () {
          var t = this,
            e = r();
          t.keyboard.enabled ||
            (g(e).on("keydown", t.keyboard.handle), (t.keyboard.enabled = !0));
        },
        disable: function () {
          var t = this,
            e = r();
          t.keyboard.enabled &&
            (g(e).off("keydown", t.keyboard.handle), (t.keyboard.enabled = !1));
        },
      },
      K = {
        name: "keyboard",
        params: {
          keyboard: { enabled: !1, onlyInViewport: !0, pageUpDown: !0 },
        },
        create: function () {
          P(this, { keyboard: e({ enabled: !1 }, X) });
        },
        on: {
          init: function (t) {
            t.params.keyboard.enabled && t.keyboard.enable();
          },
          destroy: function (t) {
            t.keyboard.enabled && t.keyboard.disable();
          },
        },
      },
      J = {
        lastScrollTime: x(),
        lastEventBeforeSnap: void 0,
        recentWheelEvents: [],
        event: function () {
          return o().navigator.userAgent.indexOf("firefox") > -1
            ? "DOMMouseScroll"
            : (function () {
                var t = r(),
                  e = "onwheel",
                  i = e in t;
                if (!i) {
                  var n = t.createElement("div");
                  n.setAttribute(e, "return;"),
                    (i = "function" == typeof n.onwheel);
                }
                return (
                  !i &&
                    t.implementation &&
                    t.implementation.hasFeature &&
                    !0 !== t.implementation.hasFeature("", "") &&
                    (i = t.implementation.hasFeature("Events.wheel", "3.0")),
                  i
                );
              })()
            ? "wheel"
            : "mousewheel";
        },
        normalize: function (t) {
          var e = 0,
            i = 0,
            n = 0,
            a = 0;
          return (
            "detail" in t && (i = t.detail),
            "wheelDelta" in t && (i = -t.wheelDelta / 120),
            "wheelDeltaY" in t && (i = -t.wheelDeltaY / 120),
            "wheelDeltaX" in t && (e = -t.wheelDeltaX / 120),
            "axis" in t && t.axis === t.HORIZONTAL_AXIS && ((e = i), (i = 0)),
            (n = 10 * e),
            (a = 10 * i),
            "deltaY" in t && (a = t.deltaY),
            "deltaX" in t && (n = t.deltaX),
            t.shiftKey && !n && ((n = a), (a = 0)),
            (n || a) &&
              t.deltaMode &&
              (1 === t.deltaMode
                ? ((n *= 40), (a *= 40))
                : ((n *= 800), (a *= 800))),
            n && !e && (e = n < 1 ? -1 : 1),
            a && !i && (i = a < 1 ? -1 : 1),
            { spinX: e, spinY: i, pixelX: n, pixelY: a }
          );
        },
        handleMouseEnter: function () {
          this.enabled && (this.mouseEntered = !0);
        },
        handleMouseLeave: function () {
          this.enabled && (this.mouseEntered = !1);
        },
        handle: function (t) {
          var e = t,
            i = this;
          if (i.enabled) {
            var n = i.params.mousewheel;
            i.params.cssMode && e.preventDefault();
            var a = i.$el;
            if (
              ("container" !== i.params.mousewheel.eventsTarget &&
                (a = g(i.params.mousewheel.eventsTarget)),
              !i.mouseEntered && !a[0].contains(e.target) && !n.releaseOnEdges)
            )
              return !0;
            e.originalEvent && (e = e.originalEvent);
            var r = 0,
              s = i.rtlTranslate ? -1 : 1,
              o = J.normalize(e);
            if (n.forceToAxis)
              if (i.isHorizontal()) {
                if (!(Math.abs(o.pixelX) > Math.abs(o.pixelY))) return !0;
                r = -o.pixelX * s;
              } else {
                if (!(Math.abs(o.pixelY) > Math.abs(o.pixelX))) return !0;
                r = -o.pixelY;
              }
            else
              r =
                Math.abs(o.pixelX) > Math.abs(o.pixelY)
                  ? -o.pixelX * s
                  : -o.pixelY;
            if (0 === r) return !0;
            n.invert && (r = -r);
            var l = i.getTranslate() + r * n.sensitivity;
            if (
              (l >= i.minTranslate() && (l = i.minTranslate()),
              l <= i.maxTranslate() && (l = i.maxTranslate()),
              (!!i.params.loop ||
                !(l === i.minTranslate() || l === i.maxTranslate())) &&
                i.params.nested &&
                e.stopPropagation(),
              i.params.freeMode)
            ) {
              var u = {
                  time: x(),
                  delta: Math.abs(r),
                  direction: Math.sign(r),
                },
                c = i.mousewheel.lastEventBeforeSnap,
                d =
                  c &&
                  u.time < c.time + 500 &&
                  u.delta <= c.delta &&
                  u.direction === c.direction;
              if (!d) {
                (i.mousewheel.lastEventBeforeSnap = void 0),
                  i.params.loop && i.loopFix();
                var h = i.getTranslate() + r * n.sensitivity,
                  p = i.isBeginning,
                  f = i.isEnd;
                if (
                  (h >= i.minTranslate() && (h = i.minTranslate()),
                  h <= i.maxTranslate() && (h = i.maxTranslate()),
                  i.setTransition(0),
                  i.setTranslate(h),
                  i.updateProgress(),
                  i.updateActiveIndex(),
                  i.updateSlidesClasses(),
                  ((!p && i.isBeginning) || (!f && i.isEnd)) &&
                    i.updateSlidesClasses(),
                  i.params.freeModeSticky)
                ) {
                  clearTimeout(i.mousewheel.timeout),
                    (i.mousewheel.timeout = void 0);
                  var m = i.mousewheel.recentWheelEvents;
                  m.length >= 15 && m.shift();
                  var v = m.length ? m[m.length - 1] : void 0,
                    _ = m[0];
                  if (
                    (m.push(u),
                    v && (u.delta > v.delta || u.direction !== v.direction))
                  )
                    m.splice(0);
                  else if (
                    m.length >= 15 &&
                    u.time - _.time < 500 &&
                    _.delta - u.delta >= 1 &&
                    u.delta <= 6
                  ) {
                    var y = r > 0 ? 0.8 : 0.2;
                    (i.mousewheel.lastEventBeforeSnap = u),
                      m.splice(0),
                      (i.mousewheel.timeout = w(function () {
                        i.slideToClosest(i.params.speed, !0, void 0, y);
                      }, 0));
                  }
                  i.mousewheel.timeout ||
                    (i.mousewheel.timeout = w(function () {
                      (i.mousewheel.lastEventBeforeSnap = u),
                        m.splice(0),
                        i.slideToClosest(i.params.speed, !0, void 0, 0.5);
                    }, 500));
                }
                if (
                  (d || i.emit("scroll", e),
                  i.params.autoplay &&
                    i.params.autoplayDisableOnInteraction &&
                    i.autoplay.stop(),
                  h === i.minTranslate() || h === i.maxTranslate())
                )
                  return !0;
              }
            } else {
              var b = {
                  time: x(),
                  delta: Math.abs(r),
                  direction: Math.sign(r),
                  raw: t,
                },
                S = i.mousewheel.recentWheelEvents;
              S.length >= 2 && S.shift();
              var T = S.length ? S[S.length - 1] : void 0;
              if (
                (S.push(b),
                T
                  ? (b.direction !== T.direction ||
                      b.delta > T.delta ||
                      b.time > T.time + 150) &&
                    i.mousewheel.animateSlider(b)
                  : i.mousewheel.animateSlider(b),
                i.mousewheel.releaseScroll(b))
              )
                return !0;
            }
            return (
              e.preventDefault ? e.preventDefault() : (e.returnValue = !1), !1
            );
          }
        },
        animateSlider: function (t) {
          var e = this,
            i = o();
          return !(
            (this.params.mousewheel.thresholdDelta &&
              t.delta < this.params.mousewheel.thresholdDelta) ||
            (this.params.mousewheel.thresholdTime &&
              x() - e.mousewheel.lastScrollTime <
                this.params.mousewheel.thresholdTime) ||
            (!(t.delta >= 6 && x() - e.mousewheel.lastScrollTime < 60) &&
              (t.direction < 0
                ? (e.isEnd && !e.params.loop) ||
                  e.animating ||
                  (e.slideNext(), e.emit("scroll", t.raw))
                : (e.isBeginning && !e.params.loop) ||
                  e.animating ||
                  (e.slidePrev(), e.emit("scroll", t.raw)),
              (e.mousewheel.lastScrollTime = new i.Date().getTime()),
              1))
          );
        },
        releaseScroll: function (t) {
          var e = this,
            i = e.params.mousewheel;
          if (t.direction < 0) {
            if (e.isEnd && !e.params.loop && i.releaseOnEdges) return !0;
          } else if (e.isBeginning && !e.params.loop && i.releaseOnEdges)
            return !0;
          return !1;
        },
        enable: function () {
          var t = this,
            e = J.event();
          if (t.params.cssMode)
            return t.wrapperEl.removeEventListener(e, t.mousewheel.handle), !0;
          if (!e) return !1;
          if (t.mousewheel.enabled) return !1;
          var i = t.$el;
          return (
            "container" !== t.params.mousewheel.eventsTarget &&
              (i = g(t.params.mousewheel.eventsTarget)),
            i.on("mouseenter", t.mousewheel.handleMouseEnter),
            i.on("mouseleave", t.mousewheel.handleMouseLeave),
            i.on(e, t.mousewheel.handle),
            (t.mousewheel.enabled = !0),
            !0
          );
        },
        disable: function () {
          var t = this,
            e = J.event();
          if (t.params.cssMode)
            return t.wrapperEl.addEventListener(e, t.mousewheel.handle), !0;
          if (!e) return !1;
          if (!t.mousewheel.enabled) return !1;
          var i = t.$el;
          return (
            "container" !== t.params.mousewheel.eventsTarget &&
              (i = g(t.params.mousewheel.eventsTarget)),
            i.off(e, t.mousewheel.handle),
            (t.mousewheel.enabled = !1),
            !0
          );
        },
      },
      Q = {
        toggleEl: function (t, e) {
          t[e ? "addClass" : "removeClass"](
            this.params.navigation.disabledClass
          ),
            t[0] && "BUTTON" === t[0].tagName && (t[0].disabled = e);
        },
        update: function () {
          var t = this,
            e = t.params.navigation,
            i = t.navigation.toggleEl;
          if (!t.params.loop) {
            var n = t.navigation,
              a = n.$nextEl,
              r = n.$prevEl;
            r &&
              r.length > 0 &&
              (t.isBeginning ? i(r, !0) : i(r, !1),
              t.params.watchOverflow &&
                t.enabled &&
                r[t.isLocked ? "addClass" : "removeClass"](e.lockClass)),
              a &&
                a.length > 0 &&
                (t.isEnd ? i(a, !0) : i(a, !1),
                t.params.watchOverflow &&
                  t.enabled &&
                  a[t.isLocked ? "addClass" : "removeClass"](e.lockClass));
          }
        },
        onPrevClick: function (t) {
          var e = this;
          t.preventDefault(),
            (e.isBeginning && !e.params.loop) || e.slidePrev();
        },
        onNextClick: function (t) {
          var e = this;
          t.preventDefault(), (e.isEnd && !e.params.loop) || e.slideNext();
        },
        init: function () {
          var t,
            e,
            i = this,
            n = i.params.navigation;
          (i.params.navigation = D(
            i.$el,
            i.params.navigation,
            i.params.createElements,
            { nextEl: "swiper-button-next", prevEl: "swiper-button-prev" }
          )),
            (n.nextEl || n.prevEl) &&
              (n.nextEl &&
                ((t = g(n.nextEl)),
                i.params.uniqueNavElements &&
                  "string" == typeof n.nextEl &&
                  t.length > 1 &&
                  1 === i.$el.find(n.nextEl).length &&
                  (t = i.$el.find(n.nextEl))),
              n.prevEl &&
                ((e = g(n.prevEl)),
                i.params.uniqueNavElements &&
                  "string" == typeof n.prevEl &&
                  e.length > 1 &&
                  1 === i.$el.find(n.prevEl).length &&
                  (e = i.$el.find(n.prevEl))),
              t && t.length > 0 && t.on("click", i.navigation.onNextClick),
              e && e.length > 0 && e.on("click", i.navigation.onPrevClick),
              E(i.navigation, {
                $nextEl: t,
                nextEl: t && t[0],
                $prevEl: e,
                prevEl: e && e[0],
              }),
              i.enabled ||
                (t && t.addClass(n.lockClass), e && e.addClass(n.lockClass)));
        },
        destroy: function () {
          var t = this,
            e = t.navigation,
            i = e.$nextEl,
            n = e.$prevEl;
          i &&
            i.length &&
            (i.off("click", t.navigation.onNextClick),
            i.removeClass(t.params.navigation.disabledClass)),
            n &&
              n.length &&
              (n.off("click", t.navigation.onPrevClick),
              n.removeClass(t.params.navigation.disabledClass));
        },
      },
      tt = {
        update: function () {
          var t = this,
            e = t.rtl,
            i = t.params.pagination;
          if (
            i.el &&
            t.pagination.el &&
            t.pagination.$el &&
            0 !== t.pagination.$el.length
          ) {
            var n,
              a =
                t.virtual && t.params.virtual.enabled
                  ? t.virtual.slides.length
                  : t.slides.length,
              r = t.pagination.$el,
              s = t.params.loop
                ? Math.ceil((a - 2 * t.loopedSlides) / t.params.slidesPerGroup)
                : t.snapGrid.length;
            if (
              (t.params.loop
                ? ((n = Math.ceil(
                    (t.activeIndex - t.loopedSlides) / t.params.slidesPerGroup
                  )) >
                    a - 1 - 2 * t.loopedSlides && (n -= a - 2 * t.loopedSlides),
                  n > s - 1 && (n -= s),
                  n < 0 && "bullets" !== t.params.paginationType && (n = s + n))
                : (n =
                    void 0 !== t.snapIndex ? t.snapIndex : t.activeIndex || 0),
              "bullets" === i.type &&
                t.pagination.bullets &&
                t.pagination.bullets.length > 0)
            ) {
              var o,
                l,
                u,
                c = t.pagination.bullets;
              if (
                (i.dynamicBullets &&
                  ((t.pagination.bulletSize = c
                    .eq(0)
                    [t.isHorizontal() ? "outerWidth" : "outerHeight"](!0)),
                  r.css(
                    t.isHorizontal() ? "width" : "height",
                    t.pagination.bulletSize * (i.dynamicMainBullets + 4) + "px"
                  ),
                  i.dynamicMainBullets > 1 &&
                    void 0 !== t.previousIndex &&
                    ((t.pagination.dynamicBulletIndex += n - t.previousIndex),
                    t.pagination.dynamicBulletIndex > i.dynamicMainBullets - 1
                      ? (t.pagination.dynamicBulletIndex =
                          i.dynamicMainBullets - 1)
                      : t.pagination.dynamicBulletIndex < 0 &&
                        (t.pagination.dynamicBulletIndex = 0)),
                  (o = n - t.pagination.dynamicBulletIndex),
                  (u =
                    ((l = o + (Math.min(c.length, i.dynamicMainBullets) - 1)) +
                      o) /
                    2)),
                c.removeClass(
                  i.bulletActiveClass +
                    " " +
                    i.bulletActiveClass +
                    "-next " +
                    i.bulletActiveClass +
                    "-next-next " +
                    i.bulletActiveClass +
                    "-prev " +
                    i.bulletActiveClass +
                    "-prev-prev " +
                    i.bulletActiveClass +
                    "-main"
                ),
                r.length > 1)
              )
                c.each(function (t) {
                  var e = g(t),
                    a = e.index();
                  a === n && e.addClass(i.bulletActiveClass),
                    i.dynamicBullets &&
                      (a >= o &&
                        a <= l &&
                        e.addClass(i.bulletActiveClass + "-main"),
                      a === o &&
                        e
                          .prev()
                          .addClass(i.bulletActiveClass + "-prev")
                          .prev()
                          .addClass(i.bulletActiveClass + "-prev-prev"),
                      a === l &&
                        e
                          .next()
                          .addClass(i.bulletActiveClass + "-next")
                          .next()
                          .addClass(i.bulletActiveClass + "-next-next"));
                });
              else {
                var d = c.eq(n),
                  h = d.index();
                if ((d.addClass(i.bulletActiveClass), i.dynamicBullets)) {
                  for (var p = c.eq(o), f = c.eq(l), m = o; m <= l; m += 1)
                    c.eq(m).addClass(i.bulletActiveClass + "-main");
                  if (t.params.loop)
                    if (h >= c.length - i.dynamicMainBullets) {
                      for (var v = i.dynamicMainBullets; v >= 0; v -= 1)
                        c.eq(c.length - v).addClass(
                          i.bulletActiveClass + "-main"
                        );
                      c.eq(c.length - i.dynamicMainBullets - 1).addClass(
                        i.bulletActiveClass + "-prev"
                      );
                    } else
                      p
                        .prev()
                        .addClass(i.bulletActiveClass + "-prev")
                        .prev()
                        .addClass(i.bulletActiveClass + "-prev-prev"),
                        f
                          .next()
                          .addClass(i.bulletActiveClass + "-next")
                          .next()
                          .addClass(i.bulletActiveClass + "-next-next");
                  else
                    p
                      .prev()
                      .addClass(i.bulletActiveClass + "-prev")
                      .prev()
                      .addClass(i.bulletActiveClass + "-prev-prev"),
                      f
                        .next()
                        .addClass(i.bulletActiveClass + "-next")
                        .next()
                        .addClass(i.bulletActiveClass + "-next-next");
                }
              }
              if (i.dynamicBullets) {
                var _ = Math.min(c.length, i.dynamicMainBullets + 4),
                  y =
                    (t.pagination.bulletSize * _ - t.pagination.bulletSize) /
                      2 -
                    u * t.pagination.bulletSize,
                  b = e ? "right" : "left";
                c.css(t.isHorizontal() ? b : "top", y + "px");
              }
            }
            if (
              ("fraction" === i.type &&
                (r.find(L(i.currentClass)).text(i.formatFractionCurrent(n + 1)),
                r.find(L(i.totalClass)).text(i.formatFractionTotal(s))),
              "progressbar" === i.type)
            ) {
              var w;
              w = i.progressbarOpposite
                ? t.isHorizontal()
                  ? "vertical"
                  : "horizontal"
                : t.isHorizontal()
                ? "horizontal"
                : "vertical";
              var x = (n + 1) / s,
                S = 1,
                T = 1;
              "horizontal" === w ? (S = x) : (T = x),
                r
                  .find(L(i.progressbarFillClass))
                  .transform(
                    "translate3d(0,0,0) scaleX(" + S + ") scaleY(" + T + ")"
                  )
                  .transition(t.params.speed);
            }
            "custom" === i.type && i.renderCustom
              ? (r.html(i.renderCustom(t, n + 1, s)),
                t.emit("paginationRender", r[0]))
              : t.emit("paginationUpdate", r[0]),
              t.params.watchOverflow &&
                t.enabled &&
                r[t.isLocked ? "addClass" : "removeClass"](i.lockClass);
          }
        },
        render: function () {
          var t = this,
            e = t.params.pagination;
          if (
            e.el &&
            t.pagination.el &&
            t.pagination.$el &&
            0 !== t.pagination.$el.length
          ) {
            var i =
                t.virtual && t.params.virtual.enabled
                  ? t.virtual.slides.length
                  : t.slides.length,
              n = t.pagination.$el,
              a = "";
            if ("bullets" === e.type) {
              var r = t.params.loop
                ? Math.ceil((i - 2 * t.loopedSlides) / t.params.slidesPerGroup)
                : t.snapGrid.length;
              t.params.freeMode && !t.params.loop && r > i && (r = i);
              for (var s = 0; s < r; s += 1)
                e.renderBullet
                  ? (a += e.renderBullet.call(t, s, e.bulletClass))
                  : (a +=
                      "<" +
                      e.bulletElement +
                      ' class="' +
                      e.bulletClass +
                      '"></' +
                      e.bulletElement +
                      ">");
              n.html(a), (t.pagination.bullets = n.find(L(e.bulletClass)));
            }
            "fraction" === e.type &&
              ((a = e.renderFraction
                ? e.renderFraction.call(t, e.currentClass, e.totalClass)
                : '<span class="' +
                  e.currentClass +
                  '"></span> / <span class="' +
                  e.totalClass +
                  '"></span>'),
              n.html(a)),
              "progressbar" === e.type &&
                ((a = e.renderProgressbar
                  ? e.renderProgressbar.call(t, e.progressbarFillClass)
                  : '<span class="' + e.progressbarFillClass + '"></span>'),
                n.html(a)),
              "custom" !== e.type &&
                t.emit("paginationRender", t.pagination.$el[0]);
          }
        },
        init: function () {
          var t = this;
          t.params.pagination = D(
            t.$el,
            t.params.pagination,
            t.params.createElements,
            { el: "swiper-pagination" }
          );
          var e = t.params.pagination;
          if (e.el) {
            var i = g(e.el);
            0 !== i.length &&
              (t.params.uniqueNavElements &&
                "string" == typeof e.el &&
                i.length > 1 &&
                (i = t.$el.find(e.el)),
              "bullets" === e.type &&
                e.clickable &&
                i.addClass(e.clickableClass),
              i.addClass(e.modifierClass + e.type),
              "bullets" === e.type &&
                e.dynamicBullets &&
                (i.addClass("" + e.modifierClass + e.type + "-dynamic"),
                (t.pagination.dynamicBulletIndex = 0),
                e.dynamicMainBullets < 1 && (e.dynamicMainBullets = 1)),
              "progressbar" === e.type &&
                e.progressbarOpposite &&
                i.addClass(e.progressbarOppositeClass),
              e.clickable &&
                i.on("click", L(e.bulletClass), function (e) {
                  e.preventDefault();
                  var i = g(this).index() * t.params.slidesPerGroup;
                  t.params.loop && (i += t.loopedSlides), t.slideTo(i);
                }),
              E(t.pagination, { $el: i, el: i[0] }),
              t.enabled || i.addClass(e.lockClass));
          }
        },
        destroy: function () {
          var t = this,
            e = t.params.pagination;
          if (
            e.el &&
            t.pagination.el &&
            t.pagination.$el &&
            0 !== t.pagination.$el.length
          ) {
            var i = t.pagination.$el;
            i.removeClass(e.hiddenClass),
              i.removeClass(e.modifierClass + e.type),
              t.pagination.bullets &&
                t.pagination.bullets.removeClass(e.bulletActiveClass),
              e.clickable && i.off("click", L(e.bulletClass));
          }
        },
      },
      et = {
        setTranslate: function () {
          var t = this;
          if (t.params.scrollbar.el && t.scrollbar.el) {
            var e = t.scrollbar,
              i = t.rtlTranslate,
              n = t.progress,
              a = e.dragSize,
              r = e.trackSize,
              s = e.$dragEl,
              o = e.$el,
              l = t.params.scrollbar,
              u = a,
              c = (r - a) * n;
            i
              ? (c = -c) > 0
                ? ((u = a - c), (c = 0))
                : -c + a > r && (u = r + c)
              : c < 0
              ? ((u = a + c), (c = 0))
              : c + a > r && (u = r - c),
              t.isHorizontal()
                ? (s.transform("translate3d(" + c + "px, 0, 0)"),
                  (s[0].style.width = u + "px"))
                : (s.transform("translate3d(0px, " + c + "px, 0)"),
                  (s[0].style.height = u + "px")),
              l.hide &&
                (clearTimeout(t.scrollbar.timeout),
                (o[0].style.opacity = 1),
                (t.scrollbar.timeout = setTimeout(function () {
                  (o[0].style.opacity = 0), o.transition(400);
                }, 1e3)));
          }
        },
        setTransition: function (t) {
          var e = this;
          e.params.scrollbar.el &&
            e.scrollbar.el &&
            e.scrollbar.$dragEl.transition(t);
        },
        updateSize: function () {
          var t = this;
          if (t.params.scrollbar.el && t.scrollbar.el) {
            var e = t.scrollbar,
              i = e.$dragEl,
              n = e.$el;
            (i[0].style.width = ""), (i[0].style.height = "");
            var a,
              r = t.isHorizontal() ? n[0].offsetWidth : n[0].offsetHeight,
              s = t.size / t.virtualSize,
              o = s * (r / t.size);
            (a =
              "auto" === t.params.scrollbar.dragSize
                ? r * s
                : parseInt(t.params.scrollbar.dragSize, 10)),
              t.isHorizontal()
                ? (i[0].style.width = a + "px")
                : (i[0].style.height = a + "px"),
              (n[0].style.display = s >= 1 ? "none" : ""),
              t.params.scrollbar.hide && (n[0].style.opacity = 0),
              E(e, { trackSize: r, divider: s, moveDivider: o, dragSize: a }),
              t.params.watchOverflow &&
                t.enabled &&
                e.$el[t.isLocked ? "addClass" : "removeClass"](
                  t.params.scrollbar.lockClass
                );
          }
        },
        getPointerPosition: function (t) {
          return this.isHorizontal()
            ? "touchstart" === t.type || "touchmove" === t.type
              ? t.targetTouches[0].clientX
              : t.clientX
            : "touchstart" === t.type || "touchmove" === t.type
            ? t.targetTouches[0].clientY
            : t.clientY;
        },
        setDragPosition: function (t) {
          var e,
            i = this,
            n = i.scrollbar,
            a = i.rtlTranslate,
            r = n.$el,
            s = n.dragSize,
            o = n.trackSize,
            l = n.dragStartPos;
          (e =
            (n.getPointerPosition(t) -
              r.offset()[i.isHorizontal() ? "left" : "top"] -
              (null !== l ? l : s / 2)) /
            (o - s)),
            (e = Math.max(Math.min(e, 1), 0)),
            a && (e = 1 - e);
          var u = i.minTranslate() + (i.maxTranslate() - i.minTranslate()) * e;
          i.updateProgress(u),
            i.setTranslate(u),
            i.updateActiveIndex(),
            i.updateSlidesClasses();
        },
        onDragStart: function (t) {
          var e = this,
            i = e.params.scrollbar,
            n = e.scrollbar,
            a = e.$wrapperEl,
            r = n.$el,
            s = n.$dragEl;
          (e.scrollbar.isTouched = !0),
            (e.scrollbar.dragStartPos =
              t.target === s[0] || t.target === s
                ? n.getPointerPosition(t) -
                  t.target.getBoundingClientRect()[
                    e.isHorizontal() ? "left" : "top"
                  ]
                : null),
            t.preventDefault(),
            t.stopPropagation(),
            a.transition(100),
            s.transition(100),
            n.setDragPosition(t),
            clearTimeout(e.scrollbar.dragTimeout),
            r.transition(0),
            i.hide && r.css("opacity", 1),
            e.params.cssMode && e.$wrapperEl.css("scroll-snap-type", "none"),
            e.emit("scrollbarDragStart", t);
        },
        onDragMove: function (t) {
          var e = this,
            i = e.scrollbar,
            n = e.$wrapperEl,
            a = i.$el,
            r = i.$dragEl;
          e.scrollbar.isTouched &&
            (t.preventDefault ? t.preventDefault() : (t.returnValue = !1),
            i.setDragPosition(t),
            n.transition(0),
            a.transition(0),
            r.transition(0),
            e.emit("scrollbarDragMove", t));
        },
        onDragEnd: function (t) {
          var e = this,
            i = e.params.scrollbar,
            n = e.scrollbar,
            a = e.$wrapperEl,
            r = n.$el;
          e.scrollbar.isTouched &&
            ((e.scrollbar.isTouched = !1),
            e.params.cssMode &&
              (e.$wrapperEl.css("scroll-snap-type", ""), a.transition("")),
            i.hide &&
              (clearTimeout(e.scrollbar.dragTimeout),
              (e.scrollbar.dragTimeout = w(function () {
                r.css("opacity", 0), r.transition(400);
              }, 1e3))),
            e.emit("scrollbarDragEnd", t),
            i.snapOnRelease && e.slideToClosest());
        },
        enableDraggable: function () {
          var t = this;
          if (t.params.scrollbar.el) {
            var e = r(),
              i = t.scrollbar,
              n = t.touchEventsTouch,
              a = t.touchEventsDesktop,
              s = t.params,
              o = t.support,
              l = i.$el[0],
              u = !(!o.passiveListener || !s.passiveListeners) && {
                passive: !1,
                capture: !1,
              },
              c = !(!o.passiveListener || !s.passiveListeners) && {
                passive: !0,
                capture: !1,
              };
            l &&
              (o.touch
                ? (l.addEventListener(n.start, t.scrollbar.onDragStart, u),
                  l.addEventListener(n.move, t.scrollbar.onDragMove, u),
                  l.addEventListener(n.end, t.scrollbar.onDragEnd, c))
                : (l.addEventListener(a.start, t.scrollbar.onDragStart, u),
                  e.addEventListener(a.move, t.scrollbar.onDragMove, u),
                  e.addEventListener(a.end, t.scrollbar.onDragEnd, c)));
          }
        },
        disableDraggable: function () {
          var t = this;
          if (t.params.scrollbar.el) {
            var e = r(),
              i = t.scrollbar,
              n = t.touchEventsTouch,
              a = t.touchEventsDesktop,
              s = t.params,
              o = t.support,
              l = i.$el[0],
              u = !(!o.passiveListener || !s.passiveListeners) && {
                passive: !1,
                capture: !1,
              },
              c = !(!o.passiveListener || !s.passiveListeners) && {
                passive: !0,
                capture: !1,
              };
            l &&
              (o.touch
                ? (l.removeEventListener(n.start, t.scrollbar.onDragStart, u),
                  l.removeEventListener(n.move, t.scrollbar.onDragMove, u),
                  l.removeEventListener(n.end, t.scrollbar.onDragEnd, c))
                : (l.removeEventListener(a.start, t.scrollbar.onDragStart, u),
                  e.removeEventListener(a.move, t.scrollbar.onDragMove, u),
                  e.removeEventListener(a.end, t.scrollbar.onDragEnd, c)));
          }
        },
        init: function () {
          var t = this,
            e = t.scrollbar,
            i = t.$el;
          t.params.scrollbar = D(
            i,
            t.params.scrollbar,
            t.params.createElements,
            { el: "swiper-scrollbar" }
          );
          var n = t.params.scrollbar;
          if (n.el) {
            var a = g(n.el);
            t.params.uniqueNavElements &&
              "string" == typeof n.el &&
              a.length > 1 &&
              1 === i.find(n.el).length &&
              (a = i.find(n.el));
            var r = a.find("." + t.params.scrollbar.dragClass);
            0 === r.length &&
              ((r = g(
                '<div class="' + t.params.scrollbar.dragClass + '"></div>'
              )),
              a.append(r)),
              E(e, { $el: a, el: a[0], $dragEl: r, dragEl: r[0] }),
              n.draggable && e.enableDraggable(),
              a &&
                a[t.enabled ? "removeClass" : "addClass"](
                  t.params.scrollbar.lockClass
                );
          }
        },
        destroy: function () {
          this.scrollbar.disableDraggable();
        },
      },
      it = {
        setTransform: function (t, e) {
          var i = this.rtl,
            n = g(t),
            a = i ? -1 : 1,
            r = n.attr("data-swiper-parallax") || "0",
            s = n.attr("data-swiper-parallax-x"),
            o = n.attr("data-swiper-parallax-y"),
            l = n.attr("data-swiper-parallax-scale"),
            u = n.attr("data-swiper-parallax-opacity");
          if (
            (s || o
              ? ((s = s || "0"), (o = o || "0"))
              : this.isHorizontal()
              ? ((s = r), (o = "0"))
              : ((o = r), (s = "0")),
            (s =
              s.indexOf("%") >= 0
                ? parseInt(s, 10) * e * a + "%"
                : s * e * a + "px"),
            (o =
              o.indexOf("%") >= 0 ? parseInt(o, 10) * e + "%" : o * e + "px"),
            null != u)
          ) {
            var c = u - (u - 1) * (1 - Math.abs(e));
            n[0].style.opacity = c;
          }
          if (null == l) n.transform("translate3d(" + s + ", " + o + ", 0px)");
          else {
            var d = l - (l - 1) * (1 - Math.abs(e));
            n.transform(
              "translate3d(" + s + ", " + o + ", 0px) scale(" + d + ")"
            );
          }
        },
        setTranslate: function () {
          var t = this,
            e = t.$el,
            i = t.slides,
            n = t.progress,
            a = t.snapGrid;
          e
            .children(
              "[data-swiper-parallax], [data-swiper-parallax-x], [data-swiper-parallax-y], [data-swiper-parallax-opacity], [data-swiper-parallax-scale]"
            )
            .each(function (e) {
              t.parallax.setTransform(e, n);
            }),
            i.each(function (e, i) {
              var r = e.progress;
              t.params.slidesPerGroup > 1 &&
                "auto" !== t.params.slidesPerView &&
                (r += Math.ceil(i / 2) - n * (a.length - 1)),
                (r = Math.min(Math.max(r, -1), 1)),
                g(e)
                  .find(
                    "[data-swiper-parallax], [data-swiper-parallax-x], [data-swiper-parallax-y], [data-swiper-parallax-opacity], [data-swiper-parallax-scale]"
                  )
                  .each(function (e) {
                    t.parallax.setTransform(e, r);
                  });
            });
        },
        setTransition: function (t) {
          void 0 === t && (t = this.params.speed),
            this.$el
              .find(
                "[data-swiper-parallax], [data-swiper-parallax-x], [data-swiper-parallax-y], [data-swiper-parallax-opacity], [data-swiper-parallax-scale]"
              )
              .each(function (e) {
                var i = g(e),
                  n =
                    parseInt(i.attr("data-swiper-parallax-duration"), 10) || t;
                0 === t && (n = 0), i.transition(n);
              });
        },
      },
      nt = {
        getDistanceBetweenTouches: function (t) {
          if (t.targetTouches.length < 2) return 1;
          var e = t.targetTouches[0].pageX,
            i = t.targetTouches[0].pageY,
            n = t.targetTouches[1].pageX,
            a = t.targetTouches[1].pageY;
          return Math.sqrt(Math.pow(n - e, 2) + Math.pow(a - i, 2));
        },
        onGestureStart: function (t) {
          var e = this,
            i = e.support,
            n = e.params.zoom,
            a = e.zoom,
            r = a.gesture;
          if (
            ((a.fakeGestureTouched = !1),
            (a.fakeGestureMoved = !1),
            !i.gestures)
          ) {
            if (
              "touchstart" !== t.type ||
              ("touchstart" === t.type && t.targetTouches.length < 2)
            )
              return;
            (a.fakeGestureTouched = !0),
              (r.scaleStart = nt.getDistanceBetweenTouches(t));
          }
          (r.$slideEl && r.$slideEl.length) ||
          ((r.$slideEl = g(t.target).closest("." + e.params.slideClass)),
          0 === r.$slideEl.length && (r.$slideEl = e.slides.eq(e.activeIndex)),
          (r.$imageEl = r.$slideEl.find(
            "img, svg, canvas, picture, .swiper-zoom-target"
          )),
          (r.$imageWrapEl = r.$imageEl.parent("." + n.containerClass)),
          (r.maxRatio = r.$imageWrapEl.attr("data-swiper-zoom") || n.maxRatio),
          0 !== r.$imageWrapEl.length)
            ? (r.$imageEl && r.$imageEl.transition(0), (e.zoom.isScaling = !0))
            : (r.$imageEl = void 0);
        },
        onGestureChange: function (t) {
          var e = this,
            i = e.support,
            n = e.params.zoom,
            a = e.zoom,
            r = a.gesture;
          if (!i.gestures) {
            if (
              "touchmove" !== t.type ||
              ("touchmove" === t.type && t.targetTouches.length < 2)
            )
              return;
            (a.fakeGestureMoved = !0),
              (r.scaleMove = nt.getDistanceBetweenTouches(t));
          }
          r.$imageEl && 0 !== r.$imageEl.length
            ? (i.gestures
                ? (a.scale = t.scale * a.currentScale)
                : (a.scale = (r.scaleMove / r.scaleStart) * a.currentScale),
              a.scale > r.maxRatio &&
                (a.scale =
                  r.maxRatio - 1 + Math.pow(a.scale - r.maxRatio + 1, 0.5)),
              a.scale < n.minRatio &&
                (a.scale =
                  n.minRatio + 1 - Math.pow(n.minRatio - a.scale + 1, 0.5)),
              r.$imageEl.transform("translate3d(0,0,0) scale(" + a.scale + ")"))
            : "gesturechange" === t.type && a.onGestureStart(t);
        },
        onGestureEnd: function (t) {
          var e = this,
            i = e.device,
            n = e.support,
            a = e.params.zoom,
            r = e.zoom,
            s = r.gesture;
          if (!n.gestures) {
            if (!r.fakeGestureTouched || !r.fakeGestureMoved) return;
            if (
              "touchend" !== t.type ||
              ("touchend" === t.type &&
                t.changedTouches.length < 2 &&
                !i.android)
            )
              return;
            (r.fakeGestureTouched = !1), (r.fakeGestureMoved = !1);
          }
          s.$imageEl &&
            0 !== s.$imageEl.length &&
            ((r.scale = Math.max(Math.min(r.scale, s.maxRatio), a.minRatio)),
            s.$imageEl
              .transition(e.params.speed)
              .transform("translate3d(0,0,0) scale(" + r.scale + ")"),
            (r.currentScale = r.scale),
            (r.isScaling = !1),
            1 === r.scale && (s.$slideEl = void 0));
        },
        onTouchStart: function (t) {
          var e = this.device,
            i = this.zoom,
            n = i.gesture,
            a = i.image;
          n.$imageEl &&
            0 !== n.$imageEl.length &&
            (a.isTouched ||
              (e.android && t.cancelable && t.preventDefault(),
              (a.isTouched = !0),
              (a.touchesStart.x =
                "touchstart" === t.type ? t.targetTouches[0].pageX : t.pageX),
              (a.touchesStart.y =
                "touchstart" === t.type ? t.targetTouches[0].pageY : t.pageY)));
        },
        onTouchMove: function (t) {
          var e = this,
            i = e.zoom,
            n = i.gesture,
            a = i.image,
            r = i.velocity;
          if (
            n.$imageEl &&
            0 !== n.$imageEl.length &&
            ((e.allowClick = !1), a.isTouched && n.$slideEl)
          ) {
            a.isMoved ||
              ((a.width = n.$imageEl[0].offsetWidth),
              (a.height = n.$imageEl[0].offsetHeight),
              (a.startX = S(n.$imageWrapEl[0], "x") || 0),
              (a.startY = S(n.$imageWrapEl[0], "y") || 0),
              (n.slideWidth = n.$slideEl[0].offsetWidth),
              (n.slideHeight = n.$slideEl[0].offsetHeight),
              n.$imageWrapEl.transition(0));
            var s = a.width * i.scale,
              o = a.height * i.scale;
            if (!(s < n.slideWidth && o < n.slideHeight)) {
              if (
                ((a.minX = Math.min(n.slideWidth / 2 - s / 2, 0)),
                (a.maxX = -a.minX),
                (a.minY = Math.min(n.slideHeight / 2 - o / 2, 0)),
                (a.maxY = -a.minY),
                (a.touchesCurrent.x =
                  "touchmove" === t.type ? t.targetTouches[0].pageX : t.pageX),
                (a.touchesCurrent.y =
                  "touchmove" === t.type ? t.targetTouches[0].pageY : t.pageY),
                !a.isMoved && !i.isScaling)
              ) {
                if (
                  e.isHorizontal() &&
                  ((Math.floor(a.minX) === Math.floor(a.startX) &&
                    a.touchesCurrent.x < a.touchesStart.x) ||
                    (Math.floor(a.maxX) === Math.floor(a.startX) &&
                      a.touchesCurrent.x > a.touchesStart.x))
                )
                  return void (a.isTouched = !1);
                if (
                  !e.isHorizontal() &&
                  ((Math.floor(a.minY) === Math.floor(a.startY) &&
                    a.touchesCurrent.y < a.touchesStart.y) ||
                    (Math.floor(a.maxY) === Math.floor(a.startY) &&
                      a.touchesCurrent.y > a.touchesStart.y))
                )
                  return void (a.isTouched = !1);
              }
              t.cancelable && t.preventDefault(),
                t.stopPropagation(),
                (a.isMoved = !0),
                (a.currentX = a.touchesCurrent.x - a.touchesStart.x + a.startX),
                (a.currentY = a.touchesCurrent.y - a.touchesStart.y + a.startY),
                a.currentX < a.minX &&
                  (a.currentX =
                    a.minX + 1 - Math.pow(a.minX - a.currentX + 1, 0.8)),
                a.currentX > a.maxX &&
                  (a.currentX =
                    a.maxX - 1 + Math.pow(a.currentX - a.maxX + 1, 0.8)),
                a.currentY < a.minY &&
                  (a.currentY =
                    a.minY + 1 - Math.pow(a.minY - a.currentY + 1, 0.8)),
                a.currentY > a.maxY &&
                  (a.currentY =
                    a.maxY - 1 + Math.pow(a.currentY - a.maxY + 1, 0.8)),
                r.prevPositionX || (r.prevPositionX = a.touchesCurrent.x),
                r.prevPositionY || (r.prevPositionY = a.touchesCurrent.y),
                r.prevTime || (r.prevTime = Date.now()),
                (r.x =
                  (a.touchesCurrent.x - r.prevPositionX) /
                  (Date.now() - r.prevTime) /
                  2),
                (r.y =
                  (a.touchesCurrent.y - r.prevPositionY) /
                  (Date.now() - r.prevTime) /
                  2),
                Math.abs(a.touchesCurrent.x - r.prevPositionX) < 2 && (r.x = 0),
                Math.abs(a.touchesCurrent.y - r.prevPositionY) < 2 && (r.y = 0),
                (r.prevPositionX = a.touchesCurrent.x),
                (r.prevPositionY = a.touchesCurrent.y),
                (r.prevTime = Date.now()),
                n.$imageWrapEl.transform(
                  "translate3d(" + a.currentX + "px, " + a.currentY + "px,0)"
                );
            }
          }
        },
        onTouchEnd: function () {
          var t = this.zoom,
            e = t.gesture,
            i = t.image,
            n = t.velocity;
          if (e.$imageEl && 0 !== e.$imageEl.length) {
            if (!i.isTouched || !i.isMoved)
              return (i.isTouched = !1), void (i.isMoved = !1);
            (i.isTouched = !1), (i.isMoved = !1);
            var a = 300,
              r = 300,
              s = n.x * a,
              o = i.currentX + s,
              l = n.y * r,
              u = i.currentY + l;
            0 !== n.x && (a = Math.abs((o - i.currentX) / n.x)),
              0 !== n.y && (r = Math.abs((u - i.currentY) / n.y));
            var c = Math.max(a, r);
            (i.currentX = o), (i.currentY = u);
            var d = i.width * t.scale,
              h = i.height * t.scale;
            (i.minX = Math.min(e.slideWidth / 2 - d / 2, 0)),
              (i.maxX = -i.minX),
              (i.minY = Math.min(e.slideHeight / 2 - h / 2, 0)),
              (i.maxY = -i.minY),
              (i.currentX = Math.max(Math.min(i.currentX, i.maxX), i.minX)),
              (i.currentY = Math.max(Math.min(i.currentY, i.maxY), i.minY)),
              e.$imageWrapEl
                .transition(c)
                .transform(
                  "translate3d(" + i.currentX + "px, " + i.currentY + "px,0)"
                );
          }
        },
        onTransitionEnd: function () {
          var t = this,
            e = t.zoom,
            i = e.gesture;
          i.$slideEl &&
            t.previousIndex !== t.activeIndex &&
            (i.$imageEl && i.$imageEl.transform("translate3d(0,0,0) scale(1)"),
            i.$imageWrapEl && i.$imageWrapEl.transform("translate3d(0,0,0)"),
            (e.scale = 1),
            (e.currentScale = 1),
            (i.$slideEl = void 0),
            (i.$imageEl = void 0),
            (i.$imageWrapEl = void 0));
        },
        toggle: function (t) {
          var e = this.zoom;
          e.scale && 1 !== e.scale ? e.out() : e.in(t);
        },
        in: function (t) {
          var e,
            i,
            n,
            a,
            r,
            s,
            l,
            u,
            c,
            d,
            h,
            p,
            f,
            m,
            v,
            _,
            y = this,
            b = o(),
            w = y.zoom,
            x = y.params.zoom,
            S = w.gesture,
            T = w.image;
          S.$slideEl ||
            (t &&
              t.target &&
              (S.$slideEl = g(t.target).closest("." + y.params.slideClass)),
            S.$slideEl ||
              (y.params.virtual && y.params.virtual.enabled && y.virtual
                ? (S.$slideEl = y.$wrapperEl.children(
                    "." + y.params.slideActiveClass
                  ))
                : (S.$slideEl = y.slides.eq(y.activeIndex))),
            (S.$imageEl = S.$slideEl.find(
              "img, svg, canvas, picture, .swiper-zoom-target"
            )),
            (S.$imageWrapEl = S.$imageEl.parent("." + x.containerClass))),
            S.$imageEl &&
              0 !== S.$imageEl.length &&
              S.$imageWrapEl &&
              0 !== S.$imageWrapEl.length &&
              (S.$slideEl.addClass("" + x.zoomedSlideClass),
              void 0 === T.touchesStart.x && t
                ? ((e =
                    "touchend" === t.type
                      ? t.changedTouches[0].pageX
                      : t.pageX),
                  (i =
                    "touchend" === t.type
                      ? t.changedTouches[0].pageY
                      : t.pageY))
                : ((e = T.touchesStart.x), (i = T.touchesStart.y)),
              (w.scale = S.$imageWrapEl.attr("data-swiper-zoom") || x.maxRatio),
              (w.currentScale =
                S.$imageWrapEl.attr("data-swiper-zoom") || x.maxRatio),
              t
                ? ((v = S.$slideEl[0].offsetWidth),
                  (_ = S.$slideEl[0].offsetHeight),
                  (n = S.$slideEl.offset().left + b.scrollX + v / 2 - e),
                  (a = S.$slideEl.offset().top + b.scrollY + _ / 2 - i),
                  (l = S.$imageEl[0].offsetWidth),
                  (u = S.$imageEl[0].offsetHeight),
                  (c = l * w.scale),
                  (d = u * w.scale),
                  (f = -(h = Math.min(v / 2 - c / 2, 0))),
                  (m = -(p = Math.min(_ / 2 - d / 2, 0))),
                  (r = n * w.scale) < h && (r = h),
                  r > f && (r = f),
                  (s = a * w.scale) < p && (s = p),
                  s > m && (s = m))
                : ((r = 0), (s = 0)),
              S.$imageWrapEl
                .transition(300)
                .transform("translate3d(" + r + "px, " + s + "px,0)"),
              S.$imageEl
                .transition(300)
                .transform("translate3d(0,0,0) scale(" + w.scale + ")"));
        },
        out: function () {
          var t = this,
            e = t.zoom,
            i = t.params.zoom,
            n = e.gesture;
          n.$slideEl ||
            (t.params.virtual && t.params.virtual.enabled && t.virtual
              ? (n.$slideEl = t.$wrapperEl.children(
                  "." + t.params.slideActiveClass
                ))
              : (n.$slideEl = t.slides.eq(t.activeIndex)),
            (n.$imageEl = n.$slideEl.find(
              "img, svg, canvas, picture, .swiper-zoom-target"
            )),
            (n.$imageWrapEl = n.$imageEl.parent("." + i.containerClass))),
            n.$imageEl &&
              0 !== n.$imageEl.length &&
              n.$imageWrapEl &&
              0 !== n.$imageWrapEl.length &&
              ((e.scale = 1),
              (e.currentScale = 1),
              n.$imageWrapEl.transition(300).transform("translate3d(0,0,0)"),
              n.$imageEl
                .transition(300)
                .transform("translate3d(0,0,0) scale(1)"),
              n.$slideEl.removeClass("" + i.zoomedSlideClass),
              (n.$slideEl = void 0));
        },
        toggleGestures: function (t) {
          var e = this,
            i = e.zoom,
            n = i.slideSelector,
            a = i.passiveListener;
          e.$wrapperEl[t]("gesturestart", n, i.onGestureStart, a),
            e.$wrapperEl[t]("gesturechange", n, i.onGestureChange, a),
            e.$wrapperEl[t]("gestureend", n, i.onGestureEnd, a);
        },
        enableGestures: function () {
          this.zoom.gesturesEnabled ||
            ((this.zoom.gesturesEnabled = !0), this.zoom.toggleGestures("on"));
        },
        disableGestures: function () {
          this.zoom.gesturesEnabled &&
            ((this.zoom.gesturesEnabled = !1), this.zoom.toggleGestures("off"));
        },
        enable: function () {
          var t = this,
            e = t.support,
            i = t.zoom;
          if (!i.enabled) {
            i.enabled = !0;
            var n = !(
                "touchstart" !== t.touchEvents.start ||
                !e.passiveListener ||
                !t.params.passiveListeners
              ) && { passive: !0, capture: !1 },
              a = !e.passiveListener || { passive: !1, capture: !0 },
              r = "." + t.params.slideClass;
            (t.zoom.passiveListener = n),
              (t.zoom.slideSelector = r),
              e.gestures
                ? (t.$wrapperEl.on(
                    t.touchEvents.start,
                    t.zoom.enableGestures,
                    n
                  ),
                  t.$wrapperEl.on(t.touchEvents.end, t.zoom.disableGestures, n))
                : "touchstart" === t.touchEvents.start &&
                  (t.$wrapperEl.on(t.touchEvents.start, r, i.onGestureStart, n),
                  t.$wrapperEl.on(t.touchEvents.move, r, i.onGestureChange, a),
                  t.$wrapperEl.on(t.touchEvents.end, r, i.onGestureEnd, n),
                  t.touchEvents.cancel &&
                    t.$wrapperEl.on(
                      t.touchEvents.cancel,
                      r,
                      i.onGestureEnd,
                      n
                    )),
              t.$wrapperEl.on(
                t.touchEvents.move,
                "." + t.params.zoom.containerClass,
                i.onTouchMove,
                a
              );
          }
        },
        disable: function () {
          var t = this,
            e = t.zoom;
          if (e.enabled) {
            var i = t.support;
            t.zoom.enabled = !1;
            var n = !(
                "touchstart" !== t.touchEvents.start ||
                !i.passiveListener ||
                !t.params.passiveListeners
              ) && { passive: !0, capture: !1 },
              a = !i.passiveListener || { passive: !1, capture: !0 },
              r = "." + t.params.slideClass;
            i.gestures
              ? (t.$wrapperEl.off(
                  t.touchEvents.start,
                  t.zoom.enableGestures,
                  n
                ),
                t.$wrapperEl.off(t.touchEvents.end, t.zoom.disableGestures, n))
              : "touchstart" === t.touchEvents.start &&
                (t.$wrapperEl.off(t.touchEvents.start, r, e.onGestureStart, n),
                t.$wrapperEl.off(t.touchEvents.move, r, e.onGestureChange, a),
                t.$wrapperEl.off(t.touchEvents.end, r, e.onGestureEnd, n),
                t.touchEvents.cancel &&
                  t.$wrapperEl.off(t.touchEvents.cancel, r, e.onGestureEnd, n)),
              t.$wrapperEl.off(
                t.touchEvents.move,
                "." + t.params.zoom.containerClass,
                e.onTouchMove,
                a
              );
          }
        },
      },
      at = {
        loadInSlide: function (t, e) {
          void 0 === e && (e = !0);
          var i = this,
            n = i.params.lazy;
          if (void 0 !== t && 0 !== i.slides.length) {
            var a =
                i.virtual && i.params.virtual.enabled
                  ? i.$wrapperEl.children(
                      "." +
                        i.params.slideClass +
                        '[data-swiper-slide-index="' +
                        t +
                        '"]'
                    )
                  : i.slides.eq(t),
              r = a.find(
                "." +
                  n.elementClass +
                  ":not(." +
                  n.loadedClass +
                  "):not(." +
                  n.loadingClass +
                  ")"
              );
            !a.hasClass(n.elementClass) ||
              a.hasClass(n.loadedClass) ||
              a.hasClass(n.loadingClass) ||
              r.push(a[0]),
              0 !== r.length &&
                r.each(function (t) {
                  var r = g(t);
                  r.addClass(n.loadingClass);
                  var s = r.attr("data-background"),
                    o = r.attr("data-src"),
                    l = r.attr("data-srcset"),
                    u = r.attr("data-sizes"),
                    c = r.parent("picture");
                  i.loadImage(r[0], o || s, l, u, !1, function () {
                    if (null != i && i && (!i || i.params) && !i.destroyed) {
                      if (
                        (s
                          ? (r.css("background-image", 'url("' + s + '")'),
                            r.removeAttr("data-background"))
                          : (l &&
                              (r.attr("srcset", l),
                              r.removeAttr("data-srcset")),
                            u &&
                              (r.attr("sizes", u), r.removeAttr("data-sizes")),
                            c.length &&
                              c.children("source").each(function (t) {
                                var e = g(t);
                                e.attr("data-srcset") &&
                                  (e.attr("srcset", e.attr("data-srcset")),
                                  e.removeAttr("data-srcset"));
                              }),
                            o && (r.attr("src", o), r.removeAttr("data-src"))),
                        r.addClass(n.loadedClass).removeClass(n.loadingClass),
                        a.find("." + n.preloaderClass).remove(),
                        i.params.loop && e)
                      ) {
                        var t = a.attr("data-swiper-slide-index");
                        if (a.hasClass(i.params.slideDuplicateClass)) {
                          var d = i.$wrapperEl.children(
                            '[data-swiper-slide-index="' +
                              t +
                              '"]:not(.' +
                              i.params.slideDuplicateClass +
                              ")"
                          );
                          i.lazy.loadInSlide(d.index(), !1);
                        } else {
                          var h = i.$wrapperEl.children(
                            "." +
                              i.params.slideDuplicateClass +
                              '[data-swiper-slide-index="' +
                              t +
                              '"]'
                          );
                          i.lazy.loadInSlide(h.index(), !1);
                        }
                      }
                      i.emit("lazyImageReady", a[0], r[0]),
                        i.params.autoHeight && i.updateAutoHeight();
                    }
                  }),
                    i.emit("lazyImageLoad", a[0], r[0]);
                });
          }
        },
        load: function () {
          var t = this,
            e = t.$wrapperEl,
            i = t.params,
            n = t.slides,
            a = t.activeIndex,
            r = t.virtual && i.virtual.enabled,
            s = i.lazy,
            o = i.slidesPerView;
          function l(t) {
            if (r) {
              if (
                e.children(
                  "." + i.slideClass + '[data-swiper-slide-index="' + t + '"]'
                ).length
              )
                return !0;
            } else if (n[t]) return !0;
            return !1;
          }
          function u(t) {
            return r ? g(t).attr("data-swiper-slide-index") : g(t).index();
          }
          if (
            ("auto" === o && (o = 0),
            t.lazy.initialImageLoaded || (t.lazy.initialImageLoaded = !0),
            t.params.watchSlidesVisibility)
          )
            e.children("." + i.slideVisibleClass).each(function (e) {
              var i = r ? g(e).attr("data-swiper-slide-index") : g(e).index();
              t.lazy.loadInSlide(i);
            });
          else if (o > 1)
            for (var c = a; c < a + o; c += 1) l(c) && t.lazy.loadInSlide(c);
          else t.lazy.loadInSlide(a);
          if (s.loadPrevNext)
            if (o > 1 || (s.loadPrevNextAmount && s.loadPrevNextAmount > 1)) {
              for (
                var d = s.loadPrevNextAmount,
                  h = o,
                  p = Math.min(a + h + Math.max(d, h), n.length),
                  f = Math.max(a - Math.max(h, d), 0),
                  m = a + o;
                m < p;
                m += 1
              )
                l(m) && t.lazy.loadInSlide(m);
              for (var v = f; v < a; v += 1) l(v) && t.lazy.loadInSlide(v);
            } else {
              var _ = e.children("." + i.slideNextClass);
              _.length > 0 && t.lazy.loadInSlide(u(_));
              var y = e.children("." + i.slidePrevClass);
              y.length > 0 && t.lazy.loadInSlide(u(y));
            }
        },
        checkInViewOnLoad: function () {
          var t = o(),
            e = this;
          if (e && !e.destroyed) {
            var i = e.params.lazy.scrollingElement
                ? g(e.params.lazy.scrollingElement)
                : g(t),
              n = i[0] === t,
              a = n ? t.innerWidth : i[0].offsetWidth,
              r = n ? t.innerHeight : i[0].offsetHeight,
              s = e.$el.offset(),
              l = !1;
            e.rtlTranslate && (s.left -= e.$el[0].scrollLeft);
            for (
              var u = [
                  [s.left, s.top],
                  [s.left + e.width, s.top],
                  [s.left, s.top + e.height],
                  [s.left + e.width, s.top + e.height],
                ],
                c = 0;
              c < u.length;
              c += 1
            ) {
              var d = u[c];
              if (d[0] >= 0 && d[0] <= a && d[1] >= 0 && d[1] <= r) {
                if (0 === d[0] && 0 === d[1]) continue;
                l = !0;
              }
            }
            var h = !(
              "touchstart" !== e.touchEvents.start ||
              !e.support.passiveListener ||
              !e.params.passiveListeners
            ) && { passive: !0, capture: !1 };
            l
              ? (e.lazy.load(), i.off("scroll", e.lazy.checkInViewOnLoad, h))
              : e.lazy.scrollHandlerAttached ||
                ((e.lazy.scrollHandlerAttached = !0),
                i.on("scroll", e.lazy.checkInViewOnLoad, h));
          }
        },
      },
      rt = {
        LinearSpline: function (t, e) {
          var i, n, a, r, s;
          return (
            (this.x = t),
            (this.y = e),
            (this.lastIndex = t.length - 1),
            (this.interpolate = function (t) {
              return t
                ? ((s = (function (t, e) {
                    for (n = -1, i = t.length; i - n > 1; )
                      t[(a = (i + n) >> 1)] <= e ? (n = a) : (i = a);
                    return i;
                  })(this.x, t)),
                  (r = s - 1),
                  ((t - this.x[r]) * (this.y[s] - this.y[r])) /
                    (this.x[s] - this.x[r]) +
                    this.y[r])
                : 0;
            }),
            this
          );
        },
        getInterpolateFunction: function (t) {
          var e = this;
          e.controller.spline ||
            (e.controller.spline = e.params.loop
              ? new rt.LinearSpline(e.slidesGrid, t.slidesGrid)
              : new rt.LinearSpline(e.snapGrid, t.snapGrid));
        },
        setTranslate: function (t, e) {
          var i,
            n,
            a = this,
            r = a.controller.control,
            s = a.constructor;
          function o(t) {
            var e = a.rtlTranslate ? -a.translate : a.translate;
            "slide" === a.params.controller.by &&
              (a.controller.getInterpolateFunction(t),
              (n = -a.controller.spline.interpolate(-e))),
              (n && "container" !== a.params.controller.by) ||
                ((i =
                  (t.maxTranslate() - t.minTranslate()) /
                  (a.maxTranslate() - a.minTranslate())),
                (n = (e - a.minTranslate()) * i + t.minTranslate())),
              a.params.controller.inverse && (n = t.maxTranslate() - n),
              t.updateProgress(n),
              t.setTranslate(n, a),
              t.updateActiveIndex(),
              t.updateSlidesClasses();
          }
          if (Array.isArray(r))
            for (var l = 0; l < r.length; l += 1)
              r[l] !== e && r[l] instanceof s && o(r[l]);
          else r instanceof s && e !== r && o(r);
        },
        setTransition: function (t, e) {
          var i,
            n = this,
            a = n.constructor,
            r = n.controller.control;
          function s(e) {
            e.setTransition(t, n),
              0 !== t &&
                (e.transitionStart(),
                e.params.autoHeight &&
                  w(function () {
                    e.updateAutoHeight();
                  }),
                e.$wrapperEl.transitionEnd(function () {
                  r &&
                    (e.params.loop &&
                      "slide" === n.params.controller.by &&
                      e.loopFix(),
                    e.transitionEnd());
                }));
          }
          if (Array.isArray(r))
            for (i = 0; i < r.length; i += 1)
              r[i] !== e && r[i] instanceof a && s(r[i]);
          else r instanceof a && e !== r && s(r);
        },
      },
      st = {
        getRandomNumber: function (t) {
          return (
            void 0 === t && (t = 16),
            "x".repeat(t).replace(/x/g, function () {
              return Math.round(16 * Math.random()).toString(16);
            })
          );
        },
        makeElFocusable: function (t) {
          return t.attr("tabIndex", "0"), t;
        },
        makeElNotFocusable: function (t) {
          return t.attr("tabIndex", "-1"), t;
        },
        addElRole: function (t, e) {
          return t.attr("role", e), t;
        },
        addElRoleDescription: function (t, e) {
          return t.attr("aria-roledescription", e), t;
        },
        addElControls: function (t, e) {
          return t.attr("aria-controls", e), t;
        },
        addElLabel: function (t, e) {
          return t.attr("aria-label", e), t;
        },
        addElId: function (t, e) {
          return t.attr("id", e), t;
        },
        addElLive: function (t, e) {
          return t.attr("aria-live", e), t;
        },
        disableEl: function (t) {
          return t.attr("aria-disabled", !0), t;
        },
        enableEl: function (t) {
          return t.attr("aria-disabled", !1), t;
        },
        onEnterOrSpaceKey: function (t) {
          if (13 === t.keyCode || 32 === t.keyCode) {
            var e = this,
              i = e.params.a11y,
              n = g(t.target);
            e.navigation &&
              e.navigation.$nextEl &&
              n.is(e.navigation.$nextEl) &&
              ((e.isEnd && !e.params.loop) || e.slideNext(),
              e.isEnd
                ? e.a11y.notify(i.lastSlideMessage)
                : e.a11y.notify(i.nextSlideMessage)),
              e.navigation &&
                e.navigation.$prevEl &&
                n.is(e.navigation.$prevEl) &&
                ((e.isBeginning && !e.params.loop) || e.slidePrev(),
                e.isBeginning
                  ? e.a11y.notify(i.firstSlideMessage)
                  : e.a11y.notify(i.prevSlideMessage)),
              e.pagination &&
                n.is(L(e.params.pagination.bulletClass)) &&
                n[0].click();
          }
        },
        notify: function (t) {
          var e = this.a11y.liveRegion;
          0 !== e.length && (e.html(""), e.html(t));
        },
        updateNavigation: function () {
          var t = this;
          if (!t.params.loop && t.navigation) {
            var e = t.navigation,
              i = e.$nextEl,
              n = e.$prevEl;
            n &&
              n.length > 0 &&
              (t.isBeginning
                ? (t.a11y.disableEl(n), t.a11y.makeElNotFocusable(n))
                : (t.a11y.enableEl(n), t.a11y.makeElFocusable(n))),
              i &&
                i.length > 0 &&
                (t.isEnd
                  ? (t.a11y.disableEl(i), t.a11y.makeElNotFocusable(i))
                  : (t.a11y.enableEl(i), t.a11y.makeElFocusable(i)));
          }
        },
        updatePagination: function () {
          var t = this,
            e = t.params.a11y;
          t.pagination &&
            t.params.pagination.clickable &&
            t.pagination.bullets &&
            t.pagination.bullets.length &&
            t.pagination.bullets.each(function (i) {
              var n = g(i);
              t.a11y.makeElFocusable(n),
                t.params.pagination.renderBullet ||
                  (t.a11y.addElRole(n, "button"),
                  t.a11y.addElLabel(
                    n,
                    e.paginationBulletMessage.replace(
                      /\{\{index\}\}/,
                      n.index() + 1
                    )
                  ));
            });
        },
        init: function () {
          var t = this,
            e = t.params.a11y;
          t.$el.append(t.a11y.liveRegion);
          var i = t.$el;
          e.containerRoleDescriptionMessage &&
            t.a11y.addElRoleDescription(i, e.containerRoleDescriptionMessage),
            e.containerMessage && t.a11y.addElLabel(i, e.containerMessage);
          var n = t.$wrapperEl,
            a = n.attr("id") || "swiper-wrapper-" + t.a11y.getRandomNumber(16),
            r =
              t.params.autoplay && t.params.autoplay.enabled ? "off" : "polite";
          t.a11y.addElId(n, a),
            t.a11y.addElLive(n, r),
            e.itemRoleDescriptionMessage &&
              t.a11y.addElRoleDescription(
                g(t.slides),
                e.itemRoleDescriptionMessage
              ),
            t.a11y.addElRole(g(t.slides), e.slideRole);
          var s,
            o,
            l = t.params.loop
              ? t.slides.filter(function (e) {
                  return !e.classList.contains(t.params.slideDuplicateClass);
                }).length
              : t.slides.length;
          t.slides.each(function (i, n) {
            var a = g(i),
              r = t.params.loop
                ? parseInt(a.attr("data-swiper-slide-index"), 10)
                : n,
              s = e.slideLabelMessage
                .replace(/\{\{index\}\}/, r + 1)
                .replace(/\{\{slidesLength\}\}/, l);
            t.a11y.addElLabel(a, s);
          }),
            t.navigation && t.navigation.$nextEl && (s = t.navigation.$nextEl),
            t.navigation && t.navigation.$prevEl && (o = t.navigation.$prevEl),
            s &&
              s.length &&
              (t.a11y.makeElFocusable(s),
              "BUTTON" !== s[0].tagName &&
                (t.a11y.addElRole(s, "button"),
                s.on("keydown", t.a11y.onEnterOrSpaceKey)),
              t.a11y.addElLabel(s, e.nextSlideMessage),
              t.a11y.addElControls(s, a)),
            o &&
              o.length &&
              (t.a11y.makeElFocusable(o),
              "BUTTON" !== o[0].tagName &&
                (t.a11y.addElRole(o, "button"),
                o.on("keydown", t.a11y.onEnterOrSpaceKey)),
              t.a11y.addElLabel(o, e.prevSlideMessage),
              t.a11y.addElControls(o, a)),
            t.pagination &&
              t.params.pagination.clickable &&
              t.pagination.bullets &&
              t.pagination.bullets.length &&
              t.pagination.$el.on(
                "keydown",
                L(t.params.pagination.bulletClass),
                t.a11y.onEnterOrSpaceKey
              );
        },
        destroy: function () {
          var t,
            e,
            i = this;
          i.a11y.liveRegion &&
            i.a11y.liveRegion.length > 0 &&
            i.a11y.liveRegion.remove(),
            i.navigation && i.navigation.$nextEl && (t = i.navigation.$nextEl),
            i.navigation && i.navigation.$prevEl && (e = i.navigation.$prevEl),
            t && t.off("keydown", i.a11y.onEnterOrSpaceKey),
            e && e.off("keydown", i.a11y.onEnterOrSpaceKey),
            i.pagination &&
              i.params.pagination.clickable &&
              i.pagination.bullets &&
              i.pagination.bullets.length &&
              i.pagination.$el.off(
                "keydown",
                L(i.params.pagination.bulletClass),
                i.a11y.onEnterOrSpaceKey
              );
        },
      },
      ot = {
        init: function () {
          var t = this,
            e = o();
          if (t.params.history) {
            if (!e.history || !e.history.pushState)
              return (
                (t.params.history.enabled = !1),
                void (t.params.hashNavigation.enabled = !0)
              );
            var i = t.history;
            (i.initialized = !0),
              (i.paths = ot.getPathValues(t.params.url)),
              (i.paths.key || i.paths.value) &&
                (i.scrollToSlide(0, i.paths.value, t.params.runCallbacksOnInit),
                t.params.history.replaceState ||
                  e.addEventListener("popstate", t.history.setHistoryPopState));
          }
        },
        destroy: function () {
          var t = o();
          this.params.history.replaceState ||
            t.removeEventListener("popstate", this.history.setHistoryPopState);
        },
        setHistoryPopState: function () {
          var t = this;
          (t.history.paths = ot.getPathValues(t.params.url)),
            t.history.scrollToSlide(t.params.speed, t.history.paths.value, !1);
        },
        getPathValues: function (t) {
          var e = o(),
            i = (t ? new URL(t) : e.location).pathname
              .slice(1)
              .split("/")
              .filter(function (t) {
                return "" !== t;
              }),
            n = i.length;
          return { key: i[n - 2], value: i[n - 1] };
        },
        setHistory: function (t, e) {
          var i = this,
            n = o();
          if (i.history.initialized && i.params.history.enabled) {
            var a;
            a = i.params.url ? new URL(i.params.url) : n.location;
            var r = i.slides.eq(e),
              s = ot.slugify(r.attr("data-history"));
            if (i.params.history.root.length > 0) {
              var l = i.params.history.root;
              "/" === l[l.length - 1] && (l = l.slice(0, l.length - 1)),
                (s = l + "/" + t + "/" + s);
            } else a.pathname.includes(t) || (s = t + "/" + s);
            var u = n.history.state;
            (u && u.value === s) ||
              (i.params.history.replaceState
                ? n.history.replaceState({ value: s }, null, s)
                : n.history.pushState({ value: s }, null, s));
          }
        },
        slugify: function (t) {
          return t
            .toString()
            .replace(/\s+/g, "-")
            .replace(/[^\w-]+/g, "")
            .replace(/--+/g, "-")
            .replace(/^-+/, "")
            .replace(/-+$/, "");
        },
        scrollToSlide: function (t, e, i) {
          var n = this;
          if (e)
            for (var a = 0, r = n.slides.length; a < r; a += 1) {
              var s = n.slides.eq(a);
              if (
                ot.slugify(s.attr("data-history")) === e &&
                !s.hasClass(n.params.slideDuplicateClass)
              ) {
                var o = s.index();
                n.slideTo(o, t, i);
              }
            }
          else n.slideTo(0, t, i);
        },
      },
      lt = {
        onHashChange: function () {
          var t = this,
            e = r();
          t.emit("hashChange");
          var i = e.location.hash.replace("#", "");
          if (i !== t.slides.eq(t.activeIndex).attr("data-hash")) {
            var n = t.$wrapperEl
              .children("." + t.params.slideClass + '[data-hash="' + i + '"]')
              .index();
            if (void 0 === n) return;
            t.slideTo(n);
          }
        },
        setHash: function () {
          var t = this,
            e = o(),
            i = r();
          if (t.hashNavigation.initialized && t.params.hashNavigation.enabled)
            if (
              t.params.hashNavigation.replaceState &&
              e.history &&
              e.history.replaceState
            )
              e.history.replaceState(
                null,
                null,
                "#" + t.slides.eq(t.activeIndex).attr("data-hash") || ""
              ),
                t.emit("hashSet");
            else {
              var n = t.slides.eq(t.activeIndex),
                a = n.attr("data-hash") || n.attr("data-history");
              (i.location.hash = a || ""), t.emit("hashSet");
            }
        },
        init: function () {
          var t = this,
            e = r(),
            i = o();
          if (
            !(
              !t.params.hashNavigation.enabled ||
              (t.params.history && t.params.history.enabled)
            )
          ) {
            t.hashNavigation.initialized = !0;
            var n = e.location.hash.replace("#", "");
            if (n)
              for (var a = 0, s = t.slides.length; a < s; a += 1) {
                var l = t.slides.eq(a);
                if (
                  (l.attr("data-hash") || l.attr("data-history")) === n &&
                  !l.hasClass(t.params.slideDuplicateClass)
                ) {
                  var u = l.index();
                  t.slideTo(u, 0, t.params.runCallbacksOnInit, !0);
                }
              }
            t.params.hashNavigation.watchState &&
              g(i).on("hashchange", t.hashNavigation.onHashChange);
          }
        },
        destroy: function () {
          var t = o();
          this.params.hashNavigation.watchState &&
            g(t).off("hashchange", this.hashNavigation.onHashChange);
        },
      },
      ut = {
        run: function () {
          var t = this,
            e = t.slides.eq(t.activeIndex),
            i = t.params.autoplay.delay;
          e.attr("data-swiper-autoplay") &&
            (i = e.attr("data-swiper-autoplay") || t.params.autoplay.delay),
            clearTimeout(t.autoplay.timeout),
            (t.autoplay.timeout = w(function () {
              var e;
              t.params.autoplay.reverseDirection
                ? t.params.loop
                  ? (t.loopFix(),
                    (e = t.slidePrev(t.params.speed, !0, !0)),
                    t.emit("autoplay"))
                  : t.isBeginning
                  ? t.params.autoplay.stopOnLastSlide
                    ? t.autoplay.stop()
                    : ((e = t.slideTo(
                        t.slides.length - 1,
                        t.params.speed,
                        !0,
                        !0
                      )),
                      t.emit("autoplay"))
                  : ((e = t.slidePrev(t.params.speed, !0, !0)),
                    t.emit("autoplay"))
                : t.params.loop
                ? (t.loopFix(),
                  (e = t.slideNext(t.params.speed, !0, !0)),
                  t.emit("autoplay"))
                : t.isEnd
                ? t.params.autoplay.stopOnLastSlide
                  ? t.autoplay.stop()
                  : ((e = t.slideTo(0, t.params.speed, !0, !0)),
                    t.emit("autoplay"))
                : ((e = t.slideNext(t.params.speed, !0, !0)),
                  t.emit("autoplay")),
                ((t.params.cssMode && t.autoplay.running) || !1 === e) &&
                  t.autoplay.run();
            }, i));
        },
        start: function () {
          var t = this;
          return (
            void 0 === t.autoplay.timeout &&
            !t.autoplay.running &&
            ((t.autoplay.running = !0),
            t.emit("autoplayStart"),
            t.autoplay.run(),
            !0)
          );
        },
        stop: function () {
          var t = this;
          return (
            !!t.autoplay.running &&
            void 0 !== t.autoplay.timeout &&
            (t.autoplay.timeout &&
              (clearTimeout(t.autoplay.timeout), (t.autoplay.timeout = void 0)),
            (t.autoplay.running = !1),
            t.emit("autoplayStop"),
            !0)
          );
        },
        pause: function (t) {
          var e = this;
          e.autoplay.running &&
            (e.autoplay.paused ||
              (e.autoplay.timeout && clearTimeout(e.autoplay.timeout),
              (e.autoplay.paused = !0),
              0 !== t && e.params.autoplay.waitForTransition
                ? ["transitionend", "webkitTransitionEnd"].forEach(function (
                    t
                  ) {
                    e.$wrapperEl[0].addEventListener(
                      t,
                      e.autoplay.onTransitionEnd
                    );
                  })
                : ((e.autoplay.paused = !1), e.autoplay.run())));
        },
        onVisibilityChange: function () {
          var t = this,
            e = r();
          "hidden" === e.visibilityState &&
            t.autoplay.running &&
            t.autoplay.pause(),
            "visible" === e.visibilityState &&
              t.autoplay.paused &&
              (t.autoplay.run(), (t.autoplay.paused = !1));
        },
        onTransitionEnd: function (t) {
          var e = this;
          e &&
            !e.destroyed &&
            e.$wrapperEl &&
            t.target === e.$wrapperEl[0] &&
            (["transitionend", "webkitTransitionEnd"].forEach(function (t) {
              e.$wrapperEl[0].removeEventListener(
                t,
                e.autoplay.onTransitionEnd
              );
            }),
            (e.autoplay.paused = !1),
            e.autoplay.running ? e.autoplay.run() : e.autoplay.stop());
        },
        onMouseEnter: function () {
          var t = this;
          t.params.autoplay.disableOnInteraction
            ? t.autoplay.stop()
            : t.autoplay.pause(),
            ["transitionend", "webkitTransitionEnd"].forEach(function (e) {
              t.$wrapperEl[0].removeEventListener(
                e,
                t.autoplay.onTransitionEnd
              );
            });
        },
        onMouseLeave: function () {
          var t = this;
          t.params.autoplay.disableOnInteraction ||
            ((t.autoplay.paused = !1), t.autoplay.run());
        },
        attachMouseEvents: function () {
          var t = this;
          t.params.autoplay.pauseOnMouseEnter &&
            (t.$el.on("mouseenter", t.autoplay.onMouseEnter),
            t.$el.on("mouseleave", t.autoplay.onMouseLeave));
        },
        detachMouseEvents: function () {
          var t = this;
          t.$el.off("mouseenter", t.autoplay.onMouseEnter),
            t.$el.off("mouseleave", t.autoplay.onMouseLeave);
        },
      },
      ct = {
        setTranslate: function () {
          for (var t = this, e = t.slides, i = 0; i < e.length; i += 1) {
            var n = t.slides.eq(i),
              a = -n[0].swiperSlideOffset;
            t.params.virtualTranslate || (a -= t.translate);
            var r = 0;
            t.isHorizontal() || ((r = a), (a = 0));
            var s = t.params.fadeEffect.crossFade
              ? Math.max(1 - Math.abs(n[0].progress), 0)
              : 1 + Math.min(Math.max(n[0].progress, -1), 0);
            n.css({ opacity: s }).transform(
              "translate3d(" + a + "px, " + r + "px, 0px)"
            );
          }
        },
        setTransition: function (t) {
          var e = this,
            i = e.slides,
            n = e.$wrapperEl;
          if ((i.transition(t), e.params.virtualTranslate && 0 !== t)) {
            var a = !1;
            i.transitionEnd(function () {
              if (!a && e && !e.destroyed) {
                (a = !0), (e.animating = !1);
                for (
                  var t = ["webkitTransitionEnd", "transitionend"], i = 0;
                  i < t.length;
                  i += 1
                )
                  n.trigger(t[i]);
              }
            });
          }
        },
      },
      dt = {
        setTranslate: function () {
          var t,
            e = this,
            i = e.$el,
            n = e.$wrapperEl,
            a = e.slides,
            r = e.width,
            s = e.height,
            o = e.rtlTranslate,
            l = e.size,
            u = e.browser,
            c = e.params.cubeEffect,
            d = e.isHorizontal(),
            h = e.virtual && e.params.virtual.enabled,
            p = 0;
          c.shadow &&
            (d
              ? (0 === (t = n.find(".swiper-cube-shadow")).length &&
                  ((t = g('<div class="swiper-cube-shadow"></div>')),
                  n.append(t)),
                t.css({ height: r + "px" }))
              : 0 === (t = i.find(".swiper-cube-shadow")).length &&
                ((t = g('<div class="swiper-cube-shadow"></div>')),
                i.append(t)));
          for (var f = 0; f < a.length; f += 1) {
            var m = a.eq(f),
              v = f;
            h && (v = parseInt(m.attr("data-swiper-slide-index"), 10));
            var _ = 90 * v,
              y = Math.floor(_ / 360);
            o && ((_ = -_), (y = Math.floor(-_ / 360)));
            var b = Math.max(Math.min(m[0].progress, 1), -1),
              w = 0,
              x = 0,
              S = 0;
            v % 4 == 0
              ? ((w = 4 * -y * l), (S = 0))
              : (v - 1) % 4 == 0
              ? ((w = 0), (S = 4 * -y * l))
              : (v - 2) % 4 == 0
              ? ((w = l + 4 * y * l), (S = l))
              : (v - 3) % 4 == 0 && ((w = -l), (S = 3 * l + 4 * l * y)),
              o && (w = -w),
              d || ((x = w), (w = 0));
            var T =
              "rotateX(" +
              (d ? 0 : -_) +
              "deg) rotateY(" +
              (d ? _ : 0) +
              "deg) translate3d(" +
              w +
              "px, " +
              x +
              "px, " +
              S +
              "px)";
            if (
              (b <= 1 &&
                b > -1 &&
                ((p = 90 * v + 90 * b), o && (p = 90 * -v - 90 * b)),
              m.transform(T),
              c.slideShadows)
            ) {
              var C = d
                  ? m.find(".swiper-slide-shadow-left")
                  : m.find(".swiper-slide-shadow-top"),
                E = d
                  ? m.find(".swiper-slide-shadow-right")
                  : m.find(".swiper-slide-shadow-bottom");
              0 === C.length &&
                ((C = g(
                  '<div class="swiper-slide-shadow-' +
                    (d ? "left" : "top") +
                    '"></div>'
                )),
                m.append(C)),
                0 === E.length &&
                  ((E = g(
                    '<div class="swiper-slide-shadow-' +
                      (d ? "right" : "bottom") +
                      '"></div>'
                  )),
                  m.append(E)),
                C.length && (C[0].style.opacity = Math.max(-b, 0)),
                E.length && (E[0].style.opacity = Math.max(b, 0));
            }
          }
          if (
            (n.css({
              "-webkit-transform-origin": "50% 50% -" + l / 2 + "px",
              "-moz-transform-origin": "50% 50% -" + l / 2 + "px",
              "-ms-transform-origin": "50% 50% -" + l / 2 + "px",
              "transform-origin": "50% 50% -" + l / 2 + "px",
            }),
            c.shadow)
          )
            if (d)
              t.transform(
                "translate3d(0px, " +
                  (r / 2 + c.shadowOffset) +
                  "px, " +
                  -r / 2 +
                  "px) rotateX(90deg) rotateZ(0deg) scale(" +
                  c.shadowScale +
                  ")"
              );
            else {
              var P = Math.abs(p) - 90 * Math.floor(Math.abs(p) / 90),
                L =
                  1.5 -
                  (Math.sin((2 * P * Math.PI) / 360) / 2 +
                    Math.cos((2 * P * Math.PI) / 360) / 2),
                D = c.shadowScale,
                M = c.shadowScale / L,
                k = c.shadowOffset;
              t.transform(
                "scale3d(" +
                  D +
                  ", 1, " +
                  M +
                  ") translate3d(0px, " +
                  (s / 2 + k) +
                  "px, " +
                  -s / 2 / M +
                  "px) rotateX(-90deg)"
              );
            }
          var A = u.isSafari || u.isWebView ? -l / 2 : 0;
          n.transform(
            "translate3d(0px,0," +
              A +
              "px) rotateX(" +
              (e.isHorizontal() ? 0 : p) +
              "deg) rotateY(" +
              (e.isHorizontal() ? -p : 0) +
              "deg)"
          );
        },
        setTransition: function (t) {
          var e = this,
            i = e.$el;
          e.slides
            .transition(t)
            .find(
              ".swiper-slide-shadow-top, .swiper-slide-shadow-right, .swiper-slide-shadow-bottom, .swiper-slide-shadow-left"
            )
            .transition(t),
            e.params.cubeEffect.shadow &&
              !e.isHorizontal() &&
              i.find(".swiper-cube-shadow").transition(t);
        },
      },
      ht = {
        setTranslate: function () {
          for (
            var t = this, e = t.slides, i = t.rtlTranslate, n = 0;
            n < e.length;
            n += 1
          ) {
            var a = e.eq(n),
              r = a[0].progress;
            t.params.flipEffect.limitRotation &&
              (r = Math.max(Math.min(a[0].progress, 1), -1));
            var s = -180 * r,
              o = 0,
              l = -a[0].swiperSlideOffset,
              u = 0;
            if (
              (t.isHorizontal()
                ? i && (s = -s)
                : ((u = l), (l = 0), (o = -s), (s = 0)),
              (a[0].style.zIndex = -Math.abs(Math.round(r)) + e.length),
              t.params.flipEffect.slideShadows)
            ) {
              var c = t.isHorizontal()
                  ? a.find(".swiper-slide-shadow-left")
                  : a.find(".swiper-slide-shadow-top"),
                d = t.isHorizontal()
                  ? a.find(".swiper-slide-shadow-right")
                  : a.find(".swiper-slide-shadow-bottom");
              0 === c.length &&
                ((c = g(
                  '<div class="swiper-slide-shadow-' +
                    (t.isHorizontal() ? "left" : "top") +
                    '"></div>'
                )),
                a.append(c)),
                0 === d.length &&
                  ((d = g(
                    '<div class="swiper-slide-shadow-' +
                      (t.isHorizontal() ? "right" : "bottom") +
                      '"></div>'
                  )),
                  a.append(d)),
                c.length && (c[0].style.opacity = Math.max(-r, 0)),
                d.length && (d[0].style.opacity = Math.max(r, 0));
            }
            a.transform(
              "translate3d(" +
                l +
                "px, " +
                u +
                "px, 0px) rotateX(" +
                o +
                "deg) rotateY(" +
                s +
                "deg)"
            );
          }
        },
        setTransition: function (t) {
          var e = this,
            i = e.slides,
            n = e.activeIndex,
            a = e.$wrapperEl;
          if (
            (i
              .transition(t)
              .find(
                ".swiper-slide-shadow-top, .swiper-slide-shadow-right, .swiper-slide-shadow-bottom, .swiper-slide-shadow-left"
              )
              .transition(t),
            e.params.virtualTranslate && 0 !== t)
          ) {
            var r = !1;
            i.eq(n).transitionEnd(function () {
              if (!r && e && !e.destroyed) {
                (r = !0), (e.animating = !1);
                for (
                  var t = ["webkitTransitionEnd", "transitionend"], i = 0;
                  i < t.length;
                  i += 1
                )
                  a.trigger(t[i]);
              }
            });
          }
        },
      },
      pt = {
        setTranslate: function () {
          for (
            var t = this,
              e = t.width,
              i = t.height,
              n = t.slides,
              a = t.slidesSizesGrid,
              r = t.params.coverflowEffect,
              s = t.isHorizontal(),
              o = t.translate,
              l = s ? e / 2 - o : i / 2 - o,
              u = s ? r.rotate : -r.rotate,
              c = r.depth,
              d = 0,
              h = n.length;
            d < h;
            d += 1
          ) {
            var p = n.eq(d),
              f = a[d],
              m = ((l - p[0].swiperSlideOffset - f / 2) / f) * r.modifier,
              v = s ? u * m : 0,
              _ = s ? 0 : u * m,
              y = -c * Math.abs(m),
              b = r.stretch;
            "string" == typeof b &&
              -1 !== b.indexOf("%") &&
              (b = (parseFloat(r.stretch) / 100) * f);
            var w = s ? 0 : b * m,
              x = s ? b * m : 0,
              S = 1 - (1 - r.scale) * Math.abs(m);
            Math.abs(x) < 0.001 && (x = 0),
              Math.abs(w) < 0.001 && (w = 0),
              Math.abs(y) < 0.001 && (y = 0),
              Math.abs(v) < 0.001 && (v = 0),
              Math.abs(_) < 0.001 && (_ = 0),
              Math.abs(S) < 0.001 && (S = 0);
            var T =
              "translate3d(" +
              x +
              "px," +
              w +
              "px," +
              y +
              "px)  rotateX(" +
              _ +
              "deg) rotateY(" +
              v +
              "deg) scale(" +
              S +
              ")";
            if (
              (p.transform(T),
              (p[0].style.zIndex = 1 - Math.abs(Math.round(m))),
              r.slideShadows)
            ) {
              var C = s
                  ? p.find(".swiper-slide-shadow-left")
                  : p.find(".swiper-slide-shadow-top"),
                E = s
                  ? p.find(".swiper-slide-shadow-right")
                  : p.find(".swiper-slide-shadow-bottom");
              0 === C.length &&
                ((C = g(
                  '<div class="swiper-slide-shadow-' +
                    (s ? "left" : "top") +
                    '"></div>'
                )),
                p.append(C)),
                0 === E.length &&
                  ((E = g(
                    '<div class="swiper-slide-shadow-' +
                      (s ? "right" : "bottom") +
                      '"></div>'
                  )),
                  p.append(E)),
                C.length && (C[0].style.opacity = m > 0 ? m : 0),
                E.length && (E[0].style.opacity = -m > 0 ? -m : 0);
            }
          }
        },
        setTransition: function (t) {
          this.slides
            .transition(t)
            .find(
              ".swiper-slide-shadow-top, .swiper-slide-shadow-right, .swiper-slide-shadow-bottom, .swiper-slide-shadow-left"
            )
            .transition(t);
        },
      },
      ft = {
        init: function () {
          var t = this,
            e = t.params.thumbs;
          if (t.thumbs.initialized) return !1;
          t.thumbs.initialized = !0;
          var i = t.constructor;
          return (
            e.swiper instanceof i
              ? ((t.thumbs.swiper = e.swiper),
                E(t.thumbs.swiper.originalParams, {
                  watchSlidesProgress: !0,
                  slideToClickedSlide: !1,
                }),
                E(t.thumbs.swiper.params, {
                  watchSlidesProgress: !0,
                  slideToClickedSlide: !1,
                }))
              : T(e.swiper) &&
                ((t.thumbs.swiper = new i(
                  E({}, e.swiper, {
                    watchSlidesVisibility: !0,
                    watchSlidesProgress: !0,
                    slideToClickedSlide: !1,
                  })
                )),
                (t.thumbs.swiperCreated = !0)),
            t.thumbs.swiper.$el.addClass(t.params.thumbs.thumbsContainerClass),
            t.thumbs.swiper.on("tap", t.thumbs.onThumbClick),
            !0
          );
        },
        onThumbClick: function () {
          var t = this,
            e = t.thumbs.swiper;
          if (e) {
            var i = e.clickedIndex,
              n = e.clickedSlide;
            if (
              !(
                (n && g(n).hasClass(t.params.thumbs.slideThumbActiveClass)) ||
                null == i
              )
            ) {
              var a;
              if (
                ((a = e.params.loop
                  ? parseInt(
                      g(e.clickedSlide).attr("data-swiper-slide-index"),
                      10
                    )
                  : i),
                t.params.loop)
              ) {
                var r = t.activeIndex;
                t.slides.eq(r).hasClass(t.params.slideDuplicateClass) &&
                  (t.loopFix(),
                  (t._clientLeft = t.$wrapperEl[0].clientLeft),
                  (r = t.activeIndex));
                var s = t.slides
                    .eq(r)
                    .prevAll('[data-swiper-slide-index="' + a + '"]')
                    .eq(0)
                    .index(),
                  o = t.slides
                    .eq(r)
                    .nextAll('[data-swiper-slide-index="' + a + '"]')
                    .eq(0)
                    .index();
                a = void 0 === s ? o : void 0 === o ? s : o - r < r - s ? o : s;
              }
              t.slideTo(a);
            }
          }
        },
        update: function (t) {
          var e = this,
            i = e.thumbs.swiper;
          if (i) {
            var n =
                "auto" === i.params.slidesPerView
                  ? i.slidesPerViewDynamic()
                  : i.params.slidesPerView,
              a = e.params.thumbs.autoScrollOffset,
              r = a && !i.params.loop;
            if (e.realIndex !== i.realIndex || r) {
              var s,
                o,
                l = i.activeIndex;
              if (i.params.loop) {
                i.slides.eq(l).hasClass(i.params.slideDuplicateClass) &&
                  (i.loopFix(),
                  (i._clientLeft = i.$wrapperEl[0].clientLeft),
                  (l = i.activeIndex));
                var u = i.slides
                    .eq(l)
                    .prevAll('[data-swiper-slide-index="' + e.realIndex + '"]')
                    .eq(0)
                    .index(),
                  c = i.slides
                    .eq(l)
                    .nextAll('[data-swiper-slide-index="' + e.realIndex + '"]')
                    .eq(0)
                    .index();
                (s =
                  void 0 === u
                    ? c
                    : void 0 === c
                    ? u
                    : c - l == l - u
                    ? i.params.slidesPerGroup > 1
                      ? c
                      : l
                    : c - l < l - u
                    ? c
                    : u),
                  (o = e.activeIndex > e.previousIndex ? "next" : "prev");
              } else o = (s = e.realIndex) > e.previousIndex ? "next" : "prev";
              r && (s += "next" === o ? a : -1 * a),
                i.visibleSlidesIndexes &&
                  i.visibleSlidesIndexes.indexOf(s) < 0 &&
                  (i.params.centeredSlides
                    ? (s =
                        s > l
                          ? s - Math.floor(n / 2) + 1
                          : s + Math.floor(n / 2) - 1)
                    : s > l && i.params.slidesPerGroup,
                  i.slideTo(s, t ? 0 : void 0));
            }
            var d = 1,
              h = e.params.thumbs.slideThumbActiveClass;
            if (
              (e.params.slidesPerView > 1 &&
                !e.params.centeredSlides &&
                (d = e.params.slidesPerView),
              e.params.thumbs.multipleActiveThumbs || (d = 1),
              (d = Math.floor(d)),
              i.slides.removeClass(h),
              i.params.loop || (i.params.virtual && i.params.virtual.enabled))
            )
              for (var p = 0; p < d; p += 1)
                i.$wrapperEl
                  .children(
                    '[data-swiper-slide-index="' + (e.realIndex + p) + '"]'
                  )
                  .addClass(h);
            else
              for (var f = 0; f < d; f += 1)
                i.slides.eq(e.realIndex + f).addClass(h);
          }
        },
      },
      mt = [
        q,
        K,
        {
          name: "mousewheel",
          params: {
            mousewheel: {
              enabled: !1,
              releaseOnEdges: !1,
              invert: !1,
              forceToAxis: !1,
              sensitivity: 1,
              eventsTarget: "container",
              thresholdDelta: null,
              thresholdTime: null,
            },
          },
          create: function () {
            P(this, {
              mousewheel: {
                enabled: !1,
                lastScrollTime: x(),
                lastEventBeforeSnap: void 0,
                recentWheelEvents: [],
                enable: J.enable,
                disable: J.disable,
                handle: J.handle,
                handleMouseEnter: J.handleMouseEnter,
                handleMouseLeave: J.handleMouseLeave,
                animateSlider: J.animateSlider,
                releaseScroll: J.releaseScroll,
              },
            });
          },
          on: {
            init: function (t) {
              !t.params.mousewheel.enabled &&
                t.params.cssMode &&
                t.mousewheel.disable(),
                t.params.mousewheel.enabled && t.mousewheel.enable();
            },
            destroy: function (t) {
              t.params.cssMode && t.mousewheel.enable(),
                t.mousewheel.enabled && t.mousewheel.disable();
            },
          },
        },
        {
          name: "navigation",
          params: {
            navigation: {
              nextEl: null,
              prevEl: null,
              hideOnClick: !1,
              disabledClass: "swiper-button-disabled",
              hiddenClass: "swiper-button-hidden",
              lockClass: "swiper-button-lock",
            },
          },
          create: function () {
            P(this, { navigation: e({}, Q) });
          },
          on: {
            init: function (t) {
              t.navigation.init(), t.navigation.update();
            },
            toEdge: function (t) {
              t.navigation.update();
            },
            fromEdge: function (t) {
              t.navigation.update();
            },
            destroy: function (t) {
              t.navigation.destroy();
            },
            "enable disable": function (t) {
              var e = t.navigation,
                i = e.$nextEl,
                n = e.$prevEl;
              i &&
                i[t.enabled ? "removeClass" : "addClass"](
                  t.params.navigation.lockClass
                ),
                n &&
                  n[t.enabled ? "removeClass" : "addClass"](
                    t.params.navigation.lockClass
                  );
            },
            click: function (t, e) {
              var i = t.navigation,
                n = i.$nextEl,
                a = i.$prevEl,
                r = e.target;
              if (
                t.params.navigation.hideOnClick &&
                !g(r).is(a) &&
                !g(r).is(n)
              ) {
                if (
                  t.pagination &&
                  t.params.pagination &&
                  t.params.pagination.clickable &&
                  (t.pagination.el === r || t.pagination.el.contains(r))
                )
                  return;
                var s;
                n
                  ? (s = n.hasClass(t.params.navigation.hiddenClass))
                  : a && (s = a.hasClass(t.params.navigation.hiddenClass)),
                  !0 === s
                    ? t.emit("navigationShow")
                    : t.emit("navigationHide"),
                  n && n.toggleClass(t.params.navigation.hiddenClass),
                  a && a.toggleClass(t.params.navigation.hiddenClass);
              }
            },
          },
        },
        {
          name: "pagination",
          params: {
            pagination: {
              el: null,
              bulletElement: "span",
              clickable: !1,
              hideOnClick: !1,
              renderBullet: null,
              renderProgressbar: null,
              renderFraction: null,
              renderCustom: null,
              progressbarOpposite: !1,
              type: "bullets",
              dynamicBullets: !1,
              dynamicMainBullets: 1,
              formatFractionCurrent: function (t) {
                return t;
              },
              formatFractionTotal: function (t) {
                return t;
              },
              bulletClass: "swiper-pagination-bullet",
              bulletActiveClass: "swiper-pagination-bullet-active",
              modifierClass: "swiper-pagination-",
              currentClass: "swiper-pagination-current",
              totalClass: "swiper-pagination-total",
              hiddenClass: "swiper-pagination-hidden",
              progressbarFillClass: "swiper-pagination-progressbar-fill",
              progressbarOppositeClass:
                "swiper-pagination-progressbar-opposite",
              clickableClass: "swiper-pagination-clickable",
              lockClass: "swiper-pagination-lock",
            },
          },
          create: function () {
            P(this, { pagination: e({ dynamicBulletIndex: 0 }, tt) });
          },
          on: {
            init: function (t) {
              t.pagination.init(), t.pagination.render(), t.pagination.update();
            },
            activeIndexChange: function (t) {
              (t.params.loop || void 0 === t.snapIndex) &&
                t.pagination.update();
            },
            snapIndexChange: function (t) {
              t.params.loop || t.pagination.update();
            },
            slidesLengthChange: function (t) {
              t.params.loop && (t.pagination.render(), t.pagination.update());
            },
            snapGridLengthChange: function (t) {
              t.params.loop || (t.pagination.render(), t.pagination.update());
            },
            destroy: function (t) {
              t.pagination.destroy();
            },
            "enable disable": function (t) {
              var e = t.pagination.$el;
              e &&
                e[t.enabled ? "removeClass" : "addClass"](
                  t.params.pagination.lockClass
                );
            },
            click: function (t, e) {
              var i = e.target;
              if (
                t.params.pagination.el &&
                t.params.pagination.hideOnClick &&
                t.pagination.$el.length > 0 &&
                !g(i).hasClass(t.params.pagination.bulletClass)
              ) {
                if (
                  t.navigation &&
                  ((t.navigation.nextEl && i === t.navigation.nextEl) ||
                    (t.navigation.prevEl && i === t.navigation.prevEl))
                )
                  return;
                !0 ===
                t.pagination.$el.hasClass(t.params.pagination.hiddenClass)
                  ? t.emit("paginationShow")
                  : t.emit("paginationHide"),
                  t.pagination.$el.toggleClass(t.params.pagination.hiddenClass);
              }
            },
          },
        },
        {
          name: "scrollbar",
          params: {
            scrollbar: {
              el: null,
              dragSize: "auto",
              hide: !1,
              draggable: !1,
              snapOnRelease: !0,
              lockClass: "swiper-scrollbar-lock",
              dragClass: "swiper-scrollbar-drag",
            },
          },
          create: function () {
            P(this, {
              scrollbar: e(
                { isTouched: !1, timeout: null, dragTimeout: null },
                et
              ),
            });
          },
          on: {
            init: function (t) {
              t.scrollbar.init(),
                t.scrollbar.updateSize(),
                t.scrollbar.setTranslate();
            },
            update: function (t) {
              t.scrollbar.updateSize();
            },
            resize: function (t) {
              t.scrollbar.updateSize();
            },
            observerUpdate: function (t) {
              t.scrollbar.updateSize();
            },
            setTranslate: function (t) {
              t.scrollbar.setTranslate();
            },
            setTransition: function (t, e) {
              t.scrollbar.setTransition(e);
            },
            "enable disable": function (t) {
              var e = t.scrollbar.$el;
              e &&
                e[t.enabled ? "removeClass" : "addClass"](
                  t.params.scrollbar.lockClass
                );
            },
            destroy: function (t) {
              t.scrollbar.destroy();
            },
          },
        },
        {
          name: "parallax",
          params: { parallax: { enabled: !1 } },
          create: function () {
            P(this, { parallax: e({}, it) });
          },
          on: {
            beforeInit: function (t) {
              t.params.parallax.enabled &&
                ((t.params.watchSlidesProgress = !0),
                (t.originalParams.watchSlidesProgress = !0));
            },
            init: function (t) {
              t.params.parallax.enabled && t.parallax.setTranslate();
            },
            setTranslate: function (t) {
              t.params.parallax.enabled && t.parallax.setTranslate();
            },
            setTransition: function (t, e) {
              t.params.parallax.enabled && t.parallax.setTransition(e);
            },
          },
        },
        {
          name: "zoom",
          params: {
            zoom: {
              enabled: !1,
              maxRatio: 3,
              minRatio: 1,
              toggle: !0,
              containerClass: "swiper-zoom-container",
              zoomedSlideClass: "swiper-slide-zoomed",
            },
          },
          create: function () {
            var t = this;
            P(t, {
              zoom: e(
                {
                  enabled: !1,
                  scale: 1,
                  currentScale: 1,
                  isScaling: !1,
                  gesture: {
                    $slideEl: void 0,
                    slideWidth: void 0,
                    slideHeight: void 0,
                    $imageEl: void 0,
                    $imageWrapEl: void 0,
                    maxRatio: 3,
                  },
                  image: {
                    isTouched: void 0,
                    isMoved: void 0,
                    currentX: void 0,
                    currentY: void 0,
                    minX: void 0,
                    minY: void 0,
                    maxX: void 0,
                    maxY: void 0,
                    width: void 0,
                    height: void 0,
                    startX: void 0,
                    startY: void 0,
                    touchesStart: {},
                    touchesCurrent: {},
                  },
                  velocity: {
                    x: void 0,
                    y: void 0,
                    prevPositionX: void 0,
                    prevPositionY: void 0,
                    prevTime: void 0,
                  },
                },
                nt
              ),
            });
            var i = 1;
            Object.defineProperty(t.zoom, "scale", {
              get: function () {
                return i;
              },
              set: function (e) {
                if (i !== e) {
                  var n = t.zoom.gesture.$imageEl
                      ? t.zoom.gesture.$imageEl[0]
                      : void 0,
                    a = t.zoom.gesture.$slideEl
                      ? t.zoom.gesture.$slideEl[0]
                      : void 0;
                  t.emit("zoomChange", e, n, a);
                }
                i = e;
              },
            });
          },
          on: {
            init: function (t) {
              t.params.zoom.enabled && t.zoom.enable();
            },
            destroy: function (t) {
              t.zoom.disable();
            },
            touchStart: function (t, e) {
              t.zoom.enabled && t.zoom.onTouchStart(e);
            },
            touchEnd: function (t, e) {
              t.zoom.enabled && t.zoom.onTouchEnd(e);
            },
            doubleTap: function (t, e) {
              !t.animating &&
                t.params.zoom.enabled &&
                t.zoom.enabled &&
                t.params.zoom.toggle &&
                t.zoom.toggle(e);
            },
            transitionEnd: function (t) {
              t.zoom.enabled &&
                t.params.zoom.enabled &&
                t.zoom.onTransitionEnd();
            },
            slideChange: function (t) {
              t.zoom.enabled &&
                t.params.zoom.enabled &&
                t.params.cssMode &&
                t.zoom.onTransitionEnd();
            },
          },
        },
        {
          name: "lazy",
          params: {
            lazy: {
              checkInView: !1,
              enabled: !1,
              loadPrevNext: !1,
              loadPrevNextAmount: 1,
              loadOnTransitionStart: !1,
              scrollingElement: "",
              elementClass: "swiper-lazy",
              loadingClass: "swiper-lazy-loading",
              loadedClass: "swiper-lazy-loaded",
              preloaderClass: "swiper-lazy-preloader",
            },
          },
          create: function () {
            P(this, { lazy: e({ initialImageLoaded: !1 }, at) });
          },
          on: {
            beforeInit: function (t) {
              t.params.lazy.enabled &&
                t.params.preloadImages &&
                (t.params.preloadImages = !1);
            },
            init: function (t) {
              t.params.lazy.enabled &&
                !t.params.loop &&
                0 === t.params.initialSlide &&
                (t.params.lazy.checkInView
                  ? t.lazy.checkInViewOnLoad()
                  : t.lazy.load());
            },
            scroll: function (t) {
              t.params.freeMode && !t.params.freeModeSticky && t.lazy.load();
            },
            "scrollbarDragMove resize _freeModeNoMomentumRelease": function (
              t
            ) {
              t.params.lazy.enabled && t.lazy.load();
            },
            transitionStart: function (t) {
              t.params.lazy.enabled &&
                (t.params.lazy.loadOnTransitionStart ||
                  (!t.params.lazy.loadOnTransitionStart &&
                    !t.lazy.initialImageLoaded)) &&
                t.lazy.load();
            },
            transitionEnd: function (t) {
              t.params.lazy.enabled &&
                !t.params.lazy.loadOnTransitionStart &&
                t.lazy.load();
            },
            slideChange: function (t) {
              var e = t.params,
                i = e.lazy,
                n = e.cssMode,
                a = e.watchSlidesVisibility,
                r = e.watchSlidesProgress,
                s = e.touchReleaseOnEdges,
                o = e.resistanceRatio;
              i.enabled && (n || ((a || r) && (s || 0 === o))) && t.lazy.load();
            },
          },
        },
        {
          name: "controller",
          params: { controller: { control: void 0, inverse: !1, by: "slide" } },
          create: function () {
            P(this, {
              controller: e({ control: this.params.controller.control }, rt),
            });
          },
          on: {
            update: function (t) {
              t.controller.control &&
                t.controller.spline &&
                ((t.controller.spline = void 0), delete t.controller.spline);
            },
            resize: function (t) {
              t.controller.control &&
                t.controller.spline &&
                ((t.controller.spline = void 0), delete t.controller.spline);
            },
            observerUpdate: function (t) {
              t.controller.control &&
                t.controller.spline &&
                ((t.controller.spline = void 0), delete t.controller.spline);
            },
            setTranslate: function (t, e, i) {
              t.controller.control && t.controller.setTranslate(e, i);
            },
            setTransition: function (t, e, i) {
              t.controller.control && t.controller.setTransition(e, i);
            },
          },
        },
        {
          name: "a11y",
          params: {
            a11y: {
              enabled: !0,
              notificationClass: "swiper-notification",
              prevSlideMessage: "Previous slide",
              nextSlideMessage: "Next slide",
              firstSlideMessage: "This is the first slide",
              lastSlideMessage: "This is the last slide",
              paginationBulletMessage: "Go to slide {{index}}",
              slideLabelMessage: "{{index}} / {{slidesLength}}",
              containerMessage: null,
              containerRoleDescriptionMessage: null,
              itemRoleDescriptionMessage: null,
              slideRole: "group",
            },
          },
          create: function () {
            P(this, {
              a11y: e({}, st, {
                liveRegion: g(
                  '<span class="' +
                    this.params.a11y.notificationClass +
                    '" aria-live="assertive" aria-atomic="true"></span>'
                ),
              }),
            });
          },
          on: {
            afterInit: function (t) {
              t.params.a11y.enabled &&
                (t.a11y.init(), t.a11y.updateNavigation());
            },
            toEdge: function (t) {
              t.params.a11y.enabled && t.a11y.updateNavigation();
            },
            fromEdge: function (t) {
              t.params.a11y.enabled && t.a11y.updateNavigation();
            },
            paginationUpdate: function (t) {
              t.params.a11y.enabled && t.a11y.updatePagination();
            },
            destroy: function (t) {
              t.params.a11y.enabled && t.a11y.destroy();
            },
          },
        },
        {
          name: "history",
          params: {
            history: { enabled: !1, root: "", replaceState: !1, key: "slides" },
          },
          create: function () {
            P(this, { history: e({}, ot) });
          },
          on: {
            init: function (t) {
              t.params.history.enabled && t.history.init();
            },
            destroy: function (t) {
              t.params.history.enabled && t.history.destroy();
            },
            "transitionEnd _freeModeNoMomentumRelease": function (t) {
              t.history.initialized &&
                t.history.setHistory(t.params.history.key, t.activeIndex);
            },
            slideChange: function (t) {
              t.history.initialized &&
                t.params.cssMode &&
                t.history.setHistory(t.params.history.key, t.activeIndex);
            },
          },
        },
        {
          name: "hash-navigation",
          params: {
            hashNavigation: { enabled: !1, replaceState: !1, watchState: !1 },
          },
          create: function () {
            P(this, { hashNavigation: e({ initialized: !1 }, lt) });
          },
          on: {
            init: function (t) {
              t.params.hashNavigation.enabled && t.hashNavigation.init();
            },
            destroy: function (t) {
              t.params.hashNavigation.enabled && t.hashNavigation.destroy();
            },
            "transitionEnd _freeModeNoMomentumRelease": function (t) {
              t.hashNavigation.initialized && t.hashNavigation.setHash();
            },
            slideChange: function (t) {
              t.hashNavigation.initialized &&
                t.params.cssMode &&
                t.hashNavigation.setHash();
            },
          },
        },
        {
          name: "autoplay",
          params: {
            autoplay: {
              enabled: !1,
              delay: 3e3,
              waitForTransition: !0,
              disableOnInteraction: !0,
              stopOnLastSlide: !1,
              reverseDirection: !1,
              pauseOnMouseEnter: !1,
            },
          },
          create: function () {
            P(this, { autoplay: e({}, ut, { running: !1, paused: !1 }) });
          },
          on: {
            init: function (t) {
              t.params.autoplay.enabled &&
                (t.autoplay.start(),
                r().addEventListener(
                  "visibilitychange",
                  t.autoplay.onVisibilityChange
                ),
                t.autoplay.attachMouseEvents());
            },
            beforeTransitionStart: function (t, e, i) {
              t.autoplay.running &&
                (i || !t.params.autoplay.disableOnInteraction
                  ? t.autoplay.pause(e)
                  : t.autoplay.stop());
            },
            sliderFirstMove: function (t) {
              t.autoplay.running &&
                (t.params.autoplay.disableOnInteraction
                  ? t.autoplay.stop()
                  : t.autoplay.pause());
            },
            touchEnd: function (t) {
              t.params.cssMode &&
                t.autoplay.paused &&
                !t.params.autoplay.disableOnInteraction &&
                t.autoplay.run();
            },
            destroy: function (t) {
              t.autoplay.detachMouseEvents(),
                t.autoplay.running && t.autoplay.stop(),
                r().removeEventListener(
                  "visibilitychange",
                  t.autoplay.onVisibilityChange
                );
            },
          },
        },
        {
          name: "effect-fade",
          params: { fadeEffect: { crossFade: !1 } },
          create: function () {
            P(this, { fadeEffect: e({}, ct) });
          },
          on: {
            beforeInit: function (t) {
              if ("fade" === t.params.effect) {
                t.classNames.push(t.params.containerModifierClass + "fade");
                var e = {
                  slidesPerView: 1,
                  slidesPerColumn: 1,
                  slidesPerGroup: 1,
                  watchSlidesProgress: !0,
                  spaceBetween: 0,
                  virtualTranslate: !0,
                };
                E(t.params, e), E(t.originalParams, e);
              }
            },
            setTranslate: function (t) {
              "fade" === t.params.effect && t.fadeEffect.setTranslate();
            },
            setTransition: function (t, e) {
              "fade" === t.params.effect && t.fadeEffect.setTransition(e);
            },
          },
        },
        {
          name: "effect-cube",
          params: {
            cubeEffect: {
              slideShadows: !0,
              shadow: !0,
              shadowOffset: 20,
              shadowScale: 0.94,
            },
          },
          create: function () {
            P(this, { cubeEffect: e({}, dt) });
          },
          on: {
            beforeInit: function (t) {
              if ("cube" === t.params.effect) {
                t.classNames.push(t.params.containerModifierClass + "cube"),
                  t.classNames.push(t.params.containerModifierClass + "3d");
                var e = {
                  slidesPerView: 1,
                  slidesPerColumn: 1,
                  slidesPerGroup: 1,
                  watchSlidesProgress: !0,
                  resistanceRatio: 0,
                  spaceBetween: 0,
                  centeredSlides: !1,
                  virtualTranslate: !0,
                };
                E(t.params, e), E(t.originalParams, e);
              }
            },
            setTranslate: function (t) {
              "cube" === t.params.effect && t.cubeEffect.setTranslate();
            },
            setTransition: function (t, e) {
              "cube" === t.params.effect && t.cubeEffect.setTransition(e);
            },
          },
        },
        {
          name: "effect-flip",
          params: { flipEffect: { slideShadows: !0, limitRotation: !0 } },
          create: function () {
            P(this, { flipEffect: e({}, ht) });
          },
          on: {
            beforeInit: function (t) {
              if ("flip" === t.params.effect) {
                t.classNames.push(t.params.containerModifierClass + "flip"),
                  t.classNames.push(t.params.containerModifierClass + "3d");
                var e = {
                  slidesPerView: 1,
                  slidesPerColumn: 1,
                  slidesPerGroup: 1,
                  watchSlidesProgress: !0,
                  spaceBetween: 0,
                  virtualTranslate: !0,
                };
                E(t.params, e), E(t.originalParams, e);
              }
            },
            setTranslate: function (t) {
              "flip" === t.params.effect && t.flipEffect.setTranslate();
            },
            setTransition: function (t, e) {
              "flip" === t.params.effect && t.flipEffect.setTransition(e);
            },
          },
        },
        {
          name: "effect-coverflow",
          params: {
            coverflowEffect: {
              rotate: 50,
              stretch: 0,
              depth: 100,
              scale: 1,
              modifier: 1,
              slideShadows: !0,
            },
          },
          create: function () {
            P(this, { coverflowEffect: e({}, pt) });
          },
          on: {
            beforeInit: function (t) {
              "coverflow" === t.params.effect &&
                (t.classNames.push(
                  t.params.containerModifierClass + "coverflow"
                ),
                t.classNames.push(t.params.containerModifierClass + "3d"),
                (t.params.watchSlidesProgress = !0),
                (t.originalParams.watchSlidesProgress = !0));
            },
            setTranslate: function (t) {
              "coverflow" === t.params.effect &&
                t.coverflowEffect.setTranslate();
            },
            setTransition: function (t, e) {
              "coverflow" === t.params.effect &&
                t.coverflowEffect.setTransition(e);
            },
          },
        },
        {
          name: "thumbs",
          params: {
            thumbs: {
              swiper: null,
              multipleActiveThumbs: !0,
              autoScrollOffset: 0,
              slideThumbActiveClass: "swiper-slide-thumb-active",
              thumbsContainerClass: "swiper-container-thumbs",
            },
          },
          create: function () {
            P(this, { thumbs: e({ swiper: null, initialized: !1 }, ft) });
          },
          on: {
            beforeInit: function (t) {
              var e = t.params.thumbs;
              e && e.swiper && (t.thumbs.init(), t.thumbs.update(!0));
            },
            slideChange: function (t) {
              t.thumbs.swiper && t.thumbs.update();
            },
            update: function (t) {
              t.thumbs.swiper && t.thumbs.update();
            },
            resize: function (t) {
              t.thumbs.swiper && t.thumbs.update();
            },
            observerUpdate: function (t) {
              t.thumbs.swiper && t.thumbs.update();
            },
            setTransition: function (t, e) {
              var i = t.thumbs.swiper;
              i && i.setTransition(e);
            },
            beforeDestroy: function (t) {
              var e = t.thumbs.swiper;
              e && t.thumbs.swiperCreated && e && e.destroy();
            },
          },
        },
      ];
    return U.use(mt), U;
  }),
  (function (t, e) {
    "function" == typeof define && define.amd
      ? define([], e)
      : "object" ===
          ("undefined" == typeof module ? "undefined" : _typeof(module)) &&
        module.exports
      ? (module.exports = e())
      : (t.CircleProgress = e());
  })("undefined" != typeof self ? self : void 0, function () {
    function t(e, i, n, a, r) {
      var s,
        o = "string" == typeof e ? t.easings[e] : e;
      requestAnimationFrame(function t(e) {
        (e -= s = s || e), (e = Math.min(e, a));
        var l = o(e, i, n, a);
        r(l), e < a ? requestAnimationFrame(t) : r(i + n);
      });
    }
    !(function () {
      try {
        if (
          "undefined" == typeof SVGElement ||
          Boolean(SVGElement.prototype.innerHTML)
        )
          return;
      } catch (t) {
        return;
      }
      function t(e) {
        switch (e.nodeType) {
          case 1:
            return (function (e) {
              var i = "";
              return (
                (i += "<" + e.tagName),
                e.hasAttributes() &&
                  [].forEach.call(e.attributes, function (t) {
                    i += " " + t.name + '="' + t.value + '"';
                  }),
                (i += ">"),
                e.hasChildNodes() &&
                  [].forEach.call(e.childNodes, function (e) {
                    i += t(e);
                  }),
                (i += "</" + e.tagName + ">")
              );
            })(e);
          case 3:
            return e.textContent
              .replace(/&/g, "&amp;")
              .replace(/</g, "&lt;")
              .replace(/>/g, "&gt;");
          case 8:
            return "\x3c!--" + e.nodeValue + "--\x3e";
        }
      }
      Object.defineProperty(SVGElement.prototype, "innerHTML", {
        get: function () {
          var e = "";
          return (
            [].forEach.call(this.childNodes, function (i) {
              e += t(i);
            }),
            e
          );
        },
        set: function (t) {
          for (; this.firstChild; ) this.removeChild(this.firstChild);
          try {
            var e = new DOMParser();
            e.async = !1;
            var i =
                "<svg xmlns='http://www.w3.org/2000/svg' xmlns:xlink='http://www.w3.org/1999/xlink'>" +
                t +
                "</svg>",
              n = e.parseFromString(i, "text/xml").documentElement;
            [].forEach.call(
              n.childNodes,
              function (t) {
                this.appendChild(this.ownerDocument.importNode(t, !0));
              }.bind(this)
            );
          } catch (t) {
            throw new Error("Error parsing markup string");
          }
        },
      }),
        Object.defineProperty(SVGElement.prototype, "innerSVG", {
          get: function () {
            return this.innerHTML;
          },
          set: function (t) {
            this.innerHTML = t;
          },
        });
    })();
    var e,
      i,
      n,
      a,
      r,
      s,
      o =
        ((e = function (t, e, n, a) {
          var r, s;
          if (
            ((a = a || document),
            (s = Object.create(i)),
            "string" == typeof t && (t = a.querySelector(t)),
            t)
          )
            return (
              (r = a.createElementNS(
                "http://www.w3.org/2000/svg",
                "svg"
              )).setAttribute("version", "1.1"),
              e && r.setAttribute("width", e),
              n && r.setAttribute("height", n),
              e && n && r.setAttribute("viewBox", "0 0 " + e + " " + n),
              t.appendChild(r),
              (s.svg = r),
              s
            );
        }),
        (i = {
          element: function (t, e, i, a) {
            var r = n(this, t, e, a);
            return i && (r.el.innerHTML = i), r;
          },
        }),
        (n = function (t, e, i, n, r) {
          var s;
          return (
            (r = r || document),
            ((s = Object.create(a)).el = r.createElementNS(
              "http://www.w3.org/2000/svg",
              e
            )),
            s.attr(i),
            (n ? n.el || n : t.svg).appendChild(s.el),
            s
          );
        }),
        (a = {
          attr: function (t, e) {
            if (void 0 === t) return this;
            if ("object" !== _typeof(t))
              return void 0 === e
                ? this.el.getAttributeNS(null, t)
                : (this.el.setAttribute(t, e), this);
            for (var i in t) this.attr(i, t[i]);
            return this;
          },
          content: function (t) {
            return (this.el.innerHTML = t), this;
          },
        }),
        e);
    return (
      (t.easings = {
        linear: function (t, e, i, n) {
          return (i * t) / n + e;
        },
        easeInQuad: function (t, e, i, n) {
          return i * (t /= n) * t + e;
        },
        easeOutQuad: function (t, e, i, n) {
          return -i * (t /= n) * (t - 2) + e;
        },
        easeInOutQuad: function (t, e, i, n) {
          return (t /= n / 2) < 1
            ? (i / 2) * t * t + e
            : (-i / 2) * (--t * (t - 2) - 1) + e;
        },
        easeInCubic: function (t, e, i, n) {
          return i * (t /= n) * t * t + e;
        },
        easeOutCubic: function (t, e, i, n) {
          return (t /= n), i * (--t * t * t + 1) + e;
        },
        easeInOutCubic: function (t, e, i, n) {
          return (t /= n / 2) < 1
            ? (i / 2) * t * t * t + e
            : (i / 2) * ((t -= 2) * t * t + 2) + e;
        },
        easeInQuart: function (t, e, i, n) {
          return i * (t /= n) * t * t * t + e;
        },
        easeOutQuart: function (t, e, i, n) {
          return (t /= n), -i * (--t * t * t * t - 1) + e;
        },
        easeInOutQuart: function (t, e, i, n) {
          return (t /= n / 2) < 1
            ? (i / 2) * t * t * t * t + e
            : (-i / 2) * ((t -= 2) * t * t * t - 2) + e;
        },
        easeInQuint: function (t, e, i, n) {
          return i * (t /= n) * t * t * t * t + e;
        },
        easeOutQuint: function (t, e, i, n) {
          return (t /= n), i * (--t * t * t * t * t + 1) + e;
        },
        easeInOutQuint: function (t, e, i, n) {
          return (t /= n / 2) < 1
            ? (i / 2) * t * t * t * t * t + e
            : (i / 2) * ((t -= 2) * t * t * t * t + 2) + e;
        },
        easeInSine: function (t, e, i, n) {
          return -i * Math.cos((t / n) * (Math.PI / 2)) + i + e;
        },
        easeOutSine: function (t, e, i, n) {
          return i * Math.sin((t / n) * (Math.PI / 2)) + e;
        },
        easeInOutSine: function (t, e, i, n) {
          return (-i / 2) * (Math.cos((Math.PI * t) / n) - 1) + e;
        },
        easeInExpo: function (t, e, i, n) {
          return i * Math.pow(2, 10 * (t / n - 1)) + e;
        },
        easeOutExpo: function (t, e, i, n) {
          return i * (1 - Math.pow(2, (-10 * t) / n)) + e;
        },
        easeInOutExpo: function (t, e, i, n) {
          return (t /= n / 2) < 1
            ? (i / 2) * Math.pow(2, 10 * (t - 1)) + e
            : (t--, (i / 2) * (2 - Math.pow(2, -10 * t)) + e);
        },
        easeInCirc: function (t, e, i, n) {
          return (t /= n), -i * (Math.sqrt(1 - t * t) - 1) + e;
        },
        easeOutCirc: function (t, e, i, n) {
          return (t /= n), t--, i * Math.sqrt(1 - t * t) + e;
        },
        easeInOutCirc: function (t, e, i, n) {
          return (t /= n / 2) < 1
            ? (-i / 2) * (Math.sqrt(1 - t * t) - 1) + e
            : ((t -= 2), (i / 2) * (Math.sqrt(1 - t * t) + 1) + e);
        },
      }),
      (r = {
        polarToCartesian: function (t, e) {
          return {
            x: t * Math.cos((e * Math.PI) / 180),
            y: t * Math.sin((e * Math.PI) / 180),
          };
        },
      }),
      ((s = (function () {
        function e(t) {
          var i,
            n =
              1 < arguments.length && void 0 !== arguments[1]
                ? arguments[1]
                : {},
            a =
              2 < arguments.length && void 0 !== arguments[2]
                ? arguments[2]
                : document;
          if (
            (_classCallCheck(this, e),
            "string" == typeof t && (t = a.querySelector(t)),
            !t)
          )
            throw new Error(
              "CircleProgress: you must pass the container element as the first argument"
            );
          if (t.circleProgress) return t.circleProgress;
          ((t.circleProgress = this).doc = a),
            t.setAttribute("role", "progressbar"),
            (this.el = t),
            (n = _objectSpread(_objectSpread({}, e.defaults), n)),
            Object.defineProperty(this, "_attrs", {
              value: {},
              enumerable: !1,
            }),
            (i = "valueOnCircle" === n.textFormat ? 16 : 8),
            (this.graph = { paper: o(t, 100, 100), angle: 0 }),
            this.graph.paper.svg.setAttribute("class", "circle-progress"),
            (this.graph.circle = this.graph.paper
              .element("circle")
              .attr({
                class: "circle-progress-circle",
                cx: 50,
                cy: 50,
                r: 50 - i / 2,
                fill: "none",
                stroke: "#ddd",
                "stroke-width": i,
              })),
            (this.graph.sector = this.graph.paper
              .element("path")
              .attr({
                d: e._makeSectorPath(50, 50, 50 - i / 2, 0, 0),
                class: "circle-progress-value",
                fill: "none",
                stroke: "#00E699",
                "stroke-width": i,
              })),
            (this.graph.text = this.graph.paper.element("text", {
              class: "circle-progress-text",
              x: 50,
              y: 50,
              font: "16px Arial, sans-serif",
              "text-anchor": "middle",
              fill: "#999",
            })),
            this._initText(),
            this.attr(
              [
                "indeterminateText",
                "textFormat",
                "startAngle",
                "clockwise",
                "animation",
                "animationDuration",
                "constrain",
                "min",
                "max",
                "value",
              ]
                .filter(function (t) {
                  return t in n;
                })
                .map(function (t) {
                  return [t, n[t]];
                })
            );
        }
        return (
          _createClass(e, [
            {
              key: "value",
              get: function () {
                return this._attrs.value;
              },
              set: function (t) {
                this.attr("value", t);
              },
            },
            {
              key: "min",
              get: function () {
                return this._attrs.min;
              },
              set: function (t) {
                this.attr("min", t);
              },
            },
            {
              key: "max",
              get: function () {
                return this._attrs.max;
              },
              set: function (t) {
                this.attr("max", t);
              },
            },
            {
              key: "startAngle",
              get: function () {
                return this._attrs.startAngle;
              },
              set: function (t) {
                this.attr("startAngle", t);
              },
            },
            {
              key: "clockwise",
              get: function () {
                return this._attrs.clockwise;
              },
              set: function (t) {
                this.attr("clockwise", t);
              },
            },
            {
              key: "constrain",
              get: function () {
                return this._attrs.constrain;
              },
              set: function (t) {
                this.attr("constrain", t);
              },
            },
            {
              key: "indeterminateText",
              get: function () {
                return this._attrs.indeterminateText;
              },
              set: function (t) {
                this.attr("indeterminateText", t);
              },
            },
            {
              key: "textFormat",
              get: function () {
                return this._attrs.textFormat;
              },
              set: function (t) {
                this.attr("textFormat", t);
              },
            },
            {
              key: "animation",
              get: function () {
                return this._attrs.animation;
              },
              set: function (t) {
                this.attr("animation", t);
              },
            },
            {
              key: "animationDuration",
              get: function () {
                return this._attrs.animationDuration;
              },
              set: function (t) {
                this.attr("animationDuration", t);
              },
            },
          ]),
          _createClass(
            e,
            [
              {
                key: "attr",
                value: function (t, e) {
                  var i = this;
                  if ("string" == typeof t)
                    return 1 === arguments.length
                      ? this._attrs[t]
                      : (this._set(arguments[0], e), this._updateGraph(), this);
                  if ("object" !== _typeof(t))
                    throw new TypeError(
                      'Wrong argument passed to attr. Expected object, got "'.concat(
                        _typeof(t),
                        '"'
                      )
                    );
                  return (
                    Array.isArray(t) ||
                      (t = Object.keys(t).map(function (e) {
                        return [e, t[e]];
                      })),
                    t.forEach(function (t) {
                      return i._set(t[0], t[1]);
                    }),
                    this._updateGraph(),
                    this
                  );
                },
              },
              {
                key: "_set",
                value: function (t, e) {
                  var i,
                    n = {
                      value: "aria-valuenow",
                      min: "aria-valuemin",
                      max: "aria-valuemax",
                    };
                  if (void 0 === (e = this._formatValue(t, e)))
                    throw new TypeError(
                      "Failed to set the ".concat(
                        t,
                        " property on CircleProgress: The provided value is non-finite."
                      )
                    );
                  this._attrs[t] !== e &&
                    (("min" === t && e >= this.max) ||
                      ("max" === t && e <= this.min) ||
                      ("value" === t &&
                        void 0 !== e &&
                        this.constrain &&
                        (null != this.min && e < this.min && (e = this.min),
                        null != this.max && e > this.max && (e = this.max)),
                      (this._attrs[t] = e),
                      t in n &&
                        (void 0 !== e
                          ? this.el.setAttribute(n[t], e)
                          : this.el.removeAttribute(n[t])),
                      -1 !== ["min", "max", "constrain"].indexOf(t) &&
                        (this.value > this.max || this.value < this.min) &&
                        (this.value = Math.min(
                          this.max,
                          Math.max(this.min, this.value)
                        )),
                      "textFormat" === t &&
                        (this._initText(),
                        (i = "valueOnCircle" === e ? 16 : 8),
                        this.graph.sector.attr("stroke-width", i),
                        this.graph.circle.attr("stroke-width", i))));
                },
              },
              {
                key: "_formatValue",
                value: function (e, i) {
                  switch (e) {
                    case "value":
                    case "min":
                    case "max":
                      (i = parseFloat(i)), isFinite(i) || (i = void 0);
                      break;
                    case "startAngle":
                      (i = parseFloat(i)),
                        (i = isFinite(i)
                          ? Math.max(0, Math.min(360, i))
                          : void 0);
                      break;
                    case "clockwise":
                    case "constrain":
                      i = !!i;
                      break;
                    case "indeterminateText":
                      i = "" + i;
                      break;
                    case "textFormat":
                      if (
                        "function" != typeof i &&
                        -1 ===
                          [
                            "valueOnCircle",
                            "horizontal",
                            "vertical",
                            "percent",
                            "value",
                            "none",
                          ].indexOf(i)
                      )
                        throw new Error(
                          'Failed to set the "textFormat" property on CircleProgress: the provided value "'.concat(
                            i,
                            '" is not a legal textFormat identifier.'
                          )
                        );
                      break;
                    case "animation":
                      if ("string" != typeof i && "function" != typeof i)
                        throw new TypeError(
                          'Failed to set "animation" property on CircleProgress: the value must be either string or function, '.concat(
                            _typeof(i),
                            " passed."
                          )
                        );
                      if ("string" == typeof i && "none" !== i && !t.easings[i])
                        throw new Error(
                          'Failed to set "animation" on CircleProgress: the provided value '.concat(
                            i,
                            " is not a legal easing function name."
                          )
                        );
                  }
                  return i;
                },
              },
              {
                key: "_valToAngle",
                value: function () {
                  var t;
                  return this._isIndeterminate()
                    ? 0
                    : 0 === this.max
                    ? this.value
                      ? 360
                      : 0
                    : ((t = ((this.value - this.min) / this.max) * 360),
                      Math.min(360, Math.max(0, t)));
                },
              },
              {
                key: "_isIndeterminate",
                value: function () {
                  return !(
                    "number" == typeof this.value &&
                    "number" == typeof this.max &&
                    "number" == typeof this.min
                  );
                },
              },
              {
                key: "_positionValueText",
                value: function (t) {
                  var e = r.polarToCartesian(this._getRadius(), t);
                  this.graph.textVal.attr({ x: 50 + e.x, y: 50 + e.y });
                },
              },
              {
                key: "_initText",
                value: function () {
                  switch ((this.graph.text.content(""), this.textFormat)) {
                    case "valueOnCircle":
                      (this.graph.textVal = this.graph.paper.element(
                        "tspan",
                        {
                          x: 0,
                          y: 0,
                          class: "circle-progress-text-value",
                          "font-size": "12",
                          fill:
                            "valueOnCircle" === this.textFormat
                              ? "#fff"
                              : "#888",
                        },
                        "",
                        this.graph.text
                      )),
                        (this.graph.textMax = this.graph.paper.element(
                          "tspan",
                          {
                            x: 50,
                            y: 50,
                            class: "circle-progress-text-max",
                            "font-size": "22",
                            "font-weight": "bold",
                            fill: "#ddd",
                          },
                          "",
                          this.graph.text
                        )),
                        this.graph.text.el.hasAttribute("dominant-baseline") ||
                          this.graph.textMax.attr("dy", "0.4em");
                      break;
                    case "horizontal":
                      (this.graph.textVal = this.graph.paper.element(
                        "tspan",
                        { class: "circle-progress-text-value" },
                        "",
                        this.graph.text
                      )),
                        (this.graph.textSeparator = this.graph.paper.element(
                          "tspan",
                          { class: "circle-progress-text-separator" },
                          "/",
                          this.graph.text
                        )),
                        (this.graph.textMax = this.graph.paper.element(
                          "tspan",
                          { class: "circle-progress-text-max" },
                          "",
                          this.graph.text
                        ));
                      break;
                    case "vertical":
                      this.graph.text.el.hasAttribute("dominant-baseline") &&
                        this.graph.text.attr(
                          "dominant-baseline",
                          "text-after-edge"
                        ),
                        (this.graph.textVal = this.graph.paper.element(
                          "tspan",
                          {
                            class: "circle-progress-text-value",
                            x: 50,
                            dy: "-0.2em",
                          },
                          "",
                          this.graph.text
                        )),
                        (this.graph.textSeparator = this.graph.paper.element(
                          "tspan",
                          {
                            class: "circle-progress-text-separator",
                            x: 50,
                            dy: "0.1em",
                            "font-family": "Arial, sans-serif",
                          },
                          "___",
                          this.graph.text
                        )),
                        (this.graph.textMax = this.graph.paper.element(
                          "tspan",
                          {
                            class: "circle-progress-text-max",
                            x: 50,
                            dy: "1.2em",
                          },
                          "",
                          this.graph.text
                        ));
                  }
                  "vertical" !== this.textFormat &&
                    (this.graph.text.el.hasAttribute("dominant-baseline")
                      ? this.graph.text.attr("dominant-baseline", "central")
                      : this.graph.text.attr("dy", "0.4em"));
                },
              },
              {
                key: "_updateGraph",
                value: function () {
                  var i,
                    n,
                    a,
                    r = this,
                    s = this.startAngle - 90;
                  this._isIndeterminate()
                    ? "valueOnCircle" === this.textFormat &&
                      this._positionValueText(s)
                    : ((i = this._valToAngle(this.value)),
                      (n = this._getRadius()),
                      (a = this.clockwise),
                      this.graph.circle.attr("r", n),
                      "none" !== this.animation && i !== this.graph.angle
                        ? t(
                            this.animation,
                            this.graph.angle,
                            i - this.graph.angle,
                            this.animationDuration,
                            function (t) {
                              r.graph.sector.attr(
                                "d",
                                e._makeSectorPath(50, 50, n, s, t, a)
                              );
                            }
                          )
                        : this.graph.sector.attr(
                            "d",
                            e._makeSectorPath(50, 50, n, s, i, a)
                          ),
                      (this.graph.angle = i),
                      "valueOnCircle" === this.textFormat &&
                        this._positionValueText((2 * s + i) / 2)),
                    "function" == typeof this.textFormat
                      ? this.graph.text.content(
                          this.textFormat(this.value, this.max)
                        )
                      : "value" === this.textFormat
                      ? (this.graph.text.el.textContent =
                          void 0 !== this.value
                            ? this.value
                            : this.indeterminateText)
                      : "percent" === this.textFormat
                      ? (this.graph.text.el.textContent =
                          (void 0 !== this.value && null != this.max
                            ? Math.round((this.value / this.max) * 100)
                            : this.indeterminateText) + "%")
                      : "none" === this.textFormat
                      ? (this.graph.text.el.textContent = "")
                      : ((this.graph.textVal.el.textContent =
                          void 0 !== this.value
                            ? this.value
                            : this.indeterminateText),
                        (this.graph.textMax.el.textContent =
                          void 0 !== this.max
                            ? this.max
                            : this.indeterminateText));
                },
              },
              {
                key: "_getRadius",
                value: function () {
                  return (
                    50 -
                    Math.max(
                      parseFloat(
                        this.doc.defaultView.getComputedStyle(
                          this.graph.circle.el,
                          null
                        )["stroke-width"]
                      ),
                      parseFloat(
                        this.doc.defaultView.getComputedStyle(
                          this.graph.sector.el,
                          null
                        )["stroke-width"]
                      )
                    ) /
                      2
                  );
                },
              },
            ],
            [
              {
                key: "_makeSectorPath",
                value: function (t, e, i, n, a, s) {
                  0 < a && a < 0.3 ? (a = 0) : 359.999 < a && (a = 359.999);
                  var o = n + a * (2 * (s = !!s) - 1),
                    l = r.polarToCartesian(i, n),
                    u = r.polarToCartesian(i, o),
                    c = t + l.x,
                    d = t + u.x;
                  return [
                    "M",
                    c,
                    e + l.y,
                    "A",
                    i,
                    i,
                    0,
                    +(180 < a),
                    +s,
                    d,
                    e + u.y,
                  ].join(" ");
                },
              },
            ]
          ),
          e
        );
      })()).defaults = {
        startAngle: 0,
        min: 0,
        max: 1,
        constrain: !0,
        indeterminateText: "?",
        clockwise: !0,
        textFormat: "horizontal",
        animation: "easeInOutCubic",
        animationDuration: 600,
      }),
      s
    );
  }),
  (function (t, e) {
    "object" == typeof exports && "undefined" != typeof module
      ? e(exports)
      : "function" == typeof define && define.amd
      ? define(["exports"], e)
      : e((t.L = {}));
  })(this, function (t) {
    "use strict";
    function e(t) {
      for (var e, i, n = 1, a = arguments.length; n < a; n++)
        for (e in (i = arguments[n])) t[e] = i[e];
      return t;
    }
    var i =
      Object.create ||
      function (t) {
        return (n.prototype = t), new n();
      };
    function n() {}
    function a(t, e) {
      var i = Array.prototype.slice;
      if (t.bind) return t.bind.apply(t, i.call(arguments, 1));
      var n = i.call(arguments, 2);
      return function () {
        return t.apply(e, n.length ? n.concat(i.call(arguments)) : arguments);
      };
    }
    var r = 0;
    function s(t) {
      return (t._leaflet_id = t._leaflet_id || ++r), t._leaflet_id;
    }
    function o(t, e, i) {
      var n,
        a,
        r = function () {
          (n = !1), a && (s.apply(i, a), (a = !1));
        },
        s = function () {
          n
            ? (a = arguments)
            : (t.apply(i, arguments), setTimeout(r, e), (n = !0));
        };
      return s;
    }
    function l(t, e, i) {
      var n = e[1],
        a = e[0],
        r = n - a;
      return t === n && i ? t : ((((t - a) % r) + r) % r) + a;
    }
    function u() {
      return !1;
    }
    function c(t, e) {
      var i = Math.pow(10, void 0 === e ? 6 : e);
      return Math.round(t * i) / i;
    }
    function d(t) {
      return t.trim ? t.trim() : t.replace(/^\s+|\s+$/g, "");
    }
    function h(t) {
      return d(t).split(/\s+/);
    }
    function p(t, e) {
      for (var n in (Object.prototype.hasOwnProperty.call(t, "options") ||
        (t.options = t.options ? i(t.options) : {}),
      e))
        t.options[n] = e[n];
      return t.options;
    }
    function f(t, e, i) {
      var n = [];
      for (var a in t)
        n.push(
          encodeURIComponent(i ? a.toUpperCase() : a) +
            "=" +
            encodeURIComponent(t[a])
        );
      return (e && -1 !== e.indexOf("?") ? "&" : "?") + n.join("&");
    }
    var m = /\{ *([\w_-]+) *\}/g;
    function g(t, e) {
      return t.replace(m, function (t, i) {
        var n = e[i];
        if (void 0 === n)
          throw new Error("No value provided for variable " + t);
        return "function" == typeof n && (n = n(e)), n;
      });
    }
    var v =
      Array.isArray ||
      function (t) {
        return "[object Array]" === Object.prototype.toString.call(t);
      };
    function _(t, e) {
      for (var i = 0; i < t.length; i++) if (t[i] === e) return i;
      return -1;
    }
    var y = "data:image/gif;base64,R0lGODlhAQABAAD/ACwAAAAAAQABAAACADs=";
    function b(t) {
      return window["webkit" + t] || window["moz" + t] || window["ms" + t];
    }
    var w = 0;
    function x(t) {
      var e = +new Date(),
        i = Math.max(0, 16 - (e - w));
      return (w = e + i), window.setTimeout(t, i);
    }
    var S = window.requestAnimationFrame || b("RequestAnimationFrame") || x,
      T =
        window.cancelAnimationFrame ||
        b("CancelAnimationFrame") ||
        b("CancelRequestAnimationFrame") ||
        function (t) {
          window.clearTimeout(t);
        };
    function C(t, e, i) {
      if (!i || S !== x) return S.call(window, a(t, e));
      t.call(e);
    }
    function E(t) {
      t && T.call(window, t);
    }
    var P = {
      extend: e,
      create: i,
      bind: a,
      lastId: r,
      stamp: s,
      throttle: o,
      wrapNum: l,
      falseFn: u,
      formatNum: c,
      trim: d,
      splitWords: h,
      setOptions: p,
      getParamString: f,
      template: g,
      isArray: v,
      indexOf: _,
      emptyImageUrl: y,
      requestFn: S,
      cancelFn: T,
      requestAnimFrame: C,
      cancelAnimFrame: E,
    };
    function D() {}
    (D.extend = function (t) {
      function n() {
        this.initialize && this.initialize.apply(this, arguments),
          this.callInitHooks();
      }
      var a = (n.__super__ = this.prototype),
        r = i(a);
      for (var s in (((r.constructor = n).prototype = r), this))
        Object.prototype.hasOwnProperty.call(this, s) &&
          "prototype" !== s &&
          "__super__" !== s &&
          (n[s] = this[s]);
      return (
        t.statics && (e(n, t.statics), delete t.statics),
        t.includes &&
          ((function (t) {
            if ("undefined" != typeof L && L && L.Mixin) {
              t = v(t) ? t : [t];
              for (var e = 0; e < t.length; e++)
                t[e] === L.Mixin.Events &&
                  console.warn(
                    "Deprecated include of L.Mixin.Events: this property will be removed in future releases, please inherit from L.Evented instead.",
                    new Error().stack
                  );
            }
          })(t.includes),
          e.apply(null, [r].concat(t.includes)),
          delete t.includes),
        r.options && (t.options = e(i(r.options), t.options)),
        e(r, t),
        (r._initHooks = []),
        (r.callInitHooks = function () {
          if (!this._initHooksCalled) {
            a.callInitHooks && a.callInitHooks.call(this),
              (this._initHooksCalled = !0);
            for (var t = 0, e = r._initHooks.length; t < e; t++)
              r._initHooks[t].call(this);
          }
        }),
        n
      );
    }),
      (D.include = function (t) {
        return e(this.prototype, t), this;
      }),
      (D.mergeOptions = function (t) {
        return e(this.prototype.options, t), this;
      }),
      (D.addInitHook = function (t) {
        var e = Array.prototype.slice.call(arguments, 1),
          i =
            "function" == typeof t
              ? t
              : function () {
                  this[t].apply(this, e);
                };
        return (
          (this.prototype._initHooks = this.prototype._initHooks || []),
          this.prototype._initHooks.push(i),
          this
        );
      });
    var M = {
      on: function (t, e, i) {
        if ("object" == typeof t) for (var n in t) this._on(n, t[n], e);
        else
          for (var a = 0, r = (t = h(t)).length; a < r; a++)
            this._on(t[a], e, i);
        return this;
      },
      off: function (t, e, i) {
        if (t)
          if ("object" == typeof t) for (var n in t) this._off(n, t[n], e);
          else
            for (var a = 0, r = (t = h(t)).length; a < r; a++)
              this._off(t[a], e, i);
        else delete this._events;
        return this;
      },
      _on: function (t, e, i) {
        this._events = this._events || {};
        var n = this._events[t];
        n || ((n = []), (this._events[t] = n)), i === this && (i = void 0);
        for (var a = { fn: e, ctx: i }, r = n, s = 0, o = r.length; s < o; s++)
          if (r[s].fn === e && r[s].ctx === i) return;
        r.push(a);
      },
      _off: function (t, e, i) {
        var n, a, r;
        if (this._events && (n = this._events[t]))
          if (e) {
            if ((i === this && (i = void 0), n))
              for (a = 0, r = n.length; a < r; a++) {
                var s = n[a];
                if (s.ctx === i && s.fn === e)
                  return (
                    (s.fn = u),
                    this._firingCount && (this._events[t] = n = n.slice()),
                    void n.splice(a, 1)
                  );
              }
          } else {
            for (a = 0, r = n.length; a < r; a++) n[a].fn = u;
            delete this._events[t];
          }
      },
      fire: function (t, i, n) {
        if (!this.listens(t, n)) return this;
        var a = e({}, i, {
          type: t,
          target: this,
          sourceTarget: (i && i.sourceTarget) || this,
        });
        if (this._events) {
          var r = this._events[t];
          if (r) {
            this._firingCount = this._firingCount + 1 || 1;
            for (var s = 0, o = r.length; s < o; s++) {
              var l = r[s];
              l.fn.call(l.ctx || this, a);
            }
            this._firingCount--;
          }
        }
        return n && this._propagateEvent(a), this;
      },
      listens: function (t, e) {
        var i = this._events && this._events[t];
        if (i && i.length) return !0;
        if (e)
          for (var n in this._eventParents)
            if (this._eventParents[n].listens(t, e)) return !0;
        return !1;
      },
      once: function (t, e, i) {
        if ("object" == typeof t) {
          for (var n in t) this.once(n, t[n], e);
          return this;
        }
        var r = a(function () {
          this.off(t, e, i).off(t, r, i);
        }, this);
        return this.on(t, e, i).on(t, r, i);
      },
      addEventParent: function (t) {
        return (
          (this._eventParents = this._eventParents || {}),
          (this._eventParents[s(t)] = t),
          this
        );
      },
      removeEventParent: function (t) {
        return this._eventParents && delete this._eventParents[s(t)], this;
      },
      _propagateEvent: function (t) {
        for (var i in this._eventParents)
          this._eventParents[i].fire(
            t.type,
            e({ layer: t.target, propagatedFrom: t.target }, t),
            !0
          );
      },
    };
    (M.addEventListener = M.on),
      (M.removeEventListener = M.clearAllEventListeners = M.off),
      (M.addOneTimeEventListener = M.once),
      (M.fireEvent = M.fire),
      (M.hasEventListeners = M.listens);
    var k = D.extend(M);
    function A(t, e, i) {
      (this.x = i ? Math.round(t) : t), (this.y = i ? Math.round(e) : e);
    }
    var I =
      Math.trunc ||
      function (t) {
        return 0 < t ? Math.floor(t) : Math.ceil(t);
      };
    function z(t, e, i) {
      return t instanceof A
        ? t
        : v(t)
        ? new A(t[0], t[1])
        : null == t
        ? t
        : "object" == typeof t && "x" in t && "y" in t
        ? new A(t.x, t.y)
        : new A(t, e, i);
    }
    function O(t, e) {
      if (t)
        for (var i = e ? [t, e] : t, n = 0, a = i.length; n < a; n++)
          this.extend(i[n]);
    }
    function N(t, e) {
      return !t || t instanceof O ? t : new O(t, e);
    }
    function B(t, e) {
      if (t)
        for (var i = e ? [t, e] : t, n = 0, a = i.length; n < a; n++)
          this.extend(i[n]);
    }
    function j(t, e) {
      return t instanceof B ? t : new B(t, e);
    }
    function F(t, e, i) {
      if (isNaN(t) || isNaN(e))
        throw new Error("Invalid LatLng object: (" + t + ", " + e + ")");
      (this.lat = +t), (this.lng = +e), void 0 !== i && (this.alt = +i);
    }
    function R(t, e, i) {
      return t instanceof F
        ? t
        : v(t) && "object" != typeof t[0]
        ? 3 === t.length
          ? new F(t[0], t[1], t[2])
          : 2 === t.length
          ? new F(t[0], t[1])
          : null
        : null == t
        ? t
        : "object" == typeof t && "lat" in t
        ? new F(t.lat, "lng" in t ? t.lng : t.lon, t.alt)
        : void 0 === e
        ? null
        : new F(t, e, i);
    }
    (A.prototype = {
      clone: function () {
        return new A(this.x, this.y);
      },
      add: function (t) {
        return this.clone()._add(z(t));
      },
      _add: function (t) {
        return (this.x += t.x), (this.y += t.y), this;
      },
      subtract: function (t) {
        return this.clone()._subtract(z(t));
      },
      _subtract: function (t) {
        return (this.x -= t.x), (this.y -= t.y), this;
      },
      divideBy: function (t) {
        return this.clone()._divideBy(t);
      },
      _divideBy: function (t) {
        return (this.x /= t), (this.y /= t), this;
      },
      multiplyBy: function (t) {
        return this.clone()._multiplyBy(t);
      },
      _multiplyBy: function (t) {
        return (this.x *= t), (this.y *= t), this;
      },
      scaleBy: function (t) {
        return new A(this.x * t.x, this.y * t.y);
      },
      unscaleBy: function (t) {
        return new A(this.x / t.x, this.y / t.y);
      },
      round: function () {
        return this.clone()._round();
      },
      _round: function () {
        return (
          (this.x = Math.round(this.x)), (this.y = Math.round(this.y)), this
        );
      },
      floor: function () {
        return this.clone()._floor();
      },
      _floor: function () {
        return (
          (this.x = Math.floor(this.x)), (this.y = Math.floor(this.y)), this
        );
      },
      ceil: function () {
        return this.clone()._ceil();
      },
      _ceil: function () {
        return (this.x = Math.ceil(this.x)), (this.y = Math.ceil(this.y)), this;
      },
      trunc: function () {
        return this.clone()._trunc();
      },
      _trunc: function () {
        return (this.x = I(this.x)), (this.y = I(this.y)), this;
      },
      distanceTo: function (t) {
        var e = (t = z(t)).x - this.x,
          i = t.y - this.y;
        return Math.sqrt(e * e + i * i);
      },
      equals: function (t) {
        return (t = z(t)).x === this.x && t.y === this.y;
      },
      contains: function (t) {
        return (
          (t = z(t)),
          Math.abs(t.x) <= Math.abs(this.x) && Math.abs(t.y) <= Math.abs(this.y)
        );
      },
      toString: function () {
        return "Point(" + c(this.x) + ", " + c(this.y) + ")";
      },
    }),
      (O.prototype = {
        extend: function (t) {
          return (
            (t = z(t)),
            this.min || this.max
              ? ((this.min.x = Math.min(t.x, this.min.x)),
                (this.max.x = Math.max(t.x, this.max.x)),
                (this.min.y = Math.min(t.y, this.min.y)),
                (this.max.y = Math.max(t.y, this.max.y)))
              : ((this.min = t.clone()), (this.max = t.clone())),
            this
          );
        },
        getCenter: function (t) {
          return new A(
            (this.min.x + this.max.x) / 2,
            (this.min.y + this.max.y) / 2,
            t
          );
        },
        getBottomLeft: function () {
          return new A(this.min.x, this.max.y);
        },
        getTopRight: function () {
          return new A(this.max.x, this.min.y);
        },
        getTopLeft: function () {
          return this.min;
        },
        getBottomRight: function () {
          return this.max;
        },
        getSize: function () {
          return this.max.subtract(this.min);
        },
        contains: function (t) {
          var e, i;
          return (
            (t = ("number" == typeof t[0] || t instanceof A ? z : N)(
              t
            )) instanceof O
              ? ((e = t.min), (i = t.max))
              : (e = i = t),
            e.x >= this.min.x &&
              i.x <= this.max.x &&
              e.y >= this.min.y &&
              i.y <= this.max.y
          );
        },
        intersects: function (t) {
          t = N(t);
          var e = this.min,
            i = this.max,
            n = t.min,
            a = t.max,
            r = a.x >= e.x && n.x <= i.x,
            s = a.y >= e.y && n.y <= i.y;
          return r && s;
        },
        overlaps: function (t) {
          t = N(t);
          var e = this.min,
            i = this.max,
            n = t.min,
            a = t.max,
            r = a.x > e.x && n.x < i.x,
            s = a.y > e.y && n.y < i.y;
          return r && s;
        },
        isValid: function () {
          return !(!this.min || !this.max);
        },
      }),
      (B.prototype = {
        extend: function (t) {
          var e,
            i,
            n = this._southWest,
            a = this._northEast;
          if (t instanceof F) i = e = t;
          else {
            if (!(t instanceof B)) return t ? this.extend(R(t) || j(t)) : this;
            if (((e = t._southWest), (i = t._northEast), !e || !i)) return this;
          }
          return (
            n || a
              ? ((n.lat = Math.min(e.lat, n.lat)),
                (n.lng = Math.min(e.lng, n.lng)),
                (a.lat = Math.max(i.lat, a.lat)),
                (a.lng = Math.max(i.lng, a.lng)))
              : ((this._southWest = new F(e.lat, e.lng)),
                (this._northEast = new F(i.lat, i.lng))),
            this
          );
        },
        pad: function (t) {
          var e = this._southWest,
            i = this._northEast,
            n = Math.abs(e.lat - i.lat) * t,
            a = Math.abs(e.lng - i.lng) * t;
          return new B(
            new F(e.lat - n, e.lng - a),
            new F(i.lat + n, i.lng + a)
          );
        },
        getCenter: function () {
          return new F(
            (this._southWest.lat + this._northEast.lat) / 2,
            (this._southWest.lng + this._northEast.lng) / 2
          );
        },
        getSouthWest: function () {
          return this._southWest;
        },
        getNorthEast: function () {
          return this._northEast;
        },
        getNorthWest: function () {
          return new F(this.getNorth(), this.getWest());
        },
        getSouthEast: function () {
          return new F(this.getSouth(), this.getEast());
        },
        getWest: function () {
          return this._southWest.lng;
        },
        getSouth: function () {
          return this._southWest.lat;
        },
        getEast: function () {
          return this._northEast.lng;
        },
        getNorth: function () {
          return this._northEast.lat;
        },
        contains: function (t) {
          t = ("number" == typeof t[0] || t instanceof F || "lat" in t ? R : j)(
            t
          );
          var e,
            i,
            n = this._southWest,
            a = this._northEast;
          return (
            t instanceof B
              ? ((e = t.getSouthWest()), (i = t.getNorthEast()))
              : (e = i = t),
            e.lat >= n.lat && i.lat <= a.lat && e.lng >= n.lng && i.lng <= a.lng
          );
        },
        intersects: function (t) {
          t = j(t);
          var e = this._southWest,
            i = this._northEast,
            n = t.getSouthWest(),
            a = t.getNorthEast(),
            r = a.lat >= e.lat && n.lat <= i.lat,
            s = a.lng >= e.lng && n.lng <= i.lng;
          return r && s;
        },
        overlaps: function (t) {
          t = j(t);
          var e = this._southWest,
            i = this._northEast,
            n = t.getSouthWest(),
            a = t.getNorthEast(),
            r = a.lat > e.lat && n.lat < i.lat,
            s = a.lng > e.lng && n.lng < i.lng;
          return r && s;
        },
        toBBoxString: function () {
          return [
            this.getWest(),
            this.getSouth(),
            this.getEast(),
            this.getNorth(),
          ].join(",");
        },
        equals: function (t, e) {
          return (
            !!t &&
            ((t = j(t)),
            this._southWest.equals(t.getSouthWest(), e) &&
              this._northEast.equals(t.getNorthEast(), e))
          );
        },
        isValid: function () {
          return !(!this._southWest || !this._northEast);
        },
      });
    var $,
      H = {
        latLngToPoint: function (t, e) {
          var i = this.projection.project(t),
            n = this.scale(e);
          return this.transformation._transform(i, n);
        },
        pointToLatLng: function (t, e) {
          var i = this.scale(e),
            n = this.transformation.untransform(t, i);
          return this.projection.unproject(n);
        },
        project: function (t) {
          return this.projection.project(t);
        },
        unproject: function (t) {
          return this.projection.unproject(t);
        },
        scale: function (t) {
          return 256 * Math.pow(2, t);
        },
        zoom: function (t) {
          return Math.log(t / 256) / Math.LN2;
        },
        getProjectedBounds: function (t) {
          if (this.infinite) return null;
          var e = this.projection.bounds,
            i = this.scale(t);
          return new O(
            this.transformation.transform(e.min, i),
            this.transformation.transform(e.max, i)
          );
        },
        infinite: !(F.prototype = {
          equals: function (t, e) {
            return (
              !!t &&
              ((t = R(t)),
              Math.max(
                Math.abs(this.lat - t.lat),
                Math.abs(this.lng - t.lng)
              ) <= (void 0 === e ? 1e-9 : e))
            );
          },
          toString: function (t) {
            return "LatLng(" + c(this.lat, t) + ", " + c(this.lng, t) + ")";
          },
          distanceTo: function (t) {
            return V.distance(this, R(t));
          },
          wrap: function () {
            return V.wrapLatLng(this);
          },
          toBounds: function (t) {
            var e = (180 * t) / 40075017,
              i = e / Math.cos((Math.PI / 180) * this.lat);
            return j(
              [this.lat - e, this.lng - i],
              [this.lat + e, this.lng + i]
            );
          },
          clone: function () {
            return new F(this.lat, this.lng, this.alt);
          },
        }),
        wrapLatLng: function (t) {
          var e = this.wrapLng ? l(t.lng, this.wrapLng, !0) : t.lng;
          return new F(
            this.wrapLat ? l(t.lat, this.wrapLat, !0) : t.lat,
            e,
            t.alt
          );
        },
        wrapLatLngBounds: function (t) {
          var e = t.getCenter(),
            i = this.wrapLatLng(e),
            n = e.lat - i.lat,
            a = e.lng - i.lng;
          if (0 == n && 0 == a) return t;
          var r = t.getSouthWest(),
            s = t.getNorthEast();
          return new B(
            new F(r.lat - n, r.lng - a),
            new F(s.lat - n, s.lng - a)
          );
        },
      },
      V = e({}, H, {
        wrapLng: [-180, 180],
        R: 6371e3,
        distance: function (t, e) {
          var i = Math.PI / 180,
            n = t.lat * i,
            a = e.lat * i,
            r = Math.sin(((e.lat - t.lat) * i) / 2),
            s = Math.sin(((e.lng - t.lng) * i) / 2),
            o = r * r + Math.cos(n) * Math.cos(a) * s * s,
            l = 2 * Math.atan2(Math.sqrt(o), Math.sqrt(1 - o));
          return this.R * l;
        },
      }),
      W = 6378137,
      Z = {
        R: W,
        MAX_LATITUDE: 85.0511287798,
        project: function (t) {
          var e = Math.PI / 180,
            i = this.MAX_LATITUDE,
            n = Math.max(Math.min(i, t.lat), -i),
            a = Math.sin(n * e);
          return new A(
            this.R * t.lng * e,
            (this.R * Math.log((1 + a) / (1 - a))) / 2
          );
        },
        unproject: function (t) {
          var e = 180 / Math.PI;
          return new F(
            (2 * Math.atan(Math.exp(t.y / this.R)) - Math.PI / 2) * e,
            (t.x * e) / this.R
          );
        },
        bounds: new O([-($ = W * Math.PI), -$], [$, $]),
      };
    function G(t, e, i, n) {
      if (v(t))
        return (
          (this._a = t[0]),
          (this._b = t[1]),
          (this._c = t[2]),
          void (this._d = t[3])
        );
      (this._a = t), (this._b = e), (this._c = i), (this._d = n);
    }
    function U(t, e, i, n) {
      return new G(t, e, i, n);
    }
    G.prototype = {
      transform: function (t, e) {
        return this._transform(t.clone(), e);
      },
      _transform: function (t, e) {
        return (
          (e = e || 1),
          (t.x = e * (this._a * t.x + this._b)),
          (t.y = e * (this._c * t.y + this._d)),
          t
        );
      },
      untransform: function (t, e) {
        return (
          (e = e || 1),
          new A((t.x / e - this._b) / this._a, (t.y / e - this._d) / this._c)
        );
      },
    };
    var Y,
      q = e({}, V, {
        code: "EPSG:3857",
        projection: Z,
        transformation: U((Y = 0.5 / (Math.PI * Z.R)), 0.5, -Y, 0.5),
      }),
      X = e({}, q, { code: "EPSG:900913" });
    function K(t) {
      return document.createElementNS("http://www.w3.org/2000/svg", t);
    }
    function J(t, e) {
      for (var i, n, a, r, s = "", o = 0, l = t.length; o < l; o++) {
        for (i = 0, n = (a = t[o]).length; i < n; i++)
          s += (i ? "L" : "M") + (r = a[i]).x + " " + r.y;
        s += e ? (Mt ? "z" : "x") : "";
      }
      return s || "M0 0";
    }
    var Q = document.documentElement.style,
      tt = "ActiveXObject" in window,
      et = tt && !document.addEventListener,
      it = "msLaunchUri" in navigator && !("documentMode" in document),
      nt = At("webkit"),
      at = At("android"),
      rt = At("android 2") || At("android 3"),
      st = parseInt(/WebKit\/([0-9]+)|$/.exec(navigator.userAgent)[1], 10),
      ot = at && At("Google") && st < 537 && !("AudioNode" in window),
      lt = !!window.opera,
      ut = !it && At("chrome"),
      ct = At("gecko") && !nt && !lt && !tt,
      dt = !ut && At("safari"),
      ht = At("phantom"),
      pt = "OTransition" in Q,
      ft = 0 === navigator.platform.indexOf("Win"),
      mt = tt && "transition" in Q,
      gt =
        "WebKitCSSMatrix" in window &&
        "m11" in new window.WebKitCSSMatrix() &&
        !rt,
      vt = "MozPerspective" in Q,
      _t = !window.L_DISABLE_3D && (mt || gt || vt) && !pt && !ht,
      yt = "undefined" != typeof orientation || At("mobile"),
      bt = yt && nt,
      wt = yt && gt,
      xt = !window.PointerEvent && window.MSPointerEvent,
      St = !(!window.PointerEvent && !xt),
      Tt =
        !window.L_NO_TOUCH &&
        (St ||
          "ontouchstart" in window ||
          (window.DocumentTouch && document instanceof window.DocumentTouch)),
      Ct = yt && lt,
      Et = yt && ct,
      Pt =
        1 <
        (window.devicePixelRatio ||
          window.screen.deviceXDPI / window.screen.logicalXDPI),
      Lt = (function () {
        var t = !1;
        try {
          var e = Object.defineProperty({}, "passive", {
            get: function () {
              t = !0;
            },
          });
          window.addEventListener("testPassiveEventSupport", u, e),
            window.removeEventListener("testPassiveEventSupport", u, e);
        } catch (t) {}
        return t;
      })(),
      Dt = !!document.createElement("canvas").getContext,
      Mt = !(!document.createElementNS || !K("svg").createSVGRect),
      kt =
        !Mt &&
        (function () {
          try {
            var t = document.createElement("div");
            t.innerHTML = '<v:shape adj="1"/>';
            var e = t.firstChild;
            return (
              (e.style.behavior = "url(#default#VML)"),
              e && "object" == typeof e.adj
            );
          } catch (t) {
            return !1;
          }
        })();
    function At(t) {
      return 0 <= navigator.userAgent.toLowerCase().indexOf(t);
    }
    var It = {
        ie: tt,
        ielt9: et,
        edge: it,
        webkit: nt,
        android: at,
        android23: rt,
        androidStock: ot,
        opera: lt,
        chrome: ut,
        gecko: ct,
        safari: dt,
        phantom: ht,
        opera12: pt,
        win: ft,
        ie3d: mt,
        webkit3d: gt,
        gecko3d: vt,
        any3d: _t,
        mobile: yt,
        mobileWebkit: bt,
        mobileWebkit3d: wt,
        msPointer: xt,
        pointer: St,
        touch: Tt,
        mobileOpera: Ct,
        mobileGecko: Et,
        retina: Pt,
        passiveEvents: Lt,
        canvas: Dt,
        svg: Mt,
        vml: kt,
      },
      zt = xt ? "MSPointerDown" : "pointerdown",
      Ot = xt ? "MSPointerMove" : "pointermove",
      Nt = xt ? "MSPointerUp" : "pointerup",
      Bt = xt ? "MSPointerCancel" : "pointercancel",
      jt = {},
      Ft = !1;
    function Rt(t, e, i, n) {
      function r(t) {
        Wt(t, o);
      }
      var s, o, l, u, c, d, h, p;
      function f(t) {
        (t.pointerType === (t.MSPOINTER_TYPE_MOUSE || "mouse") &&
          0 === t.buttons) ||
          Wt(t, u);
      }
      return (
        "touchstart" === e
          ? ((c = t),
            (d = i),
            (h = n),
            (p = a(function (t) {
              t.MSPOINTER_TYPE_TOUCH &&
                t.pointerType === t.MSPOINTER_TYPE_TOUCH &&
                Be(t),
                Wt(t, d);
            })),
            (c["_leaflet_touchstart" + h] = p),
            c.addEventListener(zt, p, !1),
            Ft ||
              (document.addEventListener(zt, $t, !0),
              document.addEventListener(Ot, Ht, !0),
              document.addEventListener(Nt, Vt, !0),
              document.addEventListener(Bt, Vt, !0),
              (Ft = !0)))
          : "touchmove" === e
          ? ((u = i),
            ((l = t)["_leaflet_touchmove" + n] = f),
            l.addEventListener(Ot, f, !1))
          : "touchend" === e &&
            ((o = i),
            ((s = t)["_leaflet_touchend" + n] = r),
            s.addEventListener(Nt, r, !1),
            s.addEventListener(Bt, r, !1)),
        this
      );
    }
    function $t(t) {
      jt[t.pointerId] = t;
    }
    function Ht(t) {
      jt[t.pointerId] && (jt[t.pointerId] = t);
    }
    function Vt(t) {
      delete jt[t.pointerId];
    }
    function Wt(t, e) {
      for (var i in ((t.touches = []), jt)) t.touches.push(jt[i]);
      (t.changedTouches = [t]), e(t);
    }
    var Zt,
      Gt,
      Ut,
      Yt,
      qt,
      Xt,
      Kt = xt ? "MSPointerDown" : St ? "pointerdown" : "touchstart",
      Jt = xt ? "MSPointerUp" : St ? "pointerup" : "touchend",
      Qt = "_leaflet_",
      te = ge([
        "transform",
        "webkitTransform",
        "OTransform",
        "MozTransform",
        "msTransform",
      ]),
      ee = ge([
        "webkitTransition",
        "transition",
        "OTransition",
        "MozTransition",
        "msTransition",
      ]),
      ie =
        "webkitTransition" === ee || "OTransition" === ee
          ? ee + "End"
          : "transitionend";
    function ne(t) {
      return "string" == typeof t ? document.getElementById(t) : t;
    }
    function ae(t, e) {
      var i,
        n = t.style[e] || (t.currentStyle && t.currentStyle[e]);
      return (
        (n && "auto" !== n) ||
          !document.defaultView ||
          (n = (i = document.defaultView.getComputedStyle(t, null))
            ? i[e]
            : null),
        "auto" === n ? null : n
      );
    }
    function re(t, e, i) {
      var n = document.createElement(t);
      return (n.className = e || ""), i && i.appendChild(n), n;
    }
    function se(t) {
      var e = t.parentNode;
      e && e.removeChild(t);
    }
    function oe(t) {
      for (; t.firstChild; ) t.removeChild(t.firstChild);
    }
    function le(t) {
      var e = t.parentNode;
      e && e.lastChild !== t && e.appendChild(t);
    }
    function ue(t) {
      var e = t.parentNode;
      e && e.firstChild !== t && e.insertBefore(t, e.firstChild);
    }
    function ce(t, e) {
      if (void 0 !== t.classList) return t.classList.contains(e);
      var i = fe(t);
      return 0 < i.length && new RegExp("(^|\\s)" + e + "(\\s|$)").test(i);
    }
    function de(t, e) {
      var i;
      if (void 0 !== t.classList)
        for (var n = h(e), a = 0, r = n.length; a < r; a++)
          t.classList.add(n[a]);
      else ce(t, e) || pe(t, ((i = fe(t)) ? i + " " : "") + e);
    }
    function he(t, e) {
      void 0 !== t.classList
        ? t.classList.remove(e)
        : pe(t, d((" " + fe(t) + " ").replace(" " + e + " ", " ")));
    }
    function pe(t, e) {
      void 0 === t.className.baseVal
        ? (t.className = e)
        : (t.className.baseVal = e);
    }
    function fe(t) {
      return (
        t.correspondingElement && (t = t.correspondingElement),
        void 0 === t.className.baseVal ? t.className : t.className.baseVal
      );
    }
    function me(t, e) {
      "opacity" in t.style
        ? (t.style.opacity = e)
        : "filter" in t.style &&
          (function (t, e) {
            var i = !1,
              n = "DXImageTransform.Microsoft.Alpha";
            try {
              i = t.filters.item(n);
            } catch (t) {
              if (1 === e) return;
            }
            (e = Math.round(100 * e)),
              i
                ? ((i.Enabled = 100 !== e), (i.Opacity = e))
                : (t.style.filter += " progid:" + n + "(opacity=" + e + ")");
          })(t, e);
    }
    function ge(t) {
      for (var e = document.documentElement.style, i = 0; i < t.length; i++)
        if (t[i] in e) return t[i];
      return !1;
    }
    function ve(t, e, i) {
      var n = e || new A(0, 0);
      t.style[te] =
        (mt
          ? "translate(" + n.x + "px," + n.y + "px)"
          : "translate3d(" + n.x + "px," + n.y + "px,0)") +
        (i ? " scale(" + i + ")" : "");
    }
    function _e(t, e) {
      (t._leaflet_pos = e),
        _t
          ? ve(t, e)
          : ((t.style.left = e.x + "px"), (t.style.top = e.y + "px"));
    }
    function ye(t) {
      return t._leaflet_pos || new A(0, 0);
    }
    function be() {
      Pe(window, "dragstart", Be);
    }
    function we() {
      De(window, "dragstart", Be);
    }
    function xe(t) {
      for (; -1 === t.tabIndex; ) t = t.parentNode;
      t.style &&
        (Se(),
        (Xt = (qt = t).style.outline),
        (t.style.outline = "none"),
        Pe(window, "keydown", Se));
    }
    function Se() {
      qt &&
        ((qt.style.outline = Xt),
        (Xt = qt = void 0),
        De(window, "keydown", Se));
    }
    function Te(t) {
      for (
        ;
        !(
          ((t = t.parentNode).offsetWidth && t.offsetHeight) ||
          t === document.body
        );

      );
      return t;
    }
    function Ce(t) {
      var e = t.getBoundingClientRect();
      return {
        x: e.width / t.offsetWidth || 1,
        y: e.height / t.offsetHeight || 1,
        boundingClientRect: e,
      };
    }
    Yt =
      "onselectstart" in document
        ? ((Ut = function () {
            Pe(window, "selectstart", Be);
          }),
          function () {
            De(window, "selectstart", Be);
          })
        : ((Gt = ge([
            "userSelect",
            "WebkitUserSelect",
            "OUserSelect",
            "MozUserSelect",
            "msUserSelect",
          ])),
          (Ut = function () {
            var t;
            Gt &&
              ((t = document.documentElement.style),
              (Zt = t[Gt]),
              (t[Gt] = "none"));
          }),
          function () {
            Gt && ((document.documentElement.style[Gt] = Zt), (Zt = void 0));
          });
    var Ee = {
      TRANSFORM: te,
      TRANSITION: ee,
      TRANSITION_END: ie,
      get: ne,
      getStyle: ae,
      create: re,
      remove: se,
      empty: oe,
      toFront: le,
      toBack: ue,
      hasClass: ce,
      addClass: de,
      removeClass: he,
      setClass: pe,
      getClass: fe,
      setOpacity: me,
      testProp: ge,
      setTransform: ve,
      setPosition: _e,
      getPosition: ye,
      disableTextSelection: Ut,
      enableTextSelection: Yt,
      disableImageDrag: be,
      enableImageDrag: we,
      preventOutline: xe,
      restoreOutline: Se,
      getSizedParentNode: Te,
      getScale: Ce,
    };
    function Pe(t, e, i, n) {
      if ("object" == typeof e) for (var a in e) Ae(t, a, e[a], i);
      else for (var r = 0, s = (e = h(e)).length; r < s; r++) Ae(t, e[r], i, n);
      return this;
    }
    var Le = "_leaflet_events";
    function De(t, e, i, n) {
      if ("object" == typeof e) for (var a in e) Ie(t, a, e[a], i);
      else if (e)
        for (var r = 0, s = (e = h(e)).length; r < s; r++) Ie(t, e[r], i, n);
      else {
        for (var o in t[Le]) Ie(t, o, t[Le][o]);
        delete t[Le];
      }
      return this;
    }
    function Me() {
      return St && !it && !dt;
    }
    var ke = {
      mouseenter: "mouseover",
      mouseleave: "mouseout",
      wheel: !("onwheel" in window) && "mousewheel",
    };
    function Ae(t, e, i, n) {
      var a = e + s(i) + (n ? "_" + s(n) : "");
      if (t[Le] && t[Le][a]) return this;
      var r,
        o,
        l,
        u,
        c,
        d,
        h = function (e) {
          return i.call(n || t, e || window.event);
        },
        p = h;
      function f(t) {
        if (St) {
          if (!t.isPrimary) return;
          if ("mouse" === t.pointerType) return;
        } else if (1 < t.touches.length) return;
        var e = Date.now(),
          i = e - (u || e);
        (c = t.touches ? t.touches[0] : t), (d = 0 < i && i <= 250), (u = e);
      }
      function m(t) {
        if (d && !c.cancelBubble) {
          if (St) {
            if ("mouse" === t.pointerType) return;
            var e,
              i,
              n = {};
            for (i in c) (e = c[i]), (n[i] = e && e.bind ? e.bind(c) : e);
            c = n;
          }
          (c.type = "dblclick"), (c.button = 0), o(c), (u = null);
        }
      }
      St && 0 === e.indexOf("touch")
        ? Rt(t, e, h, a)
        : Tt && "dblclick" === e && !Me()
        ? ((o = h),
          (d = !1),
          ((r = t)[Qt + Kt + (l = a)] = f),
          (r[Qt + Jt + l] = m),
          (r[Qt + "dblclick" + l] = o),
          r.addEventListener(Kt, f, !!Lt && { passive: !1 }),
          r.addEventListener(Jt, m, !!Lt && { passive: !1 }),
          r.addEventListener("dblclick", o, !1))
        : "addEventListener" in t
        ? "touchstart" === e ||
          "touchmove" === e ||
          "wheel" === e ||
          "mousewheel" === e
          ? t.addEventListener(ke[e] || e, h, !!Lt && { passive: !1 })
          : "mouseenter" === e || "mouseleave" === e
          ? ((h = function (e) {
              (e = e || window.event), Ze(t, e) && p(e);
            }),
            t.addEventListener(ke[e], h, !1))
          : t.addEventListener(e, p, !1)
        : "attachEvent" in t && t.attachEvent("on" + e, h),
        (t[Le] = t[Le] || {}),
        (t[Le][a] = h);
    }
    function Ie(t, e, i, n) {
      var a,
        r,
        o,
        l,
        u,
        c,
        d,
        h,
        p = e + s(i) + (n ? "_" + s(n) : ""),
        f = t[Le] && t[Le][p];
      if (!f) return this;
      St && 0 === e.indexOf("touch")
        ? ((h = (c = t)["_leaflet_" + (d = e) + p]),
          "touchstart" === d
            ? c.removeEventListener(zt, h, !1)
            : "touchmove" === d
            ? c.removeEventListener(Ot, h, !1)
            : "touchend" === d &&
              (c.removeEventListener(Nt, h, !1),
              c.removeEventListener(Bt, h, !1)))
        : Tt && "dblclick" === e && !Me()
        ? ((o = (a = t)[Qt + Kt + (r = p)]),
          (l = a[Qt + Jt + r]),
          (u = a[Qt + "dblclick" + r]),
          a.removeEventListener(Kt, o, !!Lt && { passive: !1 }),
          a.removeEventListener(Jt, l, !!Lt && { passive: !1 }),
          a.removeEventListener("dblclick", u, !1))
        : "removeEventListener" in t
        ? t.removeEventListener(ke[e] || e, f, !1)
        : "detachEvent" in t && t.detachEvent("on" + e, f),
        (t[Le][p] = null);
    }
    function ze(t) {
      return (
        t.stopPropagation
          ? t.stopPropagation()
          : t.originalEvent
          ? (t.originalEvent._stopped = !0)
          : (t.cancelBubble = !0),
        We(t),
        this
      );
    }
    function Oe(t) {
      return Ae(t, "wheel", ze), this;
    }
    function Ne(t) {
      return (
        Pe(t, "mousedown touchstart dblclick", ze), Ae(t, "click", Ve), this
      );
    }
    function Be(t) {
      return t.preventDefault ? t.preventDefault() : (t.returnValue = !1), this;
    }
    function je(t) {
      return Be(t), ze(t), this;
    }
    function Fe(t, e) {
      if (!e) return new A(t.clientX, t.clientY);
      var i = Ce(e),
        n = i.boundingClientRect;
      return new A(
        (t.clientX - n.left) / i.x - e.clientLeft,
        (t.clientY - n.top) / i.y - e.clientTop
      );
    }
    var Re =
      ft && ut ? 2 * window.devicePixelRatio : ct ? window.devicePixelRatio : 1;
    function $e(t) {
      return it
        ? t.wheelDeltaY / 2
        : t.deltaY && 0 === t.deltaMode
        ? -t.deltaY / Re
        : t.deltaY && 1 === t.deltaMode
        ? 20 * -t.deltaY
        : t.deltaY && 2 === t.deltaMode
        ? 60 * -t.deltaY
        : t.deltaX || t.deltaZ
        ? 0
        : t.wheelDelta
        ? (t.wheelDeltaY || t.wheelDelta) / 2
        : t.detail && Math.abs(t.detail) < 32765
        ? 20 * -t.detail
        : t.detail
        ? (t.detail / -32765) * 60
        : 0;
    }
    var He = {};
    function Ve(t) {
      He[t.type] = !0;
    }
    function We(t) {
      var e = He[t.type];
      return (He[t.type] = !1), e;
    }
    function Ze(t, e) {
      var i = e.relatedTarget;
      if (!i) return !0;
      try {
        for (; i && i !== t; ) i = i.parentNode;
      } catch (t) {
        return !1;
      }
      return i !== t;
    }
    var Ge = {
        on: Pe,
        off: De,
        stopPropagation: ze,
        disableScrollPropagation: Oe,
        disableClickPropagation: Ne,
        preventDefault: Be,
        stop: je,
        getMousePosition: Fe,
        getWheelDelta: $e,
        fakeStop: Ve,
        skipped: We,
        isExternalTarget: Ze,
        addListener: Pe,
        removeListener: De,
      },
      Ue = k.extend({
        run: function (t, e, i, n) {
          this.stop(),
            (this._el = t),
            (this._inProgress = !0),
            (this._duration = i || 0.25),
            (this._easeOutPower = 1 / Math.max(n || 0.5, 0.2)),
            (this._startPos = ye(t)),
            (this._offset = e.subtract(this._startPos)),
            (this._startTime = +new Date()),
            this.fire("start"),
            this._animate();
        },
        stop: function () {
          this._inProgress && (this._step(!0), this._complete());
        },
        _animate: function () {
          (this._animId = C(this._animate, this)), this._step();
        },
        _step: function (t) {
          var e = new Date() - this._startTime,
            i = 1e3 * this._duration;
          e < i
            ? this._runFrame(this._easeOut(e / i), t)
            : (this._runFrame(1), this._complete());
        },
        _runFrame: function (t, e) {
          var i = this._startPos.add(this._offset.multiplyBy(t));
          e && i._round(), _e(this._el, i), this.fire("step");
        },
        _complete: function () {
          E(this._animId), (this._inProgress = !1), this.fire("end");
        },
        _easeOut: function (t) {
          return 1 - Math.pow(1 - t, this._easeOutPower);
        },
      }),
      Ye = k.extend({
        options: {
          crs: q,
          center: void 0,
          zoom: void 0,
          minZoom: void 0,
          maxZoom: void 0,
          layers: [],
          maxBounds: void 0,
          renderer: void 0,
          zoomAnimation: !0,
          zoomAnimationThreshold: 4,
          fadeAnimation: !0,
          markerZoomAnimation: !0,
          transform3DLimit: 8388608,
          zoomSnap: 1,
          zoomDelta: 1,
          trackResize: !0,
        },
        initialize: function (t, e) {
          (e = p(this, e)),
            (this._handlers = []),
            (this._layers = {}),
            (this._zoomBoundLayers = {}),
            (this._sizeChanged = !0),
            this._initContainer(t),
            this._initLayout(),
            (this._onResize = a(this._onResize, this)),
            this._initEvents(),
            e.maxBounds && this.setMaxBounds(e.maxBounds),
            void 0 !== e.zoom && (this._zoom = this._limitZoom(e.zoom)),
            e.center &&
              void 0 !== e.zoom &&
              this.setView(R(e.center), e.zoom, { reset: !0 }),
            this.callInitHooks(),
            (this._zoomAnimated =
              ee && _t && !Ct && this.options.zoomAnimation),
            this._zoomAnimated &&
              (this._createAnimProxy(),
              Pe(this._proxy, ie, this._catchTransitionEnd, this)),
            this._addLayers(this.options.layers);
        },
        setView: function (t, i, n) {
          return (
            (i = void 0 === i ? this._zoom : this._limitZoom(i)),
            (t = this._limitCenter(R(t), i, this.options.maxBounds)),
            (n = n || {}),
            this._stop(),
            this._loaded &&
            !n.reset &&
            !0 !== n &&
            (void 0 !== n.animate &&
              ((n.zoom = e({ animate: n.animate }, n.zoom)),
              (n.pan = e({ animate: n.animate, duration: n.duration }, n.pan))),
            this._zoom !== i
              ? this._tryAnimatedZoom && this._tryAnimatedZoom(t, i, n.zoom)
              : this._tryAnimatedPan(t, n.pan))
              ? (clearTimeout(this._sizeTimer), this)
              : (this._resetView(t, i), this)
          );
        },
        setZoom: function (t, e) {
          return this._loaded
            ? this.setView(this.getCenter(), t, { zoom: e })
            : ((this._zoom = t), this);
        },
        zoomIn: function (t, e) {
          return (
            (t = t || (_t ? this.options.zoomDelta : 1)),
            this.setZoom(this._zoom + t, e)
          );
        },
        zoomOut: function (t, e) {
          return (
            (t = t || (_t ? this.options.zoomDelta : 1)),
            this.setZoom(this._zoom - t, e)
          );
        },
        setZoomAround: function (t, e, i) {
          var n = this.getZoomScale(e),
            a = this.getSize().divideBy(2),
            r = (t instanceof A ? t : this.latLngToContainerPoint(t))
              .subtract(a)
              .multiplyBy(1 - 1 / n),
            s = this.containerPointToLatLng(a.add(r));
          return this.setView(s, e, { zoom: i });
        },
        _getBoundsCenterZoom: function (t, e) {
          (e = e || {}), (t = t.getBounds ? t.getBounds() : j(t));
          var i = z(e.paddingTopLeft || e.padding || [0, 0]),
            n = z(e.paddingBottomRight || e.padding || [0, 0]),
            a = this.getBoundsZoom(t, !1, i.add(n));
          if (
            (a = "number" == typeof e.maxZoom ? Math.min(e.maxZoom, a) : a) ===
            1 / 0
          )
            return { center: t.getCenter(), zoom: a };
          var r = n.subtract(i).divideBy(2),
            s = this.project(t.getSouthWest(), a),
            o = this.project(t.getNorthEast(), a);
          return {
            center: this.unproject(s.add(o).divideBy(2).add(r), a),
            zoom: a,
          };
        },
        fitBounds: function (t, e) {
          if (!(t = j(t)).isValid()) throw new Error("Bounds are not valid.");
          var i = this._getBoundsCenterZoom(t, e);
          return this.setView(i.center, i.zoom, e);
        },
        fitWorld: function (t) {
          return this.fitBounds(
            [
              [-90, -180],
              [90, 180],
            ],
            t
          );
        },
        panTo: function (t, e) {
          return this.setView(t, this._zoom, { pan: e });
        },
        panBy: function (t, e) {
          return (
            (e = e || {}),
            (t = z(t).round()).x || t.y
              ? (!0 === e.animate || this.getSize().contains(t)
                  ? (this._panAnim ||
                      ((this._panAnim = new Ue()),
                      this._panAnim.on(
                        {
                          step: this._onPanTransitionStep,
                          end: this._onPanTransitionEnd,
                        },
                        this
                      )),
                    e.noMoveStart || this.fire("movestart"),
                    !1 !== e.animate
                      ? (de(this._mapPane, "leaflet-pan-anim"),
                        (i = this._getMapPanePos().subtract(t).round()),
                        this._panAnim.run(
                          this._mapPane,
                          i,
                          e.duration || 0.25,
                          e.easeLinearity
                        ))
                      : (this._rawPanBy(t), this.fire("move").fire("moveend")))
                  : this._resetView(
                      this.unproject(this.project(this.getCenter()).add(t)),
                      this.getZoom()
                    ),
                this)
              : this.fire("moveend")
          );
          var i;
        },
        flyTo: function (t, e, i) {
          if (!1 === (i = i || {}).animate || !_t) return this.setView(t, e, i);
          this._stop();
          var n = this.project(this.getCenter()),
            a = this.project(t),
            r = this.getSize(),
            s = this._zoom;
          (t = R(t)), (e = void 0 === e ? s : e);
          var o = Math.max(r.x, r.y),
            l = o * this.getZoomScale(s, e),
            u = a.distanceTo(n) || 1,
            c = 1.42,
            d = c * c;
          function h(t) {
            var e =
                (l * l - o * o + (t ? -1 : 1) * d * d * u * u) /
                (2 * (t ? l : o) * d * u),
              i = Math.sqrt(e * e + 1) - e;
            return i < 1e-9 ? -18 : Math.log(i);
          }
          function p(t) {
            return (Math.exp(t) - Math.exp(-t)) / 2;
          }
          function f(t) {
            return (Math.exp(t) + Math.exp(-t)) / 2;
          }
          var m = h(0);
          function g(t) {
            return (o * (f(m) * (p((e = m + c * t)) / f(e)) - p(m))) / d;
            var e;
          }
          var v = Date.now(),
            _ = (h(1) - m) / c,
            y = i.duration ? 1e3 * i.duration : 1e3 * _ * 0.8;
          return (
            this._moveStart(!0, i.noMoveStart),
            function i() {
              var r,
                l,
                d = (Date.now() - v) / y,
                h = ((r = d), (1 - Math.pow(1 - r, 1.5)) * _);
              d <= 1
                ? ((this._flyToFrame = C(i, this)),
                  this._move(
                    this.unproject(
                      n.add(a.subtract(n).multiplyBy(g(h) / u)),
                      s
                    ),
                    this.getScaleZoom(
                      o / ((l = h), o * (f(m) / f(m + c * l))),
                      s
                    ),
                    { flyTo: !0 }
                  ))
                : this._move(t, e)._moveEnd(!0);
            }.call(this),
            this
          );
        },
        flyToBounds: function (t, e) {
          var i = this._getBoundsCenterZoom(t, e);
          return this.flyTo(i.center, i.zoom, e);
        },
        setMaxBounds: function (t) {
          return (t = j(t)).isValid()
            ? (this.options.maxBounds &&
                this.off("moveend", this._panInsideMaxBounds),
              (this.options.maxBounds = t),
              this._loaded && this._panInsideMaxBounds(),
              this.on("moveend", this._panInsideMaxBounds))
            : ((this.options.maxBounds = null),
              this.off("moveend", this._panInsideMaxBounds));
        },
        setMinZoom: function (t) {
          var e = this.options.minZoom;
          return (
            (this.options.minZoom = t),
            this._loaded &&
            e !== t &&
            (this.fire("zoomlevelschange"),
            this.getZoom() < this.options.minZoom)
              ? this.setZoom(t)
              : this
          );
        },
        setMaxZoom: function (t) {
          var e = this.options.maxZoom;
          return (
            (this.options.maxZoom = t),
            this._loaded &&
            e !== t &&
            (this.fire("zoomlevelschange"),
            this.getZoom() > this.options.maxZoom)
              ? this.setZoom(t)
              : this
          );
        },
        panInsideBounds: function (t, e) {
          this._enforcingBounds = !0;
          var i = this.getCenter(),
            n = this._limitCenter(i, this._zoom, j(t));
          return (
            i.equals(n) || this.panTo(n, e), (this._enforcingBounds = !1), this
          );
        },
        panInside: function (t, e) {
          var i,
            n,
            a = z((e = e || {}).paddingTopLeft || e.padding || [0, 0]),
            r = z(e.paddingBottomRight || e.padding || [0, 0]),
            s = this.getCenter(),
            o = this.project(s),
            l = this.project(t),
            u = this.getPixelBounds(),
            c = u.getSize().divideBy(2),
            d = N([u.min.add(a), u.max.subtract(r)]);
          return (
            d.contains(l) ||
              ((this._enforcingBounds = !0),
              (i = o.subtract(l)),
              (n = z(l.x + i.x, l.y + i.y)),
              (l.x < d.min.x || l.x > d.max.x) &&
                ((n.x = o.x - i.x),
                0 < i.x ? (n.x += c.x - a.x) : (n.x -= c.x - r.x)),
              (l.y < d.min.y || l.y > d.max.y) &&
                ((n.y = o.y - i.y),
                0 < i.y ? (n.y += c.y - a.y) : (n.y -= c.y - r.y)),
              this.panTo(this.unproject(n), e),
              (this._enforcingBounds = !1)),
            this
          );
        },
        invalidateSize: function (t) {
          if (!this._loaded) return this;
          t = e({ animate: !1, pan: !0 }, !0 === t ? { animate: !0 } : t);
          var i = this.getSize();
          (this._sizeChanged = !0), (this._lastCenter = null);
          var n = this.getSize(),
            r = i.divideBy(2).round(),
            s = n.divideBy(2).round(),
            o = r.subtract(s);
          return o.x || o.y
            ? (t.animate && t.pan
                ? this.panBy(o)
                : (t.pan && this._rawPanBy(o),
                  this.fire("move"),
                  t.debounceMoveend
                    ? (clearTimeout(this._sizeTimer),
                      (this._sizeTimer = setTimeout(
                        a(this.fire, this, "moveend"),
                        200
                      )))
                    : this.fire("moveend")),
              this.fire("resize", { oldSize: i, newSize: n }))
            : this;
        },
        stop: function () {
          return (
            this.setZoom(this._limitZoom(this._zoom)),
            this.options.zoomSnap || this.fire("viewreset"),
            this._stop()
          );
        },
        locate: function (t) {
          if (
            ((t = this._locateOptions = e({ timeout: 1e4, watch: !1 }, t)),
            !("geolocation" in navigator))
          )
            return (
              this._handleGeolocationError({
                code: 0,
                message: "Geolocation not supported.",
              }),
              this
            );
          var i = a(this._handleGeolocationResponse, this),
            n = a(this._handleGeolocationError, this);
          return (
            t.watch
              ? (this._locationWatchId = navigator.geolocation.watchPosition(
                  i,
                  n,
                  t
                ))
              : navigator.geolocation.getCurrentPosition(i, n, t),
            this
          );
        },
        stopLocate: function () {
          return (
            navigator.geolocation &&
              navigator.geolocation.clearWatch &&
              navigator.geolocation.clearWatch(this._locationWatchId),
            this._locateOptions && (this._locateOptions.setView = !1),
            this
          );
        },
        _handleGeolocationError: function (t) {
          var e = t.code,
            i =
              t.message ||
              (1 === e
                ? "permission denied"
                : 2 === e
                ? "position unavailable"
                : "timeout");
          this._locateOptions.setView && !this._loaded && this.fitWorld(),
            this.fire("locationerror", {
              code: e,
              message: "Geolocation error: " + i + ".",
            });
        },
        _handleGeolocationResponse: function (t) {
          var e,
            i = new F(t.coords.latitude, t.coords.longitude),
            n = i.toBounds(2 * t.coords.accuracy),
            a = this._locateOptions;
          a.setView &&
            ((e = this.getBoundsZoom(n)),
            this.setView(i, a.maxZoom ? Math.min(e, a.maxZoom) : e));
          var r = { latlng: i, bounds: n, timestamp: t.timestamp };
          for (var s in t.coords)
            "number" == typeof t.coords[s] && (r[s] = t.coords[s]);
          this.fire("locationfound", r);
        },
        addHandler: function (t, e) {
          if (!e) return this;
          var i = (this[t] = new e(this));
          return this._handlers.push(i), this.options[t] && i.enable(), this;
        },
        remove: function () {
          if (
            (this._initEvents(!0),
            this.off("moveend", this._panInsideMaxBounds),
            this._containerId !== this._container._leaflet_id)
          )
            throw new Error(
              "Map container is being reused by another instance"
            );
          try {
            delete this._container._leaflet_id, delete this._containerId;
          } catch (t) {
            (this._container._leaflet_id = void 0),
              (this._containerId = void 0);
          }
          var t;
          for (t in (void 0 !== this._locationWatchId && this.stopLocate(),
          this._stop(),
          se(this._mapPane),
          this._clearControlPos && this._clearControlPos(),
          this._resizeRequest &&
            (E(this._resizeRequest), (this._resizeRequest = null)),
          this._clearHandlers(),
          this._loaded && this.fire("unload"),
          this._layers))
            this._layers[t].remove();
          for (t in this._panes) se(this._panes[t]);
          return (
            (this._layers = []),
            (this._panes = []),
            delete this._mapPane,
            delete this._renderer,
            this
          );
        },
        createPane: function (t, e) {
          var i = re(
            "div",
            "leaflet-pane" +
              (t ? " leaflet-" + t.replace("Pane", "") + "-pane" : ""),
            e || this._mapPane
          );
          return t && (this._panes[t] = i), i;
        },
        getCenter: function () {
          return (
            this._checkIfLoaded(),
            this._lastCenter && !this._moved()
              ? this._lastCenter
              : this.layerPointToLatLng(this._getCenterLayerPoint())
          );
        },
        getZoom: function () {
          return this._zoom;
        },
        getBounds: function () {
          var t = this.getPixelBounds();
          return new B(
            this.unproject(t.getBottomLeft()),
            this.unproject(t.getTopRight())
          );
        },
        getMinZoom: function () {
          return void 0 === this.options.minZoom
            ? this._layersMinZoom || 0
            : this.options.minZoom;
        },
        getMaxZoom: function () {
          return void 0 === this.options.maxZoom
            ? void 0 === this._layersMaxZoom
              ? 1 / 0
              : this._layersMaxZoom
            : this.options.maxZoom;
        },
        getBoundsZoom: function (t, e, i) {
          (t = j(t)), (i = z(i || [0, 0]));
          var n = this.getZoom() || 0,
            a = this.getMinZoom(),
            r = this.getMaxZoom(),
            s = t.getNorthWest(),
            o = t.getSouthEast(),
            l = this.getSize().subtract(i),
            u = N(this.project(o, n), this.project(s, n)).getSize(),
            c = _t ? this.options.zoomSnap : 1,
            d = l.x / u.x,
            h = l.y / u.y,
            p = e ? Math.max(d, h) : Math.min(d, h);
          n = this.getScaleZoom(p, n);
          return (
            c &&
              ((n = Math.round(n / (c / 100)) * (c / 100)),
              (n = e ? Math.ceil(n / c) * c : Math.floor(n / c) * c)),
            Math.max(a, Math.min(r, n))
          );
        },
        getSize: function () {
          return (
            (this._size && !this._sizeChanged) ||
              ((this._size = new A(
                this._container.clientWidth || 0,
                this._container.clientHeight || 0
              )),
              (this._sizeChanged = !1)),
            this._size.clone()
          );
        },
        getPixelBounds: function (t, e) {
          var i = this._getTopLeftPoint(t, e);
          return new O(i, i.add(this.getSize()));
        },
        getPixelOrigin: function () {
          return this._checkIfLoaded(), this._pixelOrigin;
        },
        getPixelWorldBounds: function (t) {
          return this.options.crs.getProjectedBounds(
            void 0 === t ? this.getZoom() : t
          );
        },
        getPane: function (t) {
          return "string" == typeof t ? this._panes[t] : t;
        },
        getPanes: function () {
          return this._panes;
        },
        getContainer: function () {
          return this._container;
        },
        getZoomScale: function (t, e) {
          var i = this.options.crs;
          return (e = void 0 === e ? this._zoom : e), i.scale(t) / i.scale(e);
        },
        getScaleZoom: function (t, e) {
          var i = this.options.crs;
          e = void 0 === e ? this._zoom : e;
          var n = i.zoom(t * i.scale(e));
          return isNaN(n) ? 1 / 0 : n;
        },
        project: function (t, e) {
          return (
            (e = void 0 === e ? this._zoom : e),
            this.options.crs.latLngToPoint(R(t), e)
          );
        },
        unproject: function (t, e) {
          return (
            (e = void 0 === e ? this._zoom : e),
            this.options.crs.pointToLatLng(z(t), e)
          );
        },
        layerPointToLatLng: function (t) {
          var e = z(t).add(this.getPixelOrigin());
          return this.unproject(e);
        },
        latLngToLayerPoint: function (t) {
          return this.project(R(t))._round()._subtract(this.getPixelOrigin());
        },
        wrapLatLng: function (t) {
          return this.options.crs.wrapLatLng(R(t));
        },
        wrapLatLngBounds: function (t) {
          return this.options.crs.wrapLatLngBounds(j(t));
        },
        distance: function (t, e) {
          return this.options.crs.distance(R(t), R(e));
        },
        containerPointToLayerPoint: function (t) {
          return z(t).subtract(this._getMapPanePos());
        },
        layerPointToContainerPoint: function (t) {
          return z(t).add(this._getMapPanePos());
        },
        containerPointToLatLng: function (t) {
          var e = this.containerPointToLayerPoint(z(t));
          return this.layerPointToLatLng(e);
        },
        latLngToContainerPoint: function (t) {
          return this.layerPointToContainerPoint(this.latLngToLayerPoint(R(t)));
        },
        mouseEventToContainerPoint: function (t) {
          return Fe(t, this._container);
        },
        mouseEventToLayerPoint: function (t) {
          return this.containerPointToLayerPoint(
            this.mouseEventToContainerPoint(t)
          );
        },
        mouseEventToLatLng: function (t) {
          return this.layerPointToLatLng(this.mouseEventToLayerPoint(t));
        },
        _initContainer: function (t) {
          var e = (this._container = ne(t));
          if (!e) throw new Error("Map container not found.");
          if (e._leaflet_id)
            throw new Error("Map container is already initialized.");
          Pe(e, "scroll", this._onScroll, this), (this._containerId = s(e));
        },
        _initLayout: function () {
          var t = this._container;
          (this._fadeAnimated = this.options.fadeAnimation && _t),
            de(
              t,
              "leaflet-container" +
                (Tt ? " leaflet-touch" : "") +
                (Pt ? " leaflet-retina" : "") +
                (et ? " leaflet-oldie" : "") +
                (dt ? " leaflet-safari" : "") +
                (this._fadeAnimated ? " leaflet-fade-anim" : "")
            );
          var e = ae(t, "position");
          "absolute" !== e &&
            "relative" !== e &&
            "fixed" !== e &&
            (t.style.position = "relative"),
            this._initPanes(),
            this._initControlPos && this._initControlPos();
        },
        _initPanes: function () {
          var t = (this._panes = {});
          (this._paneRenderers = {}),
            (this._mapPane = this.createPane("mapPane", this._container)),
            _e(this._mapPane, new A(0, 0)),
            this.createPane("tilePane"),
            this.createPane("shadowPane"),
            this.createPane("overlayPane"),
            this.createPane("markerPane"),
            this.createPane("tooltipPane"),
            this.createPane("popupPane"),
            this.options.markerZoomAnimation ||
              (de(t.markerPane, "leaflet-zoom-hide"),
              de(t.shadowPane, "leaflet-zoom-hide"));
        },
        _resetView: function (t, e) {
          _e(this._mapPane, new A(0, 0));
          var i = !this._loaded;
          (this._loaded = !0),
            (e = this._limitZoom(e)),
            this.fire("viewprereset");
          var n = this._zoom !== e;
          this._moveStart(n, !1)._move(t, e)._moveEnd(n),
            this.fire("viewreset"),
            i && this.fire("load");
        },
        _moveStart: function (t, e) {
          return t && this.fire("zoomstart"), e || this.fire("movestart"), this;
        },
        _move: function (t, e, i) {
          void 0 === e && (e = this._zoom);
          var n = this._zoom !== e;
          return (
            (this._zoom = e),
            (this._lastCenter = t),
            (this._pixelOrigin = this._getNewPixelOrigin(t)),
            (n || (i && i.pinch)) && this.fire("zoom", i),
            this.fire("move", i)
          );
        },
        _moveEnd: function (t) {
          return t && this.fire("zoomend"), this.fire("moveend");
        },
        _stop: function () {
          return (
            E(this._flyToFrame), this._panAnim && this._panAnim.stop(), this
          );
        },
        _rawPanBy: function (t) {
          _e(this._mapPane, this._getMapPanePos().subtract(t));
        },
        _getZoomSpan: function () {
          return this.getMaxZoom() - this.getMinZoom();
        },
        _panInsideMaxBounds: function () {
          this._enforcingBounds || this.panInsideBounds(this.options.maxBounds);
        },
        _checkIfLoaded: function () {
          if (!this._loaded) throw new Error("Set map center and zoom first.");
        },
        _initEvents: function (t) {
          this._targets = {};
          var e = t ? De : Pe;
          e(
            (this._targets[s(this._container)] = this)._container,
            "click dblclick mousedown mouseup mouseover mouseout mousemove contextmenu keypress keydown keyup",
            this._handleDOMEvent,
            this
          ),
            this.options.trackResize &&
              e(window, "resize", this._onResize, this),
            _t &&
              this.options.transform3DLimit &&
              (t ? this.off : this.on).call(this, "moveend", this._onMoveEnd);
        },
        _onResize: function () {
          E(this._resizeRequest),
            (this._resizeRequest = C(function () {
              this.invalidateSize({ debounceMoveend: !0 });
            }, this));
        },
        _onScroll: function () {
          (this._container.scrollTop = 0), (this._container.scrollLeft = 0);
        },
        _onMoveEnd: function () {
          var t = this._getMapPanePos();
          Math.max(Math.abs(t.x), Math.abs(t.y)) >=
            this.options.transform3DLimit &&
            this._resetView(this.getCenter(), this.getZoom());
        },
        _findEventTargets: function (t, e) {
          for (
            var i,
              n = [],
              a = "mouseout" === e || "mouseover" === e,
              r = t.target || t.srcElement,
              o = !1;
            r;

          ) {
            if (
              (i = this._targets[s(r)]) &&
              ("click" === e || "preclick" === e) &&
              !t._simulated &&
              this._draggableMoved(i)
            ) {
              o = !0;
              break;
            }
            if (i && i.listens(e, !0)) {
              if (a && !Ze(r, t)) break;
              if ((n.push(i), a)) break;
            }
            if (r === this._container) break;
            r = r.parentNode;
          }
          return n.length || o || a || !Ze(r, t) || (n = [this]), n;
        },
        _handleDOMEvent: function (t) {
          var e;
          this._loaded &&
            !We(t) &&
            (("mousedown" !== (e = t.type) &&
              "keypress" !== e &&
              "keyup" !== e &&
              "keydown" !== e) ||
              xe(t.target || t.srcElement),
            this._fireDOMEvent(t, e));
        },
        _mouseEvents: [
          "click",
          "dblclick",
          "mouseover",
          "mouseout",
          "contextmenu",
        ],
        _fireDOMEvent: function (t, i, n) {
          var a;
          if (
            ("click" === t.type &&
              (((a = e({}, t)).type = "preclick"),
              this._fireDOMEvent(a, a.type, n)),
            !t._stopped &&
              (n = (n || []).concat(this._findEventTargets(t, i))).length)
          ) {
            var r = n[0];
            "contextmenu" === i && r.listens(i, !0) && Be(t);
            var s,
              o = { originalEvent: t };
            "keypress" !== t.type &&
              "keydown" !== t.type &&
              "keyup" !== t.type &&
              ((s = r.getLatLng && (!r._radius || r._radius <= 10)),
              (o.containerPoint = s
                ? this.latLngToContainerPoint(r.getLatLng())
                : this.mouseEventToContainerPoint(t)),
              (o.layerPoint = this.containerPointToLayerPoint(
                o.containerPoint
              )),
              (o.latlng = s
                ? r.getLatLng()
                : this.layerPointToLatLng(o.layerPoint)));
            for (var l = 0; l < n.length; l++)
              if (
                (n[l].fire(i, o, !0),
                o.originalEvent._stopped ||
                  (!1 === n[l].options.bubblingMouseEvents &&
                    -1 !== _(this._mouseEvents, i)))
              )
                return;
          }
        },
        _draggableMoved: function (t) {
          return (
            ((t = t.dragging && t.dragging.enabled() ? t : this).dragging &&
              t.dragging.moved()) ||
            (this.boxZoom && this.boxZoom.moved())
          );
        },
        _clearHandlers: function () {
          for (var t = 0, e = this._handlers.length; t < e; t++)
            this._handlers[t].disable();
        },
        whenReady: function (t, e) {
          return (
            this._loaded
              ? t.call(e || this, { target: this })
              : this.on("load", t, e),
            this
          );
        },
        _getMapPanePos: function () {
          return ye(this._mapPane) || new A(0, 0);
        },
        _moved: function () {
          var t = this._getMapPanePos();
          return t && !t.equals([0, 0]);
        },
        _getTopLeftPoint: function (t, e) {
          return (
            t && void 0 !== e
              ? this._getNewPixelOrigin(t, e)
              : this.getPixelOrigin()
          ).subtract(this._getMapPanePos());
        },
        _getNewPixelOrigin: function (t, e) {
          var i = this.getSize()._divideBy(2);
          return this.project(t, e)
            ._subtract(i)
            ._add(this._getMapPanePos())
            ._round();
        },
        _latLngToNewLayerPoint: function (t, e, i) {
          var n = this._getNewPixelOrigin(i, e);
          return this.project(t, e)._subtract(n);
        },
        _latLngBoundsToNewLayerBounds: function (t, e, i) {
          var n = this._getNewPixelOrigin(i, e);
          return N([
            this.project(t.getSouthWest(), e)._subtract(n),
            this.project(t.getNorthWest(), e)._subtract(n),
            this.project(t.getSouthEast(), e)._subtract(n),
            this.project(t.getNorthEast(), e)._subtract(n),
          ]);
        },
        _getCenterLayerPoint: function () {
          return this.containerPointToLayerPoint(this.getSize()._divideBy(2));
        },
        _getCenterOffset: function (t) {
          return this.latLngToLayerPoint(t).subtract(
            this._getCenterLayerPoint()
          );
        },
        _limitCenter: function (t, e, i) {
          if (!i) return t;
          var n = this.project(t, e),
            a = this.getSize().divideBy(2),
            r = new O(n.subtract(a), n.add(a)),
            s = this._getBoundsOffset(r, i, e);
          return s.round().equals([0, 0]) ? t : this.unproject(n.add(s), e);
        },
        _limitOffset: function (t, e) {
          if (!e) return t;
          var i = this.getPixelBounds(),
            n = new O(i.min.add(t), i.max.add(t));
          return t.add(this._getBoundsOffset(n, e));
        },
        _getBoundsOffset: function (t, e, i) {
          var n = N(
              this.project(e.getNorthEast(), i),
              this.project(e.getSouthWest(), i)
            ),
            a = n.min.subtract(t.min),
            r = n.max.subtract(t.max);
          return new A(this._rebound(a.x, -r.x), this._rebound(a.y, -r.y));
        },
        _rebound: function (t, e) {
          return 0 < t + e
            ? Math.round(t - e) / 2
            : Math.max(0, Math.ceil(t)) - Math.max(0, Math.floor(e));
        },
        _limitZoom: function (t) {
          var e = this.getMinZoom(),
            i = this.getMaxZoom(),
            n = _t ? this.options.zoomSnap : 1;
          return n && (t = Math.round(t / n) * n), Math.max(e, Math.min(i, t));
        },
        _onPanTransitionStep: function () {
          this.fire("move");
        },
        _onPanTransitionEnd: function () {
          he(this._mapPane, "leaflet-pan-anim"), this.fire("moveend");
        },
        _tryAnimatedPan: function (t, e) {
          var i = this._getCenterOffset(t)._trunc();
          return !(
            (!0 !== (e && e.animate) && !this.getSize().contains(i)) ||
            (this.panBy(i, e), 0)
          );
        },
        _createAnimProxy: function () {
          var t = (this._proxy = re(
            "div",
            "leaflet-proxy leaflet-zoom-animated"
          ));
          this._panes.mapPane.appendChild(t),
            this.on(
              "zoomanim",
              function (t) {
                var e = te,
                  i = this._proxy.style[e];
                ve(
                  this._proxy,
                  this.project(t.center, t.zoom),
                  this.getZoomScale(t.zoom, 1)
                ),
                  i === this._proxy.style[e] &&
                    this._animatingZoom &&
                    this._onZoomTransitionEnd();
              },
              this
            ),
            this.on("load moveend", this._animMoveEnd, this),
            this._on("unload", this._destroyAnimProxy, this);
        },
        _destroyAnimProxy: function () {
          se(this._proxy),
            this.off("load moveend", this._animMoveEnd, this),
            delete this._proxy;
        },
        _animMoveEnd: function () {
          var t = this.getCenter(),
            e = this.getZoom();
          ve(this._proxy, this.project(t, e), this.getZoomScale(e, 1));
        },
        _catchTransitionEnd: function (t) {
          this._animatingZoom &&
            0 <= t.propertyName.indexOf("transform") &&
            this._onZoomTransitionEnd();
        },
        _nothingToAnimate: function () {
          return !this._container.getElementsByClassName(
            "leaflet-zoom-animated"
          ).length;
        },
        _tryAnimatedZoom: function (t, e, i) {
          if (this._animatingZoom) return !0;
          if (
            ((i = i || {}),
            !this._zoomAnimated ||
              !1 === i.animate ||
              this._nothingToAnimate() ||
              Math.abs(e - this._zoom) > this.options.zoomAnimationThreshold)
          )
            return !1;
          var n = this.getZoomScale(e),
            a = this._getCenterOffset(t)._divideBy(1 - 1 / n);
          return !(
            (!0 !== i.animate && !this.getSize().contains(a)) ||
            (C(function () {
              this._moveStart(!0, !1)._animateZoom(t, e, !0);
            }, this),
            0)
          );
        },
        _animateZoom: function (t, e, i, n) {
          this._mapPane &&
            (i &&
              ((this._animatingZoom = !0),
              (this._animateToCenter = t),
              (this._animateToZoom = e),
              de(this._mapPane, "leaflet-zoom-anim")),
            this.fire("zoomanim", { center: t, zoom: e, noUpdate: n }),
            setTimeout(a(this._onZoomTransitionEnd, this), 250));
        },
        _onZoomTransitionEnd: function () {
          this._animatingZoom &&
            (this._mapPane && he(this._mapPane, "leaflet-zoom-anim"),
            (this._animatingZoom = !1),
            this._move(this._animateToCenter, this._animateToZoom),
            C(function () {
              this._moveEnd(!0);
            }, this));
        },
      });
    function qe(t) {
      return new Xe(t);
    }
    var Xe = D.extend({
      options: { position: "topright" },
      initialize: function (t) {
        p(this, t);
      },
      getPosition: function () {
        return this.options.position;
      },
      setPosition: function (t) {
        var e = this._map;
        return (
          e && e.removeControl(this),
          (this.options.position = t),
          e && e.addControl(this),
          this
        );
      },
      getContainer: function () {
        return this._container;
      },
      addTo: function (t) {
        this.remove(), (this._map = t);
        var e = (this._container = this.onAdd(t)),
          i = this.getPosition(),
          n = t._controlCorners[i];
        return (
          de(e, "leaflet-control"),
          -1 !== i.indexOf("bottom")
            ? n.insertBefore(e, n.firstChild)
            : n.appendChild(e),
          this._map.on("unload", this.remove, this),
          this
        );
      },
      remove: function () {
        return (
          this._map &&
            (se(this._container),
            this.onRemove && this.onRemove(this._map),
            this._map.off("unload", this.remove, this),
            (this._map = null)),
          this
        );
      },
      _refocusOnMap: function (t) {
        this._map &&
          t &&
          0 < t.screenX &&
          0 < t.screenY &&
          this._map.getContainer().focus();
      },
    });
    Ye.include({
      addControl: function (t) {
        return t.addTo(this), this;
      },
      removeControl: function (t) {
        return t.remove(), this;
      },
      _initControlPos: function () {
        var t = (this._controlCorners = {}),
          e = "leaflet-",
          i = (this._controlContainer = re(
            "div",
            e + "control-container",
            this._container
          ));
        function n(n, a) {
          var r = e + n + " " + e + a;
          t[n + a] = re("div", r, i);
        }
        n("top", "left"),
          n("top", "right"),
          n("bottom", "left"),
          n("bottom", "right");
      },
      _clearControlPos: function () {
        for (var t in this._controlCorners) se(this._controlCorners[t]);
        se(this._controlContainer),
          delete this._controlCorners,
          delete this._controlContainer;
      },
    });
    var Ke = Xe.extend({
        options: {
          collapsed: !0,
          position: "topright",
          autoZIndex: !0,
          hideSingleBase: !1,
          sortLayers: !1,
          sortFunction: function (t, e, i, n) {
            return i < n ? -1 : n < i ? 1 : 0;
          },
        },
        initialize: function (t, e, i) {
          for (var n in (p(this, i),
          (this._layerControlInputs = []),
          (this._layers = []),
          (this._lastZIndex = 0),
          (this._handlingClick = !1),
          t))
            this._addLayer(t[n], n);
          for (n in e) this._addLayer(e[n], n, !0);
        },
        onAdd: function (t) {
          this._initLayout(),
            this._update(),
            (this._map = t).on("zoomend", this._checkDisabledLayers, this);
          for (var e = 0; e < this._layers.length; e++)
            this._layers[e].layer.on("add remove", this._onLayerChange, this);
          return this._container;
        },
        addTo: function (t) {
          return Xe.prototype.addTo.call(this, t), this._expandIfNotCollapsed();
        },
        onRemove: function () {
          this._map.off("zoomend", this._checkDisabledLayers, this);
          for (var t = 0; t < this._layers.length; t++)
            this._layers[t].layer.off("add remove", this._onLayerChange, this);
        },
        addBaseLayer: function (t, e) {
          return this._addLayer(t, e), this._map ? this._update() : this;
        },
        addOverlay: function (t, e) {
          return this._addLayer(t, e, !0), this._map ? this._update() : this;
        },
        removeLayer: function (t) {
          t.off("add remove", this._onLayerChange, this);
          var e = this._getLayer(s(t));
          return (
            e && this._layers.splice(this._layers.indexOf(e), 1),
            this._map ? this._update() : this
          );
        },
        expand: function () {
          de(this._container, "leaflet-control-layers-expanded"),
            (this._section.style.height = null);
          var t = this._map.getSize().y - (this._container.offsetTop + 50);
          return (
            t < this._section.clientHeight
              ? (de(this._section, "leaflet-control-layers-scrollbar"),
                (this._section.style.height = t + "px"))
              : he(this._section, "leaflet-control-layers-scrollbar"),
            this._checkDisabledLayers(),
            this
          );
        },
        collapse: function () {
          return he(this._container, "leaflet-control-layers-expanded"), this;
        },
        _initLayout: function () {
          var t = "leaflet-control-layers",
            e = (this._container = re("div", t)),
            i = this.options.collapsed;
          e.setAttribute("aria-haspopup", !0), Ne(e), Oe(e);
          var n = (this._section = re("section", t + "-list"));
          i &&
            (this._map.on("click", this.collapse, this),
            at ||
              Pe(
                e,
                { mouseenter: this.expand, mouseleave: this.collapse },
                this
              ));
          var a = (this._layersLink = re("a", t + "-toggle", e));
          (a.href = "#"),
            (a.title = "Layers"),
            Tt
              ? (Pe(a, "click", je), Pe(a, "click", this.expand, this))
              : Pe(a, "focus", this.expand, this),
            i || this.expand(),
            (this._baseLayersList = re("div", t + "-base", n)),
            (this._separator = re("div", t + "-separator", n)),
            (this._overlaysList = re("div", t + "-overlays", n)),
            e.appendChild(n);
        },
        _getLayer: function (t) {
          for (var e = 0; e < this._layers.length; e++)
            if (this._layers[e] && s(this._layers[e].layer) === t)
              return this._layers[e];
        },
        _addLayer: function (t, e, i) {
          this._map && t.on("add remove", this._onLayerChange, this),
            this._layers.push({ layer: t, name: e, overlay: i }),
            this.options.sortLayers &&
              this._layers.sort(
                a(function (t, e) {
                  return this.options.sortFunction(
                    t.layer,
                    e.layer,
                    t.name,
                    e.name
                  );
                }, this)
              ),
            this.options.autoZIndex &&
              t.setZIndex &&
              (this._lastZIndex++, t.setZIndex(this._lastZIndex)),
            this._expandIfNotCollapsed();
        },
        _update: function () {
          if (!this._container) return this;
          oe(this._baseLayersList),
            oe(this._overlaysList),
            (this._layerControlInputs = []);
          for (var t, e, i, n = 0, a = 0; a < this._layers.length; a++)
            (i = this._layers[a]),
              this._addItem(i),
              (e = e || i.overlay),
              (t = t || !i.overlay),
              (n += i.overlay ? 0 : 1);
          return (
            this.options.hideSingleBase &&
              ((t = t && 1 < n),
              (this._baseLayersList.style.display = t ? "" : "none")),
            (this._separator.style.display = e && t ? "" : "none"),
            this
          );
        },
        _onLayerChange: function (t) {
          this._handlingClick || this._update();
          var e = this._getLayer(s(t.target)),
            i = e.overlay
              ? "add" === t.type
                ? "overlayadd"
                : "overlayremove"
              : "add" === t.type
              ? "baselayerchange"
              : null;
          i && this._map.fire(i, e);
        },
        _createRadioElement: function (t, e) {
          var i =
              '<input type="radio" class="leaflet-control-layers-selector" name="' +
              t +
              '"' +
              (e ? ' checked="checked"' : "") +
              "/>",
            n = document.createElement("div");
          return (n.innerHTML = i), n.firstChild;
        },
        _addItem: function (t) {
          var e,
            i = document.createElement("label"),
            n = this._map.hasLayer(t.layer);
          t.overlay
            ? (((e = document.createElement("input")).type = "checkbox"),
              (e.className = "leaflet-control-layers-selector"),
              (e.defaultChecked = n))
            : (e = this._createRadioElement(
                "leaflet-base-layers_" + s(this),
                n
              )),
            this._layerControlInputs.push(e),
            (e.layerId = s(t.layer)),
            Pe(e, "click", this._onInputClick, this);
          var a = document.createElement("span");
          a.innerHTML = " " + t.name;
          var r = document.createElement("div");
          return (
            i.appendChild(r),
            r.appendChild(e),
            r.appendChild(a),
            (t.overlay ? this._overlaysList : this._baseLayersList).appendChild(
              i
            ),
            this._checkDisabledLayers(),
            i
          );
        },
        _onInputClick: function () {
          var t,
            e,
            i = this._layerControlInputs,
            n = [],
            a = [];
          this._handlingClick = !0;
          for (var r = i.length - 1; 0 <= r; r--)
            (t = i[r]),
              (e = this._getLayer(t.layerId).layer),
              t.checked ? n.push(e) : t.checked || a.push(e);
          for (r = 0; r < a.length; r++)
            this._map.hasLayer(a[r]) && this._map.removeLayer(a[r]);
          for (r = 0; r < n.length; r++)
            this._map.hasLayer(n[r]) || this._map.addLayer(n[r]);
          (this._handlingClick = !1), this._refocusOnMap();
        },
        _checkDisabledLayers: function () {
          for (
            var t,
              e,
              i = this._layerControlInputs,
              n = this._map.getZoom(),
              a = i.length - 1;
            0 <= a;
            a--
          )
            (t = i[a]),
              (e = this._getLayer(t.layerId).layer),
              (t.disabled =
                (void 0 !== e.options.minZoom && n < e.options.minZoom) ||
                (void 0 !== e.options.maxZoom && n > e.options.maxZoom));
        },
        _expandIfNotCollapsed: function () {
          return this._map && !this.options.collapsed && this.expand(), this;
        },
        _expand: function () {
          return this.expand();
        },
        _collapse: function () {
          return this.collapse();
        },
      }),
      Je = Xe.extend({
        options: {
          position: "topleft",
          zoomInText: "+",
          zoomInTitle: "Zoom in",
          zoomOutText: "&#x2212;",
          zoomOutTitle: "Zoom out",
        },
        onAdd: function (t) {
          var e = "leaflet-control-zoom",
            i = re("div", e + " leaflet-bar"),
            n = this.options;
          return (
            (this._zoomInButton = this._createButton(
              n.zoomInText,
              n.zoomInTitle,
              e + "-in",
              i,
              this._zoomIn
            )),
            (this._zoomOutButton = this._createButton(
              n.zoomOutText,
              n.zoomOutTitle,
              e + "-out",
              i,
              this._zoomOut
            )),
            this._updateDisabled(),
            t.on("zoomend zoomlevelschange", this._updateDisabled, this),
            i
          );
        },
        onRemove: function (t) {
          t.off("zoomend zoomlevelschange", this._updateDisabled, this);
        },
        disable: function () {
          return (this._disabled = !0), this._updateDisabled(), this;
        },
        enable: function () {
          return (this._disabled = !1), this._updateDisabled(), this;
        },
        _zoomIn: function (t) {
          !this._disabled &&
            this._map._zoom < this._map.getMaxZoom() &&
            this._map.zoomIn(
              this._map.options.zoomDelta * (t.shiftKey ? 3 : 1)
            );
        },
        _zoomOut: function (t) {
          !this._disabled &&
            this._map._zoom > this._map.getMinZoom() &&
            this._map.zoomOut(
              this._map.options.zoomDelta * (t.shiftKey ? 3 : 1)
            );
        },
        _createButton: function (t, e, i, n, a) {
          var r = re("a", i, n);
          return (
            (r.innerHTML = t),
            (r.href = "#"),
            (r.title = e),
            r.setAttribute("role", "button"),
            r.setAttribute("aria-label", e),
            Ne(r),
            Pe(r, "click", je),
            Pe(r, "click", a, this),
            Pe(r, "click", this._refocusOnMap, this),
            r
          );
        },
        _updateDisabled: function () {
          var t = this._map,
            e = "leaflet-disabled";
          he(this._zoomInButton, e),
            he(this._zoomOutButton, e),
            (!this._disabled && t._zoom !== t.getMinZoom()) ||
              de(this._zoomOutButton, e),
            (!this._disabled && t._zoom !== t.getMaxZoom()) ||
              de(this._zoomInButton, e);
        },
      });
    Ye.mergeOptions({ zoomControl: !0 }),
      Ye.addInitHook(function () {
        this.options.zoomControl &&
          ((this.zoomControl = new Je()), this.addControl(this.zoomControl));
      });
    var Qe = Xe.extend({
        options: {
          position: "bottomleft",
          maxWidth: 100,
          metric: !0,
          imperial: !0,
        },
        onAdd: function (t) {
          var e = "leaflet-control-scale",
            i = re("div", e),
            n = this.options;
          return (
            this._addScales(n, e + "-line", i),
            t.on(n.updateWhenIdle ? "moveend" : "move", this._update, this),
            t.whenReady(this._update, this),
            i
          );
        },
        onRemove: function (t) {
          t.off(
            this.options.updateWhenIdle ? "moveend" : "move",
            this._update,
            this
          );
        },
        _addScales: function (t, e, i) {
          t.metric && (this._mScale = re("div", e, i)),
            t.imperial && (this._iScale = re("div", e, i));
        },
        _update: function () {
          var t = this._map,
            e = t.getSize().y / 2,
            i = t.distance(
              t.containerPointToLatLng([0, e]),
              t.containerPointToLatLng([this.options.maxWidth, e])
            );
          this._updateScales(i);
        },
        _updateScales: function (t) {
          this.options.metric && t && this._updateMetric(t),
            this.options.imperial && t && this._updateImperial(t);
        },
        _updateMetric: function (t) {
          var e = this._getRoundNum(t),
            i = e < 1e3 ? e + " m" : e / 1e3 + " km";
          this._updateScale(this._mScale, i, e / t);
        },
        _updateImperial: function (t) {
          var e,
            i,
            n,
            a = 3.2808399 * t;
          5280 < a
            ? ((e = a / 5280),
              (i = this._getRoundNum(e)),
              this._updateScale(this._iScale, i + " mi", i / e))
            : ((n = this._getRoundNum(a)),
              this._updateScale(this._iScale, n + " ft", n / a));
        },
        _updateScale: function (t, e, i) {
          (t.style.width = Math.round(this.options.maxWidth * i) + "px"),
            (t.innerHTML = e);
        },
        _getRoundNum: function (t) {
          var e = Math.pow(10, (Math.floor(t) + "").length - 1),
            i = t / e;
          return e * (10 <= i ? 10 : 5 <= i ? 5 : 3 <= i ? 3 : 2 <= i ? 2 : 1);
        },
      }),
      ti = Xe.extend({
        options: {
          position: "bottomright",
          prefix:
            '<a href="https://leafletjs.com" title="A JS library for interactive maps">Leaflet</a>',
        },
        initialize: function (t) {
          p(this, t), (this._attributions = {});
        },
        onAdd: function (t) {
          for (var e in (((t.attributionControl = this)._container = re(
            "div",
            "leaflet-control-attribution"
          )),
          Ne(this._container),
          t._layers))
            t._layers[e].getAttribution &&
              this.addAttribution(t._layers[e].getAttribution());
          return this._update(), this._container;
        },
        setPrefix: function (t) {
          return (this.options.prefix = t), this._update(), this;
        },
        addAttribution: function (t) {
          return (
            t &&
              (this._attributions[t] || (this._attributions[t] = 0),
              this._attributions[t]++,
              this._update()),
            this
          );
        },
        removeAttribution: function (t) {
          return (
            t &&
              this._attributions[t] &&
              (this._attributions[t]--, this._update()),
            this
          );
        },
        _update: function () {
          if (this._map) {
            var t = [];
            for (var e in this._attributions)
              this._attributions[e] && t.push(e);
            var i = [];
            this.options.prefix && i.push(this.options.prefix),
              t.length && i.push(t.join(", ")),
              (this._container.innerHTML = i.join(" | "));
          }
        },
      });
    Ye.mergeOptions({ attributionControl: !0 }),
      Ye.addInitHook(function () {
        this.options.attributionControl && new ti().addTo(this);
      }),
      (Xe.Layers = Ke),
      (Xe.Zoom = Je),
      (Xe.Scale = Qe),
      (Xe.Attribution = ti),
      (qe.layers = function (t, e, i) {
        return new Ke(t, e, i);
      }),
      (qe.zoom = function (t) {
        return new Je(t);
      }),
      (qe.scale = function (t) {
        return new Qe(t);
      }),
      (qe.attribution = function (t) {
        return new ti(t);
      });
    var ei = D.extend({
      initialize: function (t) {
        this._map = t;
      },
      enable: function () {
        return this._enabled || ((this._enabled = !0), this.addHooks()), this;
      },
      disable: function () {
        return (
          this._enabled && ((this._enabled = !1), this.removeHooks()), this
        );
      },
      enabled: function () {
        return !!this._enabled;
      },
    });
    ei.addTo = function (t, e) {
      return t.addHandler(e, this), this;
    };
    var ii,
      ni = { Events: M },
      ai = Tt ? "touchstart mousedown" : "mousedown",
      ri = {
        mousedown: "mouseup",
        touchstart: "touchend",
        pointerdown: "touchend",
        MSPointerDown: "touchend",
      },
      si = {
        mousedown: "mousemove",
        touchstart: "touchmove",
        pointerdown: "touchmove",
        MSPointerDown: "touchmove",
      },
      oi = k.extend({
        options: { clickTolerance: 3 },
        initialize: function (t, e, i, n) {
          p(this, n),
            (this._element = t),
            (this._dragStartTarget = e || t),
            (this._preventOutline = i);
        },
        enable: function () {
          this._enabled ||
            (Pe(this._dragStartTarget, ai, this._onDown, this),
            (this._enabled = !0));
        },
        disable: function () {
          this._enabled &&
            (oi._dragging === this && this.finishDrag(),
            De(this._dragStartTarget, ai, this._onDown, this),
            (this._enabled = !1),
            (this._moved = !1));
        },
        _onDown: function (t) {
          var e, i;
          !t._simulated &&
            this._enabled &&
            ((this._moved = !1),
            ce(this._element, "leaflet-zoom-anim") ||
              oi._dragging ||
              t.shiftKey ||
              (1 !== t.which && 1 !== t.button && !t.touches) ||
              ((oi._dragging = this)._preventOutline && xe(this._element),
              be(),
              Ut(),
              this._moving ||
                (this.fire("down"),
                (e = t.touches ? t.touches[0] : t),
                (i = Te(this._element)),
                (this._startPoint = new A(e.clientX, e.clientY)),
                (this._parentScale = Ce(i)),
                Pe(document, si[t.type], this._onMove, this),
                Pe(document, ri[t.type], this._onUp, this))));
        },
        _onMove: function (t) {
          var e, i;
          !t._simulated &&
            this._enabled &&
            (t.touches && 1 < t.touches.length
              ? (this._moved = !0)
              : ((i = new A(
                  (e =
                    t.touches && 1 === t.touches.length
                      ? t.touches[0]
                      : t).clientX,
                  e.clientY
                )._subtract(this._startPoint)).x ||
                  i.y) &&
                (Math.abs(i.x) + Math.abs(i.y) < this.options.clickTolerance ||
                  ((i.x /= this._parentScale.x),
                  (i.y /= this._parentScale.y),
                  Be(t),
                  this._moved ||
                    (this.fire("dragstart"),
                    (this._moved = !0),
                    (this._startPos = ye(this._element).subtract(i)),
                    de(document.body, "leaflet-dragging"),
                    (this._lastTarget = t.target || t.srcElement),
                    window.SVGElementInstance &&
                      this._lastTarget instanceof window.SVGElementInstance &&
                      (this._lastTarget =
                        this._lastTarget.correspondingUseElement),
                    de(this._lastTarget, "leaflet-drag-target")),
                  (this._newPos = this._startPos.add(i)),
                  (this._moving = !0),
                  E(this._animRequest),
                  (this._lastEvent = t),
                  (this._animRequest = C(this._updatePosition, this, !0)))));
        },
        _updatePosition: function () {
          var t = { originalEvent: this._lastEvent };
          this.fire("predrag", t),
            _e(this._element, this._newPos),
            this.fire("drag", t);
        },
        _onUp: function (t) {
          !t._simulated && this._enabled && this.finishDrag();
        },
        finishDrag: function () {
          for (var t in (he(document.body, "leaflet-dragging"),
          this._lastTarget &&
            (he(this._lastTarget, "leaflet-drag-target"),
            (this._lastTarget = null)),
          si))
            De(document, si[t], this._onMove, this),
              De(document, ri[t], this._onUp, this);
          we(),
            Yt(),
            this._moved &&
              this._moving &&
              (E(this._animRequest),
              this.fire("dragend", {
                distance: this._newPos.distanceTo(this._startPos),
              })),
            (this._moving = !1),
            (oi._dragging = !1);
        },
      });
    function li(t, e) {
      if (!e || !t.length) return t.slice();
      var i = e * e;
      return (function (t, e) {
        var i = t.length,
          n = new (typeof Uint8Array != void 0 + "" ? Uint8Array : Array)(i);
        (n[0] = n[i - 1] = 1),
          (function t(e, i, n, a, r) {
            var s,
              o,
              l,
              u = 0;
            for (o = a + 1; o <= r - 1; o++)
              u < (l = pi(e[o], e[a], e[r], !0)) && ((s = o), (u = l));
            n < u && ((i[s] = 1), t(e, i, n, a, s), t(e, i, n, s, r));
          })(t, n, e, 0, i - 1);
        var a,
          r = [];
        for (a = 0; a < i; a++) n[a] && r.push(t[a]);
        return r;
      })(
        (t = (function (t, e) {
          for (var i = [t[0]], n = 1, a = 0, r = t.length; n < r; n++)
            (function (t, e) {
              var i = e.x - t.x,
                n = e.y - t.y;
              return i * i + n * n;
            })(t[n], t[a]) > e && (i.push(t[n]), (a = n));
          return a < r - 1 && i.push(t[r - 1]), i;
        })(t, i)),
        i
      );
    }
    function ui(t, e, i) {
      return Math.sqrt(pi(t, e, i, !0));
    }
    function ci(t, e, i, n, a) {
      var r,
        s,
        o,
        l = n ? ii : hi(t, i),
        u = hi(e, i);
      for (ii = u; ; ) {
        if (!(l | u)) return [t, e];
        if (l & u) return !1;
        (o = hi((s = di(t, e, (r = l || u), i, a)), i)),
          r === l ? ((t = s), (l = o)) : ((e = s), (u = o));
      }
    }
    function di(t, e, i, n, a) {
      var r,
        s,
        o = e.x - t.x,
        l = e.y - t.y,
        u = n.min,
        c = n.max;
      return (
        8 & i
          ? ((r = t.x + (o * (c.y - t.y)) / l), (s = c.y))
          : 4 & i
          ? ((r = t.x + (o * (u.y - t.y)) / l), (s = u.y))
          : 2 & i
          ? ((r = c.x), (s = t.y + (l * (c.x - t.x)) / o))
          : 1 & i && ((r = u.x), (s = t.y + (l * (u.x - t.x)) / o)),
        new A(r, s, a)
      );
    }
    function hi(t, e) {
      var i = 0;
      return (
        t.x < e.min.x ? (i |= 1) : t.x > e.max.x && (i |= 2),
        t.y < e.min.y ? (i |= 4) : t.y > e.max.y && (i |= 8),
        i
      );
    }
    function pi(t, e, i, n) {
      var a,
        r = e.x,
        s = e.y,
        o = i.x - r,
        l = i.y - s,
        u = o * o + l * l;
      return (
        0 < u &&
          (1 < (a = ((t.x - r) * o + (t.y - s) * l) / u)
            ? ((r = i.x), (s = i.y))
            : 0 < a && ((r += o * a), (s += l * a))),
        (o = t.x - r),
        (l = t.y - s),
        n ? o * o + l * l : new A(r, s)
      );
    }
    function fi(t) {
      return !v(t[0]) || ("object" != typeof t[0][0] && void 0 !== t[0][0]);
    }
    function mi(t) {
      return (
        console.warn(
          "Deprecated use of _flat, please use L.LineUtil.isFlat instead."
        ),
        fi(t)
      );
    }
    var gi = {
      simplify: li,
      pointToSegmentDistance: ui,
      closestPointOnSegment: function (t, e, i) {
        return pi(t, e, i);
      },
      clipSegment: ci,
      _getEdgeIntersection: di,
      _getBitCode: hi,
      _sqClosestPointOnSegment: pi,
      isFlat: fi,
      _flat: mi,
    };
    function vi(t, e, i) {
      for (
        var n, a, r, s, o, l, u, c = [1, 4, 2, 8], d = 0, h = t.length;
        d < h;
        d++
      )
        t[d]._code = hi(t[d], e);
      for (r = 0; r < 4; r++) {
        for (l = c[r], n = [], d = 0, a = (h = t.length) - 1; d < h; a = d++)
          (s = t[d]),
            (o = t[a]),
            s._code & l
              ? o._code & l ||
                (((u = di(o, s, l, e, i))._code = hi(u, e)), n.push(u))
              : (o._code & l &&
                  (((u = di(o, s, l, e, i))._code = hi(u, e)), n.push(u)),
                n.push(s));
        t = n;
      }
      return t;
    }
    var _i,
      yi = { clipPolygon: vi },
      bi = {
        project: function (t) {
          return new A(t.lng, t.lat);
        },
        unproject: function (t) {
          return new F(t.y, t.x);
        },
        bounds: new O([-180, -90], [180, 90]),
      },
      wi = {
        R: 6378137,
        R_MINOR: 6356752.314245179,
        bounds: new O(
          [-20037508.34279, -15496570.73972],
          [20037508.34279, 18764656.23138]
        ),
        project: function (t) {
          var e = Math.PI / 180,
            i = this.R,
            n = t.lat * e,
            a = this.R_MINOR / i,
            r = Math.sqrt(1 - a * a),
            s = r * Math.sin(n),
            o =
              Math.tan(Math.PI / 4 - n / 2) /
              Math.pow((1 - s) / (1 + s), r / 2);
          n = -i * Math.log(Math.max(o, 1e-10));
          return new A(t.lng * e * i, n);
        },
        unproject: function (t) {
          for (
            var e,
              i = 180 / Math.PI,
              n = this.R,
              a = this.R_MINOR / n,
              r = Math.sqrt(1 - a * a),
              s = Math.exp(-t.y / n),
              o = Math.PI / 2 - 2 * Math.atan(s),
              l = 0,
              u = 0.1;
            l < 15 && 1e-7 < Math.abs(u);
            l++
          )
            (e = r * Math.sin(o)),
              (e = Math.pow((1 - e) / (1 + e), r / 2)),
              (o += u = Math.PI / 2 - 2 * Math.atan(s * e) - o);
          return new F(o * i, (t.x * i) / n);
        },
      },
      xi = { LonLat: bi, Mercator: wi, SphericalMercator: Z },
      Si = e({}, V, {
        code: "EPSG:3395",
        projection: wi,
        transformation: U((_i = 0.5 / (Math.PI * wi.R)), 0.5, -_i, 0.5),
      }),
      Ti = e({}, V, {
        code: "EPSG:4326",
        projection: bi,
        transformation: U(1 / 180, 1, -1 / 180, 0.5),
      }),
      Ci = e({}, H, {
        projection: bi,
        transformation: U(1, 0, -1, 0),
        scale: function (t) {
          return Math.pow(2, t);
        },
        zoom: function (t) {
          return Math.log(t) / Math.LN2;
        },
        distance: function (t, e) {
          var i = e.lng - t.lng,
            n = e.lat - t.lat;
          return Math.sqrt(i * i + n * n);
        },
        infinite: !0,
      });
    (H.Earth = V),
      (H.EPSG3395 = Si),
      (H.EPSG3857 = q),
      (H.EPSG900913 = X),
      (H.EPSG4326 = Ti),
      (H.Simple = Ci);
    var Ei = k.extend({
      options: {
        pane: "overlayPane",
        attribution: null,
        bubblingMouseEvents: !0,
      },
      addTo: function (t) {
        return t.addLayer(this), this;
      },
      remove: function () {
        return this.removeFrom(this._map || this._mapToAdd);
      },
      removeFrom: function (t) {
        return t && t.removeLayer(this), this;
      },
      getPane: function (t) {
        return this._map.getPane(t ? this.options[t] || t : this.options.pane);
      },
      addInteractiveTarget: function (t) {
        return (this._map._targets[s(t)] = this);
      },
      removeInteractiveTarget: function (t) {
        return delete this._map._targets[s(t)], this;
      },
      getAttribution: function () {
        return this.options.attribution;
      },
      _layerAdd: function (t) {
        var e,
          i = t.target;
        i.hasLayer(this) &&
          ((this._map = i),
          (this._zoomAnimated = i._zoomAnimated),
          this.getEvents &&
            ((e = this.getEvents()),
            i.on(e, this),
            this.once(
              "remove",
              function () {
                i.off(e, this);
              },
              this
            )),
          this.onAdd(i),
          this.getAttribution &&
            i.attributionControl &&
            i.attributionControl.addAttribution(this.getAttribution()),
          this.fire("add"),
          i.fire("layeradd", { layer: this }));
      },
    });
    Ye.include({
      addLayer: function (t) {
        if (!t._layerAdd)
          throw new Error("The provided object is not a Layer.");
        var e = s(t);
        return (
          this._layers[e] ||
            (((this._layers[e] = t)._mapToAdd = this),
            t.beforeAdd && t.beforeAdd(this),
            this.whenReady(t._layerAdd, t)),
          this
        );
      },
      removeLayer: function (t) {
        var e = s(t);
        return (
          this._layers[e] &&
            (this._loaded && t.onRemove(this),
            t.getAttribution &&
              this.attributionControl &&
              this.attributionControl.removeAttribution(t.getAttribution()),
            delete this._layers[e],
            this._loaded &&
              (this.fire("layerremove", { layer: t }), t.fire("remove")),
            (t._map = t._mapToAdd = null)),
          this
        );
      },
      hasLayer: function (t) {
        return !!t && s(t) in this._layers;
      },
      eachLayer: function (t, e) {
        for (var i in this._layers) t.call(e, this._layers[i]);
        return this;
      },
      _addLayers: function (t) {
        for (var e = 0, i = (t = t ? (v(t) ? t : [t]) : []).length; e < i; e++)
          this.addLayer(t[e]);
      },
      _addZoomLimit: function (t) {
        (!isNaN(t.options.maxZoom) && isNaN(t.options.minZoom)) ||
          ((this._zoomBoundLayers[s(t)] = t), this._updateZoomLevels());
      },
      _removeZoomLimit: function (t) {
        var e = s(t);
        this._zoomBoundLayers[e] &&
          (delete this._zoomBoundLayers[e], this._updateZoomLevels());
      },
      _updateZoomLevels: function () {
        var t = 1 / 0,
          e = -1 / 0,
          i = this._getZoomSpan();
        for (var n in this._zoomBoundLayers) {
          var a = this._zoomBoundLayers[n].options;
          (t = void 0 === a.minZoom ? t : Math.min(t, a.minZoom)),
            (e = void 0 === a.maxZoom ? e : Math.max(e, a.maxZoom));
        }
        (this._layersMaxZoom = e === -1 / 0 ? void 0 : e),
          (this._layersMinZoom = t === 1 / 0 ? void 0 : t),
          i !== this._getZoomSpan() && this.fire("zoomlevelschange"),
          void 0 === this.options.maxZoom &&
            this._layersMaxZoom &&
            this.getZoom() > this._layersMaxZoom &&
            this.setZoom(this._layersMaxZoom),
          void 0 === this.options.minZoom &&
            this._layersMinZoom &&
            this.getZoom() < this._layersMinZoom &&
            this.setZoom(this._layersMinZoom);
      },
    });
    var Pi = Ei.extend({
        initialize: function (t, e) {
          var i, n;
          if ((p(this, e), (this._layers = {}), t))
            for (i = 0, n = t.length; i < n; i++) this.addLayer(t[i]);
        },
        addLayer: function (t) {
          var e = this.getLayerId(t);
          return (
            (this._layers[e] = t), this._map && this._map.addLayer(t), this
          );
        },
        removeLayer: function (t) {
          var e = t in this._layers ? t : this.getLayerId(t);
          return (
            this._map &&
              this._layers[e] &&
              this._map.removeLayer(this._layers[e]),
            delete this._layers[e],
            this
          );
        },
        hasLayer: function (t) {
          return (
            !!t &&
            ("number" == typeof t ? t : this.getLayerId(t)) in this._layers
          );
        },
        clearLayers: function () {
          return this.eachLayer(this.removeLayer, this);
        },
        invoke: function (t) {
          var e,
            i,
            n = Array.prototype.slice.call(arguments, 1);
          for (e in this._layers) (i = this._layers[e])[t] && i[t].apply(i, n);
          return this;
        },
        onAdd: function (t) {
          this.eachLayer(t.addLayer, t);
        },
        onRemove: function (t) {
          this.eachLayer(t.removeLayer, t);
        },
        eachLayer: function (t, e) {
          for (var i in this._layers) t.call(e, this._layers[i]);
          return this;
        },
        getLayer: function (t) {
          return this._layers[t];
        },
        getLayers: function () {
          var t = [];
          return this.eachLayer(t.push, t), t;
        },
        setZIndex: function (t) {
          return this.invoke("setZIndex", t);
        },
        getLayerId: s,
      }),
      Li = Pi.extend({
        addLayer: function (t) {
          return this.hasLayer(t)
            ? this
            : (t.addEventParent(this),
              Pi.prototype.addLayer.call(this, t),
              this.fire("layeradd", { layer: t }));
        },
        removeLayer: function (t) {
          return this.hasLayer(t)
            ? (t in this._layers && (t = this._layers[t]),
              t.removeEventParent(this),
              Pi.prototype.removeLayer.call(this, t),
              this.fire("layerremove", { layer: t }))
            : this;
        },
        setStyle: function (t) {
          return this.invoke("setStyle", t);
        },
        bringToFront: function () {
          return this.invoke("bringToFront");
        },
        bringToBack: function () {
          return this.invoke("bringToBack");
        },
        getBounds: function () {
          var t = new B();
          for (var e in this._layers) {
            var i = this._layers[e];
            t.extend(i.getBounds ? i.getBounds() : i.getLatLng());
          }
          return t;
        },
      }),
      Di = D.extend({
        options: { popupAnchor: [0, 0], tooltipAnchor: [0, 0] },
        initialize: function (t) {
          p(this, t);
        },
        createIcon: function (t) {
          return this._createIcon("icon", t);
        },
        createShadow: function (t) {
          return this._createIcon("shadow", t);
        },
        _createIcon: function (t, e) {
          var i = this._getIconUrl(t);
          if (!i) {
            if ("icon" === t)
              throw new Error(
                "iconUrl not set in Icon options (see the docs)."
              );
            return null;
          }
          var n = this._createImg(i, e && "IMG" === e.tagName ? e : null);
          return this._setIconStyles(n, t), n;
        },
        _setIconStyles: function (t, e) {
          var i = this.options,
            n = i[e + "Size"];
          "number" == typeof n && (n = [n, n]);
          var a = z(n),
            r = z(
              ("shadow" === e && i.shadowAnchor) ||
                i.iconAnchor ||
                (a && a.divideBy(2, !0))
            );
          (t.className = "leaflet-marker-" + e + " " + (i.className || "")),
            r &&
              ((t.style.marginLeft = -r.x + "px"),
              (t.style.marginTop = -r.y + "px")),
            a && ((t.style.width = a.x + "px"), (t.style.height = a.y + "px"));
        },
        _createImg: function (t, e) {
          return ((e = e || document.createElement("img")).src = t), e;
        },
        _getIconUrl: function (t) {
          return (
            (Pt && this.options[t + "RetinaUrl"]) || this.options[t + "Url"]
          );
        },
      }),
      Mi = Di.extend({
        options: {
          iconUrl: "marker-icon.png",
          iconRetinaUrl: "marker-icon-2x.png",
          shadowUrl: "marker-shadow.png",
          iconSize: [25, 41],
          iconAnchor: [12, 41],
          popupAnchor: [1, -34],
          tooltipAnchor: [16, -28],
          shadowSize: [41, 41],
        },
        _getIconUrl: function (t) {
          return (
            Mi.imagePath || (Mi.imagePath = this._detectIconPath()),
            (this.options.imagePath || Mi.imagePath) +
              Di.prototype._getIconUrl.call(this, t)
          );
        },
        _detectIconPath: function () {
          var t = re("div", "leaflet-default-icon-path", document.body),
            e = ae(t, "background-image") || ae(t, "backgroundImage");
          return (
            document.body.removeChild(t),
            null === e || 0 !== e.indexOf("url")
              ? ""
              : e
                  .replace(/^url\(["']?/, "")
                  .replace(/marker-icon\.png["']?\)$/, "")
          );
        },
      }),
      ki = ei.extend({
        initialize: function (t) {
          this._marker = t;
        },
        addHooks: function () {
          var t = this._marker._icon;
          this._draggable || (this._draggable = new oi(t, t, !0)),
            this._draggable
              .on(
                {
                  dragstart: this._onDragStart,
                  predrag: this._onPreDrag,
                  drag: this._onDrag,
                  dragend: this._onDragEnd,
                },
                this
              )
              .enable(),
            de(t, "leaflet-marker-draggable");
        },
        removeHooks: function () {
          this._draggable
            .off(
              {
                dragstart: this._onDragStart,
                predrag: this._onPreDrag,
                drag: this._onDrag,
                dragend: this._onDragEnd,
              },
              this
            )
            .disable(),
            this._marker._icon &&
              he(this._marker._icon, "leaflet-marker-draggable");
        },
        moved: function () {
          return this._draggable && this._draggable._moved;
        },
        _adjustPan: function (t) {
          var e,
            i = this._marker,
            n = i._map,
            a = this._marker.options.autoPanSpeed,
            r = this._marker.options.autoPanPadding,
            s = ye(i._icon),
            o = n.getPixelBounds(),
            l = n.getPixelOrigin(),
            u = N(o.min._subtract(l).add(r), o.max._subtract(l).subtract(r));
          u.contains(s) ||
            ((e = z(
              (Math.max(u.max.x, s.x) - u.max.x) / (o.max.x - u.max.x) -
                (Math.min(u.min.x, s.x) - u.min.x) / (o.min.x - u.min.x),
              (Math.max(u.max.y, s.y) - u.max.y) / (o.max.y - u.max.y) -
                (Math.min(u.min.y, s.y) - u.min.y) / (o.min.y - u.min.y)
            ).multiplyBy(a)),
            n.panBy(e, { animate: !1 }),
            this._draggable._newPos._add(e),
            this._draggable._startPos._add(e),
            _e(i._icon, this._draggable._newPos),
            this._onDrag(t),
            (this._panRequest = C(this._adjustPan.bind(this, t))));
        },
        _onDragStart: function () {
          (this._oldLatLng = this._marker.getLatLng()),
            this._marker.closePopup && this._marker.closePopup(),
            this._marker.fire("movestart").fire("dragstart");
        },
        _onPreDrag: function (t) {
          this._marker.options.autoPan &&
            (E(this._panRequest),
            (this._panRequest = C(this._adjustPan.bind(this, t))));
        },
        _onDrag: function (t) {
          var e = this._marker,
            i = e._shadow,
            n = ye(e._icon),
            a = e._map.layerPointToLatLng(n);
          i && _e(i, n),
            (e._latlng = a),
            (t.latlng = a),
            (t.oldLatLng = this._oldLatLng),
            e.fire("move", t).fire("drag", t);
        },
        _onDragEnd: function (t) {
          E(this._panRequest),
            delete this._oldLatLng,
            this._marker.fire("moveend").fire("dragend", t);
        },
      }),
      Ai = Ei.extend({
        options: {
          icon: new Mi(),
          interactive: !0,
          keyboard: !0,
          title: "",
          alt: "",
          zIndexOffset: 0,
          opacity: 1,
          riseOnHover: !1,
          riseOffset: 250,
          pane: "markerPane",
          shadowPane: "shadowPane",
          bubblingMouseEvents: !1,
          draggable: !1,
          autoPan: !1,
          autoPanPadding: [50, 50],
          autoPanSpeed: 10,
        },
        initialize: function (t, e) {
          p(this, e), (this._latlng = R(t));
        },
        onAdd: function (t) {
          (this._zoomAnimated =
            this._zoomAnimated && t.options.markerZoomAnimation),
            this._zoomAnimated && t.on("zoomanim", this._animateZoom, this),
            this._initIcon(),
            this.update();
        },
        onRemove: function (t) {
          this.dragging &&
            this.dragging.enabled() &&
            ((this.options.draggable = !0), this.dragging.removeHooks()),
            delete this.dragging,
            this._zoomAnimated && t.off("zoomanim", this._animateZoom, this),
            this._removeIcon(),
            this._removeShadow();
        },
        getEvents: function () {
          return { zoom: this.update, viewreset: this.update };
        },
        getLatLng: function () {
          return this._latlng;
        },
        setLatLng: function (t) {
          var e = this._latlng;
          return (
            (this._latlng = R(t)),
            this.update(),
            this.fire("move", { oldLatLng: e, latlng: this._latlng })
          );
        },
        setZIndexOffset: function (t) {
          return (this.options.zIndexOffset = t), this.update();
        },
        getIcon: function () {
          return this.options.icon;
        },
        setIcon: function (t) {
          return (
            (this.options.icon = t),
            this._map && (this._initIcon(), this.update()),
            this._popup && this.bindPopup(this._popup, this._popup.options),
            this
          );
        },
        getElement: function () {
          return this._icon;
        },
        update: function () {
          var t;
          return (
            this._icon &&
              this._map &&
              ((t = this._map.latLngToLayerPoint(this._latlng).round()),
              this._setPos(t)),
            this
          );
        },
        _initIcon: function () {
          var t = this.options,
            e = "leaflet-zoom-" + (this._zoomAnimated ? "animated" : "hide"),
            i = t.icon.createIcon(this._icon),
            n = !1;
          i !== this._icon &&
            (this._icon && this._removeIcon(),
            (n = !0),
            t.title && (i.title = t.title),
            "IMG" === i.tagName && (i.alt = t.alt || "")),
            de(i, e),
            t.keyboard && (i.tabIndex = "0"),
            (this._icon = i),
            t.riseOnHover &&
              this.on({
                mouseover: this._bringToFront,
                mouseout: this._resetZIndex,
              });
          var a = t.icon.createShadow(this._shadow),
            r = !1;
          a !== this._shadow && (this._removeShadow(), (r = !0)),
            a && (de(a, e), (a.alt = "")),
            (this._shadow = a),
            t.opacity < 1 && this._updateOpacity(),
            n && this.getPane().appendChild(this._icon),
            this._initInteraction(),
            a && r && this.getPane(t.shadowPane).appendChild(this._shadow);
        },
        _removeIcon: function () {
          this.options.riseOnHover &&
            this.off({
              mouseover: this._bringToFront,
              mouseout: this._resetZIndex,
            }),
            se(this._icon),
            this.removeInteractiveTarget(this._icon),
            (this._icon = null);
        },
        _removeShadow: function () {
          this._shadow && se(this._shadow), (this._shadow = null);
        },
        _setPos: function (t) {
          this._icon && _e(this._icon, t),
            this._shadow && _e(this._shadow, t),
            (this._zIndex = t.y + this.options.zIndexOffset),
            this._resetZIndex();
        },
        _updateZIndex: function (t) {
          this._icon && (this._icon.style.zIndex = this._zIndex + t);
        },
        _animateZoom: function (t) {
          var e = this._map
            ._latLngToNewLayerPoint(this._latlng, t.zoom, t.center)
            .round();
          this._setPos(e);
        },
        _initInteraction: function () {
          var t;
          this.options.interactive &&
            (de(this._icon, "leaflet-interactive"),
            this.addInteractiveTarget(this._icon),
            ki &&
              ((t = this.options.draggable),
              this.dragging &&
                ((t = this.dragging.enabled()), this.dragging.disable()),
              (this.dragging = new ki(this)),
              t && this.dragging.enable()));
        },
        setOpacity: function (t) {
          return (
            (this.options.opacity = t), this._map && this._updateOpacity(), this
          );
        },
        _updateOpacity: function () {
          var t = this.options.opacity;
          this._icon && me(this._icon, t), this._shadow && me(this._shadow, t);
        },
        _bringToFront: function () {
          this._updateZIndex(this.options.riseOffset);
        },
        _resetZIndex: function () {
          this._updateZIndex(0);
        },
        _getPopupAnchor: function () {
          return this.options.icon.options.popupAnchor;
        },
        _getTooltipAnchor: function () {
          return this.options.icon.options.tooltipAnchor;
        },
      }),
      Ii = Ei.extend({
        options: {
          stroke: !0,
          color: "#3388ff",
          weight: 3,
          opacity: 1,
          lineCap: "round",
          lineJoin: "round",
          dashArray: null,
          dashOffset: null,
          fill: !1,
          fillColor: null,
          fillOpacity: 0.2,
          fillRule: "evenodd",
          interactive: !0,
          bubblingMouseEvents: !0,
        },
        beforeAdd: function (t) {
          this._renderer = t.getRenderer(this);
        },
        onAdd: function () {
          this._renderer._initPath(this),
            this._reset(),
            this._renderer._addPath(this);
        },
        onRemove: function () {
          this._renderer._removePath(this);
        },
        redraw: function () {
          return this._map && this._renderer._updatePath(this), this;
        },
        setStyle: function (t) {
          return (
            p(this, t),
            this._renderer &&
              (this._renderer._updateStyle(this),
              this.options.stroke &&
                t &&
                Object.prototype.hasOwnProperty.call(t, "weight") &&
                this._updateBounds()),
            this
          );
        },
        bringToFront: function () {
          return this._renderer && this._renderer._bringToFront(this), this;
        },
        bringToBack: function () {
          return this._renderer && this._renderer._bringToBack(this), this;
        },
        getElement: function () {
          return this._path;
        },
        _reset: function () {
          this._project(), this._update();
        },
        _clickTolerance: function () {
          return (
            (this.options.stroke ? this.options.weight / 2 : 0) +
            this._renderer.options.tolerance
          );
        },
      }),
      zi = Ii.extend({
        options: { fill: !0, radius: 10 },
        initialize: function (t, e) {
          p(this, e),
            (this._latlng = R(t)),
            (this._radius = this.options.radius);
        },
        setLatLng: function (t) {
          var e = this._latlng;
          return (
            (this._latlng = R(t)),
            this.redraw(),
            this.fire("move", { oldLatLng: e, latlng: this._latlng })
          );
        },
        getLatLng: function () {
          return this._latlng;
        },
        setRadius: function (t) {
          return (this.options.radius = this._radius = t), this.redraw();
        },
        getRadius: function () {
          return this._radius;
        },
        setStyle: function (t) {
          var e = (t && t.radius) || this._radius;
          return Ii.prototype.setStyle.call(this, t), this.setRadius(e), this;
        },
        _project: function () {
          (this._point = this._map.latLngToLayerPoint(this._latlng)),
            this._updateBounds();
        },
        _updateBounds: function () {
          var t = this._radius,
            e = this._radiusY || t,
            i = this._clickTolerance(),
            n = [t + i, e + i];
          this._pxBounds = new O(this._point.subtract(n), this._point.add(n));
        },
        _update: function () {
          this._map && this._updatePath();
        },
        _updatePath: function () {
          this._renderer._updateCircle(this);
        },
        _empty: function () {
          return (
            this._radius && !this._renderer._bounds.intersects(this._pxBounds)
          );
        },
        _containsPoint: function (t) {
          return (
            t.distanceTo(this._point) <= this._radius + this._clickTolerance()
          );
        },
      }),
      Oi = zi.extend({
        initialize: function (t, i, n) {
          if (
            ("number" == typeof i && (i = e({}, n, { radius: i })),
            p(this, i),
            (this._latlng = R(t)),
            isNaN(this.options.radius))
          )
            throw new Error("Circle radius cannot be NaN");
          this._mRadius = this.options.radius;
        },
        setRadius: function (t) {
          return (this._mRadius = t), this.redraw();
        },
        getRadius: function () {
          return this._mRadius;
        },
        getBounds: function () {
          var t = [this._radius, this._radiusY || this._radius];
          return new B(
            this._map.layerPointToLatLng(this._point.subtract(t)),
            this._map.layerPointToLatLng(this._point.add(t))
          );
        },
        setStyle: Ii.prototype.setStyle,
        _project: function () {
          var t,
            e,
            i,
            n,
            a,
            r,
            s,
            o,
            l = this._latlng.lng,
            u = this._latlng.lat,
            c = this._map,
            d = c.options.crs;
          d.distance === V.distance
            ? ((t = Math.PI / 180),
              (e = this._mRadius / V.R / t),
              (i = c.project([u + e, l])),
              (n = c.project([u - e, l])),
              (a = i.add(n).divideBy(2)),
              (r = c.unproject(a).lat),
              (s =
                Math.acos(
                  (Math.cos(e * t) - Math.sin(u * t) * Math.sin(r * t)) /
                    (Math.cos(u * t) * Math.cos(r * t))
                ) / t),
              (!isNaN(s) && 0 !== s) || (s = e / Math.cos((Math.PI / 180) * u)),
              (this._point = a.subtract(c.getPixelOrigin())),
              (this._radius = isNaN(s) ? 0 : a.x - c.project([r, l - s]).x),
              (this._radiusY = a.y - i.y))
            : ((o = d.unproject(
                d.project(this._latlng).subtract([this._mRadius, 0])
              )),
              (this._point = c.latLngToLayerPoint(this._latlng)),
              (this._radius = this._point.x - c.latLngToLayerPoint(o).x)),
            this._updateBounds();
        },
      }),
      Ni = Ii.extend({
        options: { smoothFactor: 1, noClip: !1 },
        initialize: function (t, e) {
          p(this, e), this._setLatLngs(t);
        },
        getLatLngs: function () {
          return this._latlngs;
        },
        setLatLngs: function (t) {
          return this._setLatLngs(t), this.redraw();
        },
        isEmpty: function () {
          return !this._latlngs.length;
        },
        closestLayerPoint: function (t) {
          for (
            var e,
              i,
              n = 1 / 0,
              a = null,
              r = pi,
              s = 0,
              o = this._parts.length;
            s < o;
            s++
          )
            for (var l = this._parts[s], u = 1, c = l.length; u < c; u++) {
              var d = r(t, (e = l[u - 1]), (i = l[u]), !0);
              d < n && ((n = d), (a = r(t, e, i)));
            }
          return a && (a.distance = Math.sqrt(n)), a;
        },
        getCenter: function () {
          if (!this._map)
            throw new Error("Must add layer to map before using getCenter()");
          var t,
            e,
            i,
            n,
            a,
            r,
            s,
            o = this._rings[0],
            l = o.length;
          if (!l) return null;
          for (e = t = 0; t < l - 1; t++) e += o[t].distanceTo(o[t + 1]) / 2;
          if (0 === e) return this._map.layerPointToLatLng(o[0]);
          for (n = t = 0; t < l - 1; t++)
            if (((a = o[t]), (r = o[t + 1]), e < (n += i = a.distanceTo(r))))
              return (
                (s = (n - e) / i),
                this._map.layerPointToLatLng([
                  r.x - s * (r.x - a.x),
                  r.y - s * (r.y - a.y),
                ])
              );
        },
        getBounds: function () {
          return this._bounds;
        },
        addLatLng: function (t, e) {
          return (
            (e = e || this._defaultShape()),
            (t = R(t)),
            e.push(t),
            this._bounds.extend(t),
            this.redraw()
          );
        },
        _setLatLngs: function (t) {
          (this._bounds = new B()), (this._latlngs = this._convertLatLngs(t));
        },
        _defaultShape: function () {
          return fi(this._latlngs) ? this._latlngs : this._latlngs[0];
        },
        _convertLatLngs: function (t) {
          for (var e = [], i = fi(t), n = 0, a = t.length; n < a; n++)
            i
              ? ((e[n] = R(t[n])), this._bounds.extend(e[n]))
              : (e[n] = this._convertLatLngs(t[n]));
          return e;
        },
        _project: function () {
          var t = new O();
          (this._rings = []),
            this._projectLatlngs(this._latlngs, this._rings, t),
            this._bounds.isValid() &&
              t.isValid() &&
              ((this._rawPxBounds = t), this._updateBounds());
        },
        _updateBounds: function () {
          var t = this._clickTolerance(),
            e = new A(t, t);
          this._pxBounds = new O([
            this._rawPxBounds.min.subtract(e),
            this._rawPxBounds.max.add(e),
          ]);
        },
        _projectLatlngs: function (t, e, i) {
          var n,
            a,
            r = t[0] instanceof F,
            s = t.length;
          if (r) {
            for (a = [], n = 0; n < s; n++)
              (a[n] = this._map.latLngToLayerPoint(t[n])), i.extend(a[n]);
            e.push(a);
          } else for (n = 0; n < s; n++) this._projectLatlngs(t[n], e, i);
        },
        _clipPoints: function () {
          var t = this._renderer._bounds;
          if (
            ((this._parts = []), this._pxBounds && this._pxBounds.intersects(t))
          )
            if (this.options.noClip) this._parts = this._rings;
            else
              for (
                var e,
                  i,
                  n,
                  a,
                  r = this._parts,
                  s = 0,
                  o = 0,
                  l = this._rings.length;
                s < l;
                s++
              )
                for (e = 0, i = (a = this._rings[s]).length; e < i - 1; e++)
                  (n = ci(a[e], a[e + 1], t, e, !0)) &&
                    ((r[o] = r[o] || []),
                    r[o].push(n[0]),
                    (n[1] === a[e + 1] && e !== i - 2) ||
                      (r[o].push(n[1]), o++));
        },
        _simplifyPoints: function () {
          for (
            var t = this._parts,
              e = this.options.smoothFactor,
              i = 0,
              n = t.length;
            i < n;
            i++
          )
            t[i] = li(t[i], e);
        },
        _update: function () {
          this._map &&
            (this._clipPoints(), this._simplifyPoints(), this._updatePath());
        },
        _updatePath: function () {
          this._renderer._updatePoly(this);
        },
        _containsPoint: function (t, e) {
          var i,
            n,
            a,
            r,
            s,
            o,
            l = this._clickTolerance();
          if (!this._pxBounds || !this._pxBounds.contains(t)) return !1;
          for (i = 0, r = this._parts.length; i < r; i++)
            for (
              n = 0, a = (s = (o = this._parts[i]).length) - 1;
              n < s;
              a = n++
            )
              if ((e || 0 !== n) && ui(t, o[a], o[n]) <= l) return !0;
          return !1;
        },
      });
    Ni._flat = mi;
    var Bi = Ni.extend({
        options: { fill: !0 },
        isEmpty: function () {
          return !this._latlngs.length || !this._latlngs[0].length;
        },
        getCenter: function () {
          if (!this._map)
            throw new Error("Must add layer to map before using getCenter()");
          var t,
            e,
            i,
            n,
            a,
            r,
            s,
            o,
            l,
            u = this._rings[0],
            c = u.length;
          if (!c) return null;
          for (t = r = s = o = 0, e = c - 1; t < c; e = t++)
            (i = u[t]),
              (n = u[e]),
              (a = i.y * n.x - n.y * i.x),
              (s += (i.x + n.x) * a),
              (o += (i.y + n.y) * a),
              (r += 3 * a);
          return (
            (l = 0 === r ? u[0] : [s / r, o / r]),
            this._map.layerPointToLatLng(l)
          );
        },
        _convertLatLngs: function (t) {
          var e = Ni.prototype._convertLatLngs.call(this, t),
            i = e.length;
          return (
            2 <= i && e[0] instanceof F && e[0].equals(e[i - 1]) && e.pop(), e
          );
        },
        _setLatLngs: function (t) {
          Ni.prototype._setLatLngs.call(this, t),
            fi(this._latlngs) && (this._latlngs = [this._latlngs]);
        },
        _defaultShape: function () {
          return fi(this._latlngs[0]) ? this._latlngs[0] : this._latlngs[0][0];
        },
        _clipPoints: function () {
          var t = this._renderer._bounds,
            e = this.options.weight,
            i = new A(e, e);
          t = new O(t.min.subtract(i), t.max.add(i));
          if (
            ((this._parts = []), this._pxBounds && this._pxBounds.intersects(t))
          )
            if (this.options.noClip) this._parts = this._rings;
            else
              for (var n, a = 0, r = this._rings.length; a < r; a++)
                (n = vi(this._rings[a], t, !0)).length && this._parts.push(n);
        },
        _updatePath: function () {
          this._renderer._updatePoly(this, !0);
        },
        _containsPoint: function (t) {
          var e,
            i,
            n,
            a,
            r,
            s,
            o,
            l,
            u = !1;
          if (!this._pxBounds || !this._pxBounds.contains(t)) return !1;
          for (a = 0, o = this._parts.length; a < o; a++)
            for (
              r = 0, s = (l = (e = this._parts[a]).length) - 1;
              r < l;
              s = r++
            )
              (i = e[r]),
                (n = e[s]),
                i.y > t.y != n.y > t.y &&
                  t.x < ((n.x - i.x) * (t.y - i.y)) / (n.y - i.y) + i.x &&
                  (u = !u);
          return u || Ni.prototype._containsPoint.call(this, t, !0);
        },
      }),
      ji = Li.extend({
        initialize: function (t, e) {
          p(this, e), (this._layers = {}), t && this.addData(t);
        },
        addData: function (t) {
          var e,
            i,
            n,
            a = v(t) ? t : t.features;
          if (a) {
            for (e = 0, i = a.length; e < i; e++)
              ((n = a[e]).geometries ||
                n.geometry ||
                n.features ||
                n.coordinates) &&
                this.addData(n);
            return this;
          }
          var r = this.options;
          if (r.filter && !r.filter(t)) return this;
          var s = Fi(t, r);
          return s
            ? ((s.feature = Gi(t)),
              (s.defaultOptions = s.options),
              this.resetStyle(s),
              r.onEachFeature && r.onEachFeature(t, s),
              this.addLayer(s))
            : this;
        },
        resetStyle: function (t) {
          return void 0 === t
            ? this.eachLayer(this.resetStyle, this)
            : ((t.options = e({}, t.defaultOptions)),
              this._setLayerStyle(t, this.options.style),
              this);
        },
        setStyle: function (t) {
          return this.eachLayer(function (e) {
            this._setLayerStyle(e, t);
          }, this);
        },
        _setLayerStyle: function (t, e) {
          t.setStyle &&
            ("function" == typeof e && (e = e(t.feature)), t.setStyle(e));
        },
      });
    function Fi(t, e) {
      var i,
        n,
        a,
        r,
        s = "Feature" === t.type ? t.geometry : t,
        o = s ? s.coordinates : null,
        l = [],
        u = e && e.pointToLayer,
        c = (e && e.coordsToLatLng) || $i;
      if (!o && !s) return null;
      switch (s.type) {
        case "Point":
          return Ri(u, t, (i = c(o)), e);
        case "MultiPoint":
          for (a = 0, r = o.length; a < r; a++)
            (i = c(o[a])), l.push(Ri(u, t, i, e));
          return new Li(l);
        case "LineString":
        case "MultiLineString":
          return (n = Hi(o, "LineString" === s.type ? 0 : 1, c)), new Ni(n, e);
        case "Polygon":
        case "MultiPolygon":
          return (n = Hi(o, "Polygon" === s.type ? 1 : 2, c)), new Bi(n, e);
        case "GeometryCollection":
          for (a = 0, r = s.geometries.length; a < r; a++) {
            var d = Fi(
              {
                geometry: s.geometries[a],
                type: "Feature",
                properties: t.properties,
              },
              e
            );
            d && l.push(d);
          }
          return new Li(l);
        default:
          throw new Error("Invalid GeoJSON object.");
      }
    }
    function Ri(t, e, i, n) {
      return t ? t(e, i) : new Ai(i, n && n.markersInheritOptions && n);
    }
    function $i(t) {
      return new F(t[1], t[0], t[2]);
    }
    function Hi(t, e, i) {
      for (var n, a = [], r = 0, s = t.length; r < s; r++)
        (n = e ? Hi(t[r], e - 1, i) : (i || $i)(t[r])), a.push(n);
      return a;
    }
    function Vi(t, e) {
      return (
        (e = "number" == typeof e ? e : 6),
        void 0 !== t.alt
          ? [c(t.lng, e), c(t.lat, e), c(t.alt, e)]
          : [c(t.lng, e), c(t.lat, e)]
      );
    }
    function Wi(t, e, i, n) {
      for (var a = [], r = 0, s = t.length; r < s; r++)
        a.push(e ? Wi(t[r], e - 1, i, n) : Vi(t[r], n));
      return !e && i && a.push(a[0]), a;
    }
    function Zi(t, i) {
      return t.feature ? e({}, t.feature, { geometry: i }) : Gi(i);
    }
    function Gi(t) {
      return "Feature" === t.type || "FeatureCollection" === t.type
        ? t
        : { type: "Feature", properties: {}, geometry: t };
    }
    var Ui = {
      toGeoJSON: function (t) {
        return Zi(this, {
          type: "Point",
          coordinates: Vi(this.getLatLng(), t),
        });
      },
    };
    function Yi(t, e) {
      return new ji(t, e);
    }
    Ai.include(Ui),
      Oi.include(Ui),
      zi.include(Ui),
      Ni.include({
        toGeoJSON: function (t) {
          var e = !fi(this._latlngs);
          return Zi(this, {
            type: (e ? "Multi" : "") + "LineString",
            coordinates: Wi(this._latlngs, e ? 1 : 0, !1, t),
          });
        },
      }),
      Bi.include({
        toGeoJSON: function (t) {
          var e = !fi(this._latlngs),
            i = e && !fi(this._latlngs[0]),
            n = Wi(this._latlngs, i ? 2 : e ? 1 : 0, !0, t);
          return (
            e || (n = [n]),
            Zi(this, { type: (i ? "Multi" : "") + "Polygon", coordinates: n })
          );
        },
      }),
      Pi.include({
        toMultiPoint: function (t) {
          var e = [];
          return (
            this.eachLayer(function (i) {
              e.push(i.toGeoJSON(t).geometry.coordinates);
            }),
            Zi(this, { type: "MultiPoint", coordinates: e })
          );
        },
        toGeoJSON: function (t) {
          var e =
            this.feature && this.feature.geometry && this.feature.geometry.type;
          if ("MultiPoint" === e) return this.toMultiPoint(t);
          var i = "GeometryCollection" === e,
            n = [];
          return (
            this.eachLayer(function (e) {
              var a, r;
              e.toGeoJSON &&
                ((a = e.toGeoJSON(t)),
                i
                  ? n.push(a.geometry)
                  : "FeatureCollection" === (r = Gi(a)).type
                  ? n.push.apply(n, r.features)
                  : n.push(r));
            }),
            i
              ? Zi(this, { geometries: n, type: "GeometryCollection" })
              : { type: "FeatureCollection", features: n }
          );
        },
      });
    var qi = Yi,
      Xi = Ei.extend({
        options: {
          opacity: 1,
          alt: "",
          interactive: !1,
          crossOrigin: !1,
          errorOverlayUrl: "",
          zIndex: 1,
          className: "",
        },
        initialize: function (t, e, i) {
          (this._url = t), (this._bounds = j(e)), p(this, i);
        },
        onAdd: function () {
          this._image ||
            (this._initImage(),
            this.options.opacity < 1 && this._updateOpacity()),
            this.options.interactive &&
              (de(this._image, "leaflet-interactive"),
              this.addInteractiveTarget(this._image)),
            this.getPane().appendChild(this._image),
            this._reset();
        },
        onRemove: function () {
          se(this._image),
            this.options.interactive &&
              this.removeInteractiveTarget(this._image);
        },
        setOpacity: function (t) {
          return (
            (this.options.opacity = t),
            this._image && this._updateOpacity(),
            this
          );
        },
        setStyle: function (t) {
          return t.opacity && this.setOpacity(t.opacity), this;
        },
        bringToFront: function () {
          return this._map && le(this._image), this;
        },
        bringToBack: function () {
          return this._map && ue(this._image), this;
        },
        setUrl: function (t) {
          return (this._url = t), this._image && (this._image.src = t), this;
        },
        setBounds: function (t) {
          return (this._bounds = j(t)), this._map && this._reset(), this;
        },
        getEvents: function () {
          var t = { zoom: this._reset, viewreset: this._reset };
          return this._zoomAnimated && (t.zoomanim = this._animateZoom), t;
        },
        setZIndex: function (t) {
          return (this.options.zIndex = t), this._updateZIndex(), this;
        },
        getBounds: function () {
          return this._bounds;
        },
        getElement: function () {
          return this._image;
        },
        _initImage: function () {
          var t = "IMG" === this._url.tagName,
            e = (this._image = t ? this._url : re("img"));
          de(e, "leaflet-image-layer"),
            this._zoomAnimated && de(e, "leaflet-zoom-animated"),
            this.options.className && de(e, this.options.className),
            (e.onselectstart = u),
            (e.onmousemove = u),
            (e.onload = a(this.fire, this, "load")),
            (e.onerror = a(this._overlayOnError, this, "error")),
            (!this.options.crossOrigin && "" !== this.options.crossOrigin) ||
              (e.crossOrigin =
                !0 === this.options.crossOrigin
                  ? ""
                  : this.options.crossOrigin),
            this.options.zIndex && this._updateZIndex(),
            t
              ? (this._url = e.src)
              : ((e.src = this._url), (e.alt = this.options.alt));
        },
        _animateZoom: function (t) {
          var e = this._map.getZoomScale(t.zoom),
            i = this._map._latLngBoundsToNewLayerBounds(
              this._bounds,
              t.zoom,
              t.center
            ).min;
          ve(this._image, i, e);
        },
        _reset: function () {
          var t = this._image,
            e = new O(
              this._map.latLngToLayerPoint(this._bounds.getNorthWest()),
              this._map.latLngToLayerPoint(this._bounds.getSouthEast())
            ),
            i = e.getSize();
          _e(t, e.min),
            (t.style.width = i.x + "px"),
            (t.style.height = i.y + "px");
        },
        _updateOpacity: function () {
          me(this._image, this.options.opacity);
        },
        _updateZIndex: function () {
          this._image &&
            void 0 !== this.options.zIndex &&
            null !== this.options.zIndex &&
            (this._image.style.zIndex = this.options.zIndex);
        },
        _overlayOnError: function () {
          this.fire("error");
          var t = this.options.errorOverlayUrl;
          t && this._url !== t && ((this._url = t), (this._image.src = t));
        },
      }),
      Ki = Xi.extend({
        options: { autoplay: !0, loop: !0, keepAspectRatio: !0, muted: !1 },
        _initImage: function () {
          var t = "VIDEO" === this._url.tagName,
            e = (this._image = t ? this._url : re("video"));
          if (
            (de(e, "leaflet-image-layer"),
            this._zoomAnimated && de(e, "leaflet-zoom-animated"),
            this.options.className && de(e, this.options.className),
            (e.onselectstart = u),
            (e.onmousemove = u),
            (e.onloadeddata = a(this.fire, this, "load")),
            t)
          ) {
            for (
              var i = e.getElementsByTagName("source"), n = [], r = 0;
              r < i.length;
              r++
            )
              n.push(i[r].src);
            this._url = 0 < i.length ? n : [e.src];
          } else {
            v(this._url) || (this._url = [this._url]),
              !this.options.keepAspectRatio &&
                Object.prototype.hasOwnProperty.call(e.style, "objectFit") &&
                (e.style.objectFit = "fill"),
              (e.autoplay = !!this.options.autoplay),
              (e.loop = !!this.options.loop),
              (e.muted = !!this.options.muted);
            for (var s = 0; s < this._url.length; s++) {
              var o = re("source");
              (o.src = this._url[s]), e.appendChild(o);
            }
          }
        },
      }),
      Ji = Xi.extend({
        _initImage: function () {
          var t = (this._image = this._url);
          de(t, "leaflet-image-layer"),
            this._zoomAnimated && de(t, "leaflet-zoom-animated"),
            this.options.className && de(t, this.options.className),
            (t.onselectstart = u),
            (t.onmousemove = u);
        },
      }),
      Qi = Ei.extend({
        options: { offset: [0, 7], className: "", pane: "popupPane" },
        initialize: function (t, e) {
          p(this, t), (this._source = e);
        },
        onAdd: function (t) {
          (this._zoomAnimated = t._zoomAnimated),
            this._container || this._initLayout(),
            t._fadeAnimated && me(this._container, 0),
            clearTimeout(this._removeTimeout),
            this.getPane().appendChild(this._container),
            this.update(),
            t._fadeAnimated && me(this._container, 1),
            this.bringToFront();
        },
        onRemove: function (t) {
          t._fadeAnimated
            ? (me(this._container, 0),
              (this._removeTimeout = setTimeout(
                a(se, void 0, this._container),
                200
              )))
            : se(this._container);
        },
        getLatLng: function () {
          return this._latlng;
        },
        setLatLng: function (t) {
          return (
            (this._latlng = R(t)),
            this._map && (this._updatePosition(), this._adjustPan()),
            this
          );
        },
        getContent: function () {
          return this._content;
        },
        setContent: function (t) {
          return (this._content = t), this.update(), this;
        },
        getElement: function () {
          return this._container;
        },
        update: function () {
          this._map &&
            ((this._container.style.visibility = "hidden"),
            this._updateContent(),
            this._updateLayout(),
            this._updatePosition(),
            (this._container.style.visibility = ""),
            this._adjustPan());
        },
        getEvents: function () {
          var t = {
            zoom: this._updatePosition,
            viewreset: this._updatePosition,
          };
          return this._zoomAnimated && (t.zoomanim = this._animateZoom), t;
        },
        isOpen: function () {
          return !!this._map && this._map.hasLayer(this);
        },
        bringToFront: function () {
          return this._map && le(this._container), this;
        },
        bringToBack: function () {
          return this._map && ue(this._container), this;
        },
        _prepareOpen: function (t, e, i) {
          if ((e instanceof Ei || ((i = e), (e = t)), e instanceof Li))
            for (var n in t._layers) {
              e = t._layers[n];
              break;
            }
          if (!i)
            if (e.getCenter) i = e.getCenter();
            else {
              if (!e.getLatLng)
                throw new Error("Unable to get source layer LatLng.");
              i = e.getLatLng();
            }
          return (this._source = e), this.update(), i;
        },
        _updateContent: function () {
          if (this._content) {
            var t = this._contentNode,
              e =
                "function" == typeof this._content
                  ? this._content(this._source || this)
                  : this._content;
            if ("string" == typeof e) t.innerHTML = e;
            else {
              for (; t.hasChildNodes(); ) t.removeChild(t.firstChild);
              t.appendChild(e);
            }
            this.fire("contentupdate");
          }
        },
        _updatePosition: function () {
          var t, e, i, n, a;
          this._map &&
            ((t = this._map.latLngToLayerPoint(this._latlng)),
            (e = z(this.options.offset)),
            (i = this._getAnchor()),
            this._zoomAnimated
              ? _e(this._container, t.add(i))
              : (e = e.add(t).add(i)),
            (n = this._containerBottom = -e.y),
            (a = this._containerLeft =
              -Math.round(this._containerWidth / 2) + e.x),
            (this._container.style.bottom = n + "px"),
            (this._container.style.left = a + "px"));
        },
        _getAnchor: function () {
          return [0, 0];
        },
      }),
      tn = Qi.extend({
        options: {
          maxWidth: 300,
          minWidth: 50,
          maxHeight: null,
          autoPan: !0,
          autoPanPaddingTopLeft: null,
          autoPanPaddingBottomRight: null,
          autoPanPadding: [5, 5],
          keepInView: !1,
          closeButton: !0,
          autoClose: !0,
          closeOnEscapeKey: !0,
          className: "",
        },
        openOn: function (t) {
          return t.openPopup(this), this;
        },
        onAdd: function (t) {
          Qi.prototype.onAdd.call(this, t),
            t.fire("popupopen", { popup: this }),
            this._source &&
              (this._source.fire("popupopen", { popup: this }, !0),
              this._source instanceof Ii || this._source.on("preclick", ze));
        },
        onRemove: function (t) {
          Qi.prototype.onRemove.call(this, t),
            t.fire("popupclose", { popup: this }),
            this._source &&
              (this._source.fire("popupclose", { popup: this }, !0),
              this._source instanceof Ii || this._source.off("preclick", ze));
        },
        getEvents: function () {
          var t = Qi.prototype.getEvents.call(this);
          return (
            (void 0 !== this.options.closeOnClick
              ? this.options.closeOnClick
              : this._map.options.closePopupOnClick) &&
              (t.preclick = this._close),
            this.options.keepInView && (t.moveend = this._adjustPan),
            t
          );
        },
        _close: function () {
          this._map && this._map.closePopup(this);
        },
        _initLayout: function () {
          var t,
            e = "leaflet-popup",
            i = (this._container = re(
              "div",
              e +
                " " +
                (this.options.className || "") +
                " leaflet-zoom-animated"
            )),
            n = (this._wrapper = re("div", e + "-content-wrapper", i));
          (this._contentNode = re("div", e + "-content", n)),
            Ne(i),
            Oe(this._contentNode),
            Pe(i, "contextmenu", ze),
            (this._tipContainer = re("div", e + "-tip-container", i)),
            (this._tip = re("div", e + "-tip", this._tipContainer)),
            this.options.closeButton &&
              (((t = this._closeButton = re("a", e + "-close-button", i)).href =
                "#close"),
              (t.innerHTML = "&#215;"),
              Pe(t, "click", this._onCloseButtonClick, this));
        },
        _updateLayout: function () {
          var t = this._contentNode,
            e = t.style;
          (e.width = ""), (e.whiteSpace = "nowrap");
          var i = t.offsetWidth;
          i = Math.min(i, this.options.maxWidth);
          (i = Math.max(i, this.options.minWidth)),
            (e.width = i + 1 + "px"),
            (e.whiteSpace = ""),
            (e.height = "");
          var n = t.offsetHeight,
            a = this.options.maxHeight,
            r = "leaflet-popup-scrolled";
          a && a < n ? ((e.height = a + "px"), de(t, r)) : he(t, r),
            (this._containerWidth = this._container.offsetWidth);
        },
        _animateZoom: function (t) {
          var e = this._map._latLngToNewLayerPoint(
              this._latlng,
              t.zoom,
              t.center
            ),
            i = this._getAnchor();
          _e(this._container, e.add(i));
        },
        _adjustPan: function () {
          var t, e, i, n, a, r, s, o, l, u, c, d;
          this.options.autoPan &&
            (this._map._panAnim && this._map._panAnim.stop(),
            (t = this._map),
            (e = parseInt(ae(this._container, "marginBottom"), 10) || 0),
            (i = this._container.offsetHeight + e),
            (n = this._containerWidth),
            (a = new A(this._containerLeft, -i - this._containerBottom))._add(
              ye(this._container)
            ),
            (r = t.layerPointToContainerPoint(a)),
            (s = z(this.options.autoPanPadding)),
            (o = z(this.options.autoPanPaddingTopLeft || s)),
            (l = z(this.options.autoPanPaddingBottomRight || s)),
            (u = t.getSize()),
            (d = c = 0),
            r.x + n + l.x > u.x && (c = r.x + n - u.x + l.x),
            r.x - c - o.x < 0 && (c = r.x - o.x),
            r.y + i + l.y > u.y && (d = r.y + i - u.y + l.y),
            r.y - d - o.y < 0 && (d = r.y - o.y),
            (c || d) && t.fire("autopanstart").panBy([c, d]));
        },
        _onCloseButtonClick: function (t) {
          this._close(), je(t);
        },
        _getAnchor: function () {
          return z(
            this._source && this._source._getPopupAnchor
              ? this._source._getPopupAnchor()
              : [0, 0]
          );
        },
      });
    Ye.mergeOptions({ closePopupOnClick: !0 }),
      Ye.include({
        openPopup: function (t, e, i) {
          return (
            t instanceof tn || (t = new tn(i).setContent(t)),
            e && t.setLatLng(e),
            this.hasLayer(t)
              ? this
              : (this._popup &&
                  this._popup.options.autoClose &&
                  this.closePopup(),
                (this._popup = t),
                this.addLayer(t))
          );
        },
        closePopup: function (t) {
          return (
            (t && t !== this._popup) ||
              ((t = this._popup), (this._popup = null)),
            t && this.removeLayer(t),
            this
          );
        },
      }),
      Ei.include({
        bindPopup: function (t, e) {
          return (
            t instanceof tn
              ? (p(t, e), ((this._popup = t)._source = this))
              : ((this._popup && !e) || (this._popup = new tn(e, this)),
                this._popup.setContent(t)),
            this._popupHandlersAdded ||
              (this.on({
                click: this._openPopup,
                keypress: this._onKeyPress,
                remove: this.closePopup,
                move: this._movePopup,
              }),
              (this._popupHandlersAdded = !0)),
            this
          );
        },
        unbindPopup: function () {
          return (
            this._popup &&
              (this.off({
                click: this._openPopup,
                keypress: this._onKeyPress,
                remove: this.closePopup,
                move: this._movePopup,
              }),
              (this._popupHandlersAdded = !1),
              (this._popup = null)),
            this
          );
        },
        openPopup: function (t, e) {
          return (
            this._popup &&
              this._map &&
              ((e = this._popup._prepareOpen(this, t, e)),
              this._map.openPopup(this._popup, e)),
            this
          );
        },
        closePopup: function () {
          return this._popup && this._popup._close(), this;
        },
        togglePopup: function (t) {
          return (
            this._popup &&
              (this._popup._map ? this.closePopup() : this.openPopup(t)),
            this
          );
        },
        isPopupOpen: function () {
          return !!this._popup && this._popup.isOpen();
        },
        setPopupContent: function (t) {
          return this._popup && this._popup.setContent(t), this;
        },
        getPopup: function () {
          return this._popup;
        },
        _openPopup: function (t) {
          var e = t.layer || t.target;
          this._popup &&
            this._map &&
            (je(t),
            e instanceof Ii
              ? this.openPopup(t.layer || t.target, t.latlng)
              : this._map.hasLayer(this._popup) && this._popup._source === e
              ? this.closePopup()
              : this.openPopup(e, t.latlng));
        },
        _movePopup: function (t) {
          this._popup.setLatLng(t.latlng);
        },
        _onKeyPress: function (t) {
          13 === t.originalEvent.keyCode && this._openPopup(t);
        },
      });
    var en = Qi.extend({
      options: {
        pane: "tooltipPane",
        offset: [0, 0],
        direction: "auto",
        permanent: !1,
        sticky: !1,
        interactive: !1,
        opacity: 0.9,
      },
      onAdd: function (t) {
        Qi.prototype.onAdd.call(this, t),
          this.setOpacity(this.options.opacity),
          t.fire("tooltipopen", { tooltip: this }),
          this._source &&
            this._source.fire("tooltipopen", { tooltip: this }, !0);
      },
      onRemove: function (t) {
        Qi.prototype.onRemove.call(this, t),
          t.fire("tooltipclose", { tooltip: this }),
          this._source &&
            this._source.fire("tooltipclose", { tooltip: this }, !0);
      },
      getEvents: function () {
        var t = Qi.prototype.getEvents.call(this);
        return Tt && !this.options.permanent && (t.preclick = this._close), t;
      },
      _close: function () {
        this._map && this._map.closeTooltip(this);
      },
      _initLayout: function () {
        var t =
          "leaflet-tooltip " +
          (this.options.className || "") +
          " leaflet-zoom-" +
          (this._zoomAnimated ? "animated" : "hide");
        this._contentNode = this._container = re("div", t);
      },
      _updateLayout: function () {},
      _adjustPan: function () {},
      _setPosition: function (t) {
        var e,
          i = this._map,
          n = this._container,
          a = i.latLngToContainerPoint(i.getCenter()),
          r = i.layerPointToContainerPoint(t),
          s = this.options.direction,
          o = n.offsetWidth,
          l = n.offsetHeight,
          u = z(this.options.offset),
          c = this._getAnchor(),
          d =
            "top" === s
              ? ((e = o / 2), l)
              : "bottom" === s
              ? ((e = o / 2), 0)
              : ((e =
                  "center" === s
                    ? o / 2
                    : "right" === s
                    ? 0
                    : "left" === s
                    ? o
                    : r.x < a.x
                    ? ((s = "right"), 0)
                    : ((s = "left"), o + 2 * (u.x + c.x))),
                l / 2);
        (t = t.subtract(z(e, d, !0)).add(u).add(c)),
          he(n, "leaflet-tooltip-right"),
          he(n, "leaflet-tooltip-left"),
          he(n, "leaflet-tooltip-top"),
          he(n, "leaflet-tooltip-bottom"),
          de(n, "leaflet-tooltip-" + s),
          _e(n, t);
      },
      _updatePosition: function () {
        var t = this._map.latLngToLayerPoint(this._latlng);
        this._setPosition(t);
      },
      setOpacity: function (t) {
        (this.options.opacity = t), this._container && me(this._container, t);
      },
      _animateZoom: function (t) {
        var e = this._map._latLngToNewLayerPoint(
          this._latlng,
          t.zoom,
          t.center
        );
        this._setPosition(e);
      },
      _getAnchor: function () {
        return z(
          this._source && this._source._getTooltipAnchor && !this.options.sticky
            ? this._source._getTooltipAnchor()
            : [0, 0]
        );
      },
    });
    Ye.include({
      openTooltip: function (t, e, i) {
        return (
          t instanceof en || (t = new en(i).setContent(t)),
          e && t.setLatLng(e),
          this.hasLayer(t) ? this : this.addLayer(t)
        );
      },
      closeTooltip: function (t) {
        return t && this.removeLayer(t), this;
      },
    }),
      Ei.include({
        bindTooltip: function (t, e) {
          return (
            t instanceof en
              ? (p(t, e), ((this._tooltip = t)._source = this))
              : ((this._tooltip && !e) || (this._tooltip = new en(e, this)),
                this._tooltip.setContent(t)),
            this._initTooltipInteractions(),
            this._tooltip.options.permanent &&
              this._map &&
              this._map.hasLayer(this) &&
              this.openTooltip(),
            this
          );
        },
        unbindTooltip: function () {
          return (
            this._tooltip &&
              (this._initTooltipInteractions(!0),
              this.closeTooltip(),
              (this._tooltip = null)),
            this
          );
        },
        _initTooltipInteractions: function (t) {
          var e, i;
          (!t && this._tooltipHandlersAdded) ||
            ((e = t ? "off" : "on"),
            (i = { remove: this.closeTooltip, move: this._moveTooltip }),
            this._tooltip.options.permanent
              ? (i.add = this._openTooltip)
              : ((i.mouseover = this._openTooltip),
                (i.mouseout = this.closeTooltip),
                this._tooltip.options.sticky &&
                  (i.mousemove = this._moveTooltip),
                Tt && (i.click = this._openTooltip)),
            this[e](i),
            (this._tooltipHandlersAdded = !t));
        },
        openTooltip: function (t, e) {
          return (
            this._tooltip &&
              this._map &&
              ((e = this._tooltip._prepareOpen(this, t, e)),
              this._map.openTooltip(this._tooltip, e),
              this._tooltip.options.interactive &&
                this._tooltip._container &&
                (de(this._tooltip._container, "leaflet-clickable"),
                this.addInteractiveTarget(this._tooltip._container))),
            this
          );
        },
        closeTooltip: function () {
          return (
            this._tooltip &&
              (this._tooltip._close(),
              this._tooltip.options.interactive &&
                this._tooltip._container &&
                (he(this._tooltip._container, "leaflet-clickable"),
                this.removeInteractiveTarget(this._tooltip._container))),
            this
          );
        },
        toggleTooltip: function (t) {
          return (
            this._tooltip &&
              (this._tooltip._map ? this.closeTooltip() : this.openTooltip(t)),
            this
          );
        },
        isTooltipOpen: function () {
          return this._tooltip.isOpen();
        },
        setTooltipContent: function (t) {
          return this._tooltip && this._tooltip.setContent(t), this;
        },
        getTooltip: function () {
          return this._tooltip;
        },
        _openTooltip: function (t) {
          var e = t.layer || t.target;
          this._tooltip &&
            this._map &&
            this.openTooltip(
              e,
              this._tooltip.options.sticky ? t.latlng : void 0
            );
        },
        _moveTooltip: function (t) {
          var e,
            i,
            n = t.latlng;
          this._tooltip.options.sticky &&
            t.originalEvent &&
            ((e = this._map.mouseEventToContainerPoint(t.originalEvent)),
            (i = this._map.containerPointToLayerPoint(e)),
            (n = this._map.layerPointToLatLng(i))),
            this._tooltip.setLatLng(n);
        },
      });
    var nn = Di.extend({
      options: {
        iconSize: [12, 12],
        html: !1,
        bgPos: null,
        className: "leaflet-div-icon",
      },
      createIcon: function (t) {
        var e,
          i = t && "DIV" === t.tagName ? t : document.createElement("div"),
          n = this.options;
        return (
          n.html instanceof Element
            ? (oe(i), i.appendChild(n.html))
            : (i.innerHTML = !1 !== n.html ? n.html : ""),
          n.bgPos &&
            ((e = z(n.bgPos)),
            (i.style.backgroundPosition = -e.x + "px " + -e.y + "px")),
          this._setIconStyles(i, "icon"),
          i
        );
      },
      createShadow: function () {
        return null;
      },
    });
    Di.Default = Mi;
    var an = Ei.extend({
        options: {
          tileSize: 256,
          opacity: 1,
          updateWhenIdle: yt,
          updateWhenZooming: !0,
          updateInterval: 200,
          zIndex: 1,
          bounds: null,
          minZoom: 0,
          maxZoom: void 0,
          maxNativeZoom: void 0,
          minNativeZoom: void 0,
          noWrap: !1,
          pane: "tilePane",
          className: "",
          keepBuffer: 2,
        },
        initialize: function (t) {
          p(this, t);
        },
        onAdd: function () {
          this._initContainer(),
            (this._levels = {}),
            (this._tiles = {}),
            this._resetView(),
            this._update();
        },
        beforeAdd: function (t) {
          t._addZoomLimit(this);
        },
        onRemove: function (t) {
          this._removeAllTiles(),
            se(this._container),
            t._removeZoomLimit(this),
            (this._container = null),
            (this._tileZoom = void 0);
        },
        bringToFront: function () {
          return (
            this._map && (le(this._container), this._setAutoZIndex(Math.max)),
            this
          );
        },
        bringToBack: function () {
          return (
            this._map && (ue(this._container), this._setAutoZIndex(Math.min)),
            this
          );
        },
        getContainer: function () {
          return this._container;
        },
        setOpacity: function (t) {
          return (this.options.opacity = t), this._updateOpacity(), this;
        },
        setZIndex: function (t) {
          return (this.options.zIndex = t), this._updateZIndex(), this;
        },
        isLoading: function () {
          return this._loading;
        },
        redraw: function () {
          return this._map && (this._removeAllTiles(), this._update()), this;
        },
        getEvents: function () {
          var t = {
            viewprereset: this._invalidateAll,
            viewreset: this._resetView,
            zoom: this._resetView,
            moveend: this._onMoveEnd,
          };
          return (
            this.options.updateWhenIdle ||
              (this._onMove ||
                (this._onMove = o(
                  this._onMoveEnd,
                  this.options.updateInterval,
                  this
                )),
              (t.move = this._onMove)),
            this._zoomAnimated && (t.zoomanim = this._animateZoom),
            t
          );
        },
        createTile: function () {
          return document.createElement("div");
        },
        getTileSize: function () {
          var t = this.options.tileSize;
          return t instanceof A ? t : new A(t, t);
        },
        _updateZIndex: function () {
          this._container &&
            void 0 !== this.options.zIndex &&
            null !== this.options.zIndex &&
            (this._container.style.zIndex = this.options.zIndex);
        },
        _setAutoZIndex: function (t) {
          for (
            var e,
              i = this.getPane().children,
              n = -t(-1 / 0, 1 / 0),
              a = 0,
              r = i.length;
            a < r;
            a++
          )
            (e = i[a].style.zIndex),
              i[a] !== this._container && e && (n = t(n, +e));
          isFinite(n) &&
            ((this.options.zIndex = n + t(-1, 1)), this._updateZIndex());
        },
        _updateOpacity: function () {
          if (this._map && !et) {
            me(this._container, this.options.opacity);
            var t = +new Date(),
              e = !1,
              i = !1;
            for (var n in this._tiles) {
              var a,
                r = this._tiles[n];
              r.current &&
                r.loaded &&
                ((a = Math.min(1, (t - r.loaded) / 200)),
                me(r.el, a),
                a < 1
                  ? (e = !0)
                  : (r.active ? (i = !0) : this._onOpaqueTile(r),
                    (r.active = !0)));
            }
            i && !this._noPrune && this._pruneTiles(),
              e &&
                (E(this._fadeFrame),
                (this._fadeFrame = C(this._updateOpacity, this)));
          }
        },
        _onOpaqueTile: u,
        _initContainer: function () {
          this._container ||
            ((this._container = re(
              "div",
              "leaflet-layer " + (this.options.className || "")
            )),
            this._updateZIndex(),
            this.options.opacity < 1 && this._updateOpacity(),
            this.getPane().appendChild(this._container));
        },
        _updateLevels: function () {
          var t = this._tileZoom,
            e = this.options.maxZoom;
          if (void 0 !== t) {
            for (var i in this._levels)
              (i = Number(i)),
                this._levels[i].el.children.length || i === t
                  ? ((this._levels[i].el.style.zIndex = e - Math.abs(t - i)),
                    this._onUpdateLevel(i))
                  : (se(this._levels[i].el),
                    this._removeTilesAtZoom(i),
                    this._onRemoveLevel(i),
                    delete this._levels[i]);
            var n = this._levels[t],
              a = this._map;
            return (
              n ||
                (((n = this._levels[t] = {}).el = re(
                  "div",
                  "leaflet-tile-container leaflet-zoom-animated",
                  this._container
                )),
                (n.el.style.zIndex = e),
                (n.origin = a
                  .project(a.unproject(a.getPixelOrigin()), t)
                  .round()),
                (n.zoom = t),
                this._setZoomTransform(n, a.getCenter(), a.getZoom()),
                n.el.offsetWidth,
                this._onCreateLevel(n)),
              (this._level = n)
            );
          }
        },
        _onUpdateLevel: u,
        _onRemoveLevel: u,
        _onCreateLevel: u,
        _pruneTiles: function () {
          if (this._map) {
            var t,
              e,
              i,
              n = this._map.getZoom();
            if (n > this.options.maxZoom || n < this.options.minZoom)
              this._removeAllTiles();
            else {
              for (t in this._tiles) (i = this._tiles[t]).retain = i.current;
              for (t in this._tiles)
                (i = this._tiles[t]).current &&
                  !i.active &&
                  ((e = i.coords),
                  this._retainParent(e.x, e.y, e.z, e.z - 5) ||
                    this._retainChildren(e.x, e.y, e.z, e.z + 2));
              for (t in this._tiles)
                this._tiles[t].retain || this._removeTile(t);
            }
          }
        },
        _removeTilesAtZoom: function (t) {
          for (var e in this._tiles)
            this._tiles[e].coords.z === t && this._removeTile(e);
        },
        _removeAllTiles: function () {
          for (var t in this._tiles) this._removeTile(t);
        },
        _invalidateAll: function () {
          for (var t in this._levels)
            se(this._levels[t].el),
              this._onRemoveLevel(Number(t)),
              delete this._levels[t];
          this._removeAllTiles(), (this._tileZoom = void 0);
        },
        _retainParent: function (t, e, i, n) {
          var a = Math.floor(t / 2),
            r = Math.floor(e / 2),
            s = i - 1,
            o = new A(+a, +r);
          o.z = +s;
          var l = this._tileCoordsToKey(o),
            u = this._tiles[l];
          return u && u.active
            ? (u.retain = !0)
            : (u && u.loaded && (u.retain = !0),
              n < s && this._retainParent(a, r, s, n));
        },
        _retainChildren: function (t, e, i, n) {
          for (var a = 2 * t; a < 2 * t + 2; a++)
            for (var r = 2 * e; r < 2 * e + 2; r++) {
              var s = new A(a, r);
              s.z = i + 1;
              var o = this._tileCoordsToKey(s),
                l = this._tiles[o];
              l && l.active
                ? (l.retain = !0)
                : (l && l.loaded && (l.retain = !0),
                  i + 1 < n && this._retainChildren(a, r, i + 1, n));
            }
        },
        _resetView: function (t) {
          var e = t && (t.pinch || t.flyTo);
          this._setView(this._map.getCenter(), this._map.getZoom(), e, e);
        },
        _animateZoom: function (t) {
          this._setView(t.center, t.zoom, !0, t.noUpdate);
        },
        _clampZoom: function (t) {
          var e = this.options;
          return void 0 !== e.minNativeZoom && t < e.minNativeZoom
            ? e.minNativeZoom
            : void 0 !== e.maxNativeZoom && e.maxNativeZoom < t
            ? e.maxNativeZoom
            : t;
        },
        _setView: function (t, e, i, n) {
          var a = Math.round(e),
            r =
              ((a =
                (void 0 !== this.options.maxZoom && a > this.options.maxZoom) ||
                (void 0 !== this.options.minZoom && a < this.options.minZoom)
                  ? void 0
                  : this._clampZoom(a)),
              this.options.updateWhenZooming && a !== this._tileZoom);
          (n && !r) ||
            ((this._tileZoom = a),
            this._abortLoading && this._abortLoading(),
            this._updateLevels(),
            this._resetGrid(),
            void 0 !== a && this._update(t),
            i || this._pruneTiles(),
            (this._noPrune = !!i)),
            this._setZoomTransforms(t, e);
        },
        _setZoomTransforms: function (t, e) {
          for (var i in this._levels)
            this._setZoomTransform(this._levels[i], t, e);
        },
        _setZoomTransform: function (t, e, i) {
          var n = this._map.getZoomScale(i, t.zoom),
            a = t.origin
              .multiplyBy(n)
              .subtract(this._map._getNewPixelOrigin(e, i))
              .round();
          _t ? ve(t.el, a, n) : _e(t.el, a);
        },
        _resetGrid: function () {
          var t = this._map,
            e = t.options.crs,
            i = (this._tileSize = this.getTileSize()),
            n = this._tileZoom,
            a = this._map.getPixelWorldBounds(this._tileZoom);
          a && (this._globalTileRange = this._pxBoundsToTileRange(a)),
            (this._wrapX = e.wrapLng &&
              !this.options.noWrap && [
                Math.floor(t.project([0, e.wrapLng[0]], n).x / i.x),
                Math.ceil(t.project([0, e.wrapLng[1]], n).x / i.y),
              ]),
            (this._wrapY = e.wrapLat &&
              !this.options.noWrap && [
                Math.floor(t.project([e.wrapLat[0], 0], n).y / i.x),
                Math.ceil(t.project([e.wrapLat[1], 0], n).y / i.y),
              ]);
        },
        _onMoveEnd: function () {
          this._map && !this._map._animatingZoom && this._update();
        },
        _getTiledPixelBounds: function (t) {
          var e = this._map,
            i = e._animatingZoom
              ? Math.max(e._animateToZoom, e.getZoom())
              : e.getZoom(),
            n = e.getZoomScale(i, this._tileZoom),
            a = e.project(t, this._tileZoom).floor(),
            r = e.getSize().divideBy(2 * n);
          return new O(a.subtract(r), a.add(r));
        },
        _update: function (t) {
          var e = this._map;
          if (e) {
            var i = this._clampZoom(e.getZoom());
            if (
              (void 0 === t && (t = e.getCenter()), void 0 !== this._tileZoom)
            ) {
              var n = this._getTiledPixelBounds(t),
                a = this._pxBoundsToTileRange(n),
                r = a.getCenter(),
                s = [],
                o = this.options.keepBuffer,
                l = new O(
                  a.getBottomLeft().subtract([o, -o]),
                  a.getTopRight().add([o, -o])
                );
              if (
                !(
                  isFinite(a.min.x) &&
                  isFinite(a.min.y) &&
                  isFinite(a.max.x) &&
                  isFinite(a.max.y)
                )
              )
                throw new Error(
                  "Attempted to load an infinite number of tiles"
                );
              for (var u in this._tiles) {
                var c = this._tiles[u].coords;
                (c.z === this._tileZoom && l.contains(new A(c.x, c.y))) ||
                  (this._tiles[u].current = !1);
              }
              if (1 < Math.abs(i - this._tileZoom)) this._setView(t, i);
              else {
                for (var d = a.min.y; d <= a.max.y; d++)
                  for (var h = a.min.x; h <= a.max.x; h++) {
                    var p,
                      f = new A(h, d);
                    (f.z = this._tileZoom),
                      this._isValidTile(f) &&
                        ((p = this._tiles[this._tileCoordsToKey(f)])
                          ? (p.current = !0)
                          : s.push(f));
                  }
                if (
                  (s.sort(function (t, e) {
                    return t.distanceTo(r) - e.distanceTo(r);
                  }),
                  0 !== s.length)
                ) {
                  this._loading || ((this._loading = !0), this.fire("loading"));
                  var m = document.createDocumentFragment();
                  for (h = 0; h < s.length; h++) this._addTile(s[h], m);
                  this._level.el.appendChild(m);
                }
              }
            }
          }
        },
        _isValidTile: function (t) {
          var e = this._map.options.crs;
          if (!e.infinite) {
            var i = this._globalTileRange;
            if (
              (!e.wrapLng && (t.x < i.min.x || t.x > i.max.x)) ||
              (!e.wrapLat && (t.y < i.min.y || t.y > i.max.y))
            )
              return !1;
          }
          if (!this.options.bounds) return !0;
          var n = this._tileCoordsToBounds(t);
          return j(this.options.bounds).overlaps(n);
        },
        _keyToBounds: function (t) {
          return this._tileCoordsToBounds(this._keyToTileCoords(t));
        },
        _tileCoordsToNwSe: function (t) {
          var e = this._map,
            i = this.getTileSize(),
            n = t.scaleBy(i),
            a = n.add(i);
          return [e.unproject(n, t.z), e.unproject(a, t.z)];
        },
        _tileCoordsToBounds: function (t) {
          var e = this._tileCoordsToNwSe(t),
            i = new B(e[0], e[1]);
          return this.options.noWrap || (i = this._map.wrapLatLngBounds(i)), i;
        },
        _tileCoordsToKey: function (t) {
          return t.x + ":" + t.y + ":" + t.z;
        },
        _keyToTileCoords: function (t) {
          var e = t.split(":"),
            i = new A(+e[0], +e[1]);
          return (i.z = +e[2]), i;
        },
        _removeTile: function (t) {
          var e = this._tiles[t];
          e &&
            (se(e.el),
            delete this._tiles[t],
            this.fire("tileunload", {
              tile: e.el,
              coords: this._keyToTileCoords(t),
            }));
        },
        _initTile: function (t) {
          de(t, "leaflet-tile");
          var e = this.getTileSize();
          (t.style.width = e.x + "px"),
            (t.style.height = e.y + "px"),
            (t.onselectstart = u),
            (t.onmousemove = u),
            et && this.options.opacity < 1 && me(t, this.options.opacity),
            at && !rt && (t.style.WebkitBackfaceVisibility = "hidden");
        },
        _addTile: function (t, e) {
          var i = this._getTilePos(t),
            n = this._tileCoordsToKey(t),
            r = this.createTile(
              this._wrapCoords(t),
              a(this._tileReady, this, t)
            );
          this._initTile(r),
            this.createTile.length < 2 &&
              C(a(this._tileReady, this, t, null, r)),
            _e(r, i),
            (this._tiles[n] = { el: r, coords: t, current: !0 }),
            e.appendChild(r),
            this.fire("tileloadstart", { tile: r, coords: t });
        },
        _tileReady: function (t, e, i) {
          e && this.fire("tileerror", { error: e, tile: i, coords: t });
          var n = this._tileCoordsToKey(t);
          (i = this._tiles[n]) &&
            ((i.loaded = +new Date()),
            this._map._fadeAnimated
              ? (me(i.el, 0),
                E(this._fadeFrame),
                (this._fadeFrame = C(this._updateOpacity, this)))
              : ((i.active = !0), this._pruneTiles()),
            e ||
              (de(i.el, "leaflet-tile-loaded"),
              this.fire("tileload", { tile: i.el, coords: t })),
            this._noTilesToLoad() &&
              ((this._loading = !1),
              this.fire("load"),
              et || !this._map._fadeAnimated
                ? C(this._pruneTiles, this)
                : setTimeout(a(this._pruneTiles, this), 250)));
        },
        _getTilePos: function (t) {
          return t.scaleBy(this.getTileSize()).subtract(this._level.origin);
        },
        _wrapCoords: function (t) {
          var e = new A(
            this._wrapX ? l(t.x, this._wrapX) : t.x,
            this._wrapY ? l(t.y, this._wrapY) : t.y
          );
          return (e.z = t.z), e;
        },
        _pxBoundsToTileRange: function (t) {
          var e = this.getTileSize();
          return new O(
            t.min.unscaleBy(e).floor(),
            t.max.unscaleBy(e).ceil().subtract([1, 1])
          );
        },
        _noTilesToLoad: function () {
          for (var t in this._tiles) if (!this._tiles[t].loaded) return !1;
          return !0;
        },
      }),
      rn = an.extend({
        options: {
          minZoom: 0,
          maxZoom: 18,
          subdomains: "abc",
          errorTileUrl: "",
          zoomOffset: 0,
          tms: !1,
          zoomReverse: !1,
          detectRetina: !1,
          crossOrigin: !1,
        },
        initialize: function (t, e) {
          (this._url = t),
            (e = p(this, e)).detectRetina &&
              Pt &&
              0 < e.maxZoom &&
              ((e.tileSize = Math.floor(e.tileSize / 2)),
              e.zoomReverse
                ? (e.zoomOffset--, e.minZoom++)
                : (e.zoomOffset++, e.maxZoom--),
              (e.minZoom = Math.max(0, e.minZoom))),
            "string" == typeof e.subdomains &&
              (e.subdomains = e.subdomains.split("")),
            at || this.on("tileunload", this._onTileRemove);
        },
        setUrl: function (t, e) {
          return (
            this._url === t && void 0 === e && (e = !0),
            (this._url = t),
            e || this.redraw(),
            this
          );
        },
        createTile: function (t, e) {
          var i = document.createElement("img");
          return (
            Pe(i, "load", a(this._tileOnLoad, this, e, i)),
            Pe(i, "error", a(this._tileOnError, this, e, i)),
            (!this.options.crossOrigin && "" !== this.options.crossOrigin) ||
              (i.crossOrigin =
                !0 === this.options.crossOrigin
                  ? ""
                  : this.options.crossOrigin),
            (i.alt = ""),
            i.setAttribute("role", "presentation"),
            (i.src = this.getTileUrl(t)),
            i
          );
        },
        getTileUrl: function (t) {
          var i,
            n = {
              r: Pt ? "@2x" : "",
              s: this._getSubdomain(t),
              x: t.x,
              y: t.y,
              z: this._getZoomForUrl(),
            };
          return (
            this._map &&
              !this._map.options.crs.infinite &&
              ((i = this._globalTileRange.max.y - t.y),
              this.options.tms && (n.y = i),
              (n["-y"] = i)),
            g(this._url, e(n, this.options))
          );
        },
        _tileOnLoad: function (t, e) {
          et ? setTimeout(a(t, this, null, e), 0) : t(null, e);
        },
        _tileOnError: function (t, e, i) {
          var n = this.options.errorTileUrl;
          n && e.getAttribute("src") !== n && (e.src = n), t(i, e);
        },
        _onTileRemove: function (t) {
          t.tile.onload = null;
        },
        _getZoomForUrl: function () {
          var t = this._tileZoom,
            e = this.options.maxZoom;
          return (
            this.options.zoomReverse && (t = e - t), t + this.options.zoomOffset
          );
        },
        _getSubdomain: function (t) {
          var e = Math.abs(t.x + t.y) % this.options.subdomains.length;
          return this.options.subdomains[e];
        },
        _abortLoading: function () {
          var t, e;
          for (t in this._tiles)
            this._tiles[t].coords.z !== this._tileZoom &&
              (((e = this._tiles[t].el).onload = u),
              (e.onerror = u),
              e.complete || ((e.src = y), se(e), delete this._tiles[t]));
        },
        _removeTile: function (t) {
          var e = this._tiles[t];
          if (e)
            return (
              ot || e.el.setAttribute("src", y),
              an.prototype._removeTile.call(this, t)
            );
        },
        _tileReady: function (t, e, i) {
          if (this._map && (!i || i.getAttribute("src") !== y))
            return an.prototype._tileReady.call(this, t, e, i);
        },
      });
    function sn(t, e) {
      return new rn(t, e);
    }
    var on = rn.extend({
      defaultWmsParams: {
        service: "WMS",
        request: "GetMap",
        layers: "",
        styles: "",
        format: "image/jpeg",
        transparent: !1,
        version: "1.1.1",
      },
      options: { crs: null, uppercase: !1 },
      initialize: function (t, i) {
        this._url = t;
        var n = e({}, this.defaultWmsParams);
        for (var a in i) a in this.options || (n[a] = i[a]);
        var r = (i = p(this, i)).detectRetina && Pt ? 2 : 1,
          s = this.getTileSize();
        (n.width = s.x * r), (n.height = s.y * r), (this.wmsParams = n);
      },
      onAdd: function (t) {
        (this._crs = this.options.crs || t.options.crs),
          (this._wmsVersion = parseFloat(this.wmsParams.version));
        var e = 1.3 <= this._wmsVersion ? "crs" : "srs";
        (this.wmsParams[e] = this._crs.code), rn.prototype.onAdd.call(this, t);
      },
      getTileUrl: function (t) {
        var e = this._tileCoordsToNwSe(t),
          i = this._crs,
          n = N(i.project(e[0]), i.project(e[1])),
          a = n.min,
          r = n.max,
          s = (
            1.3 <= this._wmsVersion && this._crs === Ti
              ? [a.y, a.x, r.y, r.x]
              : [a.x, a.y, r.x, r.y]
          ).join(","),
          o = rn.prototype.getTileUrl.call(this, t);
        return (
          o +
          f(this.wmsParams, o, this.options.uppercase) +
          (this.options.uppercase ? "&BBOX=" : "&bbox=") +
          s
        );
      },
      setParams: function (t, i) {
        return e(this.wmsParams, t), i || this.redraw(), this;
      },
    });
    (rn.WMS = on),
      (sn.wms = function (t, e) {
        return new on(t, e);
      });
    var ln = Ei.extend({
        options: { padding: 0.1, tolerance: 0 },
        initialize: function (t) {
          p(this, t), s(this), (this._layers = this._layers || {});
        },
        onAdd: function () {
          this._container ||
            (this._initContainer(),
            this._zoomAnimated && de(this._container, "leaflet-zoom-animated")),
            this.getPane().appendChild(this._container),
            this._update(),
            this.on("update", this._updatePaths, this);
        },
        onRemove: function () {
          this.off("update", this._updatePaths, this), this._destroyContainer();
        },
        getEvents: function () {
          var t = {
            viewreset: this._reset,
            zoom: this._onZoom,
            moveend: this._update,
            zoomend: this._onZoomEnd,
          };
          return this._zoomAnimated && (t.zoomanim = this._onAnimZoom), t;
        },
        _onAnimZoom: function (t) {
          this._updateTransform(t.center, t.zoom);
        },
        _onZoom: function () {
          this._updateTransform(this._map.getCenter(), this._map.getZoom());
        },
        _updateTransform: function (t, e) {
          var i = this._map.getZoomScale(e, this._zoom),
            n = ye(this._container),
            a = this._map.getSize().multiplyBy(0.5 + this.options.padding),
            r = this._map.project(this._center, e),
            s = this._map.project(t, e).subtract(r),
            o = a.multiplyBy(-i).add(n).add(a).subtract(s);
          _t ? ve(this._container, o, i) : _e(this._container, o);
        },
        _reset: function () {
          for (var t in (this._update(),
          this._updateTransform(this._center, this._zoom),
          this._layers))
            this._layers[t]._reset();
        },
        _onZoomEnd: function () {
          for (var t in this._layers) this._layers[t]._project();
        },
        _updatePaths: function () {
          for (var t in this._layers) this._layers[t]._update();
        },
        _update: function () {
          var t = this.options.padding,
            e = this._map.getSize(),
            i = this._map.containerPointToLayerPoint(e.multiplyBy(-t)).round();
          (this._bounds = new O(i, i.add(e.multiplyBy(1 + 2 * t)).round())),
            (this._center = this._map.getCenter()),
            (this._zoom = this._map.getZoom());
        },
      }),
      un = ln.extend({
        getEvents: function () {
          var t = ln.prototype.getEvents.call(this);
          return (t.viewprereset = this._onViewPreReset), t;
        },
        _onViewPreReset: function () {
          this._postponeUpdatePaths = !0;
        },
        onAdd: function () {
          ln.prototype.onAdd.call(this), this._draw();
        },
        _initContainer: function () {
          var t = (this._container = document.createElement("canvas"));
          Pe(t, "mousemove", this._onMouseMove, this),
            Pe(
              t,
              "click dblclick mousedown mouseup contextmenu",
              this._onClick,
              this
            ),
            Pe(t, "mouseout", this._handleMouseOut, this),
            (this._ctx = t.getContext("2d"));
        },
        _destroyContainer: function () {
          E(this._redrawRequest),
            delete this._ctx,
            se(this._container),
            De(this._container),
            delete this._container;
        },
        _updatePaths: function () {
          if (!this._postponeUpdatePaths) {
            for (var t in ((this._redrawBounds = null), this._layers))
              this._layers[t]._update();
            this._redraw();
          }
        },
        _update: function () {
          var t, e, i, n;
          (this._map._animatingZoom && this._bounds) ||
            (ln.prototype._update.call(this),
            (t = this._bounds),
            (e = this._container),
            (i = t.getSize()),
            (n = Pt ? 2 : 1),
            _e(e, t.min),
            (e.width = n * i.x),
            (e.height = n * i.y),
            (e.style.width = i.x + "px"),
            (e.style.height = i.y + "px"),
            Pt && this._ctx.scale(2, 2),
            this._ctx.translate(-t.min.x, -t.min.y),
            this.fire("update"));
        },
        _reset: function () {
          ln.prototype._reset.call(this),
            this._postponeUpdatePaths &&
              ((this._postponeUpdatePaths = !1), this._updatePaths());
        },
        _initPath: function (t) {
          this._updateDashArray(t);
          var e = ((this._layers[s(t)] = t)._order = {
            layer: t,
            prev: this._drawLast,
            next: null,
          });
          this._drawLast && (this._drawLast.next = e),
            (this._drawLast = e),
            (this._drawFirst = this._drawFirst || this._drawLast);
        },
        _addPath: function (t) {
          this._requestRedraw(t);
        },
        _removePath: function (t) {
          var e = t._order,
            i = e.next,
            n = e.prev;
          i ? (i.prev = n) : (this._drawLast = n),
            n ? (n.next = i) : (this._drawFirst = i),
            delete t._order,
            delete this._layers[s(t)],
            this._requestRedraw(t);
        },
        _updatePath: function (t) {
          this._extendRedrawBounds(t),
            t._project(),
            t._update(),
            this._requestRedraw(t);
        },
        _updateStyle: function (t) {
          this._updateDashArray(t), this._requestRedraw(t);
        },
        _updateDashArray: function (t) {
          if ("string" == typeof t.options.dashArray) {
            for (
              var e, i = t.options.dashArray.split(/[, ]+/), n = [], a = 0;
              a < i.length;
              a++
            ) {
              if (((e = Number(i[a])), isNaN(e))) return;
              n.push(e);
            }
            t.options._dashArray = n;
          } else t.options._dashArray = t.options.dashArray;
        },
        _requestRedraw: function (t) {
          this._map &&
            (this._extendRedrawBounds(t),
            (this._redrawRequest =
              this._redrawRequest || C(this._redraw, this)));
        },
        _extendRedrawBounds: function (t) {
          var e;
          t._pxBounds &&
            ((e = (t.options.weight || 0) + 1),
            (this._redrawBounds = this._redrawBounds || new O()),
            this._redrawBounds.extend(t._pxBounds.min.subtract([e, e])),
            this._redrawBounds.extend(t._pxBounds.max.add([e, e])));
        },
        _redraw: function () {
          (this._redrawRequest = null),
            this._redrawBounds &&
              (this._redrawBounds.min._floor(), this._redrawBounds.max._ceil()),
            this._clear(),
            this._draw(),
            (this._redrawBounds = null);
        },
        _clear: function () {
          var t,
            e = this._redrawBounds;
          e
            ? ((t = e.getSize()),
              this._ctx.clearRect(e.min.x, e.min.y, t.x, t.y))
            : (this._ctx.save(),
              this._ctx.setTransform(1, 0, 0, 1, 0, 0),
              this._ctx.clearRect(
                0,
                0,
                this._container.width,
                this._container.height
              ),
              this._ctx.restore());
        },
        _draw: function () {
          var t,
            e,
            i = this._redrawBounds;
          this._ctx.save(),
            i &&
              ((e = i.getSize()),
              this._ctx.beginPath(),
              this._ctx.rect(i.min.x, i.min.y, e.x, e.y),
              this._ctx.clip()),
            (this._drawing = !0);
          for (var n = this._drawFirst; n; n = n.next)
            (t = n.layer),
              (!i || (t._pxBounds && t._pxBounds.intersects(i))) &&
                t._updatePath();
          (this._drawing = !1), this._ctx.restore();
        },
        _updatePoly: function (t, e) {
          if (this._drawing) {
            var i,
              n,
              a,
              r,
              s = t._parts,
              o = s.length,
              l = this._ctx;
            if (o) {
              for (l.beginPath(), i = 0; i < o; i++) {
                for (n = 0, a = s[i].length; n < a; n++)
                  (r = s[i][n]), l[n ? "lineTo" : "moveTo"](r.x, r.y);
                e && l.closePath();
              }
              this._fillStroke(l, t);
            }
          }
        },
        _updateCircle: function (t) {
          var e, i, n, a;
          this._drawing &&
            !t._empty() &&
            ((e = t._point),
            (i = this._ctx),
            (n = Math.max(Math.round(t._radius), 1)),
            1 != (a = (Math.max(Math.round(t._radiusY), 1) || n) / n) &&
              (i.save(), i.scale(1, a)),
            i.beginPath(),
            i.arc(e.x, e.y / a, n, 0, 2 * Math.PI, !1),
            1 != a && i.restore(),
            this._fillStroke(i, t));
        },
        _fillStroke: function (t, e) {
          var i = e.options;
          i.fill &&
            ((t.globalAlpha = i.fillOpacity),
            (t.fillStyle = i.fillColor || i.color),
            t.fill(i.fillRule || "evenodd")),
            i.stroke &&
              0 !== i.weight &&
              (t.setLineDash &&
                t.setLineDash((e.options && e.options._dashArray) || []),
              (t.globalAlpha = i.opacity),
              (t.lineWidth = i.weight),
              (t.strokeStyle = i.color),
              (t.lineCap = i.lineCap),
              (t.lineJoin = i.lineJoin),
              t.stroke());
        },
        _onClick: function (t) {
          for (
            var e,
              i,
              n = this._map.mouseEventToLayerPoint(t),
              a = this._drawFirst;
            a;
            a = a.next
          )
            (e = a.layer).options.interactive &&
              e._containsPoint(n) &&
              ((("click" === t.type || "preclick" !== t.type) &&
                this._map._draggableMoved(e)) ||
                (i = e));
          i && (Ve(t), this._fireEvent([i], t));
        },
        _onMouseMove: function (t) {
          var e;
          !this._map ||
            this._map.dragging.moving() ||
            this._map._animatingZoom ||
            ((e = this._map.mouseEventToLayerPoint(t)),
            this._handleMouseHover(t, e));
        },
        _handleMouseOut: function (t) {
          var e = this._hoveredLayer;
          e &&
            (he(this._container, "leaflet-interactive"),
            this._fireEvent([e], t, "mouseout"),
            (this._hoveredLayer = null),
            (this._mouseHoverThrottled = !1));
        },
        _handleMouseHover: function (t, e) {
          if (!this._mouseHoverThrottled) {
            for (var i, n, r = this._drawFirst; r; r = r.next)
              (i = r.layer).options.interactive &&
                i._containsPoint(e) &&
                (n = i);
            n !== this._hoveredLayer &&
              (this._handleMouseOut(t),
              n &&
                (de(this._container, "leaflet-interactive"),
                this._fireEvent([n], t, "mouseover"),
                (this._hoveredLayer = n))),
              this._hoveredLayer && this._fireEvent([this._hoveredLayer], t),
              (this._mouseHoverThrottled = !0),
              setTimeout(
                a(function () {
                  this._mouseHoverThrottled = !1;
                }, this),
                32
              );
          }
        },
        _fireEvent: function (t, e, i) {
          this._map._fireDOMEvent(e, i || e.type, t);
        },
        _bringToFront: function (t) {
          var e,
            i,
            n = t._order;
          n &&
            ((e = n.next),
            (i = n.prev),
            e &&
              ((e.prev = i) ? (i.next = e) : e && (this._drawFirst = e),
              (n.prev = this._drawLast),
              ((this._drawLast.next = n).next = null),
              (this._drawLast = n),
              this._requestRedraw(t)));
        },
        _bringToBack: function (t) {
          var e,
            i,
            n = t._order;
          n &&
            ((e = n.next),
            (i = n.prev) &&
              ((i.next = e) ? (e.prev = i) : i && (this._drawLast = i),
              (n.prev = null),
              (n.next = this._drawFirst),
              (this._drawFirst.prev = n),
              (this._drawFirst = n),
              this._requestRedraw(t)));
        },
      });
    function cn(t) {
      return Dt ? new un(t) : null;
    }
    var dn = (function () {
        try {
          return (
            document.namespaces.add("lvml", "urn:schemas-microsoft-com:vml"),
            function (t) {
              return document.createElement("<lvml:" + t + ' class="lvml">');
            }
          );
        } catch (t) {
          return function (t) {
            return document.createElement(
              "<" + t + ' xmlns="urn:schemas-microsoft.com:vml" class="lvml">'
            );
          };
        }
      })(),
      hn = {
        _initContainer: function () {
          this._container = re("div", "leaflet-vml-container");
        },
        _update: function () {
          this._map._animatingZoom ||
            (ln.prototype._update.call(this), this.fire("update"));
        },
        _initPath: function (t) {
          var e = (t._container = dn("shape"));
          de(e, "leaflet-vml-shape " + (this.options.className || "")),
            (e.coordsize = "1 1"),
            (t._path = dn("path")),
            e.appendChild(t._path),
            this._updateStyle(t),
            (this._layers[s(t)] = t);
        },
        _addPath: function (t) {
          var e = t._container;
          this._container.appendChild(e),
            t.options.interactive && t.addInteractiveTarget(e);
        },
        _removePath: function (t) {
          var e = t._container;
          se(e), t.removeInteractiveTarget(e), delete this._layers[s(t)];
        },
        _updateStyle: function (t) {
          var e = t._stroke,
            i = t._fill,
            n = t.options,
            a = t._container;
          (a.stroked = !!n.stroke),
            (a.filled = !!n.fill),
            n.stroke
              ? ((e = e || (t._stroke = dn("stroke"))),
                a.appendChild(e),
                (e.weight = n.weight + "px"),
                (e.color = n.color),
                (e.opacity = n.opacity),
                n.dashArray
                  ? (e.dashStyle = v(n.dashArray)
                      ? n.dashArray.join(" ")
                      : n.dashArray.replace(/( *, *)/g, " "))
                  : (e.dashStyle = ""),
                (e.endcap = n.lineCap.replace("butt", "flat")),
                (e.joinstyle = n.lineJoin))
              : e && (a.removeChild(e), (t._stroke = null)),
            n.fill
              ? ((i = i || (t._fill = dn("fill"))),
                a.appendChild(i),
                (i.color = n.fillColor || n.color),
                (i.opacity = n.fillOpacity))
              : i && (a.removeChild(i), (t._fill = null));
        },
        _updateCircle: function (t) {
          var e = t._point.round(),
            i = Math.round(t._radius),
            n = Math.round(t._radiusY || i);
          this._setPath(
            t,
            t._empty()
              ? "M0 0"
              : "AL " + e.x + "," + e.y + " " + i + "," + n + " 0,23592600"
          );
        },
        _setPath: function (t, e) {
          t._path.v = e;
        },
        _bringToFront: function (t) {
          le(t._container);
        },
        _bringToBack: function (t) {
          ue(t._container);
        },
      },
      pn = kt ? dn : K,
      fn = ln.extend({
        getEvents: function () {
          var t = ln.prototype.getEvents.call(this);
          return (t.zoomstart = this._onZoomStart), t;
        },
        _initContainer: function () {
          (this._container = pn("svg")),
            this._container.setAttribute("pointer-events", "none"),
            (this._rootGroup = pn("g")),
            this._container.appendChild(this._rootGroup);
        },
        _destroyContainer: function () {
          se(this._container),
            De(this._container),
            delete this._container,
            delete this._rootGroup,
            delete this._svgSize;
        },
        _onZoomStart: function () {
          this._update();
        },
        _update: function () {
          var t, e, i;
          (this._map._animatingZoom && this._bounds) ||
            (ln.prototype._update.call(this),
            (e = (t = this._bounds).getSize()),
            (i = this._container),
            (this._svgSize && this._svgSize.equals(e)) ||
              ((this._svgSize = e),
              i.setAttribute("width", e.x),
              i.setAttribute("height", e.y)),
            _e(i, t.min),
            i.setAttribute("viewBox", [t.min.x, t.min.y, e.x, e.y].join(" ")),
            this.fire("update"));
        },
        _initPath: function (t) {
          var e = (t._path = pn("path"));
          t.options.className && de(e, t.options.className),
            t.options.interactive && de(e, "leaflet-interactive"),
            this._updateStyle(t),
            (this._layers[s(t)] = t);
        },
        _addPath: function (t) {
          this._rootGroup || this._initContainer(),
            this._rootGroup.appendChild(t._path),
            t.addInteractiveTarget(t._path);
        },
        _removePath: function (t) {
          se(t._path),
            t.removeInteractiveTarget(t._path),
            delete this._layers[s(t)];
        },
        _updatePath: function (t) {
          t._project(), t._update();
        },
        _updateStyle: function (t) {
          var e = t._path,
            i = t.options;
          e &&
            (i.stroke
              ? (e.setAttribute("stroke", i.color),
                e.setAttribute("stroke-opacity", i.opacity),
                e.setAttribute("stroke-width", i.weight),
                e.setAttribute("stroke-linecap", i.lineCap),
                e.setAttribute("stroke-linejoin", i.lineJoin),
                i.dashArray
                  ? e.setAttribute("stroke-dasharray", i.dashArray)
                  : e.removeAttribute("stroke-dasharray"),
                i.dashOffset
                  ? e.setAttribute("stroke-dashoffset", i.dashOffset)
                  : e.removeAttribute("stroke-dashoffset"))
              : e.setAttribute("stroke", "none"),
            i.fill
              ? (e.setAttribute("fill", i.fillColor || i.color),
                e.setAttribute("fill-opacity", i.fillOpacity),
                e.setAttribute("fill-rule", i.fillRule || "evenodd"))
              : e.setAttribute("fill", "none"));
        },
        _updatePoly: function (t, e) {
          this._setPath(t, J(t._parts, e));
        },
        _updateCircle: function (t) {
          var e = t._point,
            i = Math.max(Math.round(t._radius), 1),
            n =
              "a" +
              i +
              "," +
              (Math.max(Math.round(t._radiusY), 1) || i) +
              " 0 1,0 ",
            a = t._empty()
              ? "M0 0"
              : "M" +
                (e.x - i) +
                "," +
                e.y +
                n +
                2 * i +
                ",0 " +
                n +
                2 * -i +
                ",0 ";
          this._setPath(t, a);
        },
        _setPath: function (t, e) {
          t._path.setAttribute("d", e);
        },
        _bringToFront: function (t) {
          le(t._path);
        },
        _bringToBack: function (t) {
          ue(t._path);
        },
      });
    function mn(t) {
      return Mt || kt ? new fn(t) : null;
    }
    kt && fn.include(hn),
      Ye.include({
        getRenderer: function (t) {
          var e =
            (e =
              t.options.renderer ||
              this._getPaneRenderer(t.options.pane) ||
              this.options.renderer ||
              this._renderer) || (this._renderer = this._createRenderer());
          return this.hasLayer(e) || this.addLayer(e), e;
        },
        _getPaneRenderer: function (t) {
          if ("overlayPane" === t || void 0 === t) return !1;
          var e = this._paneRenderers[t];
          return (
            void 0 === e &&
              ((e = this._createRenderer({ pane: t })),
              (this._paneRenderers[t] = e)),
            e
          );
        },
        _createRenderer: function (t) {
          return (this.options.preferCanvas && cn(t)) || mn(t);
        },
      });
    var gn = Bi.extend({
      initialize: function (t, e) {
        Bi.prototype.initialize.call(this, this._boundsToLatLngs(t), e);
      },
      setBounds: function (t) {
        return this.setLatLngs(this._boundsToLatLngs(t));
      },
      _boundsToLatLngs: function (t) {
        return [
          (t = j(t)).getSouthWest(),
          t.getNorthWest(),
          t.getNorthEast(),
          t.getSouthEast(),
        ];
      },
    });
    (fn.create = pn),
      (fn.pointsToPath = J),
      (ji.geometryToLayer = Fi),
      (ji.coordsToLatLng = $i),
      (ji.coordsToLatLngs = Hi),
      (ji.latLngToCoords = Vi),
      (ji.latLngsToCoords = Wi),
      (ji.getFeature = Zi),
      (ji.asFeature = Gi),
      Ye.mergeOptions({ boxZoom: !0 });
    var vn = ei.extend({
      initialize: function (t) {
        (this._map = t),
          (this._container = t._container),
          (this._pane = t._panes.overlayPane),
          (this._resetStateTimeout = 0),
          t.on("unload", this._destroy, this);
      },
      addHooks: function () {
        Pe(this._container, "mousedown", this._onMouseDown, this);
      },
      removeHooks: function () {
        De(this._container, "mousedown", this._onMouseDown, this);
      },
      moved: function () {
        return this._moved;
      },
      _destroy: function () {
        se(this._pane), delete this._pane;
      },
      _resetState: function () {
        (this._resetStateTimeout = 0), (this._moved = !1);
      },
      _clearDeferredResetState: function () {
        0 !== this._resetStateTimeout &&
          (clearTimeout(this._resetStateTimeout),
          (this._resetStateTimeout = 0));
      },
      _onMouseDown: function (t) {
        if (!t.shiftKey || (1 !== t.which && 1 !== t.button)) return !1;
        this._clearDeferredResetState(),
          this._resetState(),
          Ut(),
          be(),
          (this._startPoint = this._map.mouseEventToContainerPoint(t)),
          Pe(
            document,
            {
              contextmenu: je,
              mousemove: this._onMouseMove,
              mouseup: this._onMouseUp,
              keydown: this._onKeyDown,
            },
            this
          );
      },
      _onMouseMove: function (t) {
        this._moved ||
          ((this._moved = !0),
          (this._box = re("div", "leaflet-zoom-box", this._container)),
          de(this._container, "leaflet-crosshair"),
          this._map.fire("boxzoomstart")),
          (this._point = this._map.mouseEventToContainerPoint(t));
        var e = new O(this._point, this._startPoint),
          i = e.getSize();
        _e(this._box, e.min),
          (this._box.style.width = i.x + "px"),
          (this._box.style.height = i.y + "px");
      },
      _finish: function () {
        this._moved &&
          (se(this._box), he(this._container, "leaflet-crosshair")),
          Yt(),
          we(),
          De(
            document,
            {
              contextmenu: je,
              mousemove: this._onMouseMove,
              mouseup: this._onMouseUp,
              keydown: this._onKeyDown,
            },
            this
          );
      },
      _onMouseUp: function (t) {
        var e;
        (1 !== t.which && 1 !== t.button) ||
          (this._finish(),
          this._moved &&
            (this._clearDeferredResetState(),
            (this._resetStateTimeout = setTimeout(
              a(this._resetState, this),
              0
            )),
            (e = new B(
              this._map.containerPointToLatLng(this._startPoint),
              this._map.containerPointToLatLng(this._point)
            )),
            this._map.fitBounds(e).fire("boxzoomend", { boxZoomBounds: e })));
      },
      _onKeyDown: function (t) {
        27 === t.keyCode && this._finish();
      },
    });
    Ye.addInitHook("addHandler", "boxZoom", vn),
      Ye.mergeOptions({ doubleClickZoom: !0 });
    var _n = ei.extend({
      addHooks: function () {
        this._map.on("dblclick", this._onDoubleClick, this);
      },
      removeHooks: function () {
        this._map.off("dblclick", this._onDoubleClick, this);
      },
      _onDoubleClick: function (t) {
        var e = this._map,
          i = e.getZoom(),
          n = e.options.zoomDelta,
          a = t.originalEvent.shiftKey ? i - n : i + n;
        "center" === e.options.doubleClickZoom
          ? e.setZoom(a)
          : e.setZoomAround(t.containerPoint, a);
      },
    });
    Ye.addInitHook("addHandler", "doubleClickZoom", _n),
      Ye.mergeOptions({
        dragging: !0,
        inertia: !rt,
        inertiaDeceleration: 3400,
        inertiaMaxSpeed: 1 / 0,
        easeLinearity: 0.2,
        worldCopyJump: !1,
        maxBoundsViscosity: 0,
      });
    var yn = ei.extend({
      addHooks: function () {
        var t;
        this._draggable ||
          ((t = this._map),
          (this._draggable = new oi(t._mapPane, t._container)),
          this._draggable.on(
            {
              dragstart: this._onDragStart,
              drag: this._onDrag,
              dragend: this._onDragEnd,
            },
            this
          ),
          this._draggable.on("predrag", this._onPreDragLimit, this),
          t.options.worldCopyJump &&
            (this._draggable.on("predrag", this._onPreDragWrap, this),
            t.on("zoomend", this._onZoomEnd, this),
            t.whenReady(this._onZoomEnd, this))),
          de(this._map._container, "leaflet-grab leaflet-touch-drag"),
          this._draggable.enable(),
          (this._positions = []),
          (this._times = []);
      },
      removeHooks: function () {
        he(this._map._container, "leaflet-grab"),
          he(this._map._container, "leaflet-touch-drag"),
          this._draggable.disable();
      },
      moved: function () {
        return this._draggable && this._draggable._moved;
      },
      moving: function () {
        return this._draggable && this._draggable._moving;
      },
      _onDragStart: function () {
        var t,
          e = this._map;
        e._stop(),
          this._map.options.maxBounds && this._map.options.maxBoundsViscosity
            ? ((t = j(this._map.options.maxBounds)),
              (this._offsetLimit = N(
                this._map
                  .latLngToContainerPoint(t.getNorthWest())
                  .multiplyBy(-1),
                this._map
                  .latLngToContainerPoint(t.getSouthEast())
                  .multiplyBy(-1)
                  .add(this._map.getSize())
              )),
              (this._viscosity = Math.min(
                1,
                Math.max(0, this._map.options.maxBoundsViscosity)
              )))
            : (this._offsetLimit = null),
          e.fire("movestart").fire("dragstart"),
          e.options.inertia && ((this._positions = []), (this._times = []));
      },
      _onDrag: function (t) {
        var e, i;
        this._map.options.inertia &&
          ((e = this._lastTime = +new Date()),
          (i = this._lastPos =
            this._draggable._absPos || this._draggable._newPos),
          this._positions.push(i),
          this._times.push(e),
          this._prunePositions(e)),
          this._map.fire("move", t).fire("drag", t);
      },
      _prunePositions: function (t) {
        for (; 1 < this._positions.length && 50 < t - this._times[0]; )
          this._positions.shift(), this._times.shift();
      },
      _onZoomEnd: function () {
        var t = this._map.getSize().divideBy(2),
          e = this._map.latLngToLayerPoint([0, 0]);
        (this._initialWorldOffset = e.subtract(t).x),
          (this._worldWidth = this._map.getPixelWorldBounds().getSize().x);
      },
      _viscousLimit: function (t, e) {
        return t - (t - e) * this._viscosity;
      },
      _onPreDragLimit: function () {
        var t, e;
        this._viscosity &&
          this._offsetLimit &&
          ((t = this._draggable._newPos.subtract(this._draggable._startPos)),
          (e = this._offsetLimit),
          t.x < e.min.x && (t.x = this._viscousLimit(t.x, e.min.x)),
          t.y < e.min.y && (t.y = this._viscousLimit(t.y, e.min.y)),
          t.x > e.max.x && (t.x = this._viscousLimit(t.x, e.max.x)),
          t.y > e.max.y && (t.y = this._viscousLimit(t.y, e.max.y)),
          (this._draggable._newPos = this._draggable._startPos.add(t)));
      },
      _onPreDragWrap: function () {
        var t = this._worldWidth,
          e = Math.round(t / 2),
          i = this._initialWorldOffset,
          n = this._draggable._newPos.x,
          a = ((n - e + i) % t) + e - i,
          r = ((n + e + i) % t) - e - i,
          s = Math.abs(a + i) < Math.abs(r + i) ? a : r;
        (this._draggable._absPos = this._draggable._newPos.clone()),
          (this._draggable._newPos.x = s);
      },
      _onDragEnd: function (t) {
        var e,
          i,
          n,
          a,
          r,
          s,
          o,
          l,
          u,
          c = this._map,
          d = c.options,
          h = !d.inertia || this._times.length < 2;
        c.fire("dragend", t),
          h
            ? c.fire("moveend")
            : (this._prunePositions(+new Date()),
              (e = this._lastPos.subtract(this._positions[0])),
              (i = (this._lastTime - this._times[0]) / 1e3),
              (n = d.easeLinearity),
              (r = (a = e.multiplyBy(n / i)).distanceTo([0, 0])),
              (s = Math.min(d.inertiaMaxSpeed, r)),
              (o = a.multiplyBy(s / r)),
              (l = s / (d.inertiaDeceleration * n)),
              (u = o.multiplyBy(-l / 2).round()).x || u.y
                ? ((u = c._limitOffset(u, c.options.maxBounds)),
                  C(function () {
                    c.panBy(u, {
                      duration: l,
                      easeLinearity: n,
                      noMoveStart: !0,
                      animate: !0,
                    });
                  }))
                : c.fire("moveend"));
      },
    });
    Ye.addInitHook("addHandler", "dragging", yn),
      Ye.mergeOptions({ keyboard: !0, keyboardPanDelta: 80 });
    var bn = ei.extend({
      keyCodes: {
        left: [37],
        right: [39],
        down: [40],
        up: [38],
        zoomIn: [187, 107, 61, 171],
        zoomOut: [189, 109, 54, 173],
      },
      initialize: function (t) {
        (this._map = t),
          this._setPanDelta(t.options.keyboardPanDelta),
          this._setZoomDelta(t.options.zoomDelta);
      },
      addHooks: function () {
        var t = this._map._container;
        t.tabIndex <= 0 && (t.tabIndex = "0"),
          Pe(
            t,
            {
              focus: this._onFocus,
              blur: this._onBlur,
              mousedown: this._onMouseDown,
            },
            this
          ),
          this._map.on(
            { focus: this._addHooks, blur: this._removeHooks },
            this
          );
      },
      removeHooks: function () {
        this._removeHooks(),
          De(
            this._map._container,
            {
              focus: this._onFocus,
              blur: this._onBlur,
              mousedown: this._onMouseDown,
            },
            this
          ),
          this._map.off(
            { focus: this._addHooks, blur: this._removeHooks },
            this
          );
      },
      _onMouseDown: function () {
        var t, e, i, n;
        this._focused ||
          ((t = document.body),
          (e = document.documentElement),
          (i = t.scrollTop || e.scrollTop),
          (n = t.scrollLeft || e.scrollLeft),
          this._map._container.focus(),
          window.scrollTo(n, i));
      },
      _onFocus: function () {
        (this._focused = !0), this._map.fire("focus");
      },
      _onBlur: function () {
        (this._focused = !1), this._map.fire("blur");
      },
      _setPanDelta: function (t) {
        for (
          var e = (this._panKeys = {}),
            i = this.keyCodes,
            n = 0,
            a = i.left.length;
          n < a;
          n++
        )
          e[i.left[n]] = [-1 * t, 0];
        for (n = 0, a = i.right.length; n < a; n++) e[i.right[n]] = [t, 0];
        for (n = 0, a = i.down.length; n < a; n++) e[i.down[n]] = [0, t];
        for (n = 0, a = i.up.length; n < a; n++) e[i.up[n]] = [0, -1 * t];
      },
      _setZoomDelta: function (t) {
        for (
          var e = (this._zoomKeys = {}),
            i = this.keyCodes,
            n = 0,
            a = i.zoomIn.length;
          n < a;
          n++
        )
          e[i.zoomIn[n]] = t;
        for (n = 0, a = i.zoomOut.length; n < a; n++) e[i.zoomOut[n]] = -t;
      },
      _addHooks: function () {
        Pe(document, "keydown", this._onKeyDown, this);
      },
      _removeHooks: function () {
        De(document, "keydown", this._onKeyDown, this);
      },
      _onKeyDown: function (t) {
        if (!(t.altKey || t.ctrlKey || t.metaKey)) {
          var e,
            i = t.keyCode,
            n = this._map;
          if (i in this._panKeys)
            (n._panAnim && n._panAnim._inProgress) ||
              ((e = this._panKeys[i]),
              t.shiftKey && (e = z(e).multiplyBy(3)),
              n.panBy(e),
              n.options.maxBounds && n.panInsideBounds(n.options.maxBounds));
          else if (i in this._zoomKeys)
            n.setZoom(n.getZoom() + (t.shiftKey ? 3 : 1) * this._zoomKeys[i]);
          else {
            if (27 !== i || !n._popup || !n._popup.options.closeOnEscapeKey)
              return;
            n.closePopup();
          }
          je(t);
        }
      },
    });
    Ye.addInitHook("addHandler", "keyboard", bn),
      Ye.mergeOptions({
        scrollWheelZoom: !0,
        wheelDebounceTime: 40,
        wheelPxPerZoomLevel: 60,
      });
    var wn = ei.extend({
      addHooks: function () {
        Pe(this._map._container, "wheel", this._onWheelScroll, this),
          (this._delta = 0);
      },
      removeHooks: function () {
        De(this._map._container, "wheel", this._onWheelScroll, this);
      },
      _onWheelScroll: function (t) {
        var e = $e(t),
          i = this._map.options.wheelDebounceTime;
        (this._delta += e),
          (this._lastMousePos = this._map.mouseEventToContainerPoint(t)),
          this._startTime || (this._startTime = +new Date());
        var n = Math.max(i - (new Date() - this._startTime), 0);
        clearTimeout(this._timer),
          (this._timer = setTimeout(a(this._performZoom, this), n)),
          je(t);
      },
      _performZoom: function () {
        var t = this._map,
          e = t.getZoom(),
          i = this._map.options.zoomSnap || 0;
        t._stop();
        var n = this._delta / (4 * this._map.options.wheelPxPerZoomLevel),
          a = (4 * Math.log(2 / (1 + Math.exp(-Math.abs(n))))) / Math.LN2,
          r = i ? Math.ceil(a / i) * i : a,
          s = t._limitZoom(e + (0 < this._delta ? r : -r)) - e;
        (this._delta = 0),
          (this._startTime = null),
          s &&
            ("center" === t.options.scrollWheelZoom
              ? t.setZoom(e + s)
              : t.setZoomAround(this._lastMousePos, e + s));
      },
    });
    Ye.addInitHook("addHandler", "scrollWheelZoom", wn),
      Ye.mergeOptions({ tap: !0, tapTolerance: 15 });
    var xn = ei.extend({
      addHooks: function () {
        Pe(this._map._container, "touchstart", this._onDown, this);
      },
      removeHooks: function () {
        De(this._map._container, "touchstart", this._onDown, this);
      },
      _onDown: function (t) {
        if (t.touches) {
          if ((Be(t), (this._fireClick = !0), 1 < t.touches.length))
            return (this._fireClick = !1), void clearTimeout(this._holdTimeout);
          var e = t.touches[0],
            i = e.target;
          (this._startPos = this._newPos = new A(e.clientX, e.clientY)),
            i.tagName &&
              "a" === i.tagName.toLowerCase() &&
              de(i, "leaflet-active"),
            (this._holdTimeout = setTimeout(
              a(function () {
                this._isTapValid() &&
                  ((this._fireClick = !1),
                  this._onUp(),
                  this._simulateEvent("contextmenu", e));
              }, this),
              1e3
            )),
            this._simulateEvent("mousedown", e),
            Pe(
              document,
              { touchmove: this._onMove, touchend: this._onUp },
              this
            );
        }
      },
      _onUp: function (t) {
        var e, i;
        clearTimeout(this._holdTimeout),
          De(document, { touchmove: this._onMove, touchend: this._onUp }, this),
          this._fireClick &&
            t &&
            t.changedTouches &&
            ((i = (e = t.changedTouches[0]).target) &&
              i.tagName &&
              "a" === i.tagName.toLowerCase() &&
              he(i, "leaflet-active"),
            this._simulateEvent("mouseup", e),
            this._isTapValid() && this._simulateEvent("click", e));
      },
      _isTapValid: function () {
        return (
          this._newPos.distanceTo(this._startPos) <=
          this._map.options.tapTolerance
        );
      },
      _onMove: function (t) {
        var e = t.touches[0];
        (this._newPos = new A(e.clientX, e.clientY)),
          this._simulateEvent("mousemove", e);
      },
      _simulateEvent: function (t, e) {
        var i = document.createEvent("MouseEvents");
        (i._simulated = !0),
          (e.target._simulatedClick = !0),
          i.initMouseEvent(
            t,
            !0,
            !0,
            window,
            1,
            e.screenX,
            e.screenY,
            e.clientX,
            e.clientY,
            !1,
            !1,
            !1,
            !1,
            0,
            null
          ),
          e.target.dispatchEvent(i);
      },
    });
    !Tt || (St && !dt) || Ye.addInitHook("addHandler", "tap", xn),
      Ye.mergeOptions({ touchZoom: Tt && !rt, bounceAtZoomLimits: !0 });
    var Sn = ei.extend({
      addHooks: function () {
        de(this._map._container, "leaflet-touch-zoom"),
          Pe(this._map._container, "touchstart", this._onTouchStart, this);
      },
      removeHooks: function () {
        he(this._map._container, "leaflet-touch-zoom"),
          De(this._map._container, "touchstart", this._onTouchStart, this);
      },
      _onTouchStart: function (t) {
        var e,
          i,
          n = this._map;
        !t.touches ||
          2 !== t.touches.length ||
          n._animatingZoom ||
          this._zooming ||
          ((e = n.mouseEventToContainerPoint(t.touches[0])),
          (i = n.mouseEventToContainerPoint(t.touches[1])),
          (this._centerPoint = n.getSize()._divideBy(2)),
          (this._startLatLng = n.containerPointToLatLng(this._centerPoint)),
          "center" !== n.options.touchZoom &&
            (this._pinchStartLatLng = n.containerPointToLatLng(
              e.add(i)._divideBy(2)
            )),
          (this._startDist = e.distanceTo(i)),
          (this._startZoom = n.getZoom()),
          (this._moved = !1),
          (this._zooming = !0),
          n._stop(),
          Pe(document, "touchmove", this._onTouchMove, this),
          Pe(document, "touchend", this._onTouchEnd, this),
          Be(t));
      },
      _onTouchMove: function (t) {
        if (t.touches && 2 === t.touches.length && this._zooming) {
          var e = this._map,
            i = e.mouseEventToContainerPoint(t.touches[0]),
            n = e.mouseEventToContainerPoint(t.touches[1]),
            r = i.distanceTo(n) / this._startDist;
          if (
            ((this._zoom = e.getScaleZoom(r, this._startZoom)),
            !e.options.bounceAtZoomLimits &&
              ((this._zoom < e.getMinZoom() && r < 1) ||
                (this._zoom > e.getMaxZoom() && 1 < r)) &&
              (this._zoom = e._limitZoom(this._zoom)),
            "center" === e.options.touchZoom)
          ) {
            if (((this._center = this._startLatLng), 1 == r)) return;
          } else {
            var s = i._add(n)._divideBy(2)._subtract(this._centerPoint);
            if (1 == r && 0 === s.x && 0 === s.y) return;
            this._center = e.unproject(
              e.project(this._pinchStartLatLng, this._zoom).subtract(s),
              this._zoom
            );
          }
          this._moved || (e._moveStart(!0, !1), (this._moved = !0)),
            E(this._animRequest);
          var o = a(e._move, e, this._center, this._zoom, {
            pinch: !0,
            round: !1,
          });
          (this._animRequest = C(o, this, !0)), Be(t);
        }
      },
      _onTouchEnd: function () {
        this._moved && this._zooming
          ? ((this._zooming = !1),
            E(this._animRequest),
            De(document, "touchmove", this._onTouchMove, this),
            De(document, "touchend", this._onTouchEnd, this),
            this._map.options.zoomAnimation
              ? this._map._animateZoom(
                  this._center,
                  this._map._limitZoom(this._zoom),
                  !0,
                  this._map.options.zoomSnap
                )
              : this._map._resetView(
                  this._center,
                  this._map._limitZoom(this._zoom)
                ))
          : (this._zooming = !1);
      },
    });
    Ye.addInitHook("addHandler", "touchZoom", Sn),
      (Ye.BoxZoom = vn),
      (Ye.DoubleClickZoom = _n),
      (Ye.Drag = yn),
      (Ye.Keyboard = bn),
      (Ye.ScrollWheelZoom = wn),
      (Ye.Tap = xn),
      (Ye.TouchZoom = Sn),
      (t.version = "1.7.1"),
      (t.Control = Xe),
      (t.control = qe),
      (t.Browser = It),
      (t.Evented = k),
      (t.Mixin = ni),
      (t.Util = P),
      (t.Class = D),
      (t.Handler = ei),
      (t.extend = e),
      (t.bind = a),
      (t.stamp = s),
      (t.setOptions = p),
      (t.DomEvent = Ge),
      (t.DomUtil = Ee),
      (t.PosAnimation = Ue),
      (t.Draggable = oi),
      (t.LineUtil = gi),
      (t.PolyUtil = yi),
      (t.Point = A),
      (t.point = z),
      (t.Bounds = O),
      (t.bounds = N),
      (t.Transformation = G),
      (t.transformation = U),
      (t.Projection = xi),
      (t.LatLng = F),
      (t.latLng = R),
      (t.LatLngBounds = B),
      (t.latLngBounds = j),
      (t.CRS = H),
      (t.GeoJSON = ji),
      (t.geoJSON = Yi),
      (t.geoJson = qi),
      (t.Layer = Ei),
      (t.LayerGroup = Pi),
      (t.layerGroup = function (t, e) {
        return new Pi(t, e);
      }),
      (t.FeatureGroup = Li),
      (t.featureGroup = function (t, e) {
        return new Li(t, e);
      }),
      (t.ImageOverlay = Xi),
      (t.imageOverlay = function (t, e, i) {
        return new Xi(t, e, i);
      }),
      (t.VideoOverlay = Ki),
      (t.videoOverlay = function (t, e, i) {
        return new Ki(t, e, i);
      }),
      (t.SVGOverlay = Ji),
      (t.svgOverlay = function (t, e, i) {
        return new Ji(t, e, i);
      }),
      (t.DivOverlay = Qi),
      (t.Popup = tn),
      (t.popup = function (t, e) {
        return new tn(t, e);
      }),
      (t.Tooltip = en),
      (t.tooltip = function (t, e) {
        return new en(t, e);
      }),
      (t.Icon = Di),
      (t.icon = function (t) {
        return new Di(t);
      }),
      (t.DivIcon = nn),
      (t.divIcon = function (t) {
        return new nn(t);
      }),
      (t.Marker = Ai),
      (t.marker = function (t, e) {
        return new Ai(t, e);
      }),
      (t.TileLayer = rn),
      (t.tileLayer = sn),
      (t.GridLayer = an),
      (t.gridLayer = function (t) {
        return new an(t);
      }),
      (t.SVG = fn),
      (t.svg = mn),
      (t.Renderer = ln),
      (t.Canvas = un),
      (t.canvas = cn),
      (t.Path = Ii),
      (t.CircleMarker = zi),
      (t.circleMarker = function (t, e) {
        return new zi(t, e);
      }),
      (t.Circle = Oi),
      (t.circle = function (t, e, i) {
        return new Oi(t, e, i);
      }),
      (t.Polyline = Ni),
      (t.polyline = function (t, e) {
        return new Ni(t, e);
      }),
      (t.Polygon = Bi),
      (t.polygon = function (t, e) {
        return new Bi(t, e);
      }),
      (t.Rectangle = gn),
      (t.rectangle = function (t, e) {
        return new gn(t, e);
      }),
      (t.Map = Ye),
      (t.map = function (t, e) {
        return new Ye(t, e);
      });
    var Tn = window.L;
    (t.noConflict = function () {
      return (window.L = Tn), this;
    }),
      (window.L = t);
  });
